{
 "generated": "2026-09-09",
 "release_repaired_in": "2.14.0",
 "note": "Confirmed epsilon-guard defects still OPEN after 2.14.0. Each entry was produced by a triage agent and then independently re-derived by a second agent instructed to refute it; only sites where the refutation FAILED are listed. Line numbers are as of the 2.13.0/2.14.0 tree and several were corrected by the verifier -- re-locate by the quoted guard text, not by the number.",
 "count": 74,
 "defects": [
  {
   "site": "/home/macro/Repos/hisab/src/optimize.cyr:312",
   "fn": "opt_conjugate_gradient",
   "guarded_quantity": "gg_old = _opt_dot(g, g, n) = ||g_old||^2, the SQUARED Euclidean norm of the previous gradient. Dimensions [f]^2/[x]^2 \u2014 it carries both the objective's unit and the variables' unit (rescale f by c and it moves by c^2; measure x in mm instead of m and it moves by 1e-6). Confirmed by reading: `_opt_dot` (optimize.cyr:74) is a raw sum of products with no normalisation, and g is written straight by the caller's `fncall2(grad, x, g)`. No caller normalises \u2014 the only in-tree callers (tests/hisab.tcyr:1316, tests/modules.tcyr:4368, tests/modules.tcyr:8064, tests/abuse.tcyr) pass raw hand-written or tape-backed gradients, and neither the module header nor the function's own doc block asks for a normalised gradient.",
   "protects": "The division `beta = f64_div(pr_dot, gg_old)` at optimize.cyr:320 (the Polak-Ribiere+ ratio). Verified by a no-guard control run, not argued: with the guard removed entirely, f(x)=x^2 from x0=5 with tol=0 (so the loop reaches an exactly-zero gradient) returns x = NaN and fmin = NaN \u2014 0/0 poisons d, then x. With the shipped guard, with a DBL_MIN floor, or with a bare `gg_old > 0` sign test, the same call returns x = 0.0 exactly. So a guard IS required here; only its threshold is wrong.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation routes were tried against the shipped function and all five fail on measurement. (1) NOT dimensionless: gg_old is ||g||^2 straight from the caller's gradient, nothing bounds or normalises it. (2) NOT normalised by the caller: no caller in src/, tests/, examples/ or docs normalises, and the API does not ask for it. (3) NOT a convergence tolerance: the convergence test is a SEPARATE line five above, `f64_lt(gnorm, tol)` against the caller's own tol, and it returns; this guard only silences beta. Reading the two together yields a fact the original filing missed and that sharpens it \u2014 at line 312, `sqrt(gg_old) >= tol` ALWAYS holds, because the smaller case already returned. So the guard is reachable only with gg_old >= tol^2, and an absolute 1e-12 cut is live exactly when tol < 1e-6. (4) The proposed threshold breaks NO existing test: all three in-tree CG fixtures (x^4+x^2 from 3 with tol 1e-8 / 40 iters; x^2 from 5 with tol = EPSILON_F64 / 200 iters; the (1,2) bowl with tol 1e-6 / 200000 iters) return identical rc, identical iteration counts and BIT-IDENTICAL out_x under all four guards (shipped 1e-12, DBL_MIN, 1e-24, sign) \u2014 they are 1-D or condition-number 1, where beta cannot matter. (5) The scale test DOES discriminate, and brackets the eps^2 repair. Measured on the shipped code (a verbatim copy whose mode-0 output was first proven bit-identical to the shipped entry point at all 41 scales, rc and both words, 0 mismatches): shipped returns HSB_ERR_NO_CONVERGENCE at 200 iterations for every k <= 0 INCLUDING k = 0, i.e. in default units with the ordinary tol = 1e-8, where the scale-free guard returns rc = 0 in 133; and it departs from the scale-free trajectory for every k <= 6, becoming bit-identical only at k >= 7 (L = 128). That break point is the squared-vs-unsquared error made visible: the band is live iff tol_L = L*1e-8 < sqrt(EPSILON_F64) = 1e-6, i.e. L < 100, which sits between k = 6 (64) and k = 7 (128) exactly as the mechanism predicts. The squaring error is what makes it bite at default scale, but it is NOT the primary class: the eps^2 = 1e-24 repair, which fixes only the squaring, still fails the same sweep for every k <= -13 (its own crossing, L*1e-8 < 1e-12 => L < 1e-4). Both errors are present; the absolute cut on a dimensioned quantity is the one that survives fixing the other, so DEFECT_ABSOLUTE_ON_SCALING is the correct class. Note also that beta itself is dimensionless (a ratio of two gradient^2 quantities), so thresholding the denominator alone imports a scale the formula does not have. Three sibling guards of the same shape sit in this file at lines 442, 609 and 675 on `sy`/`yy`, which are different quantities and out of scope for this object.",
   "fabricates": "No fabricated number \u2014 a silent ALGORITHM substitution with no error channel: beta is pinned to 0, degrading Polak-Ribiere+ CG to steepest descent, reported only through the ordinary tol/max_iter path. `_opt_copy(out_x, x, n)` runs on the HSB_ERR_NO_CONVERGENCE path too (optimize.cyr:343), so a caller ignoring rc receives a far-from-optimal point that looks like an answer. Measured on the shipped binary, f = 1/2(x0^2 + 100*x1^2), x0 = (L,L), tol = L*1e-8, max_iter = 200: the returned x0/L spans 1.310575e-02 (k = -24) ... 4.995144e-08 (k = 0) ... -6.158716e-09 (k >= 7), about six decades of answer for one problem in different units, against a single bit pattern (-6.158716e-09) at all 49 scales under a scale-free guard. Iteration count is degraded even where rc = 0: 182/183/184/176/163/137 at k = 1..6 versus a flat 133.",
   "correct_threshold": "gg_old >= DBL_MIN (0x0010000000000000 = 2.2250738585072014e-308) \u2014 the only value at which pr_dot/gg_old can 0/0 or overflow; equivalently, divide first and reject a non-finite beta. Both are computable without knowing the caller's units. Sharpened by what the code guarantees: because the convergence test five lines up already returned when sqrt(gg_old) < tol, the guard is only reached with gg_old >= tol^2, so a DBL_MIN floor is UNREACHABLE for every caller with a normal positive tol \u2014 it can fire only when tol is zero/subnormal AND the previous gradient is exactly zero, which is precisely the 0/0 case. Verified both ways: with no guard at all that case returns NaN; with the DBL_MIN floor (and with a bare `f64_lt(0, gg_old)` sign test) it returns 0.0 exactly, and all three in-tree CG fixtures are bit-identical to today. A repair to EPSILON_F64^2 = 1e-24 is measurably NOT correct \u2014 it keeps an absolute cut on a dimensioned quantity and merely moves the break from k <= 6 to k <= -13 (L = 1e-4), where the same family again diverges (rc = -8 with 200 iterations for k <= -20).",
   "scale_test": "Similarity family, the SAME bowl in different units: f_L(u) = L^2 * f_1(u/L), grad_L(u) = L * grad_1(u/L), x0_L = L * x0_1, tol_L = L * tol_1, with f_1(x) = 1/2(x0^2 + 100 x1^2), x0_1 = (1,1), tol_1 = 1e-8, max_iter = 200. CG-from-steepest-descent is exactly covariant under it (g -> Lg, d -> Ld, both sides of the Armijo test -> L^2, alpha and beta invariant), and L = 2^k makes every rescaling exact in binary FP, so the assertion is BIT-EXACT, not a tolerance: for L = 2^k, out_x/L must be bit-identical to the k = 0 result and rc must be identical. THREE requirements the original statement did not pin down, each established by measurement here, and each of which silently vacates the test if dropped. (a) THE OBJECTIVE MUST BE ILL-CONDITIONED AND n >= 2. The same 41-scale sweep run on a condition-number-1 family \u2014 the in-tree bowl (u0-L)^2 + (u1-2L)^2 from (-0.5L, 2.5L) \u2014 is bit-identical under ALL FOUR guards (shipped 1e-12, DBL_MIN, 1e-24, sign) at 41/41 scales, because with kappa = 1 the Armijo step lands on the exact minimiser and beta never matters. A scale sweep built on the fixture the suite already has would report a clean PASS on the defective code. kappa = 100 with n = 2 is the minimum that discriminates. (b) THE RANGE MUST REACH k <= -14, not merely below the shipped break: shipped departs at k <= 6, the eps^2 mutant only at k <= -13. k in {-160,-152,...,152,160} (41 points, gg_old sweeping 2^-320..2^320 ~ 1e-96..1e96) brackets 1e-10, the shipped 1e-12, 1e-16, EPSILON_F64^2 = 1e-24, 1e-30 and 1e-50, so every absolute threshold a repair might plausibly pick dies on the same sweep; DBL_MIN and a sign test pass at every point (49/49 on a fine k in [-32,16] sweep, 41/41 on the wide one). (c) THE ASSERTION MUST BE rc AND BIT-EXACT x/L TOGETHER: at k = 1..6 the shipped guard returns rc = 0 with a different trajectory (182/183/184/176/163/137 iterations, different x), so an rc-only assertion misses six scales, and above k = 7 it agrees exactly, so a sweep confined to large L sees nothing at all. Measured today against the shipped entry point (probe validated first: mode-0 copy bit-identical to `opt_conjugate_gradient` at all 41 scales) \u2014 scale-free: rc 0 / 133 iters / one bit pattern at every scale; shipped: rc -8 / 200 iters at every k <= 0 including k = 0."
  },
  {
   "site": "/home/macro/Repos/hisab/src/optimize.cyr:442",
   "fn": "opt_bfgs",
   "guarded_quantity": "sy = _opt_dot(s, y, n) = (x_new - x) . (g_new - g), the curvature product. Line 442 IS the guard (`if (f64_lt(EPSILON_F64, sy) == 1) {`); `rho = f64_div(_OPT_F64_ONE, sy)` is 443. Dimensions [x]*[f]/[x] = [f], the unit of the OBJECTIVE: invariant under a change of the variables' unit (s scales by lambda, y by 1/lambda) but LINEAR in the objective's unit. MEASURED, not argued: over the similarity family at L = 2^k, k in [-400,160], the live sy values run 0x0a98c3036547e504 (~1.7e-257) to 0x546e848200000000 (~2.7e98) \u2014 355 decades on ONE fixture. It is not dimensionless and it is not bounded.",
   "protects": "Two distinct things, which is why one magnitude test is wrong for both: (a) rho = f64_div(1.0, sy) on line 443, which fails only at sy = 0 or subnormal; (b) positive-definiteness of the BFGS inverse-Hessian update, which requires sy > 0 \u2014 a SIGN condition. Neither is a magnitude condition at 1e-12.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ANGLES; ALL FIVE FAILED, each against the compiled library under cycc 6.6.2 (scratch copies of the tree, repo untouched). (1) \"Dimensionless/bounded\" \u2014 refuted by the 355-decade measured range above. (2) \"Caller normalises\" \u2014 refuted by reading: opt_bfgs is a public entry point taking raw `f`/`grad` function pointers; nothing normalises f, g, s, y, or even d \u2014 _opt_armijo (optimize.cyr:117) starts at alpha = 1 on the unnormalised d = -H_inv*g. (3) \"It is a convergence tolerance\" \u2014 refuted: `tol` is a separate parameter and the convergence test is `f64_lt(gnorm, tol)` at line 405. EPSILON_F64 at 442 gates the rank-2 update only. (4) \"The repair breaks an existing test\" \u2014 REFUTED BY RUNNING IT. With line 442 changed to the sign test `f64_lt(_OPT_F64_ZERO, sy)` (mutation grep-verified installed), all five suites pass unchanged: hisab 416/416, foundation 359/359, modules 1827/1827, edge_cases 233/233, abuse 776/776 = 3611 assertions, 0 failures. No existing test discriminates in either direction. (5) \"The scale test does not discriminate\" \u2014 refuted; see scale_test, which I ran. THE DEFECT REPRODUCES IN THE SHIPPED BINARY: on f_L(v) = L^2 * (1/2)(u0^2 + 100*u1^2) with u = v/L, x0 = L*(1,1), tol = L*1e-8, max_iter 200 \u2014 for k <= -25 the shipped guard skips 200 of 200 updates, BFGS degenerates to steepest descent, and it returns rc = -8 at x/L = (1.19e-2, 1.60e-4) with f/L^2 = 7.23e-5 instead of 0; the sign guard returns rc = 0 in 4 iterations with 0 updates skipped at every one of those scales. Even at k = 0 the shipped guard already skips 1 of 4 updates. NOTE the same guard on the same quantity is duplicated at optimize.cyr:675 (opt_lbfgs), and the same absolute-epsilon-on-a-dimensioned-quantity shape appears twice more in this file \u2014 312 (gg_old = ||g||^2, unit [f]^2/[x]^2) and 609 (yy = ||y||^2). Four instances, one file. The tree already contains the right constant: `F64_TINY = 0x0010_0000_0000_0000` (DBL_MIN) was just added to src/error.cyr with a comment that scopes EPSILON_F64 to \"a genuinely dimensionless, bounded quantity\" \u2014 sy is not one, so this site is exactly what that comment excludes.",
   "fabricates": "rc = 0 with a silently wrong answer, and separately a fabricated failure. Both measured in-library. (a) WRONG ANSWER, rc = 0: at k = -20, -15, -10 the shipped guard skips 2 of 4 updates and returns rc = 0 at x0/L = 0xbd6ed89047044000 = -8.767e-13, x1/L = 0xbd1146840c848000 = -1.534e-14, where the scale-free guard returns 0xbc55094d80000000 = -4.562e-18 and 0xbcc0b565c0100000 = -4.638e-16 at every scale \u2014 the SAME problem in different units, ~6 decades apart in x/L, both reported as success. (This reproduces the filing's claimed -8.77e-13 vs -4.56e-18 exactly; its emulation was faithful.) (b) FABRICATED FAILURE: for k <= -25 all 200 updates are skipped, H_inv stays frozen at the identity, BFGS is steepest descent with no signal, and the caller gets HSB_ERR_NO_CONVERGENCE on a kappa = 100 quadratic that is not ill-conditioned \u2014 only its unit is small.",
   "correct_threshold": "`if (f64_lt(F64_TINY, sy) == 1)` \u2014 i.e. sy > DBL_MIN, using the `F64_TINY` constant now defined in src/error.cyr. This is a ONE-TOKEN change at line 442 and is the whole repair: DBL_MIN > 0, so the single test carries both duties \u2014 the sign condition BFGS's positive-definiteness needs, and the exact representability bound that makes 1/sy finite. It needs no scale chosen for it. MEASURED to be at least as robust as the bare sign test: over k in [-490,160] both are bit-exact, and in the subnormal-sy regime below k = -495 the sign test admits sy so small that rho = 1/sy overflows (it returns rc = -8 at k = -511..-499) while DBL_MIN returns rc = 0 throughout. If a numerical-quality skip is also wanted, the only scale-free form is sy >= c * ||s|| * ||y|| with c a DIMENSIONLESS cosine \u2014 never an absolute number. Apply the identical repair to the duplicate at optimize.cyr:675.",
   "scale_test": "SHARPENED AND RUN, not proposed. Similarity family f_L(v) = L^2 * f_1(v/L), grad_L(v) = L * grad_1(v/L), x0_L = L * x0_1, tol_L = L * tol_1, with f_1 = (1/2)(u0^2 + 100*u1^2), x0_1 = (1,1), tol_1 = 1e-8, max_iter = 200, L = 2^k. BFGS with H0 = I is exactly covariant under it (every Armijo alpha is chosen identically because f and g.d both scale by L^2), so assert out_x/L BIT-IDENTICAL to the k = 0 result and rc identical. RANGE: use k in [-400, +160] stepping 1, NOT the filing's [-160,160] \u2014 measured, [-160,160] drives sy only over ~3.3e-113..2.7e98 and therefore does NOT kill a 1e-200 or 1e-250 threshold, while [-400,160] drives sy over ~1.7e-257..2.7e98 and does. BRACKETING (measured counts of deviating k over k in [-400,160] step 10, 57 scales): sign sy>0 = 0 deviations, DBL_MIN = 0, 1e-250 = 0; but eps^2 (4.93e-32) = 35 wrong rc, 1e-30 = 35, 1e-16 = 37, SHIPPED 1e-12 = 38, 1e-8 = 39, 1e-50 = 32, 1e-100 = 23, 1e-200 = 7. Every absolute threshold from 1e-8 down through 1e-200 dies; both scale-free repairs survive. DO NOT WIDEN FURTHER: bit-exact covariance holds for every integer k in [-490,+160] (verified exhaustively) and breaks below k ~= -495 where sy itself goes subnormal \u2014 past that the test fails for the wrong reason, which is the trap the filing's own conservative range was avoiding. SECONDARY (weaker, and the distinction is load-bearing): scaling f ALONE by c with x held fixed is NOT exactly covariant, because H0 = I is not covariant under it; for that family assert only rc = 0 and ||out_x - x*|| < tol_x for c in 10^{-20..20}, never bit-identity."
  },
  {
   "site": "/home/macro/Repos/hisab/src/optimize.cyr:609",
   "fn": "opt_lbfgs",
   "guarded_quantity": "yy = _opt_dot(y_newest, y_newest, n) = ||g_new - g_old||^2, the SQUARED norm of the newest stored gradient difference. Dimensions [f]^2/[x]^2. Under the similarity family x -> L*x (f fixed, degree-2 objective) it scales as L^2; under a change of the objective's unit f -> c*f it scales as c^2. Note it is NOT the divisor: the divisor is f64_mul(rho_newest, yy) = yy/sy, which is dimensioned [f]/[x]^2 (a curvature) and is scale-INVARIANT in the similarity family.",
   "protects": "the division gamma = f64_div(_OPT_F64_ONE, f64_mul(rho_newest, yy)) at line 611, i.e. gamma = sy/yy, L-BFGS's initial-Hessian scaling H0 = gamma*I in the two-loop recursion. Nothing else in the block can fail; the surrounding `if (k > 0)` and line 675's store-time `sy > EPSILON_F64` already guarantee y_newest != 0, so yy == 0 exactly is unreachable except through underflow of the sum of squares.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED against the compiled library, not an emulation. Every refutation avenue was checked and failed. (1) Not dimensionless: yy is squared-gradient; measured min yy across a curvature sweep spans 0x3d9bd604b0953453 (6.4e-12) at c=1 down to 0x3c00d0e3917603c3 (2.2e-19) at c=1e-6, and across a similarity sweep from 2^-1018 to 2^262 \u2014 an absolute 1e-12 asks the caller's unit. (2) The caller does not normalise: opt_lbfgs takes an arbitrary f/grad pair and never rescales; the objective's unit is entirely the caller's. (3) It is not a convergence tolerance: convergence is decided only by `f64_lt(gnorm, tol)` at the top of the loop; this guard is on the value path and its miss is silent. (4) It is squared against an unsquared epsilon, so the effective cut is ||g_new - g_old|| <= 1e-6. (5) No existing test is affected \u2014 checked, not assumed: all four live opt_lbfgs fixtures (hisab.tcyr bowl n=3/m=5/60, Rosenbrock n=2/m=7/2000, x^4+x^2 n=1/m=5/40, modules.tcyr bowl n=2/m=5/200000) run with _P609_SKIP = 0, i.e. the guard NEVER fires in the whole suite, and the repair reproduces them bit-identically (identical rc, identical iteration counts 13/673/8/1, identical out_x, identical out_fmin). TWO CORRECTIONS TO THE FILING. (a) Its claim that the cut \"is crossed in the ENDGAME of essentially every run with a tight tol, not only in exotic units\" is REFUTED by measurement: at unit curvature, n=50, tol swept 1e-8, 1e-9, 1e-10, 1e-11, 1e-12, 1e-13 (up to 284 iterations), skip = 0 in every row and min yy is pinned at 6.4e-12, never below 1e-12. The reason is structural \u2014 once line 675 stops storing pairs the ring FREEZES with the last good pair, so yy stops shrinking. This is a units defect, not a generic endgame defect, and the distinction matters because it is why 3574 assertions never saw it. (b) The filing's own covariance sweep (b) does not isolate this guard \u2014 see scale_test. The SECONDARY point is upheld and is in fact the better repair: the guard tests yy while the divisor is rho_newest*yy.",
   "fabricates": "The gamma scaling is silently skipped, so the two-loop recursion runs with H0 = I instead of H0 = (sy/yy)*I \u2014 every search direction wrong by a factor of ~1/curvature, rc unchanged, no error channel. RE-MEASURED ON THE COMPILED LIBRARY (instrumented copy of src/optimize.cyr, real opt_lbfgs, not an emulation), n=50, m=5, f = 1/2*sum_i c*(1+9i/49)*x_i^2, x0 = ones, tol = 1e-9, max_iter = 400, line 675 held scale-free so only this guard varies: c=1e-3 shipped rc=0 in 168 iterations (151 of 167 gamma evaluations skipped) against 27 scale-free; c=1e-4, 1e-5, 1e-6 shipped returns HSB_ERR_NO_CONVERGENCE at 400 iterations (385/399, 389/399, 394/399 skipped) where scale-free returns rc=0 in 24, 20, 17. These reproduce the filing's emulated numbers exactly. With line 675 also shipped the damage is still line 609's: c=1e-2 shipped rc=0 in 356 iterations with 335 of 355 skipped, against 49 with only 609 repaired; c=1e-3/1e-4/1e-5 shipped rc=-8 at 400, against rc=0 in 31/26/21 with only 609 repaired. Worst observed silent case: c=1e-2, rc=0 both ways \u2014 the caller is told the solve succeeded and is handed a point reached in 7.3x the iterations, with out_x differing in the 8th significant digit.",
   "correct_threshold": "Guard the quantity actually divided, at DBL_MIN, and leave the gamma expression alone: `var rho_newest = load64(newest_base + 2 * n * 8); var den = f64_mul(rho_newest, yy); if (f64_lt(den, _OPT_F64_TINY) == 0 && f64_lt(den, F64_INF) == 1) { var gamma = f64_div(_OPT_F64_ONE, den); ... }` with `_OPT_F64_TINY = 0x0010000000000000` (DBL_MIN), matching geo_diff's 2.10.2 rule. No scale constant is needed: den = yy/sy is a curvature ratio and the only thing that can make 1/den fail is den underflowing to zero (gamma = +Inf) or overflowing to +Inf (gamma = 0, H0 = 0, also a fabrication) \u2014 hence both ends. PREFER THIS OVER THE FILING'S \"compute gamma = sy/yy directly and require yy >= DBL_MIN\": sy is NOT stored, only rho = 1/sy, so that form needs a double reciprocal and is measurably NOT bit-preserving \u2014 at c=1, n=50, tol=1e-9, where the guard never fires (skip = 0), the sy/yy form already shifts the trajectory (min yy 0x3d9bd604b0953453 shipped vs 0x3d9bd604b09533e9). The product form is bit-identical to shipped everywhere the guard does not fire (verified on all four live suite fixtures and on the full similarity sweep with 675 shipped, k in [-30, +20]), so the repair is provably confined to the fabricating branch. Guarding yy alone is strictly weaker: yy can be comfortably above DBL_MIN while rho_newest*yy underflows to 0 or overflows to +Inf.",
   "scale_test": "TWO PARTS. The filing's part (a) is right and I confirm it independently; its part (b) is REPLACED because it does not discriminate. (a) REACHABILITY GATE, asserted first: with line 675 shipped, shipped-609 and repaired-609 are BIT-IDENTICAL at every scale \u2014 measured on the n=50 fixture at c=1, x0 = L*ones, tol = L*1e-9, L = 2^k for k in {-30,-20,-10,0,+10,+20}: identical rc, identical iteration counts (81/99/95/106/39/39), skip = 0 throughout, and at k=-30 seen = 0 because 675 stored no pair at all. So ANY covariance sweep run against the shipped tree attributes the whole failure to 675 and proves nothing about 609. This guard must be exercised with 675 already held scale-free (sy >= DBL_MIN), or on a fixture with yy < 1e-12 < sy, which needs curvature < 1 since gamma = sy/yy ~ 1/curvature. (b) COVARIANCE, the replacement, which brackets far more than the filing's k in [-160,160] (that only reaches yy ~ 1e-96 and cannot kill a repair that picked 1e-100 or 1e-200). Use the SIMILARITY family, not the unit-of-f family: for the degree-2 objective f = 1/2*sum_i w_i*x_i^2, set x0 = L*ones and tol = L*1e-9 with f UNCHANGED; every iterate, gradient, Armijo threshold and stored pair is then exactly homogeneous, so out_x/L must be bit-identical. (The unit-of-f family f -> c*f, tol -> c*tol is NOT usable: iteration 0 runs with H0 = I before any pair exists, so the first step is not c-covariant and the trajectory diverges bitwise whatever the guard does \u2014 measured.) Sweep k in {-480,-400,-320,-240,-160,-80,0,+80,+160} with 675 scale-free; assert out_x/L and rc identical to k=0. MEASURED: repaired (product guard) gives out_x/L = 0x3def476c4fed43c6 and 39 iterations at ALL NINE k, with min yy spanning 0x005c2ea43d8035a1 (2^-1018, ~1.1e-306) at k=-480 to 0x505c2ea43d8035a6 (2^262, ~1e79) at k=+160 \u2014 a 1385-decade band that brackets every absolute threshold on yy strictly above DBL_MIN (1e-12, 2.2e-16, 1e-24, 1e-30, 1e-100, 1e-200, 1e-300). Shipped VIOLATES it with three distinct answers for the same problem in three different units: 0x3c689a6b860cfdc0 in 80 iterations with 79 of 79 gamma evaluations skipped for k <= -80, 0x3dbda224c1a69ea4 in 49 iterations with 21 of 48 skipped at k=0, 0x3def476c4fed43c6 in 39 with 0 skipped for k >= +80. Do NOT extend to k=-500: the objective itself goes subnormal there (f = 0x1537) and both variants degrade, so that row discriminates nothing. Pair this with the reachability fixture that moves rc, since covariance alone only shows a slowdown at c=1: n=50, m=5, f = 1/2*sum_i c*(1+9i/49)*x_i^2, x0 = ones, tol = 1e-9, max_iter = 400, 675 scale-free, c = 1e-4 \u2014 shipped returns HSB_ERR_NO_CONVERGENCE at 400, repaired returns rc=0 in 24. A fixture that does not satisfy yy < 1e-12 < sy is worthless: a 2-D spherical quadratic is effectively 1-D, where one stored pair reproduces the exact Newton step whatever H0 is, and all four guard combinations come back bit-identical."
  },
  {
   "site": "/home/macro/Repos/hisab/src/optimize.cyr:675",
   "fn": "opt_lbfgs",
   "guarded_quantity": "sy = _opt_dot(s_k, y_k, n) = (x_new - x) . (g_new - g), the curvature product of the pair about to be written into the history ring. Dimensions [x]*([f]/[x]) = [f] \u2014 the units of the OBJECTIVE. Invariant under a change of the variables' unit, linear in the objective's unit; unbounded above and below, and driven toward 0 by convergence itself on any problem whose minimum is 0.",
   "protects": "the division f64_div(_OPT_F64_ONE, sy) stored as rho in the ring slot (line 679), and the curvature condition sy > 0 that keeps the implicit L-BFGS Hessian positive definite so the two-loop recursion (lines 565-650) returns a descent direction. rho is subsequently MULTIPLIED by dot products of magnitude ~||s||*||q|| (line 587) and ~||y||*||q|| (line 633), so what the guard must bound is rho relative to those vectors, not rho alone.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Every refutation avenue was tested and failed. (1) NOT dimensionless: sy carries the objective's unit; it is a raw dot product of two caller-supplied difference vectors. (2) NO caller normalisation is possible: f, grad, x0 and tol all arrive through function pointers from an arbitrary consumer; nothing in opt_lbfgs rescales them, and the only other absolute constants in the loop (Armijo's alpha0 = 1.0 and c1 = 1e-4) are dimensionless in the length-unit family, which the sweep confirms \u2014 with lines 675 and 609 both made scale-free the whole solver is BIT-IDENTICAL at 41 of 41 scales, so 100% of the observed scale dependence is these two guards. (3) NOT a convergence tolerance: the convergence test is the separate caller-supplied `f64_lt(gnorm, tol)` at line 554; this one gates a data-structure write and a reciprocal. (4) NO existing test blocks the repair \u2014 all four in-tree call sites (hisab.tcyr:1162 bowl3, :1193 Rosenbrock, :1313 x^4+x^2, modules.tcyr:4370 bowl2) were re-run under five guard variants (shipped 1e-12, sy>0, sy>=DBL_MIN, relative, 1e-24) and every variant passes every assertion: rc = HSB_ERR_NONE, |x - x*| < opt_tol6, f* < TOL_1EM12. (5) The scale test DOES discriminate and DOES bracket (numbers below). \u2b50 SHARPENING THE FILING'S SEVERITY: the defect is not confined to exotic units \u2014 it is already firing AT UNIT SCALE inside this repo's own fixtures, because a problem converging to a zero minimum drives sy below 1e-12 near the solution while sy/(||s||*||y||) stays O(1). Measured at k=0: in-tree bowl3 rejects 3 of 11 curvature pairs (13 iterations vs 11); Rosenbrock rejects 1 of 34; the n=50 diagonal bowl rejects 12 of 39 and takes 106 iterations where the scale-free guard takes 39 \u2014 2.7x, at unit scale, with no change of units at all. The suite passes only because max_iter is ~3x the measured count, which is exactly the margin hisab.tcyr:1105 says is there to catch a solver \"degraded to plain steepest descent\".",
   "fabricates": "The pair is not stored, so k never increments; with k = 0 the two-loop recursion is a no-op, d = -g, and L-BFGS is EXACTLY steepest descent \u2014 the advertised algorithm silently deleted and reported through the ordinary NO_CONVERGENCE path, indistinguishable from a hard problem, with out_x written anyway. Measured on fixture (i) (2-D bowl, m=8, tol_L = L*1e-8, max_iter 200), boundary scanned at every integer k: k <= -23 returns rc = -1 at 200 iterations having stored 0-2 pairs, with x0/L = +1.19e-2 against a converged -1.25e-11; k >= -13 returns rc = 0 in 6 iterations. \u26a0 CORRECTING THE FILING'S OWN MEASUREMENT: on fixture (ii) (n=50) the shipped guard never returns rc = -1 in my run \u2014 it returns rc = 0 at 39/81/90/106 iterations depending on scale (106 at k=0, 39 at large |k|), NOT \"rc -1 / 400 iters at k <= -10, 356 at k=0, 32 at k >= 10\". The n >> m damage there is a silent 2.7x iteration inflation, not a NO_CONVERGENCE, so a scale test that asserts only rc would MISS it on that fixture.",
   "correct_threshold": "PRIMARY: the dimensionless relative curvature test sy >= c * ||s_k|| * ||y_k|| with c a machine-epsilon-scale cosine tolerance (both norms already available via _opt_norm), which is scale-free AND is the only form that bounds rho relative to the dot products it multiplies at lines 587 and 633. MINIMUM ACCEPTABLE: the sign test sy > 0 together with sy >= DBL_MIN for the reciprocal. \u26a0 SHARPENING: the bare sign test is scale-free but NOT sufficient \u2014 measured, a pair with sy = 1e-300 at ||s|| = ||y|| = 1 (near-orthogonal, i.e. catastrophic cancellation in _opt_dot, not smallness of scale) gives rho = 1e300, ai = 1e300, and beta = -inf inside the second loop, poisoning q and hence x; only the relative form rejects it. Never an absolute number. The same repair must be applied in the same change to line 609 (yy = y.y, units [f]^2/[x]^2, equally absolute) and to the identical guard at line 442 in opt_bfgs.",
   "scale_test": "Similarity family, bit-exact: f_L(u) = L^2*f_1(u/L), grad_L(u) = L*grad_1(u/L), x0_L = L*x0_1, tol_L = L*tol_1, L = 2^k. L-BFGS with H0 = I is exactly covariant under it (verified: 0 of 41 points deviate once both guards are scale-free), so assert ALL THREE of rc identical, ITERATION COUNT identical, and out_x/L BIT-IDENTICAL to the k=0 result \u2014 the iteration count and bit-identity are load-bearing, because on fixture (ii) rc alone does not discriminate. \u26a0 THE TEST IS JOINT OVER LINES 675 AND 609, measured: repairing 675 alone to sy > 0 while line 609 keeps EPSILON_F64 still deviates at 6 of 17 points on (i) and 10 of 11 on (ii), so a 675-only patch fails this sweep and both must land together. Fixtures: (i) f_1 = 1/2(x0^2 + 100*x1^2), x0_1 = (1,1), tol_1 = 1e-8, m = 8, max_iter = 200; (ii) n = 50, m = 5, f_1 = 1/2*sum_i (1 + 9i/49)*x_i^2, x0_1 = all ones, tol_1 = 1e-9, max_iter = 400 \u2014 the n >> m case. Both f_1 are degree-2 homogeneous, so f_L is the SAME function and the sweep is literally a change of the length unit. BRACKETING, measured on (i) over k in [-160,160] step 8 (41 points; sy = L^2*sy_1 spans ~1e-96..1e96): shipped 1e-12 deviates at 19/41, a repair to 1e-16 at 18/41, to eps^2 = 1e-24 at 17/41, to 1e-50 at 11/41 \u2014 every absolute repair fails \u2014 while sy>=DBL_MIN and the relative form deviate at 0/41. On (ii) over k in [-160,160] step 16 (21 points): shipped 20/21, eps^2 10/21, sign+DBL_MIN 0/21, relative 0/21. DBL_MIN cannot be bracketed without the objective itself underflowing, which is why it is the correct floor. ADD A UNIT-SCALE ASSERTION TOO (k = 0, no scaling), because the defect fires there: assert the stored-pair count equals the accepted-step count on the in-tree bowl3 fixture \u2014 shipped stores 8 of 11 and the repair stores 11."
  },
  {
   "site": "src/collision_core.cyr:319",
   "fn": "sequential_impulse",
   "guarded_quantity": "inv_mass = ColContact_inv_mass_a(c) + ColContact_inv_mass_b(c) \u2014 the sum of the two bodies' inverse masses, read verbatim out of the ColContact struct. Dimensions mass^-1. Not dimensionless, not bounded, not normalised anywhere: contact_new / contact_new_fric (collision_core.cyr:236-259) store both fields with no clamp, no validation and no scaling, and the module header declares no unit convention for them.",
   "protects": "Two divisions by inv_mass and nothing else: line 357 `f64_div(f64_sub(v_n_eff, v_bias), inv_mass)` (normal impulse \u0394\u03bb) and line 403 `f64_div(v_t_eff, inv_mass)` (tangential update). Note the sibling guard at line 397, `f64_gt(fric, EPSILON_F64)`, is on the Coulomb coefficient \u2014 genuinely dimensionless and bounded \u2014 and is CORRECT. The same constant is used correctly 78 lines later, so :319 is a mis-application, not a house convention.",
   "protects_note": "n/a",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED BY MEASUREMENT; every refutation avenue was tried and each failed against the tree. (1) Not dimensionless/bounded: inv_mass is 1/mass, caller-supplied. (2) No caller normalises \u2014 `sequential_impulse` has ZERO in-tree callers (grep over src/, examples/, dist/: only its own definition), so the caller is a consumer and the constructor validates nothing, which makes the \"in practice it is O(1)\" defence unavailable. (3) Not a convergence tolerance \u2014 the convergence parameter is `iterations`, and the function's own comments prove the solver is idempotent after one sweep (\u03bb = (1+e)\u00b7pen/inv_mass); :319 is a per-contact entry predicate sitting directly on top of two divisions. (4) Not a behaviour-preserving repair problem: I copied collision_core.cyr to scratch, changed :319 to `f64_lt(inv_mass, F64_TINY)`, and ran ALL FIVE suites against the patched module \u2014 1827 + 776 + 233 + 416 + 359 = 3611 assertions, 0 failed (baseline on the unpatched tree is identical). Every fixture in the tree uses inv_mass \u2208 {0, 1.0, 2.0}; the both-static case (modules.tcyr:5266, inv_mass = 0) still skips, because 0 < DBL_MIN. (5) The scale test discriminates \u2014 measured, see scale_test. \u26a0 ONE CORRECTION TO THE FILING'S OWN ARITHMETIC: micrograms do NOT trip it. The guard fires when total inv_mass < 1e-12, i.e. when the effective mass exceeds 1e12 in whatever unit the caller chose; two 1000 kg bodies give 2e-3 kg^-1, 2e-9 \u00b5g^-1 (safe), 2e-12 ng^-1 (still safe, 2e-12 > 1e-12), and 2e-18 pg^-1 (fires). The claim is unchanged and is now exact: the same physical pair silently stops separating once the caller expresses mass in nanograms or finer \u2014 measured first failure at inv_mass = 2^-40 = 9.0949e-13 kg^-1.",
   "fabricates": "The whole contact branch is skipped, so both output slots keep the zeros the entry loop wrote: normal impulse exactly +0.0 and friction impulse exactly +0.0, returned with rc = HSB_ERR_NONE (0) and no diagnostic. MEASURED on the shipped binary: over k = 0\u20261000 (inv_mass = 2^-k, pen = 1.0, e = 0, \u00b5 = 0), 961 of 1001 configurations returned bit-exact 0.0 with rc = 0, first at k = 40; the correct \u03bb there is 2^40 = 1.0995e12, a perfectly finite double. A heavy-but-finite contact therefore never separates and the bodies interpenetrate freely, and the output is bit-identical to the correct static-static result, so no caller can distinguish the two.",
   "correct_threshold": "`f64_lt(inv_mass, F64_TINY)` \u2014 error.cyr:45, DBL_MIN, already in scope (error.cyr is a declared requirement of collision_core.cyr; the patched build compiles and all 3611 assertions pass). It is simultaneously the divide guard and the physical predicate, rejects negative inv_mass exactly as the current form does, and needs no scale chosen for it. \u26a0 SHARPENED \u2014 there are TWO scale-free candidates, not one, and the filing's own `why` argues for the other: `f64_le(inv_mass, _COL_F64_ZERO)` (\"both static == inv_mass EXACTLY 0\"). They are NOT interchangeable, measured: over k = 1024\u20261073 (inv_mass subnormal) the exact-zero form lets the division run and returns +Inf in 50 of 50 configurations \u2014 a fabricated infinite impulse, strictly worse than the zero it replaces \u2014 while F64_TINY returns the documented static-skip 0.0. F64_TINY is therefore the repair, at a measured cost of exactly ONE over-conservative k: at k = 1023, \u03bb = 2^1023 is finite and representable yet F64_TINY skips. That is the deliberate price of the repo's DBL_MIN rule and it errs toward the documented value rather than toward +Inf; it should be stated in the comment rather than left for the next reader to rediscover.",
   "scale_test": "MASS-UNIT COVARIANCE OF THE SOLVED IMPULSE, extended past the filing's range. Fixture: pen = 1.0, restitution = 0, friction = 0, tangent_vel = 0, inv_mass_a = inv_mass_b = 2^-(k+1) so inv_mass = 2^-k exactly; call `sequential_impulse(v, 1, 1, out)`. Closed form \u03bb = pen/inv_mass = 2^k, exact in binary, so every assertion is a bit-compare. Powers of two, not ten. THREE CLAUSES, asserted separately (a zeroed run makes any ratio-of-two-runs read \"consistent\"): (a) rc == HSB_ERR_NONE and the normal slot is STRICTLY POSITIVE \u2014 never the fabricated 0.0; (b) \u03bb(k) == 2^k bit-for-bit; (c) \u03bb(k) is FINITE \u2014 never +Inf. \u26a0 SWEEP k = 0 \u2026 1074, not 0 \u2026 1000. MEASURED discrimination on this tree (first k at which clause (a)/(b) fails): shipped EPSILON_F64 = 1e-12 \u2192 k = 40; a \"tightened\" 1e-15 \u2192 k = 50; a loosened 1e-9 \u2192 k = 30; EPSILON_F64\u00b2 = 1e-24 \u2192 k = 80; F64_TINY \u2192 never over 0\u20261000 (0 of 1001 failures). So a 12-decade sweep kills the shipped constant and nothing else, exactly as 2.11.1's quaternion sweep did. \u26d4 THE FILING'S OWN 0\u20261000 RANGE IS STILL TOO SHORT: it cannot separate F64_TINY from `f64_le(inv_mass, 0)` \u2014 measured identical over k = 0\u20261022 \u2014 and they diverge in the subnormal band, where exact-zero returns +Inf for k = 1024\u20261073 (50 of 50) and F64_TINY returns 0.0. Clause (c) over k \u2265 1023 is the only thing that discriminates them, so the sweep must reach 1074. CONTROL: k = 0 \u2026 \u221260 (inv_mass growing, \u03bb shrinking) must pass all three clauses ON THE SHIPPED CODE \u2014 measured 0 of 61 failures, which proves the positive-k failures are the threshold and not overflow, the fixture, or the solver's algebra. Probes and patched module copies under /tmp/claude-1000/-home-macro-Repos-hisab/68915bf3-8a70-4292-b984-a0be0a4996e8/scratchpad/eps/refute/ (src/ untouched). \u26a0 My own first probe was wrong before the code was: the subnormal constructor shifted by a NEGATIVE amount and reported first_inf = 1022 for the exact-zero variant; the numbers above are from the corrected build."
  },
  {
   "site": "src/collision_core.cyr:443",
   "fn": "_col_xy_greater",
   "guarded_quantity": "|ax - bx| \u2014 the absolute difference of two caller-supplied HVec2 x-coordinates. Dimensions: LENGTH, in whatever unit the caller's points carry; metres \u2192 millimetres multiplies it by 1000. Compared against EPSILON_F64, verified to decode to exactly 1e-12 (src/error.cyr:26, 0x3D719799812DEA11).",
   "protects": "No arithmetic operation \u2014 no division, no sqrt, no domain. It is the equality leg of an ORDERING PREDICATE: it suppresses the \"a is greater\" answer when the two x's are within 1e-12, so the (x, then y) sort in `_col_sort_indices_by_xy` (heapsort, :511) falls through to the y tie-break at :445. The comment at :435 states its only rationale outright \u2014 bug-compatibility with the pre-2.6.15 insertion sort \u2014 never that 1e-12 is right for any caller's unit.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Every refutation route was tried against the code and closed by measurement (simulation transliterated line-for-line from :427-:630 \u2014 comparator, sift-down, heapsort, both hull passes, `c <= +0` pop \u2014 checked against an exact-Fraction reference hull). (1) NOT dimensionless/bounded: the operand is a raw coordinate difference. (2) NO caller normalisation: `convex_hull_2d` (:533) is a public entry point that validates only `points == 0` and `n > vec_len(points)`, then sorts the caller's vec unchanged; there is no bbox pass, no scaling, and the private sort is additionally called directly by tests/modules.tcyr:6202. (3) NOT a convergence tolerance: the guard sits in a comparator and protects no operation. (4) The proposed exact comparison breaks NO existing test \u2014 all seven fixtures in tests/*.tcyr reproduce identically (square+interior 4, triangle 3, collinear 2, duplicate corner 4, ab_line 2, ab_same 6-coincident 2, the 2000-point fingerprint 17, zero inversions, permutation intact); the smallest nonzero x-gap in the 2000-point fixture is 1.0, i.e. 1e12x the band, so the band is provably inert across the entire suite. \u2b50 THE FILING'S SEVERITY MECHANISM (1) IS WRONG AND THE REAL ONE IS WORSE. \"Whole set inside a 1e-12 box \u2192 sorted purely by y \u2192 not a valid input to the monotone chain\" does not hold: 300/300 random 12-point sets inside a 1e-13 box returned a hull that was convex, CCW and set-identical to the exact reference \u2014 sorting purely by y is just the monotone chain with the axes swapped, which is still correct. The damage is in the INTERMEDIATE regime, and it is present AT UNIT SCALE: with x-values whose gaps straddle the band, the shipped code returned a hull with spurious interior vertices in 1788/4000 trials and a REPEATED INDEX in 448/4000, at coordinates in [0,1] (five generators, 20,000 trials; the exact comparator scored 0 nonconvex / 0 missing / 0 spurious / 0 duplicate on every one \u2014 the control is clean). Minimal reproducer, all coordinates exactly representable: (0,0), (1,0), (0.5+2^-41, 0.25), (0.5, 0.25) \u2014 2^-41 = 4.547e-13 < 1e-12 \u2014 where `convex_hull_2d(pts, 4)` returns FIVE indices [0,2,3,1,2] for four points, index 2 twice, and the polygon is not convex; the exact comparator returns [0,1,2,3]. \u26a0 The module's own sort test cannot ever see this: tests/modules.tcyr:6207 asserts \"zero inversions\" using `_col_xy_greater` as its own oracle \u2014 measured 0 inversions on the broken order above. The filing's intransitivity claim is confirmed and is scale-independent: for {(0,2),(6e-13,1),(1.2e-12,0)} all SIX input permutations come out non-lexicographic ([2,1,0], [0,2,1], [1,0,2]).",
   "fabricates": "Returns 0 (\"not greater\") for a point whose x IS strictly greater, and there is no error channel \u2014 the function returns only a vec. Measured, the fabrication is stronger than \"a plausible polygon\": on the 4-point unit-scale fixture the public `convex_hull_2d` returns a vec of FIVE indices for FOUR input points with one index REPEATED and a non-convex, self-touching outline; at 60% of near-tie configurations it returns a hull containing strictly interior points. A caller that shoelaces or renders it gets a wrong area and a degenerate edge. Note the one cheap consequence: `vec_len(result) > n` and a repeated index are both scale-free, tolerance-free detectable \u2014 the output is malformed, not merely inaccurate.",
   "correct_threshold": "No epsilon \u2014 an exact comparison. The x leg becomes `if (f64_gt(ax, bx) == 1) { return 1; }` and the tie-break tests exact equality (`f64_eq(ax, bx)`). Exact lexicographic order is total, transitive and scale-free BY CONSTRUCTION. \u26d4 Explicitly reject the tempting repair: `_GEO_F64_TINY` = DBL_MIN, which 2.10.2 installed as this repo's standing rule for divide guards, is WRONG here and is measurably the most dangerous choice \u2014 it is a band, so it is still intransitive and still order-dependent, and it evades the filing's own scale sweep entirely (see scale_test). The 2.10.2 rule is \"guard exactly what makes the DIVISION fail\"; this guard protects no division, so the right amount of guard is none. If near-coincident points genuinely must be merged, that is a caller-supplied tolerance in the caller's units and belongs in the signature.",
   "scale_test": "Three legs; the filing's single sweep is necessary but NOT sufficient \u2014 measured, it fails to bracket the likeliest repair. (A) PRIMARY, arithmetic-free \u2014 SPACING SWEEP ON THE SORT. For j = -1074..0, \u03b4 = 2^j, feed {(0,2), (\u03b4,1), (2\u03b4,0)} to `_col_sort_indices_by_xy` and assert the index order is exactly [0,1,2]. No cross products, so no underflow confound, and it fails at k=0 scale. Measured wrong-order decades: shipped 1e-12 \u2192 1035/1075 (largest \u03b4 that fools it 2^-40); 1e-15 \u2192 1025; 1e-9 \u2192 1045; 1e-24 \u2192 995; **DBL_MIN \u2192 52 (largest \u03b4 2^-1023)**; exact \u2192 0. This is the ONLY leg that brackets a DBL_MIN band. (B) MALFORMED-OUTPUT ASSERTION on the public entry point, scale-free and tolerance-free: pts = (0,0), (1,0), (0.5 + 2^-41, 0.25), (0.5, 0.25); assert `vec_len(convex_hull_2d(pts,4)) == 4` and that no index repeats. Shipped returns [0,2,3,1,2]. Assert \"no repeated index\" on every existing hull test too \u2014 it is the cheapest tree-wide canary. (C) COORDINATE-UNIT COVARIANCE, corrected from the filing. Keep the \u00b12^k fixture and the bit-identical-indices invariant, but: sweep k = -500..0 ONLY, and run the exact comparator over the same sweep as the control that ESTABLISHES the window \u2014 measured, the exact comparator itself holds for k \u2265 -537 and fails at k \u2264 -538 from cross-product underflow, so the filing's \"sweep \u00b1500\" is inside the valid window by 37 steps by luck, and a sweep to \u00b1800 would fail the CORRECT code. \u26a0 The filing's k>0 half is NOT a valid arithmetic-health control: it passes on shipped code all the way to k = +900, where every cross product has overflowed to inf. \u26a0 Its adversarial pair (0,10)/(\u03b4,0) is decorative \u2014 deleting it moves the shipped break from k = -37 to k = -39 (464 vs 462 failing k), because that point never reaches the hull; the discrimination comes from the interior cluster. \u26a0 Its predicted break points are wrong: measured 1e-12 \u2192 k=-37, 1e-15 \u2192 -47, 1e-9 \u2192 -27, 1e-24 \u2192 -77 (not -0/-10/+10/-40), and DBL_MIN and any band below 2^-1023 \u2192 NO BREAK AT ALL, indistinguishable from exact. Leg (C) alone would sign off a DBL_MIN repair."
  },
  {
   "site": "src/collision_core.cyr:445",
   "fn": "_col_xy_greater",
   "guarded_quantity": "|ax - bx|, the absolute difference of two raw caller x-coordinates read straight out of HVec2 (HVec2_x(pa), HVec2_x(pb)) with no normalisation anywhere on the path. Dimensions LENGTH, degree 1 in the caller's units, unbounded. Compared against EPSILON_F64 = 0x3D719799812DEA11 = 1e-12 (src/error.cyr:26, verified unchanged on the live tree). This is the tie-break leg: \"if the x's are within 1e-12, decide the order by y instead.\"",
   "protects": "No arithmetic operation \u2014 no division, no sqrt, no acos, no iteration. It selects which coordinate is the sort key for the pair. The only thing it can be said to protect is a policy of treating near-equal x as equal, and that policy is unimplementable at ANY threshold: a band |ax-bx| < e is not transitive, so it cannot be an equality. Measured 3-cycle at unit scale with the shipped constant: a=(0,10), b=(6e-13,5), c=(1.2e-12,0) gives greater(a,b)=1, greater(b,c)=1, greater(c,a)=1. Line 443 is the other half of the same predicate; the two share ONE constant, so they are consistent today and must be repaired together.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ROUTES; ALL FIVE ARE DEAD, MEASURED. (1) Not dimensionless/bounded: |\u0394x| is a raw coordinate difference, degree 1 in caller units. (2) No caller normalisation: `grep -rn '_col_xy_greater|_col_sort_indices_by_xy' src/` gives exactly one caller chain \u2014 _col_sift_down -> _col_sort_indices_by_xy -> convex_hull_2d:562 \u2014 and convex_hull_2d does no bounding-box scaling; it is a public entry point on raw caller coordinates. (3) Not a convergence tolerance: nothing iterates or divides here. (4) The proposed exact-equality repair does NOT break any existing test \u2014 MEASURED, not assumed: I applied it to a copy of the live tree (`if (f64_gt(ax,bx)==1) { return 1; } if (f64_lt(ax,bx)==0) { y tie-break }`) and all five suites produced BYTE-IDENTICAL output (hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776 = 3611 assertions, 0 failures on both sides, `diff` clean on all five). The suites are simply blind to this guard: no fixture anywhere has an x-gap inside the band. (5) The scale test does discriminate \u2014 see scale_test. \u26d4 TWO CORRECTIONS TO THE ORIGINAL FINDING. (a) Its partial-repair argument is mis-derived: TIGHTENING 443 does not produce a \"neither greater nor x-equal\" gap, it breaks ANTISYMMETRY \u2014 measured with 443 at 1e-15 and 445 at 1e-12, a=(5e-13,0) vs b=(0,5) gives greater(a,b)=1 AND greater(b,a)=1. The no-tie-break gap it describes comes from WIDENING 443 (443 at 1e-9, 445 at 1e-12: a=(1e-10,0) vs b=(0,5) returns 0 in both directions for a strictly greater point). (b) The usual remedy for this verdict \u2014 a relative band, |\u0394x| < e*(|ax|+|bx|) \u2014 DOES NOT REPAIR THIS SITE. Any band, absolute or relative, is intransitive and yields the same cyclic comparator; making it scale-covariant only moves which unit system is broken. Exact equality is the only repair, and that is why the verdict understates the class even while the label is right: the scale dependence is real (a length against a constant, the caller's unit picks the regime) but it is a symptom, and the disease is a band used as an equivalence relation inside an ordering predicate.",
   "fabricates": "Confirmed and WORSE than claimed, measured on the compiled library (not simulated) via a probe built against a pristine copy of the tree. Single comparison: greater((0,10),(1e-13,0)) = 1, i.e. \"greater\" for the point with the strictly SMALLER x. Escaping into the public API at UNIT SCALE \u2014 points {(0,10), (5e-13,5), (1.2e-12,0), (1,0), (1,10)}, strictly convex, no three collinear, true hull all 5 vertices \u2014 convex_hull_2d returns a well-formed CCW polygon that DROPS a true extreme point and encloses 25% less area: 2A = 14.999999999994 (bits 4624633867356074702) where the truth is 19.999999999988 (bits 4626322717216338638). rc-free, no diagnostic, correct winding, correct vec type. At every scale k <= 0 it additionally returns a ROTATION of the correct hull (60 of 121 k in the probe's sweep) \u2014 a distinct, milder failure that the area assertion cannot see, which is why the test needs both assertions. And because the comparator is cyclic (3-cycle above), the heapsort's output is not merely \"y-major inside the band\" \u2014 it is an arbitrary permutation, so no bound on the hull error can be argued from the band width.",
   "correct_threshold": "Exact equality, in FLOAT comparison semantics \u2014 NOT an i64 bit compare. The tie-break leg fires only when neither f64_lt(ax,bx) nor f64_gt(ax,bx) holds; line 443 becomes an unconditional `if (f64_gt(ax,bx) == 1) { return 1; }`. Together the two legs are then the exact lexicographic order: total, transitive, antisymmetric, scale-free, and needing no constant at all \u2014 the strongest form of \"guard exactly what makes the operation fail and nothing more\", since nothing here can fail. \u26a0 The original wording \"ax == bx bit-exactly\" is a hazard, not a nit: this repo already recorded (2.13.0) that an antiparallel cross product returns NEGATIVE ZERO, and a raw i64 compare of -0.0 against +0.0 is false while f64_lt/f64_gt are both false \u2014 so a bit compare silently DROPS the y tie-break for such a pair and leaves their order to heapsort's internal swap sequence rather than to the specification. I could not turn that into a differing hull in two attempts, so it is a specification defect in the repair wording, not a demonstrated second bug. Verified: the f64-semantics form above passes the full covariance probe 4/4 and leaves all 3611 existing assertions byte-identical.",
   "scale_test": "SHARPENED, and every number below is measured \u2014 the original sweep works but its stated control is wrong and its assertion is too weak. FIXTURE (strictly convex, so the \"it's a collinear degenerate\" objection cannot be raised): P(s) = {(0,10s), (5e-13*s, 5s), (1.2e-12*s, 0), (s,0), (s,10s)}, true hull = all 5 indices. ASSERT TWO scale-invariants, both taken against a REFERENCE COMPUTED AT k = +16, never a hand-written constant: (a) 2A/s^2 bit-identical at every k, (b) hull length and first hull index identical at every k. \u26a0 My first probe hand-wrote the expected area as 20 and FAILED ON CORRECT CODE \u2014 the true value is 19.999999999988. Check the probe before believing the probe. SWEEP k = -500..+500 CONTIGUOUSLY, s = 2^k (powers of two, so scaling is exact and the assertions are bit-exact with no tolerance; the repo's usual 1e-k decades would force one). CONTIGUITY IS LOAD-BEARING: each candidate threshold produces a wrong AREA at exactly ONE k \u2014 shipped 1e-12 at k=0, tightened 1e-15 at k=-10, widened 1e-9 at k=+10, squared 1e-24 at k=-40 \u2014 because the catastrophic regime is the single decade where the band SPLITS the point set, not the decades where it swallows it whole; a sparse sweep {-3,0,+3} kills one mutant of four. The index-sequence assertion has the wide window (shipped wrong for all k <= 0, squared 1e-24 for k <= -39) and is what kills the deep mutants; the area assertion is what proves the failure is a genuinely wrong hull rather than a benign rotation. Both are required. \u26d4 \"k > 0 as the control\" IS WRONG: the widened-1e-9 mutant is still wrong at k = +10, and on a pair-only fixture the SHIPPED constant is still wrong through k = +3. The control must sit above the widest candidate band \u2014 k >= +14 on this fixture. The +-500 bracket is arithmetically SAFE here, measured: with the exact predicate the hull and 2A/s^2 are bit-identical across all 1001 values of k, with the largest shoelace term 1.07e303 at k=+500 and the smallest 4.67e-313 at k=-500, so the sweep asserts something true about this code rather than something false about doubles. LIVE RESULT of exactly this probe (k = -60..+60 for runtime): pristine tree 3 of 4 assertions FAIL (area wrong at 1 of 121 k, index sequence wrong at 60 of 121, and 2A = 15.0 vs 20.0 at s = 1); with the exact-equality repair, 4 of 4 PASS."
  },
  {
   "site": "src/collision_core.cyr:706",
   "fn": "_col_ring_is_convex",
   "guarded_quantity": "cross = _col_triangle_area_2x(prev, p, next) = (bx-ax)(cy-ay) \u2212 (cx-ax)(by-ay), TWICE THE SIGNED AREA of the triangle at ring vertex p. Dimensions: LENGTH SQUARED. Compared against the unsquared EPSILON_F64 (1e-12, src/error.cyr:26). The identical comparison is re-derived inline at the ear gate, :800 (CCW) / :802 (CW) \u2014 four sites, one defect; the :692 comment binds them (\"the SAME EPSILON_F64 threshold as the ear gate below\").",
   "protects": "No arithmetic operation \u2014 no division, no sqrt, no domain restriction. It is a convexity/degeneracy CLASSIFICATION band: a collinear (zero-area) vertex must not be called strictly convex and clipped as a sliver ear. Verified as stated \u2014 nothing downstream of the comparison divides by or roots `cross`.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "I could not refute it on any of the five grounds, and each attempt was measured on the real shipped library (cycc 6.6.2), not argued. (1) NOT dimensionless: `cross` is an area, so scaling coordinates by 2^k moves it by exactly 2^2k. Every candidate constant breaks precisely where 2^2k crosses it \u2014 measured first-failing k: 1e-9 \u2192 \u221215, shipped 1e-12 \u2192 \u221220, 1e-15 \u2192 \u221225, 1e-24 \u2192 \u221240, 1e-300 \u2192 \u2212499, DBL_MIN \u2192 \u2212511. That is dimensional dependence, demonstrated, not asserted. (2) NO caller normalisation: `triangulate_polygon` is a public bundle entry point; vertices go from `vec_get` straight into the determinant, and the only in-tree callers are tests and benchmarks. (3) It IS a classification band rather than a divide guard, exactly as filed \u2014 but that framing does not rescue an absolute constant applied to an area; the band is the whole failure mechanism. (4) THE REPAIR BREAKS NOTHING \u2014 checked, not assumed: with the exact sign test installed at all four sites (mutant-installed proof: 4 grep hits) all five shipped suites pass 3611/3611 (hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776), and the relative-determinant-bound variant also passes 3611/3611. Every triangulate fixture (tp_q/tp_c/tp_h/tp_u/tp_w/tp_l/tpc/tpd, abuse ab_line/ab_same) uses small-INTEGER coordinates, so every `cross` is an exact integer \u2014 0 or |cross| \u2265 1 \u2014 and 1e-12 is behaviourally identical to 0 on all of them; index sequences are identical too. The shipped suite therefore cannot discriminate the defect from the fix. (5) The scale test DOES discriminate: 2886 of 3006 assertions fail on shipped code, and the k>0 half passes 3006/3006 on shipped code, so the failure is the threshold and not the fixture, the winding or overflow. SEVERITY, real code, only the caller's unit changed on one fixed 12-gon: 6 m wide \u2192 30/30 complete; 6 mm \u2192 30/30; 6 MICRONS \u2192 9/30; 6 nm \u2192 0/30; 6 km \u2192 30/30. \u26a0 The one thing I did refute is the finding's own bracketing claim \u2014 see scale_test.",
   "fabricates": "Not a plausible NUMBER \u2014 a plausible DIAGNOSIS, which is why it is worse than it looks. A strictly convex vertex is classified \"not convex\", stored as blocked[v] = 1, and re-rejected by the inline ear gate, so `if (ear_found == 0) { return result; }` (:851) fires early and the function returns a SHORT but well-formed index vec: measured 9 of 30 indices for a 6-micron-wide 12-gon, 0 of 30 at 6 nm. Whole triples, no rc, no error channel. The header at :640-653 tells callers to read exactly that signal as \"ear clipping is only defined for SIMPLE polygons; a self-intersecting input has no ear decomposition and a short return is the honest answer\". So the length channel EXISTS but carries the wrong meaning: a valid, simple, non-self-intersecting polygon is reported as non-simple, bit-identically to a genuinely degenerate one and to the n < 3 convention. A caller doing exactly what the contract instructs is misled.",
   "correct_threshold": "The SIGN of the determinant \u2014 `f64_gt(cross, _COL_F64_ZERO)` for the CCW branch, `f64_lt(cross, _COL_F64_ZERO)` for the CW branch \u2014 at all FOUR sites (:706, :709, :800, :802), which must move together or the :692 contract is broken. There is no arithmetic operation to protect here, so there is no quantity from which a nonzero threshold could be DERIVED, and any nonzero constant is therefore a scale chosen for it. This is already the file's own convention for the identical quantity: `convex_hull_2d` compares `_col_cross2d` against `_COL_F64_ZERO` exactly (:580/:608 \u2192 `f64_le(c, _COL_F64_ZERO)`), and `_col_point_in_tri` (:671-673) tests its three determinants against exact zero. Polarity is preserved: cross == 0 stays \"not convex\", so collinear vertices remain blockers and the deliberate tie handling recorded in issues/archived/2026-08-04-perf-triangulate_polygon.md:260 is untouched. \u26a0 Do NOT reach for `_GEO_F64_TINY`/DBL_MIN by analogy with 2.10.2 \u2014 that rule was \"guard exactly what makes the DIVISION fail\", and there is no division here; measured, DBL_MIN is a defect at this site (it breaks at k = \u2212511). If a genuine degeneracy band is ever wanted, the only defensible form is the forward-error bound on the 2\u00d72 determinant, |cross| <= 4\u00b7DBL_EPSILON\u00b7(|(bx-ax)(cy-ay)| + |(cx-ax)(by-ay)|), which is scale-covariant by construction; I built it and it passes the full sweep in both directions and all 3611 shipped assertions.",
   "scale_test": "POLYGON-SCALE COVARIANCE OF THE TRIANGULATION. Fixture: a 12-vertex staircase L with integer coordinates, CCW \u2014 (0,0),(6,0),(6,1),(5,1),(5,2),(4,2),(4,3),(3,3),(3,4),(2,4),(2,5),(0,5) \u2014 run in BOTH windings so :706 and :709 are both hit, every coordinate multiplied by s = 2^k (powers of two, so scaling is exact in binary and the k = 0 run is its own bit-exact reference). At every k assert (a) vec_len == 3*(n-2) == 30, (b) the index sequence equals the k = 0 sequence, (c) \u03a3|2\u00d7tri area| == |total 2\u00d7area| exactly. \u26d4 PRECONDITION THE FILING OMITS: assert the k = 0 leg is itself COMPLETE before using it as the reference \u2014 a fixture already partial at k = 0 makes every later comparison match a partial and the whole sweep passes vacuously. \u26d4 AND THE FILING'S BRACKETING CLAIM IS WRONG, MEASURED: the proposed k = \u2212500\u2026+500 does NOT bracket every constant a repair might choose. Over k = 0\u2026\u2212500 (3006 assertions) shipped 1e-12 fails 2886, 1e-9 fails 2916, 1e-15 fails 2856, EPSILON_F64\u00b2 = 1e-24 fails 2766 \u2014 but **DBL_MIN passes 3006/3006, entirely undetected**, and 1e-300 survives all but the last 2 steps (2994/3006). DBL_MIN is not a strawman: it is the exact constant this repo's own settled rule hands a repairer. FIX THE SWEEP: run the negative leg to **k = \u2212535**. Measured there, DBL_MIN fails 150 while the exact sign test and the relative bound stay at 0; the separating window is k = \u2212511 \u2026 \u2212537. \u26d4 DO NOT GO DEEPER: past \u2248 \u2212537 the fixture itself underflows and EVERY variant including the correct one fails (exact sign: 0 failures at \u2212535, 16 at \u2212540, 96 at \u2212560), so a deeper sweep stops discriminating and makes the correct fix look broken. Keep k = 0\u2026+500 as the CONTROL \u2014 shipped code passes it 3006/3006, which proves the negative-leg failure is the threshold, not the fixture, the winding or overflow. On this fixture (a), (b) and (c) fail together at every failing k, so (b)/(c) add no kills beyond (a); keep them anyway, since (a) alone cannot catch a complete-but-wrong triangulation. Every mutant run must carry a mutant-installed grep proof (4 sites) \u2014 the four thresholds are only meaningful if the binary actually contains them."
  },
  {
   "site": "src/collision_core.cyr:800",
   "fn": "triangulate_polygon",
   "guarded_quantity": "cross = _col_triangle_area_2x(ax,ay, bx,by, cx,cy) at line 795 \u2014 twice the signed area of the candidate ear (prev, i, next). Dimensions LENGTH SQUARED, exactly degree-2 homogeneous in the input coordinates (verified: scaling every vertex by 2^k reproduces a bit-identical index sequence for a scale-covariant predicate over k in [-460,+460] on a 10-gon, and k in [-500,+500] on a quad). Compared against the absolute EPSILON_F64 = 1e-12 (src/error.cyr:26) on the CCW branch; line 802 is the mirrored CW branch, f64_lt(cross, -EPSILON_F64).",
   "protects": "No arithmetic operation. This is the EAR GATE: the dead band is what stops a collinear or sliver triangle being clipped as an ear, emitted into `result`, and removed from the ring. Confirmed at the source: line 795 computes `cross` and lines 800/802 are its ONLY consumers (ax..cy are reused for the point-in-triangle pass, `cross` is not). The comment at lines 691-693 states outright that _col_ring_is_convex uses \"the SAME predicate and the SAME EPSILON_F64 threshold as the ear gate below\", i.e. lines 706/709 are the same guard at a second call site.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FAILED, and the verdict is confirmed by measurement rather than by argument. (1) Not dimensionless/bounded: `cross` is 2x an area, degree-2 homogeneous \u2014 measured invariance of the whole index sequence under 2^k scaling proves the quantity moves with s^2 while 1e-12 does not. (2) No normalisation: `triangulate_polygon` has NO caller anywhere in src/ (grep: the only hits are the definition at :713, tests/*.tcyr and tests/hisab.bcyr). It is a public leaf entry point consuming raw HVec2 coordinates in whatever units the caller holds. (3) Not a convergence tolerance: no iteration, no division, no domain to clamp \u2014 it is a degeneracy dead-band on a sign predicate, which the claim itself concedes. (4) CHECKED, NOT ASSUMED \u2014 the proposed exact sign test breaks NO existing test. I modelled triangulate_polygon line-for-line (blocked cache, 2-entry refresh, max_iters bail, boundary-inclusive point-in-tri) and validated the model against every asserted length in the real suite: modules.tcyr:5579/5590/5602/5616/5626/5637/5644 (6, 9, 12, 18, 6, 9, 0), modules.tcyr:6963/6974 (6, <=9 and %3==0), abuse.tcyr:1522-1551 (0,0,0,0,0). Running each fixture at eps=1e-12 and at eps=0 produces BYTE-IDENTICAL index sequences on all nine. Every fixture uses small-integer coordinates whose smallest |cross| is 2 \u2014 twelve orders of magnitude above the band \u2014 and the two degenerate cases that MUST return 0 (4 collinear vertices, 6 coincident vertices) compute cross == exactly 0.0, which the strict `>` still rejects, so \"no ears\" survives the repair unchanged. (5) The sweep does discriminate the four named mutants; measured first-bad k on a unit square = -15 (1e-9), -20 (1e-12 shipped), -25 (1e-15), -40 (1e-24), matching the claim's estimates. THE DEFECT IS REALISABLE, MEASURED: a unit square at k = -20 (side 9.5e-7 \u2014 a micrometre expressed in metres) returns an EMPTY vec; the CW-wound square fails at the identical k, so line 802 carries it too. TWO CORRECTIONS TO THE FILING. (A) Its repair-ordering argument is INVERTED. The filing says leaving 800 banded while repairing 706/709 is \"a worse state than the current consistent-but-wrong one\". Measured on a 10-point star over k = -10..-29: repairing 706/709 ALONE is a strict NO-OP (identical output to shipped at every k: 15, then 0), while repairing 800 ALONE is a COMPLETE repair (24 at every k) and gives index sequences identical to repairing all four on 400 random star-shaped polygons at unit scale. The asymmetry runs the other way than filed: 706/709 only populate the `blocked` cache, and a banded `blocked` is the CONSERVATIVE error (a near-collinear vertex stays marked blocked and merely earns an extra point-in-triangle test); site 800 is both where the damage is realised and where the repair lands. Repairing all four together is correct hygiene given the comment at 692, but it is not a correctness precondition and the \"worse state\" claim is refuted. (B) The prescribed sweep span is wrong at both ends \u2014 see scale_test.",
   "fabricates": "A plausible, well-formed short answer with no rc and no distinguishing signal. MEASURED on the shipped predicate: (i) a unit square scaled by 2^-20 returns an EMPTY vec \u2014 `convex` is never set for any i, the for-loop ends with ear_found = 0, and line 851 returns immediately; (ii) a 10-point star whose outer radius exceeds its inner by 1e6 returns 15 of the correct 24 indices across a 10-binade window (k = -21..-30) before collapsing to 0 \u2014 a well-formed 5-triple PARTIAL triangulation of believable length. The plateau width is measured to be ~log2(0.809 * radius_spread) binades, so the degradation is gradual and localises to a threshold on an area rather than to a global switch-off. Both outputs are shaped exactly like the function's own documented convention for degenerate input, and the header comment invites the caller to read a short return as \"your polygon is not simple\" \u2014 so a defect in the library is silently attributed to the caller's geometry. tests/abuse.tcyr:1522-1551 assert on vec_len of precisely this vec, so the fabricated answer is the same shape the suite expects.",
   "correct_threshold": "`f64_gt(cross, _COL_F64_ZERO)` on line 800 and `f64_lt(cross, _COL_F64_ZERO)` on line 802 \u2014 the exact sign test \u2014 with lines 706/709 repaired identically so the comment at 692 stays true. Verified against every existing fixture: byte-identical results, and the two \"no ears\" degeneracy contracts (collinear, coincident) hold because their cross is exactly 0.0 and `>` is strict. If a degeneracy band is genuinely wanted it must be the relative determinant bound 4*DBL_EPSILON*(|(bx-ax)(cy-ay)| + |(cx-ax)(by-ay)|), derived from the operands; I verified this expression is itself exactly degree-2 homogeneous, so its decisions are invariant under 2^k scaling and it passes the same sweep \u2014 the sweep brackets BOTH candidate repairs without needing to separate them, which is correct since neither is wrong. \u26a0 Do NOT replace the inline copy at 795-803 with a call to `_col_ring_is_convex` as the filing suggests: that function takes 5 parameters, cycc's `#inline` requires <= 2 (the 2.11.4 lesson), so the dedup would put a call/ret in the O(n^2) inner loop that `bench_triangulate_polygon` (tests/hisab.bcyr:630, \"triangulate_600gon\") measures. Keep the inline copy, make the expression textually identical to 706/709, and note the coupling in the comment.",
   "scale_test": "Polygon-scale covariance sweep, but the filing's span and fixture are BOTH wrong and are corrected here by measurement. Use s = 2^k over **k = -530 \u2026 +490** (not -500\u2026+500), fixtures = unit square (n=4, expect 6) AND a 10-point star with outer radius 1e6, inner radius 1 (n=10, expect 24), each in BOTH windings; assert the complete index sequence, vec_len == 3*(n-2) exactly, and the summed-area cross-check at every k, with k > 0 as the control. VERIFIED: the exact-sign control is correct at every k in [-530,+490] on all three fixtures, and the sweep kills 1e-9 (k=-15), 1e-12 (k=-20), 1e-15 (k=-25), 1e-24 (k=-40), 1e-30 (k=-50) and DBL_MIN (k=-511). \u26d4 CORRECTION 1 \u2014 THE FILED SPAN LEAVES A PLAUSIBLE MUTANT ALIVE: at k >= -500 a DBL_MIN band is indistinguishable from the exact sign test on BOTH fixtures (measured: they differ only in k in [-511,-537]). DBL_MIN is exactly the threshold this repo's own 2.10.2 precedent `_GEO_F64_TINY` makes the likely repair choice, so it must be bracketed; the bottom of the sweep must reach k = -530, just above where the control's own cross underflows to subnormal (measured breakdown at k = -538). A band at DBL_TRUE_MIN (5e-324) is killed by no k, but that is not a surviving mutant \u2014 it is behaviourally identical to the sign test over every input the control can represent. \u26d4 CORRECTION 2 \u2014 THE FILED FIXTURE AND SPAN ARE IN TENSION: the filing's \"spread 10^4, small ears cross ~13 decades below the large\" is measured false. The partial plateau is ~log2(0.809*spread) BINADES (the filing conflates binades in k with decades): spread 1e4 gives 6 binades (k = -21..-26), and 13 binades needs spread 1e8 \u2014 at which the CONTROL ITSELF FAILS at k = +500, because the ear cross products overflow to +Inf (measured safe top: k <= +509 at spread 1e2, +506 at 1e4, +503 at 1e6, +499 at 1e8). Spread 1e6 with a top of +490 is the measured-safe pairing: a 10-binade plateau that distinguishes a PARTIAL result from an empty one, with 13 binades of overflow headroom. Derive the top of any sweep from the fixture's own max|coord| (overflow when max|coord|^2 approaches 1.8e308), never from a fixed 2^500. Also assert 0 < len < 3*(n-2) inside the plateau explicitly \u2014 a test that only asserts \"non-empty\" passes on the half-triangulated result."
  },
  {
   "site": "src/collision_core.cyr:802",
   "fn": "triangulate_polygon",
   "guarded_quantity": "The ear `cross` from `_col_triangle_area_2x(ax,ay,bx,by,cx,cy)` (:656-659) = (bx-ax)(cy-ay) - (cx-ax)(by-ay) on RAW HVec2 world coordinates \u2014 twice the signed area of the candidate ear, dimensions LENGTH SQUARED \u2014 compared against `f64_neg(EPSILON_F64)` (1e-12, `src/error.cyr:26`) on the CLOCKWISE branch, where a convex ear has negative signed area.",
   "protects": "No arithmetic operation. The CW half of the ear gate; the inlined mirror of `_col_ring_is_convex`'s line 709, reached only when the winding computed at line 751 from the sign of `total_area` is CW (`ccw = 0`).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "NOT REFUTED \u2014 all five refutation grounds were tested and failed. (1) NOT BOUNDED: `_col_triangle_area_2x` is a raw 2x-signed-area on unnormalised world coordinates, LENGTH SQUARED; the sibling agent's own new note at `src/error.cyr` says EPSILON_F64 is right only for \"a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, a parameter in [0,1]\". This is none of those. (2) NO CALLER NORMALISES: `triangulate_polygon` is a public entry point with ZERO in-tree callers (only tests/ and hisab.bcyr), and nothing in its body rescales `vertices`. (3) NOT A CONVERGENCE TOLERANCE: it is an orientation predicate \u2014 and the file's own sibling predicate `_col_point_in_tri` (:677-682) tests the IDENTICAL `_col_triangle_area_2x` quantity against `_COL_F64_ZERO` with NO band, so exact-sign is already this file's established convention for this quantity and the ear gate is the outlier. (4) THE PROPOSED REPAIR BREAKS NOTHING \u2014 CHECKED, NOT ASSUMED: exact-sign at all four sites (706/709/800/802) run against every suite gives 416+351+1798+233+776 = 3574 passed / 0 failed, identical to shipped, and at unit scale the star's triangulation is byte-identical (index checksum -6873769297671726784 CCW / 1583088700137086604 CW under both builds). (5) THE SWEEP DOES DISCRIMINATE. MEASURED on an 8-vertex integer-coordinate concave star scaled by exact powers of two: SHIPPED is clean only on k in [-21, +509]; at k = -22 BOTH windings return 12 of 18 indices and at k <= -23 both return EMPTY, while `total_area`'s exact sign test still identifies the winding correctly \u2014 the code knows the ring is CW and then refuses to find an ear in it, exactly as filed. The exact-sign repair widens the clean window to [-538, +509], both new bounds being the FIXTURE's own IEEE underflow/overflow, not the guard. TWO CLAIMS IN THE ORIGINAL FILING ARE WRONG AND ARE CORRECTED HERE. (a) \"the branch a CCW-only fixture never executes\" is FALSE: `tests/modules.tcyr:5620-5627` (`tp_w`, the reversed square, shoelace -8 -> ccw = 0) DOES execute line 802. The gap is not coverage \u2014 it is that the repo's only CW fixture sits at one scale where the guard passes by ~12 orders of magnitude. (b) \"fails the symmetry assertion at the first negative k\" is FALSE: the partial repair (706+800 fixed, 709+802 left) was built and measured \u2014 its CW leg is clean down to k = -21 and first deviates at k = -22. A symmetry assertion at k = -1 passes. The site is still a genuinely separate necessary repair: that partial build is CCW-clean to k = -538 and CW-empty from k = -23.",
   "fabricates": "`convex` stays 0 for a genuinely convex ear of a CW-wound polygon, with no rc and no diagnostic. MEASURED, and it fails in TWO stages that are both silent: at the threshold scale the ear scan finds SOME ears and then bails, returning a PARTIAL index vec (12 of 18 at k = -22 on the reference star); one binary decade lower it returns the EMPTY vec (k <= -23) \u2014 bit-for-bit the same value `triangulate_polygon` returns for n < 3, for a null vec, for n > vec_len, and for 4 collinear vertices (`tests/abuse.tcyr:1522-1551`). A caller applying the documented `vec_len(result) == 3*(n-2)` check sees \"degenerate or self-intersecting input\", which is a lie about a perfectly simple polygon.",
   "correct_threshold": "`f64_lt(cross, _COL_F64_ZERO)` \u2014 the exact sign test. Not merely proposed: it is what `_col_point_in_tri` (:677-682) ALREADY does to the same `_col_triangle_area_2x` quantity three functions up in the same file, so the repair makes the two orientation predicates consistent rather than inventing a convention. VERIFIED: 3574/3574 assertions pass across all five suites with it, unit-scale output is byte-identical, and the clean scale window widens from k in [-21, +509] to [-538, +509]. ALL FOUR SITES MUST MOVE TOGETHER: 706/709 feed the `blocked[]` cache and 800/802 are the ear gate, and the comment at :692-693 makes their agreement an explicit contract (\"the SAME predicate and the SAME EPSILON_F64 threshold ... so 'not convex here' is exactly 'would be rejected as an ear tip there'\"), so repairing the gate without the cache desyncs that contract. If a band is wanted for near-degenerate rings it must be the RELATIVE determinant bound cross < -4*DBL_EPSILON*(|(bx-ax)(cy-ay)| + |(cx-ax)(by-ay)|), which is scale-covariant by construction; an absolute constant of any magnitude is wrong here, since a threshold that needs a scale chosen for it IS the defect.",
   "scale_test": "Power-of-two scaling of a fixed integer-coordinate concave fixture, so every multiply and subtract is EXACT and the only thing that changes across k is the comparison against the absolute constant. Reference fixture (measured): the 8-vertex CCW star (10,0),(2,2),(0,10),(-2,2),(-10,0),(-2,-2),(0,-10),(2,-2), n = 8, expected 3*(n-2) = 18 indices; run it FORWARD and REVERSED (the reversed ring is CW, ccw = 0, and is the only thing that reaches line 802). Assert `vec_len == 18` and the exact index sequence at every k. THE PLAIN LENGTH ASSERTION ON THE REVERSED RING IS THE DISCRIMINATOR \u2014 the winding-symmetry assertion is optional garnish; what is load-bearing is that the sweep runs the CW ring AT ALL, which `tp_w` at `tests/modules.tcyr:5625` does not, being pinned at one scale. MEASURED BRACKETING on this fixture (last clean k): eps 1e-6 -> -11, 1e-9 -> -16, 1e-12 (shipped) -> -21, 1e-15 -> -26, 1e-24 -> -41; exact sign -> -538. So k down to about -200 brackets every absolute threshold a repair could plausibly pick, and k = 0 or any k > 0 is the control. DO NOT HARDCODE +-500: it is over-wide AND fixture-dependently UNSAFE. The exact predicate itself stops being clean below k = -538 for THIS fixture (the 2x-area products go subnormal) and above k = +509 (they overflow to Inf, and NaN sign tests in `_col_point_in_tri` then change the triangulation) \u2014 so on a fixture with larger coordinates or a smaller minimum ear area, a hardcoded +-500 sweep would fail the \"exact index sequence at every k\" assertion ON THE CORRECT REPAIR and report a false alarm. Derive the bounds from the fixture: k_min = ceil((-1074 - log2(cross_min))/2) + margin, k_max = floor((1023 - log2(max(|cross|_max, |total_area|_max)))/2) - margin. The model was verified against the measurements \u2014 this fixture's cross_min is 4, predicting the 1e-12 break at 4*2^(2k) <= 1e-12 -> k = -21 (measured -21) and the exact-predicate floor at 4*2^(2k) >= 2^-1074 -> k = -538 (measured -538)."
  },
  {
   "site": "src/color.cyr:52",
   "fn": "unpremultiply_alpha(r, g, b, a)",
   "guarded_quantity": "|a| \u2014 the alpha (coverage) channel of a premultiplied RGBA tuple. Dimensionless and contractually [0,1], but structurally the HOMOGENEOUS WEIGHT of the 4-tuple: a valid premultiplied colour is (r,g,b,a) = a\u00b7(R,G,B,1). Exact structural twin of the `pw` guarded by the identical `f64_lt(f64_abs(pw), EPSILON_F64)` at mat4.cyr:150 (m4_transform_point's perspective divide) and the `cw` at transforms.cyr:169.",
   "protects": "The division f64_div(F64_ONE, a) on the next line, and the three multiplies r*inv_a, g*inv_a, b*inv_a that consume it. Nothing else in the function can fail. Verified it is not an overflow guard either: EPSILON_F64 does not prevent r*inv_a overflowing (r = 1e308, a = 1e-11 passes the guard and returns +Inf), so overflow cannot be the rationale.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Refutation attempted on all five avenues; all failed under measurement (probes in scratchpad/eps/refute/). (1) DIMENSIONLESS-AND-BOUNDED is true but insufficient, and it is the strongest defence available because error.cyr:41-45 literally lists \"a parameter in [0,1]\" as EPSILON_F64's territory \u2014 the clause's operative qualifier, however, is \"where an absolute tolerance MEANS something\". Measured: a DBL_MIN-guarded variant recovered the straight colour (1.0, 2.0, 4.0) BIT-EXACTLY on 1023 of 1023 rows spanning j = 0..1022, i.e. 307 decades. The recovered colour is degree 0 in the guarded quantity; the guard makes it depend on that quantity's absolute magnitude. F64_TINY's own first clause at error.cyr:39-41 matches this site verbatim: the guard protects a DIVISION and the only thing that can actually fail is a zero or subnormal denominator. (2) The graphics-conventional defence \u2014 small alpha amplifies quantisation noise, the reason real compositors floor alpha at ~1/255 \u2014 has NO basis in this API: grep of color.cyr for 255 / u8 / any quantised path returns nothing, every channel is f64, and f64's RELATIVE precision is scale-free, so no amplification exists. 1e-12 is also not any conventional coverage floor; it is simply the value of EPSILON_F64, which is evidence it was reached for rather than chosen. (3) CALLER NORMALISES: refuted \u2014 unpremultiply_alpha has zero call sites in src/; it is a leaf public entry point, and the file's OWN composite_src_in/composite_dst_in drive a downward multiplicatively. (4) CONVERGENCE TOLERANCE: no. (5) BREAKS A PASSING TEST: checked, not assumed \u2014 under the repair all four pinned sites stay green (abuse.tcyr:2226, 2228; modules.tcyr:7311, 7312), because the suites pin only a == 0 (modules.tcyr:7313 comment: \"a == 0 is the documented guard\") and the dyadic a = 0.5 round-trip. The one refutation avenue that partly landed was the scale_test, and it sharpens rather than overturns: measured, an exact-zero-only repair ALSO scores 1023/1023 on the original LEG A, so that sweep did not bracket the low side; low-side rows are now added. Corroboration that this is a class, not a site: mat4.cyr:150 guards the identical homogeneous weight with the identical expression, and mat4.cyr:252 (m4_inverse returning the identity) is already a CONFIRMED instance of the fabricating-guard class in the 2.11.1 audit.",
   "fabricates": "hvec4_zero() = (0,0,0,0) \u2014 fully transparent black \u2014 and it discards the caller's alpha too, since the live path returns `a` in w. Measured on the shipped function: straight HDR colour (1.0, 2.0, 4.0) at coverage 2^-49 \u2248 1.78e-15, arrived at through seven composite_src_in clips using only this file's public API, comes back (0,0,0,0) where the exact answer is (1.0, 2.0, 4.0, 2^-49). Silent: the return type is HVec4, there is no error channel, and the caller cannot distinguish this from a genuinely empty pixel. It also breaks the unpremultiply\u2192premultiply round trip, destroying the pixel rather than merely rounding it.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) \u2014 the constant this project already extracted for exactly this case (10 uses in geo.cyr, 8 in calc_ext.cyr, 5 in autodiff.cyr, 3 in quat.cyr, 2 in vec3.cyr). One-token change: color.cyr:52 already reads EPSILON_F64, a sibling global in the same file, so no new plumbing. MEASURED to be the exact boundary, not merely a safe one, using a full-mantissa straight colour R = 1 + 2^-52 (0x3FF0000000000001): at a = 2^-1022 = DBL_MIN the recovery is BIT-EXACT (so the cut may not sit any higher); one binade lower at a = 2^-1023 the premultiplied product rounds and the recovery is NO LONGER exact (so the cut may not sit any lower); two binades lower at a = 2^-1024 the reciprocal overflows and an unguarded divide returns +Inf. DBL_MIN is therefore the largest alpha at which r/a recovers every straight colour exactly and the smallest at which 1/a is guaranteed finite. Every existing assertion stays green because 0 < DBL_MIN, so unpremultiply_alpha(1,1,1,0) still returns (0,0,0,0) \u2014 verified by running the four pinned assertions against the repaired variant, 4/4 pass.",
   "scale_test": "Homogeneity of the premultiplied representation: rgb is degree 0 in the weight, alpha degree 1. All comparisons BIT-EXACT (`==` on the f64 bit pattern), never through f64_to \u2014 truncation is the measured reason 2.13.0 found a core function could be wrong by 0.9 with all five suites green. LEG A (normal band, brackets every repair ABOVE DBL_MIN): straight colour C = (1.0, 2.0, 4.0), k = 2^-j for j = 0..1022 (powers of two, so every product and every reciprocal is exact and every component stays a normal f64; at j = 1022 the tuple is (DBL_MIN, 2^-1021, 2^-1020, DBL_MIN)). Assert x/y/z == (1.0, 2.0, 4.0) and w == k exactly. MEASURED: shipped code passes j = 0..39 and returns (0,0,0,0) for every j >= 40 \u2014 40 of 1023 rows pass, first failure at j = 40, which reports the threshold actually in force; the F64_TINY repair passes 1023/1023. The first-failing j discriminates among mutants: 1/255 -> j=8, 1e-6 -> j=20, 1e-9 -> j=30, EPSILON_F64 -> j=40 (measured), 1e-15 -> j=50, 1e-30 -> j=100, 1e-100 -> j=333, 1e-300 -> j=997. LEG A2 (\u26a0 NEW \u2014 the original sweep did NOT bracket the low side: MEASURED, a repair guarding only `a == 0` also scores 1023/1023 on LEG A, so LEG A alone cannot tell DBL_MIN from no guard at all). Three rows, all with the full-mantissa colour R = 1 + 2^-52 so the exactness test is not accidentally satisfied by a dyadic value: (i) a = 2^-1022 = DBL_MIN must recover R BIT-EXACTLY \u2014 kills any repair with a threshold above DBL_MIN (measured: passes); (ii) a = 2^-1023 must NOT be handed back as a recovered colour \u2014 measured, recovery there is silently inexact, so a repair at any subnormal threshold fabricates a wrong finite colour instead of returning the designed zero; (iii) a = 2^-1024 \u2014 measured, an unguarded/`a == 0`-only divide returns +Inf while the F64_TINY repair returns (0,0,0,0), so this single row kills `a == 0`, kills a threshold at the smallest subnormal (2^-1074), and kills \"delete the guard\". With A2 the only guard passing every row is one at exactly DBL_MIN. Precedent for the width: this repo's 2.11.1 quaternion sweep killed 1 of 3 mutants at 12 decades and 3 of 3 at 20. LEG B (independent, built only from this file's public API, so it cannot be dismissed as a synthetic input): c = premultiply_alpha(1.0, 2.0, 4.0, 1.0), then seven composite_src_in clips against da = 2^-7 = 0.0078125 \u2014 src_in multiplies all four components by da, a power of two, so the straight colour is preserved bit-exactly \u2014 leaving a = 2^-49 \u2248 1.78e-15, below the window. unpremultiply_alpha of that tuple must return (1.0, 2.0, 4.0). MEASURED: shipped returns (0,0,0,0) (the probe's only LEG B failure); the F64_TINY repair returns 1.0 and 4.0 exactly."
  },
  {
   "site": "src/color.cyr:157",
   "fn": "linearize_depth_reverse_z(ndc, near)",
   "guarded_quantity": "|ndc| \u2014 a reverse-Z normalised device depth. Dimensionless, in (0,1]. Confirmed against this repo's own projection: m4_perspective_reverse_z (src/mat4.cyr:342) builds from columns c2 = (0,0,0,-1) and c3 = (0,0,near,0), so for M*v = c0*vx + c1*vy + c2*vz + c3*vw we get clip.z = near and clip.w = -z_view, hence ndc = near/|z_view|. It is the RECIPROCAL of the frustum's depth dynamic range |z|/near, and the projection has NO far plane (nothing in the matrix mentions one), so ndc has no positive lower bound.",
   "protects": "The division f64_div(near, ndc) on the next line \u2014 the only operation in the function. Not an iteration, not a convergence loop, not a normalisation: a single divide with the guarded value as the denominator.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED AND FAILED ON ALL FIVE ROUTES; every claim below is measured, not argued (probe: scratchpad/eps/refute/probe.tcyr, run via `cyrius test` against the real included src/color.cyr on 6.6.2). (1) \"Dimensionless/bounded, so an absolute epsilon is correct\" is the strongest counter and it has a written basis in this repo: src/error.cyr:28's own comment carves out exactly this case \u2014 \"EPSILON_F64 remains right for a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, A PARAMETER IN [0,1] \u2014 where an absolute tolerance means something.\" ndc IS a parameter in [0,1]. The carve-out still does not apply, because its stated condition is that the absolute tolerance MEAN something, i.e. that values far below 1 be indistinguishable from 0 downstream. Under a reciprocal map they are the opposite of indistinguishable: near/1e-13 and near/1e-12 differ by 10x, both are computed to 0.5 ulp, and the whole reason reverse-Z exists is to concentrate float precision exactly where ndc -> 0. The quantity is bounded on the wrong end \u2014 the interesting end is open. Contrast the carve-out's own examples: a cosine of 1e-13 really is 1e-13 rad from perpendicular and nothing downstream can tell. (2) \"The caller normalises\" \u2014 there is no caller. `grep -rn reverse_z src/ tests/ examples/` returns only the definition, m4_perspective_reverse_z, and four test lines. It is a public entry point taking a raw f64. (3) \"Convergence tolerance\" \u2014 one division, no loop. (4) \"The repair breaks a passing test\" \u2014 CHECKED, NOT ASSUMED: I replayed abuse.tcyr:2270, modules.tcyr:7343 and modules.tcyr:7344 verbatim against both candidate repairs; all six assertions PASS (0 < F64_TINY is true and ndc == 0 is true, so the pinned ndc=0 -> 0 behaviour is untouched, and ndc=0.5 -> 2 never reaches the guard). near=0, ndc=0 also stays 0 under both \u2014 no new 0/0 NaN. (5) \"The scale test doesn't discriminate\" \u2014 it does: shipped code fails 983 of 1023 rows with first-failing j = 40, the exact signature of 1e-12 (2^-39 = 1.819e-12 passes, 2^-40 = 9.095e-13 fires). EPSILON_F64 = 0x3D719799812DEA11 = 1e-12 exactly (src/error.cyr:25). The label is precise and deliberately narrow: ndc does NOT move under a unit change \u2014 near and |z| scale together \u2014 and LEG B measured that (m/mm/um give bit-identical ndc = 1e-4 and results 1e3/1e6/1e9, exactly 1000x apart, PASSING on shipped code). What ndc scales with is the frustum's depth dynamic range: the guard fires iff |z| > 1e12 * near, and an infinite-far reverse-Z projection is the technique that exists specifically for ranges that large, so it fires hardest in the regime the function was written for. The division itself fails at nothing in that window; near/0 = +Inf is arguably the correct linear depth for an infinite far plane, and near > 0 means 0/0 never arises. Both governing documents state the intended guard as exactly zero \u2014 docs/audit/2026-04-15.md H6 (\"Fix: Guard ndc==0\", naming color.cyr:157, which this line implements) and docs/development/threat-model.md:42 (\"Division by ndc=0 | Returns 0\") \u2014 so the 1e-12 window is undocumented drift from the recorded fix. NEW: an f32 reverse-Z depth buffer, the normal source of this input, represents ndc down to 1.175e-38 normal (1.4e-45 subnormal) with a full 24-bit mantissa at every one; the guard discards 26 decades of legitimately representable, precision-rich depth.",
   "fabricates": "0.0 \u2014 and the comment beside it, \"# At far plane\", has the meaning inverted. Linear depth 0 is AT THE EYE; the far plane of this infinite projection is at +Inf. Measured (LEG C): near = 0.01, |z| = 1e11 gives ndc = 1e-13, shipped returns 0 where the truth is 1e11 \u2014 the most distant fragment in the scene reported as the nearest. Every downstream use silently inverts (depth compare, fog density, DOF circle-of-confusion). No error channel: bare f64 return, and 0 is indistinguishable from a legitimate at-the-eye depth. It is also the lone fabrication in its own file \u2014 abuse.tcyr pins linearize_depth's pole as NaN \"not a fabricated depth\", tonemap_reinhard's as a signed Inf \"not silently clamped\", luminance_to_ev100(0) as -Inf \"not a fabricated number\".",
   "correct_threshold": "SHARPENED BY MEASUREMENT \u2014 literally `if (ndc == 0)`, NOT F64_TINY. The original filing offered F64_TINY (DBL_MIN) or exact zero as equivalent; LEG D shows they are not, and F64_TINY loses. F64_TINY is the right rule when a subnormal denominator yields no usable answer; here it does yield one: near/ndc stays FINITE for subnormal ndc over most of that range (measured 1/(DBL_MIN/2) = 8.98846567431158e+307, bit pattern 0x7FE0000000000000, i.e. a perfectly good number), and only below ~5.56e-309*near does it reach +Inf \u2014 which is the exact linear depth of an infinite far plane anyway. So F64_TINY would still fabricate 0 across a band where the division succeeds. `ndc == 0` is one token, is what both the audit finding and the threat model already say this guard is, and my probe confirms it keeps ndc=0 -> 0 (abuse.tcyr:2270 and modules.tcyr:7343 \u2014 note the original filing miscited the latter as modules.tcyr:7274) and leaves ndc=0.5 -> 2 (modules.tcyr:7344) untouched. Fix the \"# At far plane\" comment in the same edit; it is inverted. Worth deciding SEPARATELY and not folded into this narrowing: whether ndc == 0 should return +Inf like the rest of this file's poles do. That is a contract change against a pinned assertion; narrowing the window is not.",
   "scale_test": "Homogeneity: degree -1 in ndc, degree +1 in near. All four legs RUN against the shipped function, results quoted. LEG A (brackets every absolute-epsilon repair): near = 1.0, ndc = 2^-j for j = 0..1022 built by repeated halving (exact); assert linearize_depth_reverse_z(2^-j, 1.0) == 2^j BIT-EXACTLY \u2014 never through f64_to, which truncates. MEASURED: shipped fails 983/1023, first_bad_j = 40; F64_TINY fails 0/1023; ndc==0 fails 0/1023. The first failing j names the threshold in force and so brackets the candidates rather than only the proposed one: 1e-6 first-fails at j=20, 1e-9 at j=30, EPSILON_F64 at j=40 (observed), 1e-15 at j=50, 1e-30 at j=100, 1e-100 at j=333, 1e-300 at j=997; the final row is DBL_MIN = 2^-1022, so only a guard at or below DBL_MIN survives all 1023. LEG B (unit CONTROL \u2014 must PASS before and after any repair; its job is to stop this being mis-filed as a units defect): the same fragment in m, mm, um \u2014 (near,|z|) = (0.1,1e3), (100,1e6), (1e5,1e9) \u2014 gives ndc = 1e-4 identically and results that must be exactly 1e3, 1e6, 1e9. MEASURED on shipped code: 1000 / 1000000 / 1000000000, exactly 1000x apart, PASSES. If LEG B ever fails, this finding is wrong. LEG C (reachability, so LEG A is not dismissed as synthetic): near = 0.01, |z| = 1e11 -> ndc = 1e-13 must return 1e11. MEASURED: shipped returns 0; both repairs return 100000000000. LEG D \u2014 NEW, and the leg that discriminates the two REPAIRS from each other, which LEG A cannot: ndc = DBL_MIN/2 (a subnormal), near = 1.0. F64_TINY fabricates 0; ndc==0 returns the correct finite 8.98846567431158e+307. Assert the finite value, and the repair is pinned to exact zero rather than to DBL_MIN. LEG E (regression pins, all six MEASURED PASSING under both repairs, so the narrowing is provably non-breaking): abuse.tcyr:2270 ndc=0 -> f64_from(0); modules.tcyr:7343 ndc=0 -> 0; modules.tcyr:7344 ndc=0.5,near=1 -> 2; plus near=0,ndc=0 -> 0 (no new 0/0 NaN)."
  },
  {
   "site": "src/complex.cyr:62",
   "fn": "cx_div",
   "guarded_quantity": "d = f64_add(f64_mul(br,br), f64_mul(bi,bi)) \u2014 the SQUARED MODULUS |b|^2 of the divisor, computed from the caller's raw operands with no prior normalisation. Dimensions (value)^2: if b carries a physical unit u, d is in u^2. Unbounded above and below; its magnitude is entirely the caller's data scale, squared. CONFIRMED by reading the function: br/bi come straight from HComplex_re(b)/HComplex_im(b) with nothing between the argument and the multiply.",
   "protects": "The two divisions on line 64-65: f64_div(ar*br + ai*bi, d) and f64_div(ai*br - ar*bi, d). Nothing else in the function \u2014 no sqrt, no acos, no loop bound, no reciprocal reused elsewhere. CONFIRMED by reading complex.cyr:54-66 in full.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ANGLES AND FAILED ON ALL FIVE. Verdict stands, unchanged. Recording what was tried, so the next reader does not repeat it:\n\n(1) DIMENSIONLESS/BOUNDED? No. d is built from the caller's operands by two multiplies with nothing in between. Not refutable.\n\n(2) CALLER NORMALISES? Checked with a word-boundary grep over all of src/, not assumed. The only in-tree cx_div call is linalg_precision.cyr:1430, `phase = cx_div(x1, cx_real(abs_x1))`, guarded one line above by `f64_gt(abs_x1, EPSILON_F64)` on the UNSQUARED modulus, so d = abs_x1^2 > 1e-24 and the shipped guard is unreachable from there. The claim already conceded this and it is correct. \u2b50 It cuts BOTH ways and the claim understated its own case: F64_TINY is equally unreachable from that site, so the repair is a provable NO-OP for every in-tree caller and cannot regress the QR path. The exposure is the public API only (cx_div is `#must_use`, complex.cyr is listed in cyrius.cyml `[lib]` line 69, and the guard is present verbatim at dist/hisab.cyr:1735).\n\n(3) CONVERGENCE TOLERANCE? No. It gates a return, not an iteration; the function has no loop.\n\n(4) BREAKS AN EXISTING TEST? Checked, not assumed \u2014 all six assertion sites that reach this guard or its two siblings: edge_cases.tcyr:478 (asserts the HANDLE is non-null, not the value), :848 (cx_div by 1e-9 -> d = 1e-18, above BOTH thresholds, unchanged), :850, :852, :855-856 (exact zero -> d = 0 < DBL_MIN, still fires, still returns 0), abuse.tcyr:2435-2437 (exact zero, same), modules.tcyr:3458 (unit scale). Not one moves under F64_TINY. The 2.6.13 regression pin at edge_cases.tcyr:848 is specifically what would have caught a threshold that went UP; it says nothing about one that should go down.\n\n(5) SCALE TEST DOESN'T DISCRIMINATE? It discriminates. I re-derived all five counts independently in exact f64 and got 241 / 235 / 170 / 93 / 89 with first-wrong decades -7 / -13 / -78 / -155 / -158 \u2014 reproducing the claim to the digit.\n\n\u26d4 ONE ANGLE I THOUGHT WOULD LAND AND WHICH THE REPO ITSELF REFUTES. I tried to argue the precedent is a FALSE ANALOGY \u2014 that F64_TINY = DBL_MIN is dimension-exact only for an UNSQUARED denominator (d < DBL_MIN <=> d subnormal <=> 1/d degrades), and that geo.cyr's cited sites are all unsquared ray-direction components (verified: geo.cyr:546/583/620/767/809/849 all guard `f64_abs(dx)`-shaped scalars). That would have made cx_div's squared d a different case. It does not land: src/vec3.cyr:180's own comment calls its guarded `denom = |a|*|b|` \"a length SQUARED, so it scales as the square of the caller's units\" and guards it with F64_TINY at :197. Applying F64_TINY to a squared quantity IS the repo's settled shape, decided deliberately. The claim's appeal to precedent is stronger than it argued.\n\nTHE DEFECT ITSELF, restated with the units named: d is in u^2 and EPSILON_F64*EPSILON_F64 = 1e-24 is a dimensionless constant. Changing the caller from metres to millimetres multiplies d by 1e6 while the threshold does not move, so the same problem in different units gets a different answer. The effective cutoff is |b| < 1e-12, an absolute magnitude on a quantity with no absolute meaning. |b| = 1e-13 is an ordinary double whose reciprocal 1e13 is ordinary; the division cannot fail there. Reproduced: cx_div(1+0i, 1e-13+0i) -> d = 1e-26 < 1e-24 -> cx_zero(), where the truth is 1e13+0i.\n\nThe two byte-identical siblings are cx_inv (complex.cyr:94) and cx_powf (complex.cyr:129); cx_inv's own in-tree caller (linalg_ext.cyr:1163) is likewise pre-guarded at linalg_ext.cyr:1150.",
   "fabricates": "cx_zero() \u2014 exactly 0+0i, silently, with no error channel (the return is an HComplex handle; there is nowhere to put a code). \u26d4 The fabricated value is BIT-IDENTICAL to a legitimate 0+0i quotient, so no caller can distinguish a flushed result from a real zero. complex.cyr contains the argument against itself: the cmat accessor block rejects returning cx_zero() on a bad index because \"a guard whose rejection value cannot be told apart from a valid answer is not a fix.\" Magnitude of the fabrication at the worst reproduced input: 1e13 reported as 0. \u26a0 SHARPENED \u2014 the fabrication is not confined to the shipped threshold. Under the PROPOSED F64_TINY repair the same fabrication still occurs at e = -155, -156, -157, where the unguarded division returns the exactly-correct 0.44 (measured, see correct_threshold). The repair narrows the fabricating window by 142 decades; it does not close it.",
   "correct_threshold": "SHARPENED \u2014 the claim's one-token `f64_lt(d, F64_TINY)` is right in direction and is NOT the measured optimum. Three candidates, ranked by measurement over the 401-decade sweep, all preserving the singularity pin:\n\n(a) `f64_lt(d, F64_TINY)` \u2014 DBL_MIN, src/error.cyr:45, spelled as at src/vec3.cyr:197 and via geo.cyr:26's alias. ONE-TOKEN edit at complex.cyr:62 and the two siblings :94 (cx_inv) and :129 (cx_powf). 93/401 fail. Recovers 142 decades. Verified no existing test moves and no in-tree caller can reach it. THE CONSERVATIVE CHOICE, and the one consistent with the 22 sibling sites.\n\n(b) `d == 0` only \u2014 the LITERAL reading of the rule 2.10.2 settled (\"guard exactly what makes the division fail and nothing more\"). \u2b50 THE CLAIM DID NOT BRACKET THIS AND IT BEATS THE CLAIM'S OWN PROPOSAL: 89/401 fail vs 93. Measured, (a) returns 0 at e = -155/-156/-157 where (b) returns 0.44 correctly \u2014 because every NONZERO d, subnormal included, yields a well-defined IEEE quotient; only d == 0 makes the division fail. DBL_MIN on a SQUARED denominator is one dimensional step too coarse: it is the right cut for |b|, applied to |b|^2.\n\n(c) Smith's algorithm (scale by m = max(|br|,|bi|) before squaring; guard m == 0). \u26d4 MEASURED 0/401 \u2014 PERFECT. The claim files this as a residual that \"closes the rest\" of 4 decades. It closes ALL 89, INCLUDING the 47-decade OVERFLOW end at e >= +154 that NO threshold choice can touch, because there d = +Inf and the guard never fires at all. \u26a0 THEREFORE THE CLAIM'S \"IRREDUCIBLE FLOOR OF 89\" IS WRONG AS STATED: 89 is the floor of the NAIVE br^2+bi^2 FORMULATION, not of the problem. A repair must be judged against 0, not against 89, or the framing silently licenses leaving 89 decades broken.\n\nRECOMMENDATION: ship (a) as the mechanical one-token fix across all three sites \u2014 it is behaviour-preserving in-tree, matches the 22 siblings, and the constant already exists \u2014 and file (b)/(c) as an EXPLICIT decision with these numbers attached, not as a vague \"Smith's would close the rest.\" Do not let (a) be recorded as \"the correct threshold\" when measurement says it is third of three.\n\n\u26a0 ALSO: the assertion helper the scale_test names, `assert_rel`, DOES NOT EXIST \u2014 grep over tests/ and lib/ returns nothing. And it must not be respelled through `f64_to`, which truncates (2.13.0 reduced that pattern from 842 sites to 16 precisely because it hides this class).",
   "scale_test": "SCALE COVARIANCE, DEGREE 0 \u2014 cx_div((k*a)/(k*b)) must not contain k:\n\n  for e in -200 ..= +200:  k = 10^e\n    cx_div(cx(k*1, k*2), cx(k*3, k*4)) == 0.44 + 0.08i   to a RELATIVE 1e-9\n\n\u26a0 SPELLING FIX: `assert_rel` does not exist in this repo (grep of tests/ and lib/ returns nothing). Write the comparison as an explicit relative check on the returned f64 BITS \u2014 never `assert_eq(f64_to(...), N)`, which truncates and is the mechanism 2.13.0 removed from 826 sites.\n\nRE-MEASURED INDEPENDENTLY, exact f64, 401 decades. The claim's five counts reproduce to the digit:\n\n  thr = EPSILON_F64      (1e-12, pre-2.6.13) ..... 241 fail, first wrong e = -7\n  thr = EPSILON_F64^2    (1e-24, SHIPPED) ........ 235 fail, first wrong e = -13\n  thr = sqrt(DBL_MIN)    (1.49e-154) ............. 170 fail, first wrong e = -78\n  thr = F64_TINY         (DBL_MIN, proposed) .....  93 fail, first wrong e = -155\n  thr = 0                (no guard) ..............  89 fail, first wrong e = -158\n\n\u2b50 SHARPENED \u2014 TWO CANDIDATES THE CLAIM DID NOT BRACKET, AND BOTH BEAT ITS PROPOSAL. The claim asserts +-200 \"brackets every threshold a repair might plausibly pick.\" It does not. Adding them:\n\n  guard `d == 0` ONLY .............................  89 fail, first wrong e = -158\n  Smith's algorithm (scale by max(|br|,|bi|)) .....   0 fail, NEVER wrong\n\n`d == 0` only is the literal form of this repo's own rule and beats the proposed F64_TINY by 3-4 decades; Smith's is the only candidate that is covariant EVERYWHERE, including the e >= +154 overflow end where every threshold scores identically because d = +Inf and no guard fires. So the sweep must run SEVEN candidates, and its verdict line must be \"0 fail\" \u2014 not \"89, the irreducible floor.\"\n\n\u26d4 THE SWEEP ALONE CANNOT SEPARATE `d == 0 only` FROM `no guard` (both 89 \u2014 the fixture never produces an exactly-zero d). The singularity pin is what discriminates them, so it is load-bearing, not decoration:\n\n  cx_div(1+0i, 0+0i) == 0+0i          # measured: 0+0i under F64_TINY, 0+0i under\n                                       # d==0-only, 0+0i under Smith, NaN with the\n                                       # guard deleted -> kills the delete-it mutant\n  cx_div(1+0i, 1e-13+0i) ~ 1e13+0i    # the live defect, stated as an assertion\n\nSweep + pin together discriminate all seven. Neither alone does.\n\n\u26a0 WHY THE RANGE, AS THE MUTANT IT KILLS \u2014 the 2.11.1 quaternion lesson, re-measured here. The SAME assertion over k = 1e-10 .. 1e+10 (21 decades) scores 0 fail for the SHIPPED guard, 0 for sqrt(DBL_MIN), 0 for DBL_MIN and 0 for no guard at all. It separates nothing among the four live candidates. It scores 4 fail for the pre-2.6.13 EPSILON_F64 form \u2014 i.e. a narrow sweep detects ONLY the defect that was already fixed in 2.6.13, which is exactly why 3574 assertions have felt like coverage here. A sweep must reach past e = -13 to see the shipped guard and past e = -155 to separate DBL_MIN from no-guard; 21 decades reaches neither."
  },
  {
   "site": "src/complex.cyr:94",
   "fn": "cx_inv",
   "guarded_quantity": "d = cx_norm_sq(a) = re(a)^2 + im(a)^2, the SQUARED MODULUS |a|^2 of the caller-supplied operand being inverted. Dimensions (value)^2 \u2014 u^2 for an operand in units u. Unbounded above and below, scale-carrying, and never normalised anywhere on the path: cx_inv takes an arbitrary HComplex straight from the public API.",
   "protects": "The two divisions in the return: f64_div(HComplex_re(a), d) and f64_div(HComplex_im(a), d). Nothing else \u2014 there is no sqrt, no acos, no iteration in this function.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE GROUNDS; FOUR FAIL, THE FIFTH PARTIALLY LANDS AND SHARPENS THE TEST RATHER THAN THE VERDICT.\n\n(1) Dimensionless/bounded? No. `cx_norm_sq(a)` (src/complex.cyr:79) is a raw sum of squares of the two caller-supplied f64 fields. Nothing clamps, normalises or bounds it. Contrast the repo's own rule, written at src/error.cyr:40-43: EPSILON_F64 is right for \"a normalised cosine, a barycentric weight, a parameter in [0,1]\". |a|^2 is none of those.\n\n(2) Caller normalises? No \u2014 and the check produced the opposite of a refutation. The sole in-tree caller is linalg_ext.cyr:1163 inside cmat_inverse. It does not normalise; it PRE-REJECTS at line 1150 with `f64_lt(max_norm, EPSILON_F64)`, and pivot_val at 1162 is exactly the element whose cx_norm_sq == max_norm (the row was swapped into place at 1151-1160), so d == max_norm >= 1e-12 > 1e-24 and cx_inv's own guard is provably unreachable from that call site. Consequence, stated plainly: the repair is behaviour-neutral in-tree \u2014 zero regression risk \u2014 and NO in-tree test can ever exercise it, which is exactly why a direct scale sweep is required. The defect lives entirely on the public API surface shipped in dist/hisab.cyr to ten SHA-locked consumers.\n\u26a0 That same read CONFIRMS the separate, worse out-of-file finding, verified line by line rather than assumed: linalg_ext.cyr:1141 sets `var nrm = cx_norm_sq(cmat_get(aug, i, col))`, 1143 keeps it in max_norm, and 1150 compares that SQUARED quantity against the UNSQUARED EPSILON_F64 \u2014 DEFECT_SQUARED_VS_UNSQUARED, live, effective cutoff |pivot| < 1e-6, i.e. the \"cmat_inverse -> singular for a well-conditioned matrix\" item. Out of scope for this file; not modified.\n\n(3) Convergence tolerance? No. It guards two divisions and returns a fabricated value; there is no iterate and no residual.\n\n(4) Does F64_TINY break an existing test? CHECKED, not assumed \u2014 all five cx_inv/cx_div/cx_powf assertion sites (edge_cases.tcyr:848-856, abuse.tcyr:2435-2440) were evaluated against every candidate threshold. edge_cases.tcyr:850 uses 1e-9, so d = 1e-18, which is above EVERY candidate \u2014 it passes unchanged. The pole assertions (edge_cases.tcyr:856, abuse.tcyr:2437) pass under every candidate INCLUDING F64_TINY (d = 0 < DBL_MIN, guard fires, designed 0+0i preserved) and fail only under thr = 0. No test breaks.\n\n(5) Does the scale_test discriminate? PARTLY REFUTED \u2014 the finding's table is exactly reproducible (241/235/170/93/90, first-wrong at e = -7/-13/-78/-155/-158, reproduced to the digit) but the test AS WRITTEN is not committable and two of its sub-claims are wrong:\n  (a) It FAILS ON ITS OWN PROPOSED REPAIR \u2014 93 failures at DBL_MIN. 47 of those 93 sit at e >= 154 and are IDENTICAL for all five candidates: d = 5k^2 overflows to +Inf above k ~ 1.9e154, so the top quarter of the sweep has ZERO discriminating power and merely guarantees a red suite after a correct fix.\n  (b) \"Every threshold in the table passes a 21-decade sweep\" is FALSE: measured, e in -10..+10 kills the unsquared-EPSILON_F64 candidate (4 failures). The correct statement is that +-10 decades kills 1 of 4 mutants and leaves 3 alive \u2014 which is the 2.11.1 quaternion result reproduced, not weakened.\nRestricting the sweep to e in [-150, +150] fixes both: DBL_MIN scores 0, no-guard scores 0, and the three wrong thresholds score 144 / 138 / 73. Range and verdict corrected below; the defect itself stands.\n\n\u2b50 ONE DEFECT AT THREE SITES (lines 62, 94, 128) \u2014 cx_div, cx_inv, cx_powf share the copy-pasted `f64_lt(x, f64_mul(EPSILON_F64, EPSILON_F64))`, and line 94's comment says so out loud. One finding, one edit, or the roadmap over-counts.\nThe SQUARING is correct and must not be re-fixed \u2014 2.6.14 already repaired the squared-vs-unsquared half. What survives is that |a|^2 scales as u^2 (mm instead of m multiplies it by 1e6) while 1e-24 does not, so the effective cutoff is the absolute |a| < 1e-12 on a quantity with no absolute meaning. Inversion is the textbook degree -1 homogeneous op: k*inv(k*a) == inv(a) for all k, and any fixed threshold breaks it at one arbitrary k. What can actually make f64_div(re, d) fail is d zero or subnormal, nothing else: |a| = 1e-13 gives d = 1e-26, an ordinary double, and 1e13 is exactly representable.\n\u26a0 PROVENANCE: the repo is read-only for this task, so the numbers here come from exact IEEE-754 simulation of the guard's own arithmetic (scratchpad sim.py), not from a cycc build. The arithmetic is hardware IEEE and the operand-by-operand shape matches src/complex.cyr line for line, so the decade boundaries transfer; the committed assertion must still be re-measured in-tree before the finding is closed.",
   "fabricates": "cx_zero() \u2014 exactly 0+0i, silent, no error channel, bit-identical to a legitimate zero result and to the designed pole sentinel, so a caller cannot tell the two apart. Reproduced: cx_inv(1e-13 + 0i) gives d = 1e-26 < 1e-24, guard fires, returns (0.0, 0.0); truth is 1e13 + 0i. The smallest representable magnitude stands in for one of the largest a caller will meet \u2014 26 orders of magnitude wrong (0 vs 1e13 is unbounded relative error), not a perturbation, and in the wrong direction. This is the skeptic-confirmed \"cx_div / cx_inv / cx_powf -> exactly zero\" entry, reproduced.",
   "correct_threshold": "`if (f64_lt(d, F64_TINY) == 1) { return cx_zero(); }` \u2014 F64_TINY = 0x0010_0000_0000_0000 = DBL_MIN, src/error.cyr:45, whose own comment states this exact rule (\"the right threshold when the guard protects a DIVISION and the only thing that can actually fail is a zero or subnormal denominator\"). Same operation and same spelling as quat.cyr:123 (`f64_lt(f64_abs(len_sq), _QUAT_F64_TINY)`), already repaired in this tree. No f64_abs is needed here: d is a sum of squares, hence never negative, so the bare compare is the whole test \u2014 adding f64_abs would be cargo-culted from quat.cyr, not derived.\nApply the identical edit at complex.cyr:62 (cx_div, on its `d`) and complex.cyr:128 (cx_powf, on its `cx_norm_sq(a)`). Leave cx_is_zero(a, tol) at line 149 ALONE: its `tol*tol` is a caller-supplied tolerance for a predicate, not a divide guard, and it is the function the 2.6.13 comment correctly cites as the tell.\nPole behaviour is preserved, verified per-candidate: d = 0 < DBL_MIN, so cx_inv(0+0i) still returns the designed 0+0i; removing the guard entirely returns NaN and fails abuse.tcyr:2437 and edge_cases.tcyr:856.\n\u26a0 RESIDUAL, MEASURED AND EXPLICITLY NOT CHARGEABLE TO THE THRESHOLD: DBL_MIN recovers 142 decades but not all of them. d = |a|^2 underflows to zero below |a| ~ 1.49e-154 and overflows to +Inf above |a| ~ 1.9e154, so cx_inv(1e-200) still returns 0+0i (truth 1e200) and cx_inv(1e160) returns exactly 0 (truth 2e-161). Under DBL_MIN the guard fires precisely when d is subnormal or zero \u2014 i.e. exactly when the division has genuinely lost the answer \u2014 so the guard is then optimal and the remaining error belongs to the norm_sq FORMULATION. Curing the last ~154 decades at each end needs Smith-style scaling (factor out max(|re|,|im|) before squaring), an algorithm change, not a threshold change. File it separately; it is the reason the sweep below stops at +-150.",
   "scale_test": "SCALE COVARIANCE, DEGREE -1 \u2014 inversion must satisfy k * inv(k*a) == inv(a) for every k. Scaling is folded back in on the LEFT, so the expected value is the fixed unit-scale constant 0.2 - 0.4i at every k and cannot itself overflow; that is what lets one assertion span hundreds of decades.\n\n  for e in -150 ..= +150:  k = 10^e\n    q = cx_inv(cx(k*1, k*2))\n    assert_rel(k * HComplex_re(q), +0.2, 1e-9)\n    assert_rel(k * HComplex_im(q), -0.4, 1e-9)\n  # and pin the pole in the same test:\n  assert cx_inv(cx(0,0)) == 0+0i          # designed zero; NaN if the guard is deleted\n\n\u26d4 RANGE CORRECTED FROM THE FILING, AND THE CORRECTION IS THE POINT. The filing's -200..+200 sweep FAILS ON ITS OWN REPAIR (93 failures at DBL_MIN), because d = 5k^2 overflows above k ~ 1.9e154; those 47 high-side failures are byte-identical across ALL five candidates and discriminate nothing. A test that goes red after the correct fix cannot be committed and will be deleted by the next reader. Measured failure counts, same sweep body, two ranges:\n\n  threshold                first wrong (down)   fails -200..+200   fails -150..+150\n  EPSILON_F64 (1e-12)          e =   -7              241                144\n  EPSILON_F64^2 (SHIPPED)      e =  -13              235                138\n  sqrt(DBL_MIN)                e =  -78              170                 73\n  F64_TINY / DBL_MIN (fix)     none in range          93                  0\n  0 (guard deleted)            none in range          90                  0\n\n\u2b50 THE NUMBER THAT SIZES THE DEFECT: first-wrong at e = -13 versus none-in-range \u2014 142 decades of legitimate, exactly-representable input the shipped guard destroys and the repair recovers. At +-150 the repair scores a clean 0 and the three wrong thresholds score 144 / 138 / 73, so the sweep both BRACKETS and PASSES.\n\n\u26d4 BRACKETING IS MANDATORY AND SHORT SWEEPS FAIL IT \u2014 measured, not argued:\n  +-10 decades:  kills 1 of 4 mutants (EPSILON_F64 unsquared, 4 fails); SHIPPED, sqrt(DBL_MIN) and DBL_MIN all score 0 \u2014 3 mutants alive.\n  +-12 decades:  still 1 of 4 (EPSILON_F64 6 fails, SHIPPED 0).\n  +-77 decades:  2 of 4 (sqrt(DBL_MIN) still alive at 0 fails).\n  +-150 decades: 3 of 4 dead; the fourth (guard deleted) is killed by the pole assertion above, so the pole line is load-bearing, not decoration.\n(This corrects the filing's claim that \"every threshold passes a 21-decade sweep\" \u2014 the unsquared candidate does not. The conclusion is unchanged and stronger: the 2.11.1 quaternion result reproduced in a different module \u2014 12 decades killed 1 of 3 there, 150 are needed here.)\n\n\u26a0 ASSERT DIRECTION, NOT MAGNITUDE. Compare the SIGNED components (+0.2 and -0.4 separately) as written: a fabricated 0+0i and a wrongly-signed inverse both fail a |q| test the same way, so magnitude alone does not discriminate the fabrication class from a sign defect \u2014 the same reason asserting unit length did not discriminate the quaternion identity.\n\u26a0 COMPARE THROUGH assert_rel (or bit-exact), NEVER through f64_to: f64_to truncates, and 0.2 truncates to 0 exactly as a fabricated 0 does \u2014 a f64_to-based version of this sweep passes on the shipped defect at every k. That is the 2.13.0 lesson applied to the test that proves this finding.\n\u26a0 RUN THE SAME SWEEP AGAINST cx_div(cx(1,0), cx(k*1, k*2)) and cx_powf(cx(k*1,k*2), -1): one edit fixes three sites, and only three sweeps prove it."
  },
  {
   "site": "src/complex.cyr:129",
   "fn": "cx_powf",
   "guarded_quantity": "cx_norm_sq(a) = |a|^2, the SQUARED MODULUS of the BASE. Dimensions (value)^2; scale-carrying and unbounded. The exponent n is NOT in the comparison \u2014 the guard is the function's first line and runs before n is looked at. Effective cutoff measured, not inferred: the shipped guard first fires at k = 1e-13 on BOTH a purely real base (k, 0) and the complex sweep base (k, 2k) (probe2, \"first e where SHIPPED guard fires\" = -13 in both cases), i.e. an absolute |a| < 1e-12 cutoff on the base of a power.",
   "protects": "\u26a0 THE ORIGINAL FILING UNDERSTATED THIS AND I CORRECTED IT BY READING THE DEPENDENCY. Not a division \u2014 cx_powf contains none. It stands in front of `ganita_f64_pow(cx_abs(a), n)` and `cx_arg(a)`. The guard has TWO jobs at a zero base, not one, and both were measured with the guard removed (thr = 0):\n\n  (1) THE POLE. pow(0, y<0) = +inf (lib/ganita.cyr:2079). Unguarded, cx_powf(0+0i, -1) returns re = 0x7ff0000000000000 (+Inf) and im = 0xfff8000000000000 (NaN \u2014 from Inf * sin(0)). The filing said \"+Inf\"; it is (+Inf, NaN).\n\n  (2) \u2b50 THE DOCUMENTED 0^0 = 0. `ganita_f64_pow`'s FIRST line is `pow(x, 0) = 1 for every x, NaN included`. Unguarded, cx_powf(0+0i, 0) returns 0x3ff0000000000000 = exactly 1.0. hisab documents 0 (tests/abuse.tcyr:2440, \"cx_powf: 0^0 -> re 0 (documented)\"), so THE GUARD IS WHAT IMPLEMENTS THAT DEVIATION FROM C. The filing did not know this and it changes which repairs are admissible.\n\nFor n > 0 there is nothing to protect: measured, unguarded cx_powf(0+0i, +2) = 0, the correct answer.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ROUTES; ALL FIVE CLOSED BY MEASUREMENT, so the verdict stands.\n\n(a) Dimensionless/bounded? NO. |a|^2 of an arbitrary public-API complex base. 1e-24 is fixed while |a|^2 scales as (value)^2; cx_powf is homogeneous of degree n by construction, so a fixed cutoff on the base breaks the defining identity.\n(b) Caller normalises? NO CALLER AT ALL. `grep -rn cx_powf src/` returns only the definition at complex.cyr:127 \u2014 the other two src hits (geo.cyr:21, :990) are comments citing the 2.6.14 repair. Entire exposure is the public API, `#must_use`, shipped in dist/hisab.cyr:1800 to ten SHA-locked consumers.\n(c) Convergence tolerance? NO. No iteration; a single-shot magnitude cutoff in front of pow/atan2.\n(d) Does F64_TINY break an existing test? CHECKED, NOT ASSUMED \u2014 there are exactly two cx_powf assertions in tests/ and I ran both. tests/edge_cases.tcyr:852 (base 1e-9, n = 1, asserts abs > 0): passes under F64_TINY (measured 1). tests/abuse.tcyr:2440 (0^0 -> re 0): passes under F64_TINY (measured 1), because F64_TINY still fires at an exactly-zero norm_sq for every n. \u26a0 The filing's \"no existing test exercises it at any scale but 1\" is WRONG \u2014 edge_cases does exercise 1e-9, which is four decades ABOVE the 1e-12 cutoff and therefore passes on the defect. A test exists and is blind.\n(e) Does the scale_test discriminate? YES, all five candidates, reproduced exactly (241 / 235 / 170 / 93 / 90).\n\nMEASURED, shipped code, real cx_powf (bits decoded):\n    cx_powf(1e-13 + 0i, +2.0)  truth 1e-26   -> 0x0  (0)\n    cx_powf(1e-13 + 0i, -1.0)  truth +1e13   -> 0x0  (0, WRONG DIRECTION)\n    cx_powf(1e-13 + 0i, -2.0)  truth +1e26   -> 0x0  (0, WRONG DIRECTION)\nWith the threshold changed to F64_TINY and nothing else touched, the same three return 0x3a88c240c4aecb14 = 1e-26, 0x42a2309ce5400000 = 1e13 BIT-EXACT against f64_from(10000000000000), and 0x4554adf4b7320334 = 9.999999999999999e25. All ordinary representable doubles at both ends.\n\n\u2b50 THE ORDER OF THE GUARD IS THE GUARD (2.11.4's fabricated 1.0 in _ad_pow): it fires before the exponent is examined, so it returns the SMALLEST representable magnitude in exactly the case where the truth is LARGEST.\n\n\u26a0 The secondary DEFECT_WRONG_QUANTITY criticism is now REFUTED rather than merely deferred \u2014 see correct_threshold. The exponent-reading form breaks a shipped assertion.",
   "fabricates": "cx_zero() \u2014 exactly 0+0i, silent, no error channel, indistinguishable from a legitimate zero. \u26d4 The sign of the error flips with the exponent: for n > 0 it under-reports (1e-26 as 0), for n < 0 it reports 0 where the true magnitude is +1e13 or +1e26 \u2014 the maximum possible relative error, handed to a caller whose only plausible sanity check (\"small base, small result\") is satisfied in the first case and inverted in the second. Contrast the unguarded failure mode, which is LOUD: (+Inf, NaN). The guard converts a NaN a caller would notice into a zero a caller cannot.",
   "correct_threshold": "`f64_lt(cx_norm_sq(a), F64_TINY)` \u2014 DBL_MIN via src/error.cyr:45, keeping the comparison on the squared quantity, the same one-token change as cx_div:62 and cx_inv:94. Fires only where |a|^2 is subnormal or zero, which is where cx_abs has already lost the value anyway. MEASURED behaviour-preserving on BOTH shipped assertions (edge_cases.tcyr:852 -> 1, abuse.tcyr:2440 -> 1) and on both jobs at the zero base (0^0 -> 0x0, 0^-1 -> 0x0), while recovering 93-vs-235 of the 401-decade sweep.\n\n\u26d4 THE SHARPER FORM THE ORIGINAL FILING FLOATED IS REFUTED, NOT DEFERRED. `if (cx_norm_sq(a) == 0 && n <= 0)` does not fire at n = 0, so ganita's `pow(x,0) = 1` first line reaches cx_from_polar and cx_powf(0+0i, 0) returns 1.0. Measured directly (probe2): the abuse.tcyr:2440 assertion returns 0 \u2014 it FAILS. Same for deleting the guard outright (measured 1.0). So any exponent-sign repair must also decide, and re-document, hisab's 0^0; it is not a free sharpening and must not be folded into this repair.",
   "scale_test": "SCALE COVARIANCE, DEGREE n \u2014 cx_powf(k*a, n) == k^n * cx_powf(a, n). n = 1 for the wide sweep, because k^1 is exact across the whole double range so no k^n over/underflow confounds the guard.\n\n  for e in -200 ..= +200:  k = 10^e\n    assert_rel(cx_powf(cx(k, 2k), 1.0), cx(k, 2k), 1e-9)          # degree-1 identity\n  for e in -150 ..= +150:\n    assert_rel(cx_powf(cx(k, 2k), 2.0), k*k * cx(-3, +4), 1e-9)   # (1+2i)^2 = -3+4i\n\nREPRODUCED (401 decades, tol 1e-9, real cx_norm_sq/cx_abs/cx_arg path):\n  thr = EPSILON_F64 (1e-12) ........... 241 fail\n  thr = EPSILON_F64^2 (SHIPPED) ....... 235 fail\n  thr = sqrt(DBL_MIN) (1.49e-154) ..... 170 fail\n  thr = F64_TINY / DBL_MIN (proposed) .. 93 fail\n  thr = 0 (no guard) ................... 90 fail\nFive distinct counts, so every candidate is discriminated; the 90 floor is norm_sq itself over/underflowing (5k^2 exceeds 1.8e308 above e=154 and flushes to 0 below e=-161) and is present with no guard at all.\n\n\u26d4 THE NARROW SWEEP IS WORSE THAN THE FILING CLAIMED, AND I MEASURED IT. On e in [-10, +10] the counts are EPS 4, EPS2 0, sqrt(DBL_MIN) 0, F64_TINY 0, no-guard 0 \u2014 the shipped threshold is indistinguishable from NO GUARD AT ALL, and four of the five candidates tie at zero. A +-10-decade sweep does not merely \"kill nothing\", it scores the defect as absent. The shipped guard first fires at e = -13 (measured on both (k,0) and (k,2k)), so the sweep must reach at least e = -15 to see anything and e = -160 to bracket F64_TINY against no-guard.\n\n\u26d4 THE EXPONENT MUST BE SWEPT TOO (2.10.1 tie-flag lesson) \u2014 at n = 1 the fabricated 0 is merely small, never catastrophic, so a base-only sweep never reaches the sign inversion:\n    assert_bits(cx_powf(1e-13 + 0i, +2.0), 1e-26)   # shipped 0x0\n    assert_bits(cx_powf(1e-13 + 0i, -1.0), 1e13)    # shipped 0x0 \u2014 bit-exact under the repair\n    assert_bits(cx_powf(1e-13 + 0i, -2.0), 1e26)    # shipped 0x0 \u2014 error INVERTS here\nUse a bit-exact compare on the -1 row: the repaired value is 0x42a2309ce5400000, identical to f64_from(10000000000000), so it needs no tolerance (2.13.0's rule \u2014 a truncating or tolerant compare here would accept a wide band).\n\n\u2b50 ANTI-DELETION PINS, BOTH REQUIRED, ONE ALREADY IN THE TREE:\n    assert cx_powf(0+0i, +2.0) == 0+0i            # correct with or without a guard\n    assert cx_powf(0+0i, -1.0) re == 0 AND im == 0  # designed zero; unguarded (+Inf, NaN) \u2014 PIN BOTH\n                                                    #   COMPONENTS: re alone catches +Inf, but im\n                                                    #   carries the NaN and a repair could fix one\n    tests/abuse.tcyr:2440 (existing) cx_powf(0+0i, 0) re == 0   # unguarded and exponent-sign forms\n                                                                #   both return 1.0 \u2014 measured FAIL\nThat third pin already exists and is what refutes the exponent-sign repair; the sweep must be added ALONGSIDE it, never in place of it."
  },
  {
   "site": "src/diffgeo.cyr:342",
   "fn": "sectional_curvature",
   "guarded_quantity": "|denom| where denom = f64_sub(f64_mul(uu, vv), f64_mul(uv, uv)) and uu/vv/uv come from _dg_inner(metric, ...) \u2014 the 2x2 Gram determinant of u and v in the metric g, i.e. the SQUARED area of the parallelogram they span (|u^v|^2). Dimensions [g]^2*[u]^2*[v]^2; with dimensionless components and a metric carrying length^2 (the standard case, and the case the shipped suite itself builds at tests/hisab.tcyr:1844 with metric diag(4,4) for a radius-2 sphere) it is a length^4 quantity. Not a normalised sine, not a bounded ratio. VERIFIED by reading the function: uu, vv, uv are raw metric inner products of the caller's own vectors, with no normalisation anywhere in sectional_curvature or in _dg_inner.",
   "protects": "Nominally the f64_div(num, denom) on line 343, but MEASURED: it does not protect that division and never has. A variant identical to the shipped code with the guard DELETED returns the bit-exact correct K at 511/511 scales of the r = 2^k sweep (0 zeros, 0 other), and so does a variant guarded at DBL_MIN \u2014 the division is not in danger at any scale the sweep reaches. What the line actually implements is the header comment's semantic promise (\"0 for a degenerate plane (u || v)\"), i.e. a DEGENERACY TOLERANCE. That is the correct reading of the trailing `# degenerate plane` comment, and it does not rescue the guard: a degeneracy tolerance on a length^4 quantity must be relative, and an absolute one is exactly the defect. The division still needs its own (DBL_MIN) backstop, which the single line also fails to be.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FIVE FAIL UNDER MEASUREMENT. (1) Dimensionless/bounded? No. denom scales as s^4 under u,v -> s*u,s*v and as lambda^2 under g -> lambda*g, while the correct K is INVARIANT under s and scales as lambda^-1. Measured on the equatorial round-sphere fixture (metric diag(r^2,r^2), the scale-invariant (1,3) Riemann R^0_101=1, R^0_110=-1, R^1_010=1, R^1_001=-1 \u2014 which is the true equatorial Riemann of the round 2-sphere, since R^rho_sigma-mu-nu = K(delta^rho_mu g_sigma-nu - delta^rho_nu g_sigma-mu) is r-independent when K = 1/r^2 and g = r^2 delta): over r = 2^k, k in [-255,255], the shipped function returns the bit-exact 1/r^2 at 265 scales and exactly 0.0 at 246, with the failing set the contiguous tail k <= -10 and \"other bad: 0\". The original filing's measurement on k in [-250,250] reproduces to the digit: 260 exact / 241 zero / other 0 / failing tail [-250,-10]. (2) Caller normalises? No \u2014 sectional_curvature has ZERO internal callers (grep over src/, tests/, examples/: the only definition is src/diffgeo.cyr:308 and every call site is in tests/hisab.tcyr), it is a public entry point, and the suite's own radius-2 test deliberately passes the non-unit metric diag(4,4). (3) Convergence tolerance? No \u2014 it is a degeneracy tolerance, confirmed by the no-guard and DBL_MIN variants both being exact at 511/511; that refines the diagnosis (the guard is doing the degeneracy job, badly, and is not doing the division job at all) without refuting it. (4) Proposed threshold breaks an existing test? NO \u2014 measured, not assumed: all five shipped sectional assertions (hisab.tcyr:1837, :1840, :1846, :1852, :1856) pass under the proposed two-guard repair, alongside the same five under the shipped guard as control and two assertions proving the local variant reproduces the shipped function bit-for-bit (12/12 passed, 0 failed). The degenerate case survives because uu*vv = 4 there and denom is exactly 0, so the relative test still fires. (5) Scale test does not discriminate? It does \u2014 but the filing's RANGES were too narrow, see scale_test. THE FILING'S OWN ERRORS, corrected: (a) \"2^-1000 is four binades above DBL_MIN\" \u2014 it is 22 binades (2^-1000 vs 2^-1022); the conclusion (nothing underflows) is right. (b) \"the shipped guard fails the tangent-rescale sweep for j <= -5\" \u2014 measured j <= -10; j <= -5 is the 1e-6 mutant's boundary, not the shipped one. (c) The second face is real but is NOT what the filing described: with u = s*(1,1), v = s*(1, 1+2^-20) the shipped guard reports \"degenerate, K = 0\" for s <= 1 and K = 1.0 for s >= 2 \u2014 the SAME plane, only the caller's units changed \u2014 and the division returns the exactly-correct 1.0 in both regimes, so \"the division proceeds on garbage\" is unsupported; the demonstrated harm is that the degeneracy VERDICT is unit-dependent, which is the same dimensional error and needs no garbage to be a defect. The proposed relative test returns the same verdict at all 17 scales there.",
   "fabricates": "Exactly 0.0 \u2014 the value meaning FLAT SPACE \u2014 with no error channel, since sectional_curvature returns a bare f64 rather than an rc (and, noted in passing, carries none of the `dim <= 0 || dim > _DG_MAX_DIM` / null-pointer guards its sibling entry points in diffgeo.cyr have). The fabrication is provably indistinguishable from two legitimate results, because the suite pins both to the same value: hisab.tcyr:1852 \"sectional: flat = 0\" (zero Riemann) and :1856 \"sectional: degenerate plane = 0\" (u || v). MEASURED instances through the real shipped public function: metric diag(r^2,r^2) at r = 2^-10 returns bits 0x0 where the truth is 2^20 = 0x4130000000000000; at r = 2^-250 it returns 0x0 where the truth is 2^500 = 0x5F30000000000000; the last correct scale is r = 2^-9, which returns the exact 2^18 = 0x4110000000000000 \u2014 a one-binade cliff with nothing in the inputs to mark it. Second fabrication shape, also measured: u = s*(1,1), v = s*(1,1+2^-20) returns 0.0 at s <= 1 and 1.0 at s >= 2 for the identical plane.",
   "correct_threshold": "Two guards, because the line conflates two jobs. (1) DEGENERACY (what the header comment promises): test the DIMENSIONLESS ratio, not its numerator \u2014 `f64_lt(f64_abs(denom), f64_mul(EPSILON_F64, f64_abs(f64_mul(uu, vv)))) == 1` , i.e. sin^2(angle) < 1e-12. Scale-free by construction: denom and uu*vv carry identical dimensions, so both scale by s^4 and by lambda^2 and the ratio is invariant; it is also the numerically honest test, since denom is formed by a subtraction and the ratio is exactly the fraction of significant digits that survived the cancellation. (2) The DIVISION, which per the 2.10.2 rule fails only at a zero or subnormal denominator: back it with `f64_lt(f64_abs(denom), F64_TINY) == 1` (error.cyr:45, DBL_MIN, the constant extracted for exactly this purpose; its comment at src/error.cyr:28-45 states the rule and states that EPSILON_F64 stays right only for \"a normalised cosine, a barycentric weight, a parameter in [0,1]\"). THE BACKSTOP IS LOAD-BEARING AND I MEASURED THE CASE THAT PROVES IT, which the filing asserted but did not exhibit: in Lorentzian signature g = diag(-1,1) with u = v = (1,1) (a null vector), uu = vv = uv = 0, so denom = 0 AND uu*vv = 0, the relative test evaluates 0 < 0 and does NOT fire, and a rel-only repair divides 0/0 and returns NaN; the F64_TINY backstop fires and returns 0. Order matters only for readability \u2014 both must be present. Verified no regression in the same signature: for the genuinely non-degenerate null/null plane u = (1,1), v = (1,-1), denom = -4 with uu*vv = 0, and neither the relative test nor the backstop fires, matching the shipped behaviour exactly. Neither threshold needs a scale chosen for it.",
   "scale_test": "The round 2-sphere of radius r is the same geometric problem at every scale and its sectional curvature is exactly 1/r^2, so the assertion is the scale invariant K == 2^-2k BIT-EXACTLY. Build the constant-curvature (1,3) Riemann once (R^0_101 = 1, R^0_110 = -1, R^1_010 = 1, R^1_001 = -1 \u2014 r-independent, and equal to the true equatorial round-sphere Riemann), set metric = diag(r^2, r^2), u = e0, v = e1, and sweep r = 2^k. THREE CORRECTIONS TO THE FILING'S RANGES, each measured: (A) METRIC SWEEP k = -255..255 (511 scales), NOT -250..250. Every value stays normal (at k = -255, denom = 2^-1020, two binades above DBL_MIN = 2^-1022; at k = 255, denom = 2^1020) and the backstop must not fire, which needs k > -256. Measured: shipped 265 exact / 246 zero / 0 other, failing tail k in [-255,-10]; repair 511/511 exact; mutant |denom| < 1e-6 fires for k <= -5 (251 zeros); mutants |denom| < 1e-24 and \"sqrt(|denom|) < EPSILON_F64\" fire for k <= -20 (236 zeros); mutant |denom| < 1e-300 fires for k <= -250 (6 zeros); DBL_MIN-only and no-guard are 511/511 exact. On the filing's narrower [-250,250] the 1e-300 mutant is caught by EXACTLY ONE point \u2014 the widening is what makes that kill robust. (B) TANGENT-RESCALE SWEEP j = -255..255 at fixed r = 1, u = s*e0, v = s*e1, s = 2^j, K bit-exactly 1.0 at all 511 \u2014 NOT the filing's [-120,120], which is measurably insufficient: the 1e-300 mutant scores 241/241 exact over [-120,120] and is not detected at all, while over [-255,255] it fails 6. Measured on the wide range: shipped 265 exact / 246 zero, failing tail j <= -10 (the filing's \"j <= -5\" is wrong \u2014 that is the 1e-6 mutant, measured at 251 zeros with tail j <= -5); 1e-24 mutant tail j <= -20; repair 511/511. This is the cleaner statement of the defect because nothing about the SPACE changed, only the caller's choice of basis vectors. (C) TWO COMPANION ASSERTIONS, without which a repair could simply delete the guard: (i) genuine degeneracy must still return 0 \u2014 u = s*e0, v = 2s*e0 over j = -255..254, measured 510/510 zeros for shipped, for the repair and for DBL_MIN-only, and 0/510 for no-guard, so it kills deletion; (ii) the LORENTZIAN NULL CASE g = diag(-1,1), u = v = (1,1), which must return exactly 0 \u2014 measured: the relative test alone does not fire (uu*vv = 0) and returns NaN, only the F64_TINY backstop fires. Companion (ii) is the only one of the three sweeps that kills a \"relative test, no backstop\" repair, and it is absent from the filing. Every threshold a repair might plausibly choose is therefore bracketed: 1e-12 (shipped), 1e-6, 1e-24 / sqrt-first, 1e-300, guard deleted, and relative-without-backstop; the two scale-free guards are the only survivors."
  },
  {
   "site": "src/f64_util.cyr:29 (ships as dist/hisab.cyr:70)",
   "fn": "f64_fmod(x, y)",
   "guarded_quantity": "|y| -- the raw, unnormalised absolute DIVISOR. Degree 1 in the caller's units, unbounded over (0, DBL_MAX]. REFUTATION ATTEMPTED AND FAILED: f64_fmod has ZERO call sites anywhere in src/ (grep over src, tests, examples: only the definition plus 8 test assertions). It is a leaf public export of the distributed bundle -- f64_util.cyr is listed in cyrius.cyml [lib] line 60 and the function appears verbatim at dist/hisab.cyr:69 -- so every real caller is an external consumer whose units this repo never sees. There is no normalising caller to appeal to, and no possible one.",
   "protects": "The single division f64_div(x, y) on line 30, whose quotient is truncated and multiplied back: x - trunc(x/y)*y. Not a convergence tolerance -- there is no loop, no iterate, no residual; one division, one trunc, one FMA-shaped subtraction. The only failure a |y|-only predicate can genuinely address is y == 0 (quotient +-inf or NaN -> inf*0 = NaN -> x - NaN = NaN). The doc comment on line 22 states exactly that contract: \"Returns 0 if y == 0\".",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "VERDICT UPHELD; three of the finding's supporting claims REFUTED by measurement and corrected here. Upheld because fmod is exactly homogeneous of degree 1 in both operands and for k = 2^n the identity fmod(kx, ky) = k*fmod(x, y) is bit-exact -- measured: the unguarded formula reproduces glibc fmod bit-for-bit at ALL 2095 scales n = -1074..1020 for both (3,5) and (7,3), zero failures. |y| moves with the caller's unit choice; EPSILON_F64 (0x3D719799812DEA11, decoded = 1e-12 exactly) does not. Shipped guard fails 978 of the 2041 scales in n = -1020..1020 (family A; 979 for B), onset at n = -43. All five refutation routes were checked and all fail: (1) |y| is neither dimensionless nor bounded; (2) no caller normalises because there is no caller -- it is a bundle-leaf public API; (3) not a convergence tolerance; (4) no existing test breaks under repair -- every y in tests/*.tcyr is 0, 3, -3 or 2 (abuse.tcyr:2313-2315, modules.tcyr:6494-6508; the finding's cited modules.tcyr:6439 is the WRONG LINE, it is 6508); (5) the sweep does discriminate the shipped value. CORRECTIONS. (a) The finding's headline example is FABRICATED and impossible on its face -- it claims f64_fmod(1e6, 1e-13) returns 9.99999999392329e-14 \"against a true 1.0000000000851952e-13\", but a remainder can never be >= its divisor. Measured: the formula returns exactly 0, true fmod is 2.625443659963001e-14. The ratio-hazard point survives and is worse than stated (silent 0, 100% error, at a |y| the guard PERMITS). (b) DBL_MIN's stated rationale -- \"exactly the set that breaks the division and nothing more\" -- is FALSE. The overflow hazard is a ratio condition and straddles DBL_MIN on both sides: f(1e10, 1e-300) = -inf with y NORMAL and passing a DBL_MIN guard, and f(1e300, 1e-11) = -inf TODAY at a y the shipped 1e-12 permits (true 9.4666e-12). DBL_MIN separates nothing. (c) Worse, DBL_MIN is itself a scale chosen for the caller, 296 decades lower: for a subnormal divisor with a comparable dividend the formula is EXACT (x = 3*2^-1074, y = 5*2^-1074 -> exactly 3*2^-1074) and a DBL_MIN guard fabricates 0 there -- 50 of the extended sweep's scales for family A, 51 for B. Its only genuine catch, subnormal y with large x, it converts from a LOUD -inf into a SILENT 0 (x = 1.0, y = 3*2^-1074: true 4.94e-324, zero-test gives -inf, DBL_MIN gives 0). Same class as complex.cyr:58, just quieter. So the repair is the exact-zero predicate, not DBL_MIN.",
   "fabricates": "Exactly 0 -- bit-indistinguishable from the legitimate 0 a correct fmod returns for an exact multiple. No rc, no error channel, value returned directly. Measured fabrications on the shipped code: f64_fmod(3e-13, 5e-13) returns 0 where the exact answer is 3e-13 (100% relative error; the unguarded formula returns it bit-exactly), and f64_fmod(1e-300, 1e-13) returns 0 where the exact answer is 1e-300 (unguarded formula bit-exact, quotient 1e-287, nowhere near any limit). Whenever |x| < |y| < 1e-12 the true answer is x itself and the function returns 0. The band is ~312 decades wide (1e-12 down to the smallest subnormal 4.94e-324), and measurement shows it is entirely untested: the 8 assertions that touch this function pass only y in {0, 3, -3, 2}.",
   "correct_threshold": "SHARPENED, and it is NOT the F64_TINY/DBL_MIN the finding proposed -- measurement kills that: use the EXACT-ZERO predicate, `if ((y & 0x7FFF_FFFF_FFFF_FFFF) == 0) { return 0; }` (equivalently `f64_lt(f64_abs(y), 4.94e-324)`, the smallest subnormal, which is the same set). The bit form is preferred because it is the one already used two lines down in f64_copysign and it catches -0.0, which an ordered comparison against 0 handles but which the 2.6.15 copysign defect proves this file has gotten wrong before. Rationale, all measured: (i) the trunc formula is bit-exact at every representable scale including the full subnormal range, so any threshold above zero fabricates over some correctly-computable region -- DBL_MIN over 50 scales of the sweep, 1e-12 over 978; (ii) DBL_MIN does not bound the overflow hazard it claims to (y = 1e-300 normal, x = 1e10 -> -inf), so it buys no property; (iii) where DBL_MIN does fire it replaces a detectable -inf with an undetectable 0, which is the fabricating-guard class it was meant to repair. The -inf on |x/y| > DBL_MAX and the precision loss above |x/y| > 2^53 are a separate algorithmic repair (scaled reduction, or return a non-finite so the caller can see it); they are ratio conditions and no |y|-only threshold, DBL_MIN included, can address them -- that must not be used to justify keeping either 1e-12 or DBL_MIN. Test-safety verified by inspection of every call site: abuse.tcyr:2313 and modules.tcyr:6508 both pass y = +0.0 and both still return 0; modules.tcyr:6494-6505 use y in {3, -3, 2}, untouched.",
   "scale_test": "AXIS 1, SCALE COVARIANCE, with the finding's range EXTENDED because measurement shows the original does not bracket its own recommendation. For k = 2^n assert f64_fmod(k*x0, k*y0) == k*expected BIT-EXACTLY (no tolerance, no f64_to -- 2.13.0's rule), on (x0, y0, exp) = (3, 5, 3) [the |x| < |y| branch, nothing removed] and (7, 3, 1) [q != 0], sweeping n = -1074 .. +1020, i.e. 2095 scales spanning the entire representable range down to the smallest subnormal. The extension is load-bearing and is the correction to the finding: over its proposed n = -1020..1020 the smallest divisor reached is 5*2^-1020 = 4.45e-307, so thresholds of 1e-307, 1e-310, 1e-320, the smallest subnormal AND a pure zero test ALL survive with 0 failures -- its claim that \"a DBL_MIN repair is the only one that survives\" is measurably false. Measured kill counts over the extended range (family A / family B): 1e-12 -> 1032/1033, 1e-15 -> 1022/1023, machine eps 2.22e-16 -> 1020/1021, 1e-30 -> 973/973, 1e-150 -> 574/575, 1e-300 -> 76/76, 1e-307 -> 52/53, 1e-310 -> 42/43, 1e-320 -> 9/10, DBL_MIN -> 50/51, exact zero -> 0/0. Every candidate a repair might reach for is killed, including the finding's own; only the exact-zero predicate survives, and the raw formula is bit-exact at all 2095 scales so the expectation is well founded (verified: 0 exactness failures with the guard removed). AXIS 2, RATIO -- the finding's version is BROKEN and is replaced. Its family (3,5), (7,3), (1e6,3), (1e12,3) tops out at |x/y| = 3.33e11, while a relative guard |y| < 1e-12*|x| fires only above |x/y| = 1e12: measured, not one member of that family fires any relative candidate, so the axis fails to discriminate the exact repair it exists for. Use instead x = 9007199254740991 (2^53-1), y = 3 -> remainder exactly 1 (ratio 3.0024e15, formula verified bit-exact against glibc), and its negation -(2^53-1) mod 3 -> exactly -1, which re-checks trunc-not-floor at high ratio in the same assertion. That kills eps_r = 1e-12 and 1e-15 while a zero or DBL_MIN guard never fires on it. LIMIT, recorded rather than papered over: a machine-eps relative guard (2.22e-16) fires only above |x/y| = 2^52, and a search over that band found NO input with a nonzero, exactly-computable remainder -- the formula's own exactness region ends there. It therefore cannot be killed by any value assertion and must be caught by interrogating the predicate DIRECTLY (assert the guard is false for every y != 0 across a decade sweep), which is 2.11.0's lesson that a guard has to be asked, not inferred from outputs."
  },
  {
   "site": "src/geo_advanced.cyr:1056 (siblings :1060 and :1067 are the same defect and must be repaired together)",
   "fn": "_epa_degenerate_normal",
   "guarded_quantity": ":1056 and :1060 compare hvec3_length_sq(e1) \u2014 the SQUARED length of a Minkowski-difference edge, dims L^2 \u2014 against the unsquared EPSILON_F64 (1e-12); effective cutoff |e1| < 1e-6. The sibling at :1067 compares hvec3_length_sq(hvec3_cross(e1, e2)) \u2014 dims L^4, since a cross of two length-L vectors is O(L^2) \u2014 against the same 1e-12; effective cutoff |e1||e2|sin(theta) < 1e-6, i.e. L < ~1e-3. Both are absolute thresholds on quantities that carry units.",
   "protects": "Nothing that can fail. :1056/:1060 are the \"is this vertex genuinely distinct from v0\" selector; :1067 is the \"is the set planar rather than a segment\" selector. Neither divides. The only division downstream is inside _epa_unit, which carries its own scale-free `f64_gt(l, 0) == 0` guard (:496-498), so removing these thresholds entirely cannot expose a division by zero.",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "REFUTATION ATTEMPTED AND FAILED on all five avenues; the finding is CONFIRMED by measurement, and two of its own fields were too lenient. (1) Not dimensionless: measured sweep of L over 1e-100..1e100 on the fixture the code's own comment names (gjk_epa_3d:1274, two coplanar zero-height boxes) gives |dot(n, z)| = 0 for 51 of the 131 decades in 1e-30..1e100 \u2014 the returned \"separating direction\" lies IN the contact plane, so translating along it never separates the shapes. The exact answer is scale-invariant (+/-z for every L), so the verdict changing with k is definitional proof the quantity scales: e1 ~ L, length_sq(e1) ~ L^2, cross ~ L^2, length_sq(cross) ~ L^4. (2) The caller does not normalise: gjk_epa_3d:1277 stores the return value straight into out_normal and returns 1; measured end-to-end, the caller receives the fabricated vector with rc = 1 and depth 0. (3) Not a convergence tolerance: it is a one-shot selector over a fixed simplex, and the SAME FILE repaired the identical expression to `f64_gt(hvec3_length_sq(dir), 0) == 0` at :266 in 2.9.0 with the reasoning \"the only value that is genuinely unusable is zero -- and testing against zero is scale-free\". That sweep stopped 790 lines short. (4) The proposed repair breaks NOTHING: patched copies of geo_advanced.cyr run against all four suites that include it produce BYTE-IDENTICAL output (hisab 416, modules 1827, edge_cases 233, abuse 776, 0 failed) \u2014 which is also the mutation evidence that 3252 assertions are blind to this guard, because modules.tcyr:8908-8910 is the only place the degenerate normal's DIRECTION is ever asserted and it runs at scale 1 only. (5) The scale test does discriminate, but the claim got two details wrong in the lenient direction, corrected below.",
   "fabricates": "THREE distinct fabrications, all measured, none reporting an error. (a) L <= 1e-7: the :1060 test fires and the function returns a hard-coded hvec3_unit_x() \u2014 a unit vector lying IN the z=0 contact plane, so the caller cannot tell it is fabricated. (b) 1e-6 < L <= 1e-4: e1 survives but the L^4 test at :1067 fails, control reaches :1069, and _epa_perp(e1) returns a unit IN-PLANE perpendicular \u2014 also |dot(n,z)| = 0. Measured with AXIS-ALIGNED boxes, so _epa_perp does NOT rescue this case (see scale_test). (c) L >= 1e77 (24 decades): length_sq(cross) ~ L^4 overflows to +Inf, +Inf passes the `> EPSILON_F64` test, _epa_unit divides by hvec3_length = +Inf and returns the ZERO VECTOR \u2014 measured hvec3_length_sq(out_normal) == 0 with gjk_epa_3d returning rc = 1. That is precisely the NULL/zero normal the 2.8.3 comment at gjk_epa_3d:1274 says the degenerate path exists to prevent (\"Returning 0 here left out_normal holding the NULL HVec3 for every caller that trusted 'returns 1 => out_normal is a normal'\"), and modules.tcyr:8907/8908 assert against it at scale 1 only.",
   "correct_threshold": "TWO changes; the claim's single proposal is necessary but NOT SUFFICIENT \u2014 measured. (i) :1056 and :1060 \u2014 replace `f64_lt(hvec3_length_sq(e1), EPSILON_F64) == 1` with `f64_gt(hvec3_length_sq(e1), 0) == 0`, the exact 2.9.0 repair form used at :266 of this file. Nothing divides, so DBL_MIN is not needed; zero is the only unusable value. (ii) :1067 \u2014 testing the raw cross against 0 is NOT enough: measured, the `> 0` repair alone still fails 24 of 131 decades (all of L >= 1e77) because the L^4 quantity OVERFLOWS to +Inf before it is ever compared, and _epa_unit then returns the zero vector. The quantity must be made dimensionless instead of merely re-thresholded: `var c = hvec3_cross(_epa_unit(e1), _epa_unit(hvec3_sub(_gjk_simplex_get(simplex, j), v0)));` then `if (f64_gt(hvec3_length_sq(c), 0) == 1)`. That is sin^2(theta) between two unit vectors \u2014 bounded in [0,1], degree-0 homogeneous, cannot overflow or underflow from operand magnitude, and 0 is exactly the collinear case the branch is asking about. Measured: 0 of 131 decades wrong, and all four suites byte-identical to baseline. (_epa_unit already returns hvec3_zero() on a zero input, so a duplicate vertex still reads as collinear and correctly falls through to the segment branch.)",
   "scale_test": "_epa_degenerate_normal is degree-0 homogeneous: for a zero-volume Minkowski difference, scaling BOTH shapes by k must return the SAME unit vector up to sign. FIXTURE (corrected \u2014 the claim's 45-degree rotation is NOT load-bearing): the axis-aligned pair modules.tcyr:8896 already builds \u2014 _mpr_mkbox(0,0,0,L,L,0) against _mpr_mkbox(L/2,L/2,0,L,L,0) \u2014 measured to fail on exactly the same 51 of 131 decades as the rotated version, because GJK's simplex diagonal is not axis-aligned and _epa_perp therefore does not land on +z. Rotating is harmless but buys nothing. BAND (corrected \u2014 must NOT reach 1e-100): sweep L in decade steps over 1e-30 .. 1e100, 131 decades, asserting f64 bit-exact |dot(n(L), (0,0,1))| == 1 AND hvec3_length_sq(n) == 1. Do not extend below ~1e-54: measured, _gjk_core_3d there returns a COLLINEAR simplex for this fixture even under a fully correct repair, so the assertion fails for a reason that has nothing to do with this guard and the test would false-fail (that is a separate finding). BRACKETING (measured, not argued): shipped code 51/131 wrong; unsquare-only 1e-6 wrong on the same low band; eps^2 = 1e-24 at :1067 still needs L > 1e-6, wrong on 1e-30..1e-6; DBL_MIN at :1067 clears the low end but dies on the 1e77..1e100 half where the L^4 operand is +Inf before any threshold is consulted; the claim's own `> 0` repair leaves 24/131 wrong (the same top band); only the normalise-before-cross form is 0/131. A sweep confined to 1e-3..1e3 kills none of them, and a sweep that stops at 1e30 fails to distinguish `> 0` from the correct repair."
  },
  {
   "site": "src/geo_advanced.cyr:1060",
   "fn": "_epa_degenerate_normal",
   "guarded_quantity": "hvec3_length_sq(e1) \u2014 the SQUARED length of the first simplex edge e1 = v_i - v_0 of the Minkowski difference A (-) B, units [world length]^2 \u2014 compared against the unsquared EPSILON_F64 (src/error.cyr:26 = 1e-12). Effective cutoff |e1| < 1e-6, and it scales as k^2 while the threshold is fixed.",
   "protects": "Nothing that can fail. There is NO division, sqrt or acos at the site; the guard is a rank classification of the affine hull \u2014 the early return that declares the whole simplex a single point and answers with a cardinal axis. The only downstream division is inside _epa_unit (:486-490), which already self-guards on `f64_gt(l, 0)` and whose own comment (:482-485) says in as many words: \"Normalise without EPSILON_F64's ABSOLUTE cutoff ... which silently kills every direction in a scene scaled to 1e-6.\"",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "Every refutation avenue was tried and each one is refuted by measurement, not by argument. (1) NOT dimensionless: e1 is a difference of two Minkowski-difference support points, so it carries world length units and its square carries them squared; the same geometry in metres and micrometres produces length_sq values 1e24 apart. (2) The caller does NOT normalise: gjk_epa_3d (:1239-1282) hands raw support functions and raw shape data straight through, and the whole surrounding stack is deliberately built to be scale-free \u2014 _gjk_core_3d's sibling guard was repaired to `> 0` in 2.9.0 at :266 with a comment naming exactly this failure (\"A scene at scale 1e-9 has |dir|^2 ~ 1e-18, so every direction GJK ever computed was declared degenerate\"). So GJK now correctly delivers a valid micro-scale simplex, and this function throws it away. (3) NOT a convergence tolerance: no iteration, no residual, no bound \u2014 one comparison, one early return, one fabricated answer. (4) The proposed repair breaks NO existing test: I installed `f64_gt(length_sq, 0) == 0` at :1056/:1060 and `> 0` at :1067 in a scratch copy of src/ and ran all five suites against it \u2014 hisab 416/416, foundation 359/359, modules 1827/1827, edge_cases 233/233, abuse 776/776, 3611 passed, 0 failed. MEASURED at the PUBLIC entry point on a 1-D-hull fixture (two collinear zero-thickness segments along x at scale k, hull is a segment, so the only correct answer is any n with dot(n, unit_x) == 0): shipped gjk_epa_3d returns a correct perpendicular for k = 1e100 down to 1e-6, and from k = 1e-9 through k = 1e-160 it returns exactly unit_x \u2014 |dot(n, unit_x)| = 1.000000000, i.e. a direction PARALLEL to the hull that provably does not escape the set \u2014 with rc = 1 and depth 0 and no error channel. With :1060 alone repaired the same sweep is correct from 1e100 down to 1e-160. Reason nothing saw it: the only assertions on a degenerate normal in the tree are modules.tcyr:8905-8910, all at scale 1, and the 2^-20 dyadic sweep at modules.tcyr:9187-9196 uses sphere/box tangency fixtures only while _ag_eval (modules.tcyr:772-816) never inspects the normal at all.",
   "fabricates": "hvec3_unit_x() \u2014 unit length, rc = 1, depth 0, no error channel. Measured stronger than \"plausible\": on the collinear-segment fixture at k <= 1e-9 the returned unit_x is exactly PARALLEL to the 1-D hull (dot = 1.0), the one direction that cannot separate the pair; on the coplanar-flat-box fixture it lies IN the z = 0 plane (dot with the true normal = 0). |n|^2 measured 1.000000000 in every wrong case, so the suite's own \"normal is a unit vector\" assertion (modules.tcyr:8908) cannot see it \u2014 only a DIRECTION assertion can.",
   "correct_threshold": "`f64_gt(hvec3_length_sq(e1), 0) == 0`, matching this file's own 2.9.0 repair at :266 \u2014 correct in kind and measured to hold the segment fixture from k = 1e100 down to k = 1e-160. Two sharpenings from measurement. (a) It is not exactly scale-free: L^2 underflows to +0 for |e1| < ~2.2e-162, so the repaired fixture first fails at k = 1e-165. If the guard must be exact all the way to DBL_MIN in L rather than in L^2, test the components against zero (or hvec3_length(e1) > 0), not the square. (b) Repairing :1060 ALONE is provably insufficient for the planar case: the :1060-only repair is BIT-IDENTICAL to shipped at all 18 decades of the coplanar-box fixture, because :1067 guards hvec3_length_sq(cross(e1, ...)) \u2014 a quantity that scales as L^4 \u2014 and fires two decades EARLIER (last correct k = 1e-3, first wrong k = 1e-6), routing to the in-plane _epa_perp fallback. :1056, :1060 and :1067 must be repaired together. Separately filed by the same sweep, NOT this guard: at k = 1e100 the L^4 cross product overflows to +Inf, _epa_unit scales by 1/Inf, and both shipped and repaired code return the ZERO vector with rc = 1 \u2014 an overflow defect at :1067/_epa_unit.",
   "scale_test": "The claim's proposed test DOES NOT DISCRIMINATE and must be replaced \u2014 measured, not argued. On the 45-degree/coplanar-box fixture a :1060-only repair scores identically to shipped at every decade (:1067's L^4 term masks :1060 completely), and a \">= 200 decade\" span rejects the CORRECT repair at both ends for reasons that are not this guard (L^4 underflow at k <= 1e-81 gives an in-plane answer; L^4 overflow at k = 1e100 gives a zero vector on shipped and repaired alike). Use instead a 1-D-HULL fixture, where :1060 is the only guard on the path: two collinear zero-thickness boxes along x \u2014 a = box(c=0, h=(L,0,0)), b = box(c=(L/2,0,0), h=(L,0,0)) \u2014 through the public gjk_epa_3d, sweeping L = 1e100 .. 1e-165. The single assertion is on DIRECTION, `|dot(n, unit_x)| == 0` (assert unit length too and it discriminates nothing \u2014 |n| measured 1.0 in every wrong case, the same lesson as 2.11.1's fabricated-identity quaternion). Measured brackets, so every threshold a repair might plausibly choose is separated: shipped (EPSILON_F64 on L^2, |e1| < 1e-6) last correct 1e-6, first wrong 1e-9; a 1e-24-on-L^2 repair (|e1| < 1e-12) fails below ~1e-12; a DBL_MIN-on-L^2 repair fails below ~1.5e-154; `> 0` on L^2 holds to 1e-160 and fails at 1e-165; a relative or component-wise-against-zero repair passes the whole span. Run the coplanar-box fixture BESIDE it, bounded to L in [1e-75, 1e75], to score :1067."
  },
  {
   "site": "src/geo_advanced.cyr:1067",
   "fn": "_epa_degenerate_normal",
   "guarded_quantity": "hvec3_length_sq(hvec3_cross(e1, vj - v0)) \u2014 the SQUARE of the cross product of two Minkowski-difference edge vectors, dimension L^4 \u2014 compared against the unsquared EPSILON_F64 (1e-12). Equivalent to |c| > 1e-6, i.e. an absolute AREA cutoff of 1e-6 in scene units squared. MEASURED breakpoint on the shipped coplanar-flat-box fixture (Minkowski edge = 4L, so |c| = 16 L^2 sin \u03b8): correct at L = 1e-3, fabricating at L = 1e-4, exactly the predicted 16 L^2 > 1e-6 \u2192 L > 2.5e-4.",
   "protects": "Nothing that can fail. It is the planar-vs-segment CLASSIFICATION: it selects which of two different answers the function returns (the plane normal _epa_unit(c), or the segment fallback _epa_unit(_epa_perp(e1))). The only division downstream is inside _epa_unit, which carries its own independent and correct guard (`if (f64_gt(l, 0) == 0) { return hvec3_zero(); }`, geo_advanced.cyr:487) \u2014 so this guard is not standing in for a divide-by-zero test, and removing it entirely would not produce a NaN or an Inf, only a different classification.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Reclassified from DEFECT_SQUARED_VS_UNSQUARED, on measurement. The original filing was right that the guard is broken and right about the mechanism (an L^4 quantity against a fixed 1e-12) but wrong about the repair implied by its label: unsquaring is NOT the fix and the filing's bracket ran the wrong way. Isolating this guard (with the sibling e1 guard at :1056 repaired so it cannot mask) and sweeping L over 1e0..1e-12 on the shipped fixture: shipped `|c|^2 > 1e-12` first fabricates at L = 1e-4; the unsquare-only candidate `|c| > 1e-12` first fabricates at L = 1e-7 \u2014 three decades LOWER, not 1.5 decades higher as the filing's \"3e-2\" claimed. Squaring a sub-unit quantity makes the test HARDER to pass, so the shipped guard misclassifies over a WIDER range than any unsquared form; unsquaring merely relocates the same absolute cutoff. The defect is therefore that an ABSOLUTE threshold is applied to a quantity that scales as the fourth power of scene scale, and no choice of constant can be right for it. All four refutation angles were tested and all four failed: (1) not dimensionless \u2014 measured, a rigid rescale of the same geometry changes the returned normal; (2) no caller normalises \u2014 gjk_epa_3d:1277 passes the user's raw support functions and shapes at world scale; (3) not a convergence tolerance \u2014 it is a one-shot branch selector, and the division it might be thought to protect is already guarded in _epa_unit; (4) the proposed threshold does NOT break an existing test \u2014 on modules.tcyr:8892 (coplanar flat boxes, which asserts the normal has no x and no y component) all five candidate guards return exactly (0,0,-1), identical to shipped. Separately confirmed the fabrication's shape by hand-trace and by measurement: for an in-plane e1 that is a DIAGONAL of the patch, _epa_perp crosses e1 with the cardinal axis it is least aligned with \u2014 z for an xy-plane edge \u2014 giving a vector lying IN the contact plane. The 45-degree rotation the filing asked for is unnecessary; the shipped axis-aligned fixture already produces a diagonal e1.",
   "fabricates": "A unit in-plane vector presented as the escape direction for a planar contact, written to out_normal with rc = 1 and depth 0 \u2014 a \"separating direction\" that does not separate. MEASURED on the shipped gjk_epa_3d with two coplanar flat boxes (half-extent L, offset L/2, plane z = 0): at L = 1e-4, 1e-5 and 1e-6 the returned normal is (-0.447213595, -0.894427191, 0), whose dot with the true plane normal is exactly 0. It is unit length and depth is exactly 0, so modules.tcyr:8908's `_ac_len_sq_e9(ep_n) == 1000000000` and the agreement sweep's classification/depth counters all pass on it \u2014 the same trap recorded for the fabricated identity quaternion in 2.11.1. Below L ~ 2.5e-7 the sibling guard at :1056 takes over and the function returns (1,0,0) instead, which is a second fabrication attributable to that guard, not this one.",
   "correct_threshold": "The dimensionless sin form this file already uses at :530 and :682: f64_le(hvec3_length(c), f64_mul(EPSILON_F64, f64_mul(hvec3_length(e1), hvec3_length(hvec3_sub(vj, v0))))) as the REJECT test \u2014 |a x b| = |a||b| sin \u03b8, so the ratio is bounded in [0,1] and scale-free. Verified correct at every decade from 1e0 to 1e-12 and byte-identical to shipped on both unit-scale fixtures. TWO SHARPENINGS the original filing missed. (1) The `f64_gt(hvec3_length_sq(c), 0)` alternative it offered as \"if a pure divide-guard is preferred\" is the wrong framing and must not be chosen on that ground: this guard protects no division (the division has its own guard in _epa_unit), so `> 0` is not a divide-guard here but a classification threshold of zero \u2014 it accepts a simplex whose collinearity is broken only by rounding and then hands _epa_unit a cross product whose DIRECTION is pure noise. Measured, `> 0` is indistinguishable from the relative form across all 12 decades of the scale sweep, so the scale sweep cannot choose between them; the choice must be made on a sliver (near-collinear) simplex at fixed scale, where only the relative form rejects. (2) The repair is UNOBSERVABLE below L ~ 2.5e-7 unless the sibling guard at :1056 (`f64_lt(hvec3_length_sq(e1), EPSILON_F64)`, an L^2 quantity against the same absolute 1e-12) is repaired in the SAME change: measured at L = 1e-10 with :1056 left as shipped, all five candidate c-guards return (1,0,0) identically. Fix both or the sweep proves nothing about either.",
   "scale_test": "Take the SHIPPED fixture at modules.tcyr:8892 verbatim \u2014 _mpr_mkbox(0,0,0,L,L,0) against _mpr_mkbox(L/2,L/2,0,L,L,0) \u2014 and run it at every decade of L, asserting |dot(n, (0,0,1))| == 1 exactly. No rotation is needed: e1 comes out a diagonal of the patch, so _epa_perp already picks z and returns an in-plane vector. MEASURED breakpoints, which correct the filing's inverted bracket \u2014 with :1056 repaired so it cannot mask, shipped `|c|^2 > 1e-12` first fabricates at L = 1e-4 (not 1e-3), the unsquare-only candidate `|c| > 1e-12` first fabricates at L = 1e-7 (not 3e-2 \u2014 three decades BELOW shipped, not above), and `|c|^2 > DBL_MIN`, the relative-sin form and `|c|^2 > 0` are all still correct at L = 1e-12. So L in 1e0..1e-8 already separates shipped from every repair and separates the unsquare candidate from the rest; the filing's demand for 200 decades is right only for splitting DBL_MIN from the relative form, whose crossover is at |c| ~ 1.5e-154, i.e. 16 L^2 ~ 1.5e-154 \u2192 L ~ 3e-78, so 1e-100..1e100 is required for THAT pair and for nothing else. Two things this sweep provably CANNOT do, both measured: it cannot separate `|c|^2 > 0` from the relative form at any scale (identical over all 12 decades tested \u2014 that pair needs a near-collinear sliver simplex at fixed scale, where the relative form rejects and `> 0` accepts a rounding-noise direction), and it cannot see any c-guard repair at all below L ~ 2.5e-7 while :1056 stands (all five candidates return (1,0,0) at L = 1e-10). Guard the assertion against the OTHER fabrication too: assert the normal is not (1,0,0) as well as |dot| == 1, so a fix that merely trades one fabrication for the other cannot score green."
  },
  {
   "site": "src/geo_advanced.cyr:1516 (siblings: :1530 dy, :1544 dz \u2014 one defect, three copies)",
   "fn": "_bvh_ray_hits_aabb (reached only from _bvh_query_ray_rec:1566 -> public bvh_query_ray:1583)",
   "guarded_quantity": "|dx|, the x component of GeoRay.direction. Dimensionless ONLY if the direction is unit, and it is not guaranteed to be: GeoRay is #derive(accessors) at geo.cyr:35, so the public GeoRay_set_direction stores whatever it is given (the hazard is documented in-tree at geo.cyr:409 and geo.cyr:477). \u26a0 But normalization is not even a mitigation here, which is the sharpest correction to the original filing: for direction (9e-13, 1, 0), sqrt(1 + 8.1e-25) rounds to EXACTLY 1.0, so geo_ray_new hands the guard dx = 9e-13 bit-for-bit with |d| = 1.0 (measured). The quantity that actually decides whether the slab matters is |dx| * t_range against the box extent, and neither t_range nor the extent appears in the test \u2014 so even the bounded, unit-direction case has no correct constant.",
   "protects": "the reciprocal f64_div(F64_ONE, dx) on line 1517 (and 1531 / 1545) that feeds the slab arithmetic. It is a divide-by-zero guard, not a tolerance: nothing below it converges, iterates, or compares two quantities for equality.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Could not refute; all five refutation routes were tried and each is closed by measurement. (1) BOUNDED/DIMENSIONLESS \u2014 fails: with |d| = 1.0 exactly, the widened fixture below returns MISS for every dx in [DBL_MIN, 1e-13] where the truth is HIT, so the threshold is wrong even where the quantity IS dimensionless. (2) CALLER NORMALISES \u2014 fails twice: bvh_query_ray passes the ray through untouched, and geo_ray_new's normalization leaves dx = 9e-13 unchanged because the length rounds to 1.0. (3) CONVERGENCE TOLERANCE \u2014 fails: it guards a division, and the else branch is straight-line slab algebra. (4) EXISTING TEST WOULD CHANGE \u2014 checked, does not: every ray reaching bvh_query_ray in tests/*.tcyr and tests/*.bcyr has direction components exactly 0 or +-1 (hvec3_unit_x, hvec3_unit_z, F64_ONE, hvec3_zero) or NaN \u2014 modules.tcyr:8862/8868/8874/8879/8881/8888, abuse.tcyr:1763/1785, hisab.bcyr:719/908. f64_lt(0, DBL_MIN) is still 1, so exact-zero components keep taking the parallel branch, and NaN compares false under both constants. The repair is bit-identical on the whole suite \u2014 which also means the suite cannot notice it being reverted. (5) SCALE TEST DOES NOT DISCRIMINATE \u2014 this one landed partially and is corrected below rather than refuting the finding. Confirmed positively: the file is a pre-2.10.2 copy of the slab code (geo.cyr:546/583/620 \u2014 NOT :527/564/601 as filed \u2014 now read _GEO_F64_TINY), and the rationale at geo.cyr:499-507 applies verbatim. The consequence is strictly worse than in geo.cyr: geo_ray_aabb returns a wrong t, this returns a wrong PRUNE, and the function's own header (geo_advanced.cyr:1487) says pruning a node \"discards its ENTIRE subtree\".",
   "fabricates": "a confident 0 (= miss). _bvh_query_ray_rec:1566 tests `== 0` and returns immediately, so a whole subtree the ray genuinely enters is discarded and bvh_query_ray hands back a short or empty vec with no error channel and no sentinel. The filed fixture reproduces exactly as claimed (simulated against the shipped arithmetic): box x in [0,1], y,z in [-1e12,1e12], origin (-0.5,-1e12,0), unit direction (9e-13,1,0) \u2014 true entry t = 5.5556e11, true exit t = 1.6667e12; current guard -> parallel branch -> ox = -0.5 < bmin.x = 0 -> 0; with DBL_MIN -> 1.",
   "correct_threshold": "DBL_MIN, spelled as a file-local alias `var _GA_F64_TINY = F64_TINY;` beside the existing _GA_F64_POS_INF/_GA_F64_NEG_INF (geo_advanced.cyr:21-22), then `f64_lt(f64_abs(dx), _GA_F64_TINY) == 1` at :1516/:1530/:1544 \u2014 textually identical in shape to the shipped geo.cyr:546/583/620. Use the alias, not a fresh hex literal: error.cyr:45 already holds F64_TINY = 0x0010_0000_0000_0000 and the alias form matches geo.cyr:26, quat.cyr:12 and autodiff.cyr:17, so scripts/check-constants.sh gains no new hand-encoded constant to verify. Do NOT \"improve\" on DBL_MIN toward the true finite-reciprocal cut (~5.56e-309, where 1/x last stays under DBL_MAX): DBL_MIN is deliberately ~4x conservative, subnormal reciprocals lose precision, and byte-identity with geo.cyr is the point. geo_ray_obb's `!= 1` spelling (geo.cyr:767/809/849) is equivalent \u2014 pick it only if it makes the two functions read the same.",
   "scale_test": "Two assertions; BOTH filed versions needed correcting, and as filed neither is sound. (1) DIRECTION-MAGNITUDE COVARIANCE \u2014 bvh_query_ray must return the same hit set for direction d and k*d. \u26a0 The fixture choice is load-bearing and the filing omitted it: swept over k = 2^p, the three existing modules.tcyr BVH rays (interior (0.5,0.5,0.5), face (1,0.5,0.5), corner (0,0.5,0.5), all dir +x) are 0/511 non-covariant under the CURRENT guard \u2014 they kill nothing, because when the guard fires the parallel branch's inside-slab test accepts anyway. The origin must lie OUTSIDE the box in the shrinking component: with origin (-3,0.5,0.5) into the [0,1]^3 pair, 246/511 sampled k disagree under 1e-12 and 0/511 under DBL_MIN (measured). \u26a0 And the range must be p in [-1020, +1020], not the filed +-1000: a repair that chose 1e-307 is non-covariant at exactly 1 of the 511 samples (p = -1020) and SURVIVES a +-1000 sweep; 1e-300 survives with only 3 disagreeing samples there. (2) UNIT-DIRECTION FALSE NEGATIVE \u2014 \u26d4 the filed fixture cannot carry the sweep it is asked to carry: with y,z extents of 1e12 the y slab exits at t = 2e12 while entry needs t = 0.5/dx, so the TRUTH is already MISS at dx = 1e-15 and all five candidate thresholds agree from there down \u2014 it brackets 1e-12 vs 1e-6 and nothing below. Widen as: box x in [0,1], y in [-1e308,1e308], z in [-1,1]; origin (-0.5,0,0); direction (dx, sqrt(1-dx*dx), 0), which is unit to the last bit. Measured discriminating dx values on that fixture, one per candidate repair: 1e-7 (kills 1e-6), 1e-13 (kills the shipped 1e-12), 1e-17 (kills DBL_EPSILON 2.2e-16), 1e-100 (kills EPSILON^2 = 1e-24), 1e-305 (kills 1e-300), and dx = DBL_MIN itself plus 4e-308 (kills 1e-307 \u2014 the only pair the covariance sweep barely reaches). \u26d4 Assert MISS, not HIT, at dx = 1e-310, 1e-320 and 0: 1/1e-320 overflows to +Inf and the REPAIRED code returns 0 there too, so the filed sweep endpoints would fail against the correct repair. The filed rule \"truth is hit for every dx whose reciprocal is finite\" is wrong in both directions \u2014 false above (1e-320's reciprocal is not finite) and false below (dx in [5.56e-309, DBL_MIN) has a finite reciprocal that geo.cyr's contract deliberately treats as parallel). State the property as the DBL_MIN cut itself. Mutation-proof it by reinstating EPSILON_F64 at :1516 only (dy/dz left repaired) and confirming the dx assertions fail \u2014 and grep the mutant in place before believing the count."
  },
  {
   "site": "src/geo_advanced.cyr:1530",
   "fn": "_bvh_ray_hits_aabb",
   "guarded_quantity": "|dy| = f64_abs(HVec3_y(GeoRay_direction(ray))), the y component of the ray direction. Its dimension is scene-length per unit of ray parameter t, i.e. it is only \"bounded to [0,1]\" if the direction is unit \u2014 and |d| is precisely the unit in which t is measured, so it is not a dimensionless quantity, it is a scale factor. Same quantity, same missing precondition as :1516 (x) and :1544 (z).",
   "protects": "the reciprocal at :1534, `var iy = f64_div(F64_ONE, dy)` \u2014 a plain divide-by-zero guard, not a tolerance and not a convergence test.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED by measurement, and every refutation avenue was tried and closed. (1) NOT dimensionless: what decides whether the y slab may be deleted is |dy| * t_range against the box's y extent; none of those three appear in the test, which compares a per-unit-t rate against a bare 1e-12. (2) The caller does NOT protect \u2014 it is the opposite: `geo_ray_new` NORMALISES, and normalisation is what manufactures the tiny component. Measured witness, direction unit to 16 digits (asserted): box x[0,4e12] y[0,1] z[0,1], origin (0, -0.9, 0.5), direction (1, 9e-13, 0). `geo_ray_aabb` \u2014 the sibling geo.cyr repaired in 2.10.2 onto `_GEO_F64_TINY` \u2014 returns t = 1e12, a real forward hit at the y = 0 face; `_bvh_ray_hits_aabb` returns 0 and `bvh_query_ray` returns an EMPTY vec. One primitive, so the leaf IS the root: the entire tree is discarded. This is geo.cyr:486-488's own recorded failure (\"the same box expressed both ways answered 0 and 4.5e12\") reproduced one level up, now between geo.cyr and geo_advanced.cyr. (3) NOT a convergence tolerance \u2014 it guards a division literally. (4) The proposed correct_threshold breaks NO existing test \u2014 CHECKED, not assumed: with all three slabs on F64_TINY, 3611/3611 assertions pass (hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776), byte-identical counts to the pristine baseline, with the patch grep-proven installed both before and after the run. The one test that pins this exact branch, modules.tcyr:8881 `br_miss` \"parallel ray outside the y slab -> 0\", has dy bitwise +0.0 (asserted), and +0.0 < DBL_MIN, so it stays on the parallel branch and still returns 0. (5) The all-parallel path also lacks geo_ray_aabb_face's `slabs` counter, so a zero direction returns 1 for every node \u2014 a second, opposite fabrication in the same function that this threshold repair does not address and that should be filed separately.",
   "fabricates": "Returns 0, \"no hit\" \u2014 and since `_bvh_query_ray_rec` returns immediately on 0, the node AND ITS WHOLE SUBTREE are pruned. There is no error channel: the predicate's only outputs are 1 and 0, so a false 0 is indistinguishable from a true miss, and `bvh_query_ray` hands the caller a short (or empty) candidate list with no signal. Measured: 1 candidate is the truth, `vec_len` = 0 is returned, while `geo_ray_aabb` on the identical box returns 1e12.",
   "correct_threshold": "F64_TINY (DBL_MIN, 2.2250738585072014e-308) \u2014 the smallest magnitude whose reciprocal is finite, which is exactly and only what the `f64_div(F64_ONE, dy)` at :1534 needs. Spell it as a module-local alias `var _GA_F64_TINY = F64_TINY;`, matching geo.cyr:26 `_GEO_F64_TINY`, autodiff.cyr:17 `_AD_F64_TINY` and quat.cyr \u2014 geo_advanced.cyr today has no TINY alias, only `_GA_F64_NEG_INF`/`_GA_F64_POS_INF`. Apply to all THREE slabs (:1516 dx, :1530 dy, :1544 dz) in one change; a partial move makes the function disagree with itself across axes, which is the same self-contradiction geo.cyr:486-488 records. Verified: the three-slab move compiles clean and leaves all 3611 existing assertions passing.",
   "scale_test": "SHARPENED \u2014 the proposed test is inadequate two ways, both measured. (a) A single 9e-13 witness with x and y exchanged kills only the shipped 1e-12: a 1e-15 alternative repair PASSES it (measured: D1 passed on the 1e-15 mutant). It does not bracket. (b) Worse, uniform scene scaling \u2014 the ordinary \"same problem in different units\" \u2014 DOES NOT DISCRIMINATE AT ALL here, because the direction is unit and therefore scale-INVARIANT: scaling origin and box by 1e0..1e40 leaves |dy| untouched, and 41 of 41 scales answer identically on the shipped tree, on the 1e-15 mutant AND on the repair. Use instead, on all three axes: (1) DIRECTION-SCALE COVARIANCE \u2014 the predicate is homogeneous of degree 0 in d (scaling d by k>0 reparameterises t but does not move the ray as a point set), so assert `_bvh_ray_hits_aabb(ray_k, box) == (geo_ray_aabb(ray_k, box) != 0)` for d scaled by k = 1e-320 .. 1e+60 via `GeoRay_set_direction`. Measured: shipped fails 296/381 (top bad decade e = -295), the 1e-15 mutant fails 293/381 (top bad e = -295), the DBL_MIN repair passes 381/381 \u2014 so it brackets EPSILON_F64, every intermediate candidate and DBL_MIN itself, with the tail below e = -295 where dy flushes to zero agreeing on all three because 1/dy is genuinely not finite there. (2) A DECADE SWEEP of the guarded quantity itself: |dy| = 9e-j for j = 1..300, box x extent sized to 4 * (0.9/|dy|) so the ray really does climb into the y slab, asserting sibling agreement with `geo_ray_aabb`. Measured: shipped first contradicts at j = 13 (288/300 bad), the 1e-15 mutant at j = 16 (285/300 bad), the repair 0/300. (3) Keep the public-API witness as the named regression guard: a one-primitive BVH where `vec_len(bvh_query_ray(tree, ray))` must equal 1 while `geo_ray_aabb` on the same box returns 1e12 \u2014 it names the symptom (a whole subtree silently lost) that a slab-level assertion does not."
  },
  {
   "site": "src/geo_advanced.cyr:1544",
   "fn": "_bvh_ray_hits_aabb",
   "guarded_quantity": "|dz|, the z component of GeoRay_direction. NOT a length: geo_ray_new (src/geo.cyr:38-44) calls hvec3_normalize, so on the constructed path this is a DIMENSIONLESS direction cosine bounded by 1. Measured: |dz| is bit-identical across 41 binary decades of unit change (2^-20..2^+20) and moves <= 1e-14 relative (2 ulp) across 13 decimal decades.",
   "protects": "Nominally the division f64_div(F64_ONE, dz) on the else branch. Its actual effect is a SEMANTIC branch: when it fires the z slab is deleted from the intersection entirely and replaced by an origin-containment test.",
   "verdict": "DEFECT_WRONG_QUANTITY",
   "why": "The finding survives but its CLASS was wrong. |dz| is dimensionless and bounded, so it does not scale with the problem's length units - src/error.cyr:40-44 names exactly this case (\"EPSILON_F64 remains right for a genuinely dimensionless, bounded quantity - a normalised cosine\") as the exemption. I tested that exemption and it is defeated by measurement, not by dimensional analysis: an absolute tolerance on a bounded quantity is right only when \"below the tolerance\" implies \"the answer does not change\", and here it changes on 287 of 299 measured decades. The quantity that decides whether deleting the z slab is safe is |dz| * t_range against the box's z extent; none of those three appear in the test. That is the library's own settled wording for the sibling it already repaired: src/geo.cyr:489 - \"That threshold is scale-free and the consequence is not.\" geo_ray_aabb was moved to _GEO_F64_TINY in 2.10.2; the three slab guards in this BVH copy (1516/1530/1544) were never migrated, so a normalised ray gets one answer from geo_ray_aabb and the opposite from _bvh_ray_hits_aabb on the same ray and the same box. Secondary, weaker witness on the non-normalising public path (GeoRay_set_direction, which the geo_ray_aabb header already names as reachable): the predicate must be degree-0 in |d|, since scaling d reparameterises t but leaves the ray as a point set unchanged - measured, it answers HIT at 20 of 41 magnitudes of the SAME ray and flips at |d| = 2, where |dz| crosses 1e-12. That is a reparameterisation invariance, not a unit change, so it does not rescue the ABSOLUTE_ON_SCALING label either.",
   "fabricates": "A silent false MISS - the subtree prune, measured, not argued. Box x in [0, 4e13], y and z in [0,1]; ray origin (0, 0.5, -0.9), direction normalize((1, 0, 9e-13)) with hvec3_length asserted bit-exactly F64_ONE. |dz| = 9e-13 is below EPSILON_F64 (1e-12) so the guard fires, oz = -0.9 < bmin_z = 0, and the function returns 0. In the same library geo_ray_aabb returns a real forward t whose hit point is inside the box on all three axes. bvh_query_ray on a one-primitive tree therefore returns an EMPTY vec: the only leaf, discarded, with rc = 0 and no error signal. |dz| here is 295 decades above DBL_MIN, so 1/dz is finite and exact - the division the guard exists for could not have failed.",
   "correct_threshold": "F64_TINY (DBL_MIN, 0x0010_0000_0000_0000) - unchanged from the claim, now verified rather than asserted, and for the same reason src/geo.cyr gives: guard exactly what makes the division fail and nothing more. Apply to all three slabs (1516 dx, 1530 dy, 1544 dz), not 1544 alone, and reach it through a _GA_F64_TINY alias so geo_advanced matches _GEO_F64_TINY / _AD_F64_TINY / _QUAT_F64_TINY (src/error.cyr:28-45). CHECKED, NOT ASSUMED: patching 1544 alone, and patching all three, leaves all five suites BYTE-IDENTICAL against shipped - 3611/3611 passing (hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776). The existing parallel-ray fixtures (tests/modules.tcyr:8881 \"parallel ray outside the y slab -> 0\") carry dz and dy of exactly +0.0, which is below DBL_MIN, so they stay on the parallel branch and are untouched.",
   "scale_test": "REPLACED - the claimed test (\"the :1516 sweep with x and z exchanged\", framed as the same problem in different units) is REFUTED by measurement: it discriminates nothing, because geo_ray_new normalises and a unit change leaves |dz| bit-identical, so shipped and every candidate repair answer identically at every scale. The sweep that does discriminate holds the direction UNIT and varies t_range instead. Fixture Z(k), k = 1..299: box x in [0, 4e+k], y and z in [0,1]; origin (0, 0.5, -0.9); direction normalize((1, 0, 9e-k)) - |d| is bit-exactly 1.0 for every k >= 9. The ray enters the z slab at t = 1e+k, well inside the x extent, so the truth is a HIT at every k, independently confirmed by geo_ray_aabb (299/299 hits) and by an exact rational slab reference. It BRACKETS every threshold a repair might plausibly choose, each flipping to a wrong MISS at its own decade: EPSILON_F64 1e-12 at k=13 (measured on shipped: right on 12 of 299, first wrong at exactly k=13), DBL_EPSILON ~1e-16 at k=17, _GEO_F64_EPS_SQ 1e-24 at k=25, 1e-30 at k=31, 1e-100 at k=150; DBL_MIN never flips anywhere in range. Pair it with the MISS control that stops a repair from turning everything into a hit: same near-parallel dz with the x extent 100x short (k = 4..59), truth MISS, and both shipped and the DBL_MIN repair answer MISS on all 56."
  },
  {
   "site": "src/geo_advanced.cyr:2018",
   "fn": "time_of_impact",
   "guarded_quantity": "speed = hvec3_length(vel_b - vel_a), a relative SPEED with dimensions L/T. It scales linearly with the length unit and inversely with the time unit. Measured: the function's answer flips at exactly this threshold (rc = 1 at speed 2^-39 = 1.819e-12, rc = 0 at 2^-40 = 9.095e-13) for a physically identical problem.",
   "protects": "the division f64_div(F64_ONE, speed) on the next line (:2023) that builds the unit direction u0, which is used only to probe the Minkowski difference's extent (gscale). Nothing else in the function consumes speed; the loop's own tolerances are separate and both relative (_GA_TOI_REL at :1944, tol = gscale * _GA_TOI_TOL_REL at :2032).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED AND FAILED on every axis; the defect is now measured rather than argued. (1) Not dimensionless: speed is L/T. Held the geometry at unit scale and changed only the TIME unit (velocity x 2^-m, max_t x 2^m) \u2014 out_t * speed is BIT-IDENTICAL for every m tested, and rc flips 1 -> 0 at exactly m = 40 (speed 9.095e-13), with the fast side clean to 2^+200. A guard that decides \"never collides\" on the choice of second is not measuring anything physical. (2) No caller normalises: it is a public entry point taking raw HVec3 velocities; the two suites that call it feed 0 and 5e-7..200. (3) Not a convergence tolerance: it guards the division on the following line, and the file's own convergence tests are relative and elsewhere. (4) The repair breaks nothing: the only inputs whose behaviour changes are speeds in (0, 1e-12), and no assertion in tests/*.tcyr supplies one \u2014 abuse.tcyr:2035 and modules.tcyr:6691 use speed exactly 0, which f64_gt(speed,0)==0 still answers 0. The branch itself is sound (overlap at t = 0 is ruled out above, so zero relative velocity IS a correct \"never collides\"); only the threshold is invented, and it contradicts _epa_unit's stated rule at :482-487 in the same file and error.cyr:41-44's explicit \"NOT A REPLACEMENT FOR EPSILON_F64\" note for division guards. What DID fall to the refutation is the proposed scale_test, corrected below.",
   "fabricates": "return 0 \u2014 \"no collision within max_t\", a confident negative with no error channel, and out_t/out_normal are left holding whatever the caller had. Measured, not hypothesised: two unit spheres with a surface gap of 1e-3 closing head-on at 2^-39 = 1.819e-12 per unit time with max_t = 1e12 return rc = 1 and out_t = 5.4975581e8 (= gap/speed); the SAME configuration at 2^-40 = 9.095e-13 returns rc = 0 with out_t untouched, although the impact at t = 1.0995e9 is three decades inside the horizon. Expressing the identical problem in millimetres (speed 9.095e-10) answers it correctly.",
   "correct_threshold": "if (f64_gt(speed, 0) == 0) { return 0; } \u2014 this file's own _epa_unit rule (:482-487). NOT f64_lt(speed, F64_TINY): measured, the two are behaviourally IDENTICAL here and the >0 form is strictly better. `speed` comes from hvec3_length, which squares before the sqrt, so any relative velocity below 2^-538 has its square underflow and the length is returned as exactly 0 (verified: 2^-537 -> nonzero, 2^-538 -> exactly 0, i.e. the guarded quantity is either 0 or >= 2.3e-162 and can never be subnormal). DBL_MIN is therefore unreachable as an observed value and adds nothing over > 0, while > 0 additionally catches a NaN velocity that f64_lt(speed, F64_TINY) and the shipped f64_lt(speed, EPSILON_F64) both fall through (verified: f64_lt(NaN, EPSILON_F64) == 0). Nothing between 2.3e-162 and 1e-12 makes the division fail.",
   "scale_test": "THE CLAIM'S ORIGINAL LENGTH SWEEP IS NOT DISCRIMINATING AS SPECIFIED \u2014 two measured reasons, both fixed below. (a) It cannot bracket DBL_MIN: speed flushes to exactly 0 below 2^-538 (above), so every candidate repair at or under ~1e-162 returns 0 and the claimed \"DBL_MIN crossing decade\" does not exist; the usable floor for the guarded quantity is 2^-537. (b) Built from this repo's own sphere support it measures the WRONG cutoff: `_mpr_sup_sphere` (tests/modules.tcyr:700), which every shipped TOI test uses, normalises its direction with hvec3_normalize \u2014 carrying the same absolute 1e-12 (src/vec3.cyr:110). Running the claim's exact fixture (positions, radii AND velocity scaled by k = 2^-kk, max_t fixed) with that support, out_t stops being bit-covariant at kk = 30 (k = 9.3e-10), TEN decades above where the guard under test fires (kk = 40); with a scale-free support (the _epa_unit len>0 rule) the same fixture is bit-exact until exactly kk = 40 \u2014 the guard alone. So a 1e-12, a 1e-24 and a >0 repair all look identical below 1e-9 and the correct repair FAILS the test. CORRECTED TEST, in two parts, both bit-exact (power-of-two factors are exact through +,-,*,/ and sqrt, so no tolerance is needed): (1) TIME-UNIT COVARIANCE, the unconfounded discriminator \u2014 unit spheres (r = 1 at the origin, still; r = 1 at x = 2 + 1e-3 closing along -x), speed = 2^-m, max_t = 100 * 2^m, for m = 0..520 (floor set by the 2^-537 flush, not by DBL_MIN). Assert rc == 1 at every m and out_t * speed BIT-IDENTICAL to the m = 0 run. This brackets 1e-6 (m = 20), 1e-12 (m = 40, where the shipped code flips), 1e-24 (m = 80) and any eps^2 or 1e-15 variant, and no length in the problem changes, so no other tolerance in the tree can move. Verified against the shipped code: bit-identical and rc = 1 for m < 40, rc = 0 from m = 40. (2) LENGTH-UNIT COVARIANCE as a companion, but ONLY with a fixture-local scale-free support (normalise by len > 0, never hvec3_normalize) \u2014 scale positions, radii and velocity by k = 2^-kk with max_t fixed, kk = 0..60, assert rc == 1 and out_t bit-identical to kk = 0. Verified: exact to kk = 39 and broken at kk = 40 on the shipped code, exact throughout with the guard repaired; the upward direction is clean to k = 2^+300."
  },
  {
   "site": "src/geo_advanced.cyr:2272",
   "fn": "cga_geometric_product",
   "guarded_quantity": "|a_i|, a single blade COEFFICIENT of the left multivector (and at :2275 the same test on |b_j|). Dimensioned and unbounded: cga_point writes the raw coordinates x,y,z into blades 1-3 and (p^2-1)/2, (p^2+1)/2 into blades 4-5, so one object mixes L and L^2; cga_sphere carries r^2/2; cga_plane normalises only the normal and stores d raw. Verified by reading the constructors at src/geo_advanced.cyr:2450/2471/2487 \u2014 nothing bounds a coefficient to O(1).",
   "protects": "Nothing. It is a pure sparsity skip guarding a 32-iteration inner loop of multiply-accumulate \u2014 there is no division, sqrt, acos or normalisation anywhere in the loop body (src/geo_advanced.cyr:2268-2290). Confirmed it is not a convergence tolerance either: the loop is a single finite pass, not an iteration.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Could not refute on any of the five routes; all were checked by measurement, not assumed. (1) NOT dimensionless/bounded \u2014 see guarded_quantity; a CGA multivector is inhomogeneous in degree, so no single scalar normalisation could even make it dimensionless. (2) NO caller normalises: cga_sandwich, cga_norm_sq, cga_dual, cga_pseudoscalar_inv, cga_blade_inverse and cga_project all forward user multivectors verbatim, and `fn cga_geometric_product` is exported in dist/hisab.cyr, i.e. it is a public consumer entry point. (3) NOT a convergence tolerance \u2014 pure sparsity skip. (4) The proposed repair breaks NO existing test: a patched copy of the tree (both guards -> exact-zero) runs 416/359/1827/233/776 passed, 0 failed across hisab/foundation/modules/edge_cases/abuse \u2014 bit-identical counts to the unpatched baseline, because every cga coefficient any suite constructs is either exactly 0 or >= 0.5. (5) The scale test DOES discriminate the shipped threshold (measured below), so the finding stands. Since the guard protects no operation, the 2.10.2 rule (\"guard exactly what makes the DIVISION fail and nothing more\") admits only exact zero; as written the operator is not bilinear \u2014 measured, bilinearity in `a` fails at k = 2^-40 (8 of 32 blades) with a normalised so max|a_i| = 1. The same guard pair is replicated at 8 sites across 4 functions \u2014 cga_geometric_product (2272/2275), cga_outer_product (2297/2300), cga_left_contraction (2328/2331), cga_right_contraction (2358/2361) \u2014 so a repair confined to this one site leaves the projection/rejection/dual paths truncating.",
   "fabricates": "A silently truncated (often exactly zero) multivector, returned with no error channel. MEASURED, not argued: cga_sandwich(cga_point(0,0,0), cga_translator(d,0,0)) returns an e1 component of EXACTLY +0.0 for 121 of 256 magnitudes d = 2^e swept over 2^-520..2^500 \u2014 i.e. every d < 2e-12 (the translator's bivector coefficients are -d/2, so the crossing is at d = 2e-12) \u2014 while the exact-zero repair returns e1 bit-exactly equal to d for all 256, over the full 1020-binade span. cga_rotor at an angle below 2e-12 rad likewise acts as the identity (sin(theta/2) < 1e-12 is deleted while the cos scalar 1.0 survives). The truncation is invisible: the scalar part of a versor is ~1 and always survives the guard, so the returned object still looks like a well-formed versor.",
   "correct_threshold": "Exact zero. `if (ai != 0)` is correct AND safe \u2014 the -0.0 concern is measured non-existent: a -0.0 blade yields +0.0 in the result under both the shipped guard and the raw bit test, because the accumulator starts at +0.0 and +0.0 + (-0.0) = +0.0. (`f64_eq(ai, 0) == 0` is equally acceptable and additionally skips -0.0.) It is also strictly cheaper than f64_abs + f64_lt. Apply it to all 8 sites (2272/2275, 2297/2300, 2328/2331, 2358/2361), not just this one. If a magnitude-based skip is ever wanted for speed it must be relative to the multivector's own scale (e.g. EPSILON_F64 * max_k|a_k|), never absolute \u2014 but note even that breaks bilinearity in the inhomogeneous CGA basis, so exact zero is the only defensible choice here.",
   "scale_test": "CORRECTED \u2014 the original sweep's claim to bracket DBL_MIN is FALSE, measured: a DBL_MIN-thresholded variant produces 0 mismatches at EVERY rung of the a-only ladder 2^1000..2^-1000, because with max|a_i| = 1 the bottom rung 2^-1000 never reaches the 2^-1022 crossing, and pushing lower makes bit-exactness fail for legitimate subnormal reasons. Use BOTH halves: (A) a-only bilinearity, fixed a = e1 + 0.5*e2, b = cga_point(1,2,3), k an exact power of two: cga_geometric_product(cga_mv_scale(a,k), b) must equal cga_mv_scale(cga_geometric_product(a,b), k) component-for-component. MEASURED: shipped fails from k = 2^-40 (8 of 32 blades; ladder rung 2^-120), a \"merely lowered\" 1e-30 also fails at 2^-120, exact-zero repair passes all rungs 2^1000..2^-1000 with 0 mismatches (no false positives). (B) RECIPROCAL sweep, which is what actually brackets every candidate threshold down to and past DBL_MIN while keeping every product in the normal range: cga_geometric_product(cga_mv_scale(a, 2^-ea), cga_mv_scale(b, 2^+eb)) must equal cga_mv_scale(cga_geometric_product(a,b), 2^(eb-ea)) bit-for-bit, with a's coefficients dyadic. MEASURED: 4 blades mismatch for shipped 1e-12 at ea=40, for 1e-30 at ea=120, and for DBL_MIN at ea=1030/eb=1000; the exact-zero repair mismatches 0 blades at ea=1030 and even ea=1070 (deep subnormal a). (C) Geometric instance, now verified exact rather than assumed: cga_sandwich(cga_point(0,0,0), cga_translator(d,0,0)) must return e1 BIT-EXACTLY d for d = 2^e, e = -520..500 \u2014 the exact-zero repair hits all 256, shipped returns exactly 0 on 121 of them. Repeat (A)-(C) against cga_outer_product / cga_left_contraction / cga_right_contraction, or the repair will look complete while three of the four products still truncate."
  },
  {
   "site": "src/geo_advanced.cyr:2275",
   "fn": "cga_geometric_product",
   "guarded_quantity": "|b_j|, the j-th blade coefficient of the RIGHT multivector - an unnormalised, dimensioned component of a 32-blade CGA multivector (length for cga_point's e1/e2/e3 and cga_translator's -t/2, length^2 for the ep/em terms, arbitrary for anything built with cga_mv_set / cga_mv_scale). Same quantity and dimensions as :2272 on the left operand.",
   "protects": "nothing - the inner half of a pure sparsity skip. There is no division, sqrt, acos or iteration anywhere in the loop body; the only use of b_j is f64_mul(ai, bj) accumulated into r. Skipping merely avoids one _cga_geo_blades call (a 16-byte bump alloc).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ROUTES; ALL FAIL, MEASURED (scratch tree /tmp/claude-1000/-home-macro-Repos-hisab/68915bf3-8a70-4292-b984-a0be0a4996e8/scratchpad/eps/refute/cga2275_mine, cyrius 6.6.2). (1) NOT dimensionless/bounded: a blade coefficient carries the caller's units - cga_point writes x,y,z (length) and (x^2+-1)/2 (length^2), cga_translator writes -t/2, cga_plane normalises only the NORMAL and leaves d/|n| dimensioned, and cga_mv_set/cga_mv_scale are unconstrained public entry points. Measured: with a fixed at O(1) and b scaled by 2^-n, gp(a, k*b) == k*gp(a,b) bit-exactly for n = 1..39 and FIRST FAILS AT n = 40 (2^-40 = 9.09e-13), i.e. precisely at the absolute 1e-12 threshold, not at any property of the operands. At k = 1e-13 the scalar part is 0x0 where the truth is 5e-13 (0x3D619799812DEA11) - the entire product is fabricated to zero. (2) NO caller normalises: cga_rotor has unit magnitude but its bivector part is sin(theta/2), which crosses the guard as the angle shrinks - cga_sandwich(cga_point(1,0,0), cga_rotor(0,0,1,1e-12)) returns e2 = 0 where the truth is 1e-12 (repaired tree returns exactly 1e-12); cga_sandwich(cga_point(0,0,0), cga_translator(1e-13,0,0)) returns e1 = 0 where the truth is 1e-13, while the controls at t = 1, 1e-3, 1e-6 all return e1 = t exactly - a pure scale artifact, the ABSOLUTE_ON_SCALING signature. (3) NOT a convergence tolerance: one pass, no iteration, no division; it is a sparsity skip, and the repo's own freshly extracted F64_TINY comment in src/error.cyr says EPSILON_F64 is right only for \"a genuinely dimensionless, bounded quantity\". (4) The proposed threshold breaks NO existing test - CHECKED, not assumed: with :2272 and :2275 both changed to exact-zero, all five suites produce BYTE-IDENTICAL assertion output (hisab 416/416, foundation 359/359, modules 1827/1827, edge_cases and abuse pass; the only diff in the captured logs is the compiler's unreachable-fn byte count, which shrinks because the guard code is smaller). No cga test anywhere in tests/ feeds a coefficient in (0, 1e-12). (5) The scale_test DOES discriminate - confirmed by mutation, with the mutant grep-verified installed - but the proposed form is wrong twice and is sharpened below. Mutation result confirming the claim's structural point: on a tree where ONLY :2272 is repaired, the LEFT-operand sweep passes clean over 200 halvings while the RIGHT sweep still first-fails at 2^-40, and cga_sandwich with a 1e-12 rad rotor returns 5e-13 - exactly HALF the correct rotation. Repairing one loop is measurably worse than useless: it converts a visible zero into a plausible half-answer.",
   "fabricates": "Silent truncation to zero via the right operand, with rc always 0 (the function has no error channel). When every |b_j| < 1e-12 the product is IDENTICALLY the zero multivector - 100% relative error, e.g. gp(2e1+3e2, 1e-13*(e1+e2)) = 0 where the truth is 5e-13. When only part of b sits below the guard the result is a plausible partial answer: a 1e-12 rad rotor sandwich yields e2 = 0 (and 5e-13, half the truth, if only :2272 is repaired), and a 1e-13 translator sandwich leaves the point unmoved at exactly the coordinates it started from.",
   "correct_threshold": "Exact zero, as a BIT test: `if (bj != 0)`. Must be applied together with :2272's `if (ai != 0)` - measured, repairing either alone leaves the product non-bilinear in the other operand. Three notes from measurement: (a) do NOT reach for the repo's new F64_TINY/DBL_MIN here - there is no division, and a right coefficient of 4.94e-324 (bit pattern 1) against a left coefficient of 1e300 yields a perfectly representable 4.94e-24 (0x3B17E43C8800759C), which any threshold >= DBL_MIN still fabricates to zero; (b) in this codebase f64s are raw i64 bit patterns, so `!= 0` is an integer compare that treats -0.0 (0x8000000000000000) as nonzero - harmless, since the accumulator starts at +0.0 and IEEE gives +0.0 for (+0)+(-0), and the byte-identical suite output under 2.13.0's bit-exact assertions confirms no sign-of-zero change; (c) the skip keeps its whole purpose - a CGA point has 5 of 32 blades nonzero, so exact zero still elides 27 of 32 inner iterations.",
   "scale_test": "Three parts; the first replaces the proposed sweep, whose \"600 decades\" with `==` would FAIL ON CORRECT CODE (decade multipliers are not representable, so bilinearity is not bit-exact for them, and +-300 decades drives the operands subnormal). (A) RIGHT-OPERAND POWER-OF-TWO SWEEP, which is bit-exact by construction: a = 2e1 + 3e2 fixed at O(1) so :2272 never fires; for n = 1..200, k = 2^-n, assert all 32 blades of cga_geometric_product(a, cga_mv_scale(b, k)) equal cga_mv_scale(cga_geometric_product(a, b), k) bit for bit. MEASURED: shipped first-fails at n = 40; exact-zero repair passes all 200 (down to 2^-200 ~ 6.2e-61); a tree with only :2272 repaired STILL first-fails at n = 40, while the mirror-image LEFT-operand sweep passes clean on that same tree - so a left-only sweep provably leaves this site's mutant alive, and this sweep is the one that kills it. (B) SUBNORMAL BRACKET, which the uniform sweep can never reach because both operands would underflow together: b blade 1 = bit pattern 1 (4.94e-324), a blade 1 = 1e300 (0x7E37E43C8800759C); assert the scalar part is 0x3B17E43C8800759C (4.94e-24), not 0. This kills every candidate threshold >= DBL_MIN, including the repo's own F64_TINY. (C) END-TO-END ORACLE on the shipped call graph, since the versor is the right operand inside x * reverse(versor): assert cga_sandwich(cga_point(0,0,0), cga_translator(t,0,0)) has e1 == t for t = 2^-n over the same range, and cga_sandwich(cga_point(1,0,0), cga_rotor(0,0,1,theta)) has e2 == sin(theta) for theta = 1e-12. MEASURED: shipped returns 0 for both; a :2272-only repair returns exactly half; the full repair returns 1e-13 and 1e-12 exactly - so (C) discriminates the half-repair that (A) alone would also catch but that a left-only sweep would bless."
  },
  {
   "site": "src/geo_advanced.cyr:2297 (and its twin at :2300 on the right operand)",
   "fn": "cga_outer_product",
   "guarded_quantity": "|a_i|, one raw blade coefficient of the left multivector, loaded straight out of the 32-slot CGA array. It carries the caller's units \u2014 length^1 for blades 1..3 (e1,e2,e3), length^2 for blades 4/5 (ep,em, which cga_point sets to (|p|^2 +/- 1)/2), and length^k for a grade-k blade. The twin guard at :2300 does the same to |b_j|.",
   "protects": "Nothing. The function contains no division, no sqrt, no acos, no iteration. The guard is a pure sparsity skip over a 32x32 term loop; the only thing below it is f64_mul and f64_add.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "The guarded quantity is a dimensioned blade coefficient, not a ratio, so 1e-12 encodes an assumption that the caller works in units where geometry is O(1). Refutation was attempted on all five grounds and all five fail. (1) Not bounded: cga_point writes x, y, z verbatim and cga_mv_scale is public API. (2) No caller normalises because there IS no caller: grep over the tree finds cga_outer_product only at its definition (src/geo_advanced.cyr:2293), its bundle copy (dist/hisab.cyr:7017) and one assertion (tests/modules.tcyr:7982). The finding's own rationale is wrong here -- cga_point, cga_sphere and cga_plane build coefficients directly with cga_mv_set and never wedge -- but that shrinks the internal blast radius without touching the verdict, since the function is exported in the distlib bundle and the wedge is exactly how a consumer builds a CGA line/plane/sphere (P1^P2^P3^einf). (3) Not a convergence tolerance: there is no iterate and no residual, and it filters INPUTS rather than cleaning an output, so it cannot even be defended as noise hygiene. (4) The proposed exact-zero threshold does not break the one in-tree test: g9a = cga_point(1,0,0) has ep = (1-1)/2 = EXACTLY +0, so both guards skip that slot identically, and the x^x antisymmetric cancellation is bit-exact -- verified by running a shadow implementation with `ai != 0`, which passes both that assertion and the same identity for a general point cga_point(2,3,0.5). Negative zero (bits != 0, so not skipped by the repair) also agrees bit-for-bit with the shipped code, because -0.0*b added into the +0-initialised accumulator leaves +0. Sparsity is not lost either: inner-loop entry counts are identical for the repair and the shipped guard on a typical cga_point pair, since real multivectors are sparse with EXACT zeros from cga_mv_zero. (5) The scale_test discriminates but did not bracket every plausible repair, so it is sharpened below. Measured on the shipped tree: with a = e1 + 2*e2, b = e3, bit-exact scale covariance fails at 5 of 13 exponents -- every e <= -40, i.e. wherever 2^e drops a coefficient under 1e-12 -- while the k=2^+1000 control passes, proving the failure is the guard and not the harness.",
   "fabricates": "The zero multivector, or a silently truncated one, with no error channel (the function returns a pointer, never an rc). Measured: cga_outer_product(cga_mv_scale(a, 2^-45), b) returns all-zero blades where the true answer is the unscaled wedge times 2^-45; scaling BOTH operands of a bivector by 2^-50 deletes the whole result, so cga_norm_sq reports 0 and a consumer's degeneracy check reads \"these vectors are parallel\". Partial truncation is worse than total: with a = e1 + 2^-60*e2 and b = 2^60*e3 the e2^e3 coefficient -- whose exact value is 1.0 -- is deleted while the e1^e3 term survives, so the caller gets a well-formed-looking blade of the wrong grade content.",
   "correct_threshold": "Exact zero on the raw bit pattern: `if (ai != 0)` at :2297 and `if (bj != 0)` at :2300 -- both, since a repair to one leaves the other truncating. Not DBL_MIN and not a relative bound: there is no division to protect, so the ONLY defensible reason to skip a term is that it contributes nothing, and a_i * b_j contributes nothing exactly when a_i is +0. Verified free: identical inner-loop entry count on a cga_point pair (multivector sparsity in this module comes from cga_mv_zero's exact zeros, which `!= 0` catches just as well), and bit-identical output when a coefficient is -0.0.",
   "scale_test": "Three parts, because uniform scaling alone does NOT bracket the two near-repairs someone would plausibly reach for. (1) UNIFORM, kills the absolute 1e-12: with a = 1*e1 + 2*e2 and b = 1*e3, assert cga_outer_product(cga_mv_scale(a, k), b) equals cga_mv_scale(cga_outer_product(a,b), k) bit-exact on every one of the 32 blades, for k = 2^e over e in {-1000,-700,-400,-100,-45,-13,0,13,45,100,400,700,1000}. Powers of two make the scaling exact; O(1) power-of-two coefficients with |e| <= 1000 keep every product normal, so a pass is never a vacuous Inf == Inf or a subnormal-underflow tie. Measured: SHIPPED fails 5/13 (e = -1000,-700,-400,-100,-45 -- the first failure at e = -40 where 2^-40 = 9.09e-13 crosses 1e-12), the exact-zero repair 0/13. (2) MIXED MAGNITUDE WITHIN ONE OPERAND, kills a relative `EPSILON_F64 * max|a_i|` repair, which part (1) would let through: a = 1*e1 + 2^-60*e2, b = 2^60*e3; assert blade 10 (e2^e3) is exactly F64_ONE. Shipped returns 0; a relative guard also returns 0 because 2^-60/1 < 1e-12; exact zero returns 1.0. (3) SUBNORMAL, kills a DBL_MIN repair, which parts (1) and (2) would both let through: a = 1*e1 + 2^-1060*e2 (subnormal, bits 0x0000000000004000), b = 2^1000*e3; assert blade 10 is exactly 2^-60 (bits 0x3C30000000000000). Shipped returns 0; exact zero returns 2^-60. Add the in-tree regression as a fourth arm so the repair is proven non-breaking: cga_norm_sq(cga_outer_product(x, x)) == 0 for x = cga_point(1,0,0) AND for x = cga_point(2,3,0.5) (the second has a nonzero ep, which the first does not exercise)."
  },
  {
   "site": "src/geo_advanced.cyr:2300",
   "fn": "cga_outer_product",
   "guarded_quantity": "|b_j|, one blade coefficient of the caller-supplied right multivector, tested per-blade in the inner loop. Not a norm, not a ratio: the raw stored coefficient of blade gj.",
   "protects": "Nothing. Verified by reading the guarded body: f64_mul(ai,bj), a _cga_geo_blades table lookup (integer bit ops + alloc, no FP), and f64_add into the accumulator. No division, no sqrt, no acos, no iteration. It is a pure sparsity skip.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Could not refute; all five routes closed by measurement, and the filing was UNDER-stated. (1) Not dimensionless or bounded: b_j is a raw blade coefficient of a user-supplied multivector \u2014 cga_point stores world coordinates in blades 1-3 (dimension: length) and (|x|^2 +- 1)/2 in blades 4-5 (length^2), cga_sphere folds r^2 into 4-5, cga_plane stores d/|n| (length) in 4-5. So one 1e-12 cutoff is applied to a mixture of lengths, squared lengths and dimensionless components. The only constructor whose coefficients ARE bounded (cga_rotor: cos/sin, |c| <= 1) is not bounded AWAY from zero \u2014 sin(theta/2) < 1e-12 for theta < 2e-12 rad \u2014 so boundedness would not rescue an absolute epsilon even where it holds. (2) No caller normalises, and there is no caller at all: `grep -rn cga_outer_product src/` returns only the definition. It is a bare public entry point (one test at tests/modules.tcyr:7982, one row in docs/architecture/math.md); the operand arrives straight from the user. (3) Not a convergence tolerance \u2014 nothing iterates and nothing can fail. (4) MEASURED FABRICATION, and it is not a \"garbage operand\" case: A = cga_point(0,0,0) and B = cga_point(1e-13,0,0) have BIT-IDENTICAL ep/em blades (both +-0.5 \u2014 the d^2/2 correction is 5e-27, far below 0.5's ulp), so B differs from A in the e1 coefficient ALONE, which is exactly what this guard drops. cga_outer_product(A,B) returns e1p = e1m = 0x0: two distinct conformal points reported as a coincident/degenerate point pair. At 1e-12 separation the same call returns e1p = 5e-13. B is a perfectly conditioned point whose position component is merely small. (5) The proposed repair breaks nothing: `ai != 0` / `bj != 0` installed in a copied tree (patch grep-verified present before running) and all five suites re-run \u2014 416 + 359 + 1827 + 233 + 776 = 3611 passed, 0 failed, identical to that tree's own baseline. The one existing outer-product test is x^x = 0 with cga_point(1,0,0), whose coefficients are exactly 1 and 1, so it cannot discriminate. NOTE FOR THE REPAIR: the exact-zero skip is also strictly CHEAPER (an integer compare replacing f64_abs + f64_lt) and skips the same blades in practice, because CGA sparsity is structural \u2014 cga_mv_zero() stores integer 0 and cga_point sets 5 of 32 blades. The epsilon buys no speed and costs correctness. SCOPE: 8 identical instances of this predicate, all in public functions \u2014 2272/2275 (cga_geometric_product), 2297/2300 (outer), 2328/2331 (cga_left_contraction), 2358/2361 (cga_right_contraction). Because cga_norm_sq routes through cga_geometric_product, it carries the same truncation: my point-pair probe read norm_sq = 0x0 at EVERY separation and could not discriminate, so the repair must not be validated through cga_norm_sq.",
   "fabricates": "A silently truncated wedge: every term whose right-operand coefficient is below 1e-12 is dropped, and the result is returned with rc-equivalent success (a normal multivector handle, no error channel). Concretely measured on the shipped tree: cga_outer_product(e1, k*e2) returns exactly 0 on blade e12 for all k <= 1e-13 where the answer is exactly k; and A ^ B for two conformal points 1e-13 apart returns e1p = e1m = 0x0 \u2014 the caller is told two distinct points coincide. Worst case is total: if every coefficient of b is below 1e-12 the function returns the zero multivector, i.e. \"these geometric entities have no join\".",
   "correct_threshold": "Exact zero: `if (bj != 0)`. Not a smaller epsilon \u2014 no absolute threshold is admissible, because the guard protects nothing and the operation is exactly bilinear, so any nonzero tau makes outer(a, k*b) collapse to zero for k < tau/|b| while k*outer(a,b) stays a normal number down to ~1e-308. A relative form (|b_j| < eps * max_j|b_j|) is also wrong here: it still fabricates, and it would drop precisely the small position component of a well-formed conformal point whose ep/em are O(1). The exact-zero test is additionally cheaper than the one it replaces (integer compare vs f64_abs + f64_lt) and preserves the structural sparsity the guard was written for, since cga_mv_zero() stores integer 0. Apply identically to the sibling `ai` guard at :2297 and to the six matching instances at 2272/2275, 2328/2331, 2358/2361.",
   "scale_test": "SHARPENED \u2014 the filed \"20-decade sweep\" would bracket 1e-12 and 1e-15 but NOT a DBL_MIN repair. Assert exact bilinearity in the right operand over POWERS OF TWO, so every rescaling is bit-exact and the assertion is a bit-compare rather than a tolerance: with a = e1 and b = 3*e2, require cga_mv_get(cga_outer_product(a, cga_mv_scale(b, 2^-n)), 6) == f64_mul(2^-n, cga_mv_get(cga_outer_product(a, b), 6)) for n = 0..1069, i.e. all the way down through the subnormals. MEASURED: shipped tree fails 1028 of 1070 binades, first failure at n = 42 (k = 2^-42, so 3k ~ 6.8e-13, just under the 1e-12 cutoff); the exact-zero skip fails 0 of 1070. The 1070-binade span BRACKETS every absolute threshold a repair might plausibly pick \u2014 1e-12 breaks at n=42, 1e-15 at n~52, 1e-18 at n~62, even DBL_MIN at n~1021 \u2014 so only `!= 0` survives to the bottom. Pair it with the geometric form, which is the \"same problem in different units\" statement: two conformal points a fixed physical distance apart (say 1 cm) expressed in metres, kilometres and astronomical units \u2014 1e-2, 1e-5 and 6.7e-14 \u2014 must yield a wedge whose e1p/e1m blades are nonzero in all three; the shipped code returns exactly 0x0 for the AU framing. Do NOT assert on cga_norm_sq: it routes through cga_geometric_product, carries the same guard, and reads 0x0 at every scale."
  },
  {
   "site": "src/geo_advanced.cyr:2328 (identical sibling guard on the inner operand at src/geo_advanced.cyr:2331)",
   "fn": "cga_left_contraction",
   "guarded_quantity": "|a_i| \u2014 the absolute magnitude of ONE blade coefficient of the left multivector, compared against EPSILON_F64 = 1e-12 (src/error.cyr:26). It is dimensioned, not dimensionless: a grade-k coefficient carries (input unit)^k times the multivector's overall scale, and cga_mv_scale is a public degree-1 operation on it. The sibling at :2331 applies the same absolute test to |b_j|.",
   "protects": "Nothing. The guarded body contains no division, sqrt, acos, log or iteration \u2014 only `f64_mul(ai, bj)` and an `f64_add` accumulate. It is a pure sparsity skip: its only legitimate job is to avoid work for terms that contribute exactly zero.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation routes were tried against the built tree and all five close. (1) NOT dimensionless: |a_i| is a blade coefficient, degree 1 under cga_mv_scale, and the contraction is exactly bilinear, so any absolute floor on it breaks a property the function is supposed to have identically. (2) NO caller normalisation \u2014 the opposite: cga_project (:2437) feeds `cga_blade_inverse(b)` straight into the second slot, and blade_inverse (:2430) MANUFACTURES the reciprocal scale by construction, so the two operands of the outer contraction are guaranteed to have inverse magnitudes. Measured on a k = 2^-60..2^60 sweep of cga_project(e1, k*e12): 62 of 121 exponents return the zero multivector where the true answer is e1 \u2014 the small side (k <= 2^-20, i.e. |k| < 1e-6, ordinary micrometre-scale input) via cga_blade_inverse:2429, and the LARGE side (k >= 2^40) via this guard's own sibling at :2331, with no help from :2429 at all. The claim's \"combined with cga_blade_inverse:2429\" therefore understates it: this guard fabricates independently. (3) NOT a convergence tolerance \u2014 one pass, no iterate, no residual. (4) The proposed exact-zero skip changes NO existing test: a byte-for-byte candidate of the function with `f64_eq(ai,0) != 1` was compiled alongside the shipped one and compared over all 12 operand pairs tests/hisab.tcyr:399-486 and tests/modules.tcyr:7973-7990 pin (lc(e1,e12), lc(e1,e1), lc(e1,e23), lc(v,v), lc(2,5e12), lc(e12,e12), lc(e3,e12), lc(e1+e3,e12), lc(e1,0), and both legs of the three pinned projections) \u2014 32/32 blades bit-identical on every one, because no coefficient in any pinned input or intermediate lands in (0, 1e-12). (5) The scale test does discriminate the shipped guard (measured: k = 2^-1000 leaves 2 of 32 blades wrong) but does NOT bracket every plausible repair \u2014 see scale_test. And the defect needs no whole-multivector scaling at all: cga_left_contraction(2^-50*e1, 2^50*e12) returns 0 where the answer is exactly 1.0, at magnitudes any engineering code produces. The measured cutoff is exactly the epsilon: e >= 40 in lc(2^-e*e1, 2^e*e12) fabricates (2^-40 = 9.1e-13 < 1e-12), e <= 39 is exact.",
   "fabricates": "A silently truncated multivector with no error signal \u2014 terms whose |a_i| < 1e-12 are dropped no matter how large the matching b_j is, so the returned multivector is missing contributions of arbitrary magnitude. Measured: lc(2^-50*e1, 2^50*e12) returns the zero multivector where the answer is exactly 1.0*e2; lc(2^-1030*e1, 2^1000*e12) returns zero where the answer is exactly 2^-30. Through cga_project/cga_reject it fabricates the ZERO multivector (project) and an unchanged X (reject, since reject = X - project) on 62 of 121 tested scales of the reference blade \u2014 including every |b| >= 2^40 purely through this guard's sibling at :2331, and every |b| <= 2^-20 through cga_blade_inverse:2429. cga_project is mathematically degree 0 in b, so all of that is a scale on which the true answer is unchanged.",
   "correct_threshold": "Exact zero, at BOTH :2328 and :2331 \u2014 but written as a numeric compare, not a bit compare: `if (f64_eq(ai, 0) != 1) {` and `if (f64_eq(bj, 0) != 1) {`. Not the proposed `ai != 0`: `ai` holds a raw bit pattern, so `ai != 0` treats -0.0 (0x8000000000000000) as present and would newly evaluate -0.0 * bj, turning an infinite bj into a NaN the shipped code never produced. f64_eq catches both signed zeros and was measured to reproduce shipped output bit-for-bit on a -0.0 input. This is the only correct threshold here because the guard protects no operation \u2014 a term with a_i == 0 contributes exactly 0 and every other term contributes something the caller asked for. Any nonzero floor, absolute (1e-12, DBL_MIN) or relative (eps * max_j|a_j|), is a choice of what magnitude of answer to throw away silently, and the function has no basis for making it: only the caller knows b's scale.",
   "scale_test": "Two families; the second is the one the original filing was missing. (A) Bilinearity under whole-multivector scaling, extended BELOW DBL_MIN: with a = e1 + 2*e2, b = 3*e12, assert cga_left_contraction(cga_mv_scale(a,k), b) == cga_mv_scale(cga_left_contraction(a,b), k) bit-exact over all 32 blades for k = 2^1000, 2^0, 2^-40, 2^-1000 AND k = 2^-1030 (subnormal but an exact power of two, so both sides stay bit-exact \u2014 verified: the exact-zero repair passes all five, the shipped code fails at 2^-40, 2^-1000 and 2^-1030). The floor of 2^-1000 in the original filing is NOT sufficient: 2^-1000 > DBL_MIN = 2^-1022, so a DBL_MIN-based repair \u2014 the exact `_GEO_F64_TINY` pattern this repo adopted in 2.10.2 \u2014 would pass the filed test while still being an absolute floor. 2^-1030 brackets it. (B) The family that needs no scaling of the whole multivector and brackets EVERY absolute threshold including sub-DBL_MIN ones, at operand magnitudes an ordinary caller reaches: assert cga_left_contraction(2^-e * e1, 2^e * e12) == 1.0*e2 bit-exact for e = 0..1030 (each pair exactly representable, each product exactly 1.0). Shipped fails for every e >= 40; a DBL_MIN repair fails for every e >= 1022; only an exact-zero skip passes the whole sweep. Add the mirror lc(2^e * e1, 2^-e * e12) to cover :2331, and cga_project(e1, cga_mv_scale(e12, 2^e)) == e1 for e = -60..60 as the end-to-end assertion (62 of 121 currently fail)."
  },
  {
   "site": "src/geo_advanced.cyr:2331",
   "fn": "cga_left_contraction",
   "guarded_quantity": "|b_j|, one blade coefficient of the RIGHT multivector, in isolation. It carries the caller's units (cga_point writes (x^2 +- 1)/2 into blades 4/5, i.e. the SQUARE of the length unit) and is unbounded \u2014 cga_mv_set/cga_mv_scale are public and unconstrained.",
   "protects": "Nothing. There is no division, sqrt, acos or normalisation anywhere in the loop \u2014 the only operation on b_j is f64_mul(ai, bj) followed by f64_add into an accumulator. It is a pure sparsity skip in a bilinear sum.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Refutation attempted on all five angles; all fail against measurement. (1) NOT dimensionless/bounded: b_j is an arbitrary caller-supplied coefficient carrying the square of the caller's length unit. (2) NO caller normalises the right slot \u2014 and the one in-tree caller that fills it makes it strictly worse: cga_project(x,b) = lc(lc(x,b), cga_blade_inverse(b)), so the right operand is a RECIPROCAL whose coefficients scale as 1/|b|. Measured on the shipped tree, cga_project(e1, k*e12) \u2014 mathematically exactly e1 for every k != 0, projection being scale-INVARIANT in b \u2014 returns the correct e1 at k=1e11 (blade_inverse coeff -1.0e-12) and the ZERO multivector at k=1e12, 1e16, 1e20, 1e100 (coeffs -1e-13 ... -1e-101). The boundary falls exactly where |blade_inverse coeff| crosses EPSILON_F64, which pins :2331 as the sole cause: :2328 cannot fire on that call, its operand being k*e2. (3) NOT a convergence tolerance: no iteration, no residual, no step. (4) The proposed repair breaks NO existing test \u2014 verified, not assumed, in an isolated copy with ONLY line 2331 changed to `if (bj != 0)`: hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776, all 0 failed, pass counts identical to the same tree's baseline (3611/3611). Exact zero also preserves the whole sparsity win, since cga_mv_zero writes bit-exact zeros. (5) The scale test does discriminate \u2014 but the FILED one is too weak (see scale_test). Two sharpenings on the filing itself. First, `why: identical to :2328` understates it: :2331 is independently reachable through cga_project where :2328 provably cannot fire, so it is not a mirror needing joint repair for correctness \u2014 it is its own defect. And the class is SIX sites in this file, not two: 2272/2275 (cga_geometric_product), 2297/2300 (cga_outer_product), 2328/2331 (left), 2358/2361 (right), all byte-identical. Second, this is additionally a wrong-quantity defect, which is WHY no rescaled threshold repairs it: the quantity that decides whether a term is negligible is |a_i * b_j| against the accumulated result, and a_i is free, so for ANY absolute constant T on |b_j| alone the caller can choose b_j just under T and a_i = 1/b_j and the discarded term is exactly 1.0. Measured below down to 2^-1023, i.e. past DBL_MIN \u2014 so this repo's house rule of guarding with DBL_MIN is also wrong here, because nothing is divided and there is no smallest-value-that-fails to derive one from.",
   "fabricates": "The exact ZERO multivector, silently, with no error channel \u2014 not a small truncation. Measured on the shipped tree: cga_left_contraction(2^50 * e1, 2^-50 * e12) returns 0x0 where the answer is exactly 1.0 * e2; and the public cga_project(e1, 1e12 * e12) returns the zero multivector where the answer is exactly e1. Because projection is scale-invariant in b, a caller who merely changes the units of the blade being projected onto flips a correct e1 into a zero, with rc unavailable (the function returns a multivector).",
   "correct_threshold": "Exact zero \u2014 `if (bj != 0)`. Verified: repairs every failing case above and leaves all 3611 assertions across the five suites passing unchanged. Note this is a BIT-PATTERN compare (f64 values live in i64 slots here), which is already the idiom the suite itself uses at tests/hisab.tcyr:373; the only consequence is that -0.0 (0x8000000000000000) reads nonzero and is not skipped, which costs one loop iteration and contributes +-0.0 to the accumulator \u2014 never the answer. Use `(bj << 1) != 0` if both signed zeros should skip. Do NOT substitute a relative threshold (eps * max|b|, eps * |b|): it is scale-covariant but still wrong, being defeated by the mixed-magnitude case in scale_test. Do NOT substitute DBL_MIN: measured to fail, since no division is being protected.",
   "scale_test": "Three parts; the filed \"the :2328 sweep with k applied to b\" is only the weakest of them and does not bracket a repair that picks a smaller absolute constant. (A) THRESHOLD-INDEPENDENT BRACKET, kills every absolute T at once \u2014 for each candidate T pick n with 2^-n < T and assert cga_left_contraction(2^n * e1, 2^-n * e12) has e2 component bit-exactly 1.0 (powers of two make the product exact). Measured on shipped: 0x0 for n = 50 (T=1e-12, the shipped value), 60 (1e-15), 120 (1e-30), 400 (1e-100) and 1023 (below DBL_MIN = 2^-1022); with the exact-zero repair all five return 0x3ff0000000000000, and the b=a=1.0 control returns 1.0 on both. (B) BRACKET AGAINST A RELATIVE-TO-b REPAIR, which (A) does not cover \u2014 b = 1e20*e12 + 1.0*e13 lands its two terms in DIFFERENT result blades, so assert the result is bit-exactly 1e20*e2 + 1.0*e3 (measured 0x4415af1d78b58c40 and 0x3ff0000000000000 on shipped); a guard of |b_j| < eps*max|b| computes threshold 1e8 and fabricates 0 for the e3 component. (C) CALLER-LEVEL COVARIANCE, exponent 0 \u2014 cga_project(e1, k*e12) must equal e1 to within 1 ulp for k swept over roughly 1e-6 .. 1e300, since (X|kB) scales as k and (kB)^-1 as 1/k. Measured shipped: correct at k=1e10 and 1e11, ZERO at k>=1e12; repaired: correct through 1e100. (Do not read the small-k arm of (C) as evidence for this site \u2014 for k <= 1e-8 cga_blade_inverse's own nsq guard at :2429 fires first and is a separate finding.)"
  },
  {
   "site": "src/geo_advanced.cyr:2358",
   "fn": "cga_right_contraction",
   "guarded_quantity": "|a_i|, the raw magnitude of one blade coefficient of the left multivector \u2014 a single FACTOR of the product a_i*b_j, carrying whatever units the caller's geometry carries (cga_point writes x/y/z straight into blades 1/2/3 as length, and ep/em as length^2). Twin guard on |b_j| at :2361.",
   "protects": "Nothing. Pure sparsity skip in a bilinear 32x32 double loop \u2014 no division, no sqrt, no acos domain, no iteration downstream of it. Its only job is to avoid multiplying by a zero blade.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "EPSILON_F64 is an absolute 1e-12 (src/error.cyr:26) applied to a dimensioned, arbitrarily-scalable quantity, and all four refutation routes fail on inspection. (1) NOT dimensionless: cga_point(x,y,z) at src/geo_advanced.cyr:2450 stores raw x,y,z as the e1/e2/e3 coefficients \u2014 units of length, unbounded below; the ep/em slots are (x^2-1)/2 and (x^2+1)/2, units of length^2. A point at (1e-13,0,0) has e1 = 1e-13 < 1e-12 and is dropped while ep/em sit at -0.5/+0.5, so the multivector is NOT near zero \u2014 only its spatial part is deleted. (2) NO caller normalises, because there is no caller: grep over src/, dist/, examples/ finds zero internal call sites; cga_left_contraction is used by cga_project (:2437) but cga_right_contraction is reached only from tests and from dist/hisab.cyr:7078 as a public entry point of the bundle ten repos consume. (3) NOT a convergence tolerance \u2014 it is a one-shot sparsity test, unlike the genuine iteration tolerances at calc.cyr:340/347. (4) The guarded quantity is one FACTOR of a product whose other factor is unbounded above, so |a_i| carries no information about whether the term matters: a_i = 1e-13 with b_j = 1e13 yields a unit-magnitude contribution that is discarded to exactly +0.0. That secondary wrong-quantity flavour is real but subordinate \u2014 the cleanly provable class is the absolute cutoff on a scalable coefficient, identical to :2272 (cga_geometric_product) and the same shape in cga_outer_product and cga_left_contraction. The file itself already records this lesson twice against its own EPSILON_F64 (:258 squared-vs-unsquared, :436 'THE CONVERGENCE TEST WAS ABSOLUTE') and the neighbouring code at :530 and :608 has already been repaired to scale-relative form (EPSILON_F64 * |ab| * |ac|), so the correct pattern is established in this very file \u2014 the CGA block was never swept.",
   "fabricates": "A silently truncated contraction returned as a normal multivector with no rc and no error channel \u2014 the dropped terms read back as exact +0.0, indistinguishable from a genuine orthogonality zero. Concretely: cga_right_contraction(1e-13*e12, 1e13*e1) returns blade[2] = +0.0 where the exact answer is -1.0, i.e. a unit-magnitude result reported as the algebraically meaningful 'these blades do not contract'. Under uniform scaling the whole result collapses: any multivector all of whose coefficients fall below 1e-12 contracts to the zero multivector, so cga_norm of the result is 0 and a consumer's downstream cga_blade_inverse divides by that zero.",
   "correct_threshold": "Exact zero: `if (ai != 0)` at :2358 and `if (bj != 0)` at :2361 \u2014 BOTH, the site and its twin. No epsilon of any kind, absolute or relative: this guard protects no operation, so there is nothing to size a threshold against, and per the 2.10.2 rule a threshold that needs a scale chosen for it is the defect. A relative threshold (EPSILON_F64 * max|a_i|) is NOT an acceptable repair here \u2014 it still drops the 1e-13 spatial coefficient of a legitimate cga_point whose ep/em are 0.5. The exact-zero test keeps 100% of the intended sparsity win, since cga_mv_zero stores integer 0 and CGA blades are exactly zero rather than epsilon-small. Bit-pattern note: `ai != 0` admits -0.0 (0x8000000000000000) as 'nonzero'; that is harmless for value (contributing -0.0 changes no sum whose other terms are nonzero) but can flip a result slot from +0.0 to -0.0, which 2.13.0's bit-exact asserts now see \u2014 write `if (ai != 0 && ai != 0x8000000000000000)` if that sign-of-zero surface is unwanted.",
   "scale_test": "The originally proposed sweep fails on shipped code but does NOT bracket the plausible repairs, so it must be widened on three axes. Run all four: (T1) MAGNITUDE CROSS-CHECK, no scaling needed and the sharpest witness \u2014 a = 1e-13*e12, b = 1e13*e1; exact answer is blade[2] = -1.0 (this is tests/hisab.tcyr:419's own configuration, just rescaled); shipped returns +0.0. Kills the absolute 1e-12. (T2) MIXED SCALE WITHIN ONE MULTIVECTOR, kills a relative-threshold repair that T3 cannot see \u2014 a = cga_point(1e-13, 0, 0) (e1 = 1e-13, ep = -0.5, em = +0.5), b = e1; target grade 1-1 = 0, so the exact scalar part is 1e-13. Exact-zero returns 1e-13; a repair using EPSILON_F64 * max|a_i| = 5e-13 returns +0.0 and so does the shipped code. (T3) UNIFORM COVARIANCE, applied THREE ways \u2014 scale a alone, b alone, and both \u2014 bit-exact over k = 2^-1000 .. 2^1000: cga_right_contraction(cga_mv_scale(a,k), b) == cga_mv_scale(cga_right_contraction(a,b), k), and the same for b, and cga_right_contraction(scale(a,k), scale(b,k)) == scale(result, k*k). Scaling only `a` never executes the twin guard at :2361, so a half-fix at :2358 alone passes the original test unchanged. Powers of two make the scaling exact, so bit-exact is the right assertion for a correct implementation. (T4) SUBNORMAL BRACKET, kills a DBL_MIN repair that T1-T3 all pass because 2^-1000 = 9.3e-302 is above DBL_MIN = 2.2e-308 \u2014 a with blade[6] = 0x0000000000000001 (2^-1074), b with blade[1] = 2^1000; the exact answer is blade[2] = -2^-74, a normal double. Together T1/T2/T4 bracket every threshold a repair might plausibly choose (1e-12, relative-to-norm, DBL_MIN, exact zero) and T3 forces the twin guard to be fixed with it. Mutation-proof each: install a mutant restoring the absolute test at :2358 only, at :2361 only, and a relative variant, and confirm the suite fails for each \u2014 grepping for a marker to prove the mutant is actually installed before believing the count."
  },
  {
   "site": "src/geo_advanced.cyr:2361",
   "fn": "cga_right_contraction",
   "guarded_quantity": "|b_j|, the magnitude of one blade coefficient of the right multivector operand, compared against EPSILON_F64 (1e-12) as `f64_lt(f64_abs(bj), EPSILON_F64) != 1`",
   "protects": "Nothing. It is a sparsity skip over the 32x32 blade loop. Verified by reading the loop body: no division, no sqrt, no domain-restricted call, no iteration \u2014 only `_cga_geo_blades`, `f64_mul`, `f64_neg`, `f64_add`. The only division in the neighbourhood is `cga_blade_inverse` at :2429, a different site.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ANGLES; ALL FIVE FAILED UNDER MEASUREMENT. (1) Not dimensionless: b_j carries the units of whatever the caller's multivector represents \u2014 a grade-1 blade coefficient of a displacement has units of LENGTH, a grade-2 blade of AREA, so re-expressing the same geometry in kilometres instead of nanometres scales b_j by 1e12 and moves it across a fixed 1e-12 cutoff. docs/architecture/math.md \u00a71.1 states the contract as bilinear distribution over components, i.e. a \u230a (k\u00b7b) = k\u00b7(a \u230a b) exactly; the guard violates it. (2) No caller normalises: cga_right_contraction is a PUBLIC entry point (documented in math.md's product table), the only in-tree caller chain is cga_project -> cga_left_contraction (:2437), and nothing on it normalises; cga_mv_scale is public, so a caller can scale an operand arbitrarily. (3) Not a convergence tolerance \u2014 no iteration, nothing to converge. (4) Exact-zero does NOT break a shipped test \u2014 MEASURED, not assumed: I patched all 8 sites in a scratch copy to `f64_abs(x) != 0` and replayed every shipped CGA assertion (tests/hisab.tcyr:352-490 plus tests/modules.tcyr:7973-7990) against it \u2014 80/80 pass, identical to the control run against the unpatched module. Every coefficient the shipped corpus feeds a contraction is exactly 0 or >= 0.5, so nothing lands in (0, 1e-12). (5) The proposed scale test's direction is right but it under-specifies (see scale_test). The defect reproduces directly: with a = e12 at UNIT magnitude \u2014 so the outer guard at :2358 provably never fires and the effect is attributable to :2361 alone \u2014 rc(e12, 2^-40\u00b7e1) returns blade[2] = 0 where the truth is -2^-40. Confirms the finding's cross-reference: the eight sites :2272/:2275, :2297/:2300, :2328/:2331, :2358/:2361 are byte-identical in shape (grep-confirmed: exactly 8 matches tree-wide, all in geo_advanced.cyr) across cga_geometric_product, cga_outer_product, cga_left_contraction, cga_right_contraction, and all 8 repair together cleanly. TWO SHARPENINGS, both measured, neither refuting: (a) the proposed literal `bj != 0` is a RAW BIT-PATTERN compare in Cyrius, so -0.0 (0x8000000000000000) reads as non-zero \u2014 and that is live, not hypothetical: cga_blade_inverse(em) returns a multivector with 31 of 32 blades at -0.0 (f64_mul does sign, and 1/nsq is negative because em^2 = -1), which cga_project feeds straight into a contraction. Numerically harmless (cur starts at +0.0 and +0.0 + \u00b10.0 = +0.0, so no result blade can become -0.0) but it inflates the inner loop from ~1 iteration to 31 on every projection, destroying the sparsity the guard exists for. (b) a uniform scale sweep cannot discriminate exact-zero from a norm-relative repair; a mixed-scale probe is required.",
   "fabricates": "A silently truncated result blade \u2014 a plausible, well-formed multivector in which any contribution routed through a small right-operand coefficient has been replaced by exact zero, returned with no status code (the function returns only a multivector pointer, so a caller has nothing to check). MEASURED on the shipped tree: cga_right_contraction(e12, 2^-40\u00b7e1) returns blade[2] = 0 (bit pattern 0x0) where the correct value is -2^-40 = -9.0949e-13 (0xBD70000000000000); at 2^-1060 (subnormal) likewise 0 vs -8.095e-320; and for the mixed operand b = e1 + 2^-80\u00b7e2 the e1 component of the result is 0 vs the correct +2^-80 (0x3AF0000000000000). Not \"returns an error\" \u2014 the wrong answer is indistinguishable from a genuine orthogonality/grade-mismatch zero, which is exactly what these operators are used to test for (tests/hisab.tcyr:410 asserts a true zero this way).",
   "correct_threshold": "Exact zero, SIGN-AGNOSTIC \u2014 `if (f64_abs(bj) != 0) {` (equivalently `if ((bj & 0x7FFFFFFFFFFFFFFF) != 0) {`), which is ganita's own idiom for this test at lib/ganita.cyr:408. NOT the originally proposed raw `if (bj != 0)`: that compares the bit pattern, so -0.0 passes as non-zero, and cga_blade_inverse measurably emits 31 of 32 blades as -0.0 into cga_project's contraction \u2014 the naive form is numerically correct but discards the sparsity skip on every projection. The sign-agnostic form also keeps NaN flowing through (f64_abs(NaN) != 0), matching the current guard's behaviour (`f64_lt(NaN, eps)` is 0, so `!= 1` is true), so NaN propagation is unchanged. Apply identically to all eight sites (:2272, :2275, :2297, :2300, :2328, :2331, :2358, :2361) \u2014 a grep for `f64_lt(f64_abs(ai), EPSILON_F64) != 1` / `...(bj)...` returns exactly those eight and nothing else. The repaired form is verified green at 83/83 including the three discriminators below.",
   "scale_test": "Three probes, all bit-exact (every factor a power of two, so no rounding), all with a = e12 held at UNIT magnitude so the outer guard at :2358 cannot fire and only :2361 is under test. Baseline: rc(e12, e1) = -e2 (already pinned at tests/hisab.tcyr:419). P1 \u2014 brackets EPSILON_F64: assert rc(e12, 2^-40\u00b7e1) has blade[2] == -2^-40 (0xBD70000000000000). 2^-40 = 9.0949e-13 sits just under 1e-12, so this pins the threshold tightly rather than merely far below it. P2 \u2014 brackets DBL_MIN: assert rc(e12, 2^-1060\u00b7e1) has blade[2] == -2^-1060 (0x8000000000004000, a subnormal). A repair that reached for DBL_MIN (2^-1022) instead of exact zero passes P1 and FAILS P2, which is why P2 is required \u2014 the scaling sweep must reach into the subnormals, not stop at a \"very small\" decade. P3 \u2014 brackets a NORM-RELATIVE repair, which no uniform scaling can reach because a relative threshold is itself scale-covariant and passes P1 and P2: put two scales inside ONE operand, b = e1 + 2^-80\u00b7e2, and compare all 32 result blades against an oracle built only from guard-clean unit-magnitude evaluations, ref = rc(e12, e1) + 2^-80 \u00b7 rc(e12, e2) (using cga_mv_scale, which has no guard). Under `EPSILON_F64 * max|b_j|` the 2^-80 term is dropped and the result's e1 slot reads 0 instead of +2^-80; under exact zero it matches bit for bit. MEASURED on the shipped tree: P1, P2, P3 all FAIL (blade reads 0). MEASURED on a scratch copy with all eight sites set to `f64_abs(x) != 0`: P1, P2, P3 pass AND all 80 shipped CGA assertions pass unchanged \u2014 83/83. So the trio brackets every threshold a repair might plausibly choose (EPSILON_F64, DBL_MIN, norm-relative, exact zero) and separates the correct one from all three others."
  },
  {
   "site": "src/geo_advanced.cyr:2429",
   "fn": "cga_blade_inverse",
   "guarded_quantity": "|nsq| = |scalar part of B * reverse(B)|, a norm SQUARED (dims L^2 for a Euclidean blade; exactly k^2 for a blade scaled by k). Measured on the shipped binary: for B = k*e12 the shipped cga_norm_sq returns bit-exactly k^2 across k = 2^-39 .. 2^500. For a conformal sphere blade it is r^2 IN EXACT ARITHMETIC ONLY -- in double it is r^2 plus a cancellation residual of order eps_mach*|centre|^4.",
   "protects": "the division f64_div(F64_ONE, nsq) on the next line -- but the comment at :2424-2426 gives it a SECOND job, classifying a null blade (conformal points, nsq == 0 exactly) so cga_project/cga_reject return the documented zero sentinel. One constant is doing both jobs, which is why no single value can be right.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON FIVE ROUTES; the verdict survives, the fixtures do not. (1) Dimensionless/bounded: NO -- measured, k*e12 gives nsq == k^2 bit-exactly over 2^-39..2^500, so the guarded quantity scales as L^2 with the caller's units. (2) Caller normalises: NO -- cga_project/cga_reject (:2437, :2443) hand the caller's blade straight through; there is no normalisation on any path. (3) Convergence tolerance: NO -- it is a singularity classifier, and its errors are caller-visible in BOTH directions. (4) Existing test breaks under the repair: CHECKED, NO -- every CGA test (hisab.tcyr:455-487, modules.tcyr:7988-7990) uses unit blades with nsq == +-1, or the exactly-zero multivector where every candidate threshold agrees; modules.tcyr:7990 only asserts the returned POINTER is non-zero, which cga_mv_zero() satisfies. So the repair is test-neutral. (5) The scale_test does not discriminate: REFUTED THE CLAIM'S FIXTURE A, three ways, all measured by running the shipped code (probe1/probe2 under CYRIUS_ALLOW_ABSOLUTE_INCLUDES=1, cycc 6.6.2). (5a) cga_sphere(0,0,0,r) is NOT a scaled blade: it tends to the fixed null point (0,0,0,-0.5,+0.5) as r->0, because cga_point hard-codes a unit length in (|p|^2 +- 1)/2. For r <= 1e-9 the shipped nsq is bit-exactly 0x0 (ep rounds to exactly -0.5 once r^2/2 < ulp(0.5)/2, i.e. r < 1.05e-8); for r >= 1e10 ep == em bit-for-bit and nsq is 0x0 again (measured at 1e10, 1e20, 1e50, 1e70 -- so the fixture dies of cancellation ~60 decades below the claimed overflow cap). Roughly 150 of the fixture's ~230 decades assert a bit-exact inverse of a blade whose nsq is EXACTLY ZERO: unsatisfiable by every candidate threshold including the claim's own, so it discriminates nothing. The information is destroyed by the embedding before the guard runs. (5b) In the live band the fixture cannot separate the shipped guard from the claim's own repair: magsq is exactly 0.5 for every small-r origin sphere, so EPSILON_F64*magsq = 5e-13 versus the absolute 1e-12 -- a factor 2 in nsq, sqrt(2) in r, invisible to decade steps. Measured decisions are IDENTICAL on every point of the claim's own list: both fire at r = 7.07e-7 (nsq = 4.9982e-13) and neither fires at r = 1e-6 (nsq = 1.00003e-12, invep = -4.9998e11). (5c) The bit-for-bit form is unachievable anyway -- nsq is not bit-equal to fl(r*r) at general r (r = 1e-3 gives nsq = 1.0000000000010001e-06). WHAT SURVIVES, measured: the guard is wrong in both directions, just not at the radii claimed. Too HIGH: cga_project(k*e1, k*e12) -- the same geometry in different units -- returns exactly k*e1 at k = 1 and silently returns ZERO at k = 2^-20 (~9.5e-7), 2^-30, 2^-45, because nsq = k^2 falls under 1e-12 for any k < 1e-6. A micrometre-scale problem posed in metres loses its answer. Too LOW: at coordinate scale 1e2 a conformal point's cancellation residual exceeds 1e-12 for 753/3000 (25.1%) of pseudo-random points, and the function then divides by pure rounding noise -- worst returned component 5.50e11x the input's largest component (scale 1e3: 758/3000, 1.72e10x; worst raw residual 1.68e7 at scale 1e6). NEW, and the claim missed it: cga_blade_inverse is not the only absolute epsilon in this path. cga_geometric_product (:2272,:2275), cga_outer_product (:2297,:2300) and both contractions (:2328,:2331,:2358,:2361) DROP any input component with |c| < EPSILON_F64, so a blade is silently annihilated below k = 1e-12 no matter what the inverse's guard does -- measured: k = 2^-40 (9.09e-13) gives nsq = 0x0, k = 2^-39 (1.82e-12) gives nsq = 2^-78. Four more instances of the same class, and they set a hard floor on what repairing this one guard can reach. [measured: scratchpad probe1-probe4, RUNS OF THE SHIPPED CODE built by cyrius 6.6.2, not an emulation]",
   "fabricates": "Two different plausible answers, both measured on the shipped binary. Below the threshold: cga_mv_zero(), a legal multivector -- cga_project(k*e1, k*e12) returns 0 instead of k*e1 for every k < 1e-6, silently, rc-free. Above it: no guard fires at all, so it divides by rounding noise and returns a blade whose largest component is up to 5.50e11 times the input's (25.1% of pseudo-random points at coordinate scale 1e2, 25.3% at 1e3), which cga_project then propagates.",
   "correct_threshold": "Split the two jobs, and use a factor MEASURED rather than reused. (1) Divide-by-zero: f64_lt(f64_abs(nsq), F64_TINY) -- DBL_MIN, the only value that makes the division fail. This case is common, not theoretical: nsq is bit-exactly 0x0 for every conformal point, for every origin sphere with r < 1.05e-8, and for every origin sphere with r > ~1e8. (2) Null-blade CLASSIFICATION, relative to the blade's own magnitude: |nsq| <= FACTOR * sum_i b_i^2. SHARPENED: the claim's FACTOR = EPSILON_F64 (1e-12) is measured to be ~4500x too loose and throws away resolvable geometry -- a sphere of radius 10 centred at 1e4 has nsq = 100 exactly, ratio 2.0e-14, i.e. 90x above the noise floor, yet 1e-12 * magsq = 5.0e3 would declare it null. The measured bound on the null residual is 1 machine epsilon and it IS scale-free: max |nsq| / sum b_i^2 over 3000 pseudo-random conformal points per scale is 2.13e-16, 2.22e-16, 1.109e-16, 1.110e-16, 1.110e-16, 1.110e-16, 1.108e-16 at coordinate scales 1, 10, 1e2, 1e3, 1e4, 1e6, 1e8. Use FACTOR = 8 * DBL_EPSILON = 1.776e-15 (8x margin over every measured null; keeps the r=10@1e4 sphere at 11x above the bound). Do NOT reuse EPSILON_F64 for it -- that is the constant that caused this. (3) The repair does not reach below k = 1e-12 unless the four sibling filters (cga_geometric_product :2272/:2275, cga_outer_product :2297/:2300, cga_left_contraction :2328/:2331, cga_right_contraction :2358/:2361) drop their absolute EPSILON_F64 component test as well -- they zero the blade before nsq is ever formed. [measured: probe3/probe4 on the shipped binary]",
   "scale_test": "FIXTURE A REPLACED -- the claim's origin-sphere sweep is refuted above (nsq is bit-exactly 0 over ~150 of its 230 decades, and its two live decades give the shipped guard and the proposed repair IDENTICAL answers). Use a blade that actually scales. A' (invertible blade, threshold too high): B_k = k*e12 (cga_mv_set(b, 6, k)); shipped cga_norm_sq returns k^2 bit-exactly, and cga_blade_inverse(B_1) == -e12 exactly, so assert cga_mv_scale(cga_blade_inverse(B_k), k) == cga_blade_inverse(B_1) BIT-FOR-BIT with k stepped over powers of two, k = 2^-39, 2^-30, 2^-20, 2^-10, 2^0, 2^10, 2^100, 2^200, 2^400, 2^500. Powers of two make degree -1 homogeneity exact -- verified on the shipped binary: back-component is 0xbff0000000000000 (-1.0) at every k >= 2^-10 and 0x0 (guard fabricated zero) at 2^-20, 2^-30, 2^-39. Bracketing: the shipped 1e-12 on nsq is crossed at k = 1e-6 (between 2^-20 and 2^-19); a sqrt-style repair testing the unsquared norm crosses at k = 1e-12; a 1e-24 threshold at k = 1e-12; DBL_MIN at k = 2^-511. The lower cap is 2^-39, NOT DBL_MIN: below k = 1e-12 the four sibling component filters annihilate the blade regardless of this guard, so a sweep that goes lower fails the CORRECT repair and reads as a refutation -- run that sub-range only as a separate, explicitly-xfail probe of those four sites. Add the caller-visible form, which is the same problem in metres vs micrometres: cga_project(k*e1, k*e12) must equal k*e1 for every k in the sweep (shipped returns exactly k*e1 at k = 1 and exactly 0 at k = 2^-20). B (null blade, threshold too low -- the direction a sqrt-style repair makes WORSE): for P = cga_point(x,y,z) the answer must be cga_mv_zero() for EVERY point; sweep coordinate scale over 1, 10, 1e2, 1e3, 1e4, 1e6, 1e8 with several hundred pseudo-random directions per decade and assert the zero result. Shipped fails from scale 1e2 upward (753/3000 escapes at 1e2). Both are required and BOTH are needed to pin the factor: A' kills a repair that only lowers the constant (and any DBL_MIN-only repair, which passes A' but fails B); B kills a sqrt-style repair (residual 0.25 at scale 1e4 has sqrt 0.5, nowhere near any absolute epsilon) and any repair that only raises the constant; only a relative test passes both. Add a third assertion to pin the FACTOR itself, since A' and B are both satisfied by a factor anywhere in [2.2e-16, 1e-12]: a sphere of radius 10 centred at (1e4,0,0) has nsq == 100 exactly and must NOT be classified null -- that assertion fails under the claim's originally-proposed EPSILON_F64 factor and passes under 8*DBL_EPSILON."
  },
  {
   "site": "src/geo_advanced.cyr:2478",
   "fn": "cga_plane",
   "guarded_quantity": "norm = f64_sqrt(nx*nx + ny*ny + nz*nz) \u2014 the LENGTH of the caller's normal vector, dims L (or whatever unit the caller's normal carries). Compared against EPSILON_F64 = 1e-12 (src/error.cyr:26, absolute). The routine's stated job is to normalise that vector, so its magnitude is by construction a free scale.",
   "protects": "the division inv = f64_div(F64_ONE, norm) on line 2479 \u2014 and nothing else. It is a divide-by-zero guard, not a tolerance: the file declares its convergence tolerance separately and relatively (_GA_EPA_REL_TOL, 1e-10, src/geo_advanced.cyr:477, commented \"Relative, not absolute\").",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Refutation attempted on all five paths; all five fail on the finding, though the fifth lands on the finding's own test. (1) Not dimensionless: norm is the caller's supplied length, an explicitly free scale. (2) No caller normalises \u2014 a tree-wide grep for `cga_plane` (excluding dist/) returns the definition, one doc mention, and ONE call site: tests/modules.tcyr:7989. It is a public entry point with zero internal callers, so nothing upstream can bound norm. (3) Not a convergence tolerance \u2014 see `protects`; and this same file already documents the correct rule for this exact situation at :484-:491, where `_epa_unit` uses `f64_gt(l, 0) == 0` with the comment \"Normalise without EPSILON_F64's ABSOLUTE cutoff ... silently kills every direction in a scene scaled to 1e-6\". The defect is the file disagreeing with its own note. (4) No existing test breaks: the sole call site is `assert_neq(cga_plane(0, 0, F64_ONE, 0), 0, ...)`, which compares the returned ALLOCATION HANDLE against 0 \u2014 vacuous with respect to every number in the function \u2014 and passes a unit normal (norm = 1), on which no proposed threshold changes behaviour. Even the norm == 0 -> cga_mv_zero() path stays green, because cga_mv_zero() returns a non-null alloc (:2203). (5) The scale test's PREMISE is right \u2014 the blade is degree-0 homogeneous under the joint scaling (n,d) -> (kn,kd); with the metric this file builds (e_inf = ep + em, e_0 = (em-ep)/2, verified against cga_point at :2450) the plane satisfies P\u00b7pi = x\u00b7n - d, so leaving n un-normalised denotes the plane x\u00b7n_hat = d/|n| \u2014 but the sweep as written is a broken instrument and is corrected below rather than accepted. Verdict stands.",
   "fabricates": "A well-formed multivector denoting the WRONG plane, silently \u2014 the function has no error channel and always returns an allocated blade. Reproduced numerically: cga_plane(1e-13, 0, 0, 5) returns (e1, ep, em) = (1e-13, 5, 5), i.e. 1e-13*e1 + 5*e_inf, the plane at distance 5e13 where the caller asked for 5. The error factor is 1/|n|, unbounded. cga_plane(0, 0, 0, 5) returns 5*e_inf \u2014 not a plane at all (the point at infinity), rc-free. Note the corollary that makes it silent in a scaled scene: for |n| = 1e-13 the CURRENT code returns a blade whose e1 component is 1e-13, so a caller sanity-checking \"is the normal unit-length\" sees 1e-13, not the fabricated 1.0 of the identity-fabrication class \u2014 the fabrication is in the DISTANCE, which nothing downstream can re-derive.",
   "correct_threshold": "`if (f64_gt(norm, 0) == 0) { return cga_mv_zero(); }` then divide unconditionally \u2014 this file's own _epa_unit rule (:488). Sharpened on two points found by measurement. (a) The alternative offered, `f64_lt(norm, F64_TINY)`, is NOT a meaningfully different choice here and DBL_MIN is decoration: norm is always a sqrt output, and sqrt's smallest nonzero result is 2.2227587494850775e-162 \u2014 1e146x ABOVE DBL_MIN (2.2250738585072014e-308) \u2014 so the two predicates disagreed 0 times over 400,000 random sqrt-produced norms. DBL_MIN is the right threshold when guarding a division by a RAW operand; it is unreachable when the operand has been through a sqrt. The two forms differ on exactly one input, NaN: f64_lt(NaN, F64_TINY) == 0 takes the divide path and returns an all-NaN blade, while f64_gt(NaN, 0) == 0 routes it to the error return. Prefer the `>0` form for that reason and for consistency with :488. (b) Scope limit that must be stated so the repair is not later judged by a failure it does not own: the threshold fix restores exact degree-0 homogeneity only over |n| in [~1.5e-154, ~1.3e154]. Below ~1.6e-162 the SQUARES underflow (nx*nx -> 0, so norm == 0 for a perfectly good nonzero normal) and between ~1.6e-162 and ~1.5e-154 they are subnormal and lose up to 2^-24 relative; above ~1.3e154 they overflow to +Inf. That is a separate defect requiring max-component scaling (hypot-style), and it is the reason the sweep below is bounded.",
   "scale_test": "The proposed sweep is REFUTED as written and replaced; the property it tests is right. MEASURED against a scale-free repair: decade steps k = 1e-160..1e+150 with bit-exact `component[1] == 1.0 && component[4] == 5.0` FAIL at 89 of 311 sample points ON THE REPAIRED CODE \u2014 83 of them in the fully-normal range where the only error is the rounding of k*(1/k) and 5k*(1/k) (k = 1e-151 gives component[4] = 4.999999999999999; k = 1e-138 gives component[1] = 0.9999999999999999). A test that fails identically on the defect and on its repair discriminates nothing, and it invites the wrong fix \u2014 raising the threshold until it goes green. The other 6 failures are below ~1.6e-162, where nx*nx underflows to 0 so norm == 0 for EVERY threshold choice; and the claim that the span \"separates DBL_MIN\" is false, per the 0/400,000 disagreement above. CORRECTED SWEEP \u2014 k = 2^j, j = -511..+511 (1023 points, ~307 decades), base (nx,ny,nz,d) = (1,0,0,5). Powers of two make every step exact (k^2 in [2^-1022, 2^1022] is normal, 1/k is exact, k*(1/k) and 5k*(1/k) are exact), so bit-exactness is legitimate; the bounds are the largest range over which that holds. Assert ALL five components: component[1] == 1.0, component[2] == component[3] == 0.0, component[4] == component[5] == 5.0 \u2014 components 4 and 5 are what kill a repair that normalises n but forgets d, and 2/3 catch a wrong-slot write. MEASURED KILL TABLE on that sweep (failing j): current 1e-12 -> j <= -40 (472 of 1023); 1e-6 -> j <= -20; 1e-9 -> j <= -30; 1e-15 -> j <= -50; 1e-24 (EPSILON_F64^2) -> j <= -80; DBL_MIN -> 0 failures; f64_gt(norm,0) -> 0 failures. So it brackets every threshold a repair might plausibly choose, above and below the incumbent, and passes only for the scale-free forms. Two cases must be interrogated DIRECTLY rather than left to the sweep, since no perturbed sample reaches them: cga_plane(0,0,0,5) must return a blade a caller can detect as failed (not 5*e_inf), and cga_plane(1e-200,0,0,5e-200) must be recorded as the SEPARATE sum-of-squares underflow defect, expected to fail until the hypot-style scaling lands."
  },
  {
   "site": "src/geo_advanced.cyr:2516",
   "fn": "cga_rotor",
   "guarded_quantity": "norm = sqrt(ax^2+ay^2+az^2), the LENGTH of the caller's rotation axis, in the caller's own units. The header comment one line above the fn (geo_advanced.cyr:2513) states \"Axis (ax, ay, az) is normalized internally\", i.e. the contract explicitly accepts any nonzero magnitude, so the magnitude is a free scale of the problem and the function is degree-0 homogeneous in it. Compared against EPSILON_F64 = 1e-12 (src/error.cyr:26).",
   "protects": "the single division f64_div(F64_ONE, norm) at geo_advanced.cyr:2522 that normalises the axis. No iteration, no convergence, nothing else downstream of it. VERIFIED the only reachable failure of that division is norm == 0.0 exactly: norm can never be subnormal (see correct_threshold), the smallest nonzero value it can take is sqrt(DBL_TRUE_MIN) = 2.2228e-162, and 1/2.2228e-162 = 4.4989e+161, finite and exact. At norm == 0, 1/0 = +Inf and az*(+Inf) = NaN, so the guard's true job is exactly \"norm == 0\".",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Every refutation avenue was checked against the tree and all five fail. (1) NOT dimensionless/bounded: norm is a raw caller-supplied length, and line 2513 documents that the axis is normalised internally, so the same rotation described with a shorter axis vector must yield the identical rotor. (2) NO caller normalises first: a repo-wide grep (excluding .git/build) finds cga_rotor at exactly three places outside its own definition \u2014 docs/architecture/math.md:124 (prose), tests/modules.tcyr:7987, and the dist/hisab.cyr:7238 bundle mirror. It has ZERO internal callers; it is a public entry point of a SHA-locked distributed bundle whose only real callers are the ten consumers. (3) NOT a convergence tolerance: it guards one divide, no loop. (4) The proposed DBL_MIN threshold breaks NO existing test \u2014 the only test in the whole suite is assert_neq(cga_rotor(0, 0, F64_ONE, F64_PI_2), 0, \"cga_rotor builds a rotor\") at tests/modules.tcyr:7987, a UNIT axis whose assertion inspects only the returned pointer for non-null and cannot see a component value at all; hisab.tcyr's 79 cga_ sites contain no \"rotor\", and abuse/edge_cases/foundation contain zero cga_ anything. Under DBL_MIN that test is byte-identical. (5) The scale test does discriminate, but the finding stated it wrongly in three ways, all corrected below rather than fatal. Measured on a faithful IEEE-754 simulation of the exact op sequence: the shipped guard returns the identity for every axis with |a| < 1e-12 (2^-40 = 9.095e-13 -> identity, 2^-39 = 1.819e-12 -> correct rotor), while a DBL_MIN-guarded implementation returns the bit-exact correct rotor at every one of 1049 power-of-two scales from 2^-537 to 2^511. This is the same class repaired in quat.cyr in 2.11.1 and it is the sibling of cga_plane at geo_advanced.cyr:2479, which is worse still (falls back to inv = F64_ONE and silently emits an UNNORMALISED plane normal).",
   "fabricates": "The IDENTITY rotor (scalar = F64_ONE, all 31 other blades 0), with no error channel and without the requested angle ever being read. It is a UNIT rotor (cga_norm = 1), so any assertion that only checks normalisation passes \u2014 the exact discrimination failure recorded in 2.11.1 (\"the fabricated identity IS unit, so the assertion checks DIRECTION\"). cga_rotor(1e-13, 0, 0, F64_PI_2) means \"no rotation\" and cga_sandwich then leaves every point where it was. NOTE a SECOND, distinct fabrication in the same function that the guard does not cover and the DBL_MIN repair alone does not fix: for |axis| >~ 1.34e154 the sum of squares overflows to +Inf, so inv = 1/Inf = 0, every n component becomes 0, and the function returns scalar cos(theta/2) beside a bivector of NEGATIVE ZERO \u2014 an object with |R| = 0.7071 that is not a versor at all. Measured at k = 2^512 and above. That one is NOT unit, so a norm assertion catches it while the component assertion catches both.",
   "correct_threshold": "f64_lt(norm, _GA_F64_TINY) with a new var _GA_F64_TINY = F64_TINY declared beside the existing _GA_F64_* block at geo_advanced.cyr:18-22; F64_TINY already exists at src/error.cyr:45 (0x0010_0000_0000_0000 = DBL_MIN) and is already aliased this exact way at quat.cyr:12, geo.cyr:26 and autodiff.cyr:17. SHARPENING the finding: DBL_MIN here is behaviourally IDENTICAL to f64_eq(norm, 0), because norm = sqrt(sum of squares) can never be subnormal \u2014 anything whose square underflows to zero gives norm exactly 0, and the smallest nonzero norm is sqrt(DBL_TRUE_MIN) = 2.2228e-162, a normal number 146 decades ABOVE DBL_MIN. DBL_MIN is still the correct written form (it matches the three sibling aliases and stays correct if the norm is ever recomputed by a scaled/hypot route that can produce subnormals), but the finding must not claim a test can observe the difference. The repair is INCOMPLETE unless it also rejects the overflow side: add a companion check that norm is finite (or scale the axis by max(|ax|,|ay|,|az|) before squaring), otherwise |axis| >~ 1.34e154 still silently returns the non-unit -0-bivector object described in `fabricates`.",
   "scale_test": "cga_rotor is degree-0 homogeneous in the axis: cga_rotor(k*ax, k*ay, k*az, theta) must return the SAME rotor for every k > 0. Sweep axis (0,0,1) scaled by k = 2^j for every integer j in [-537, +511] (1049 points, ~315 decades) with theta = F64_PI_2, and assert component[0] == cos(pi/4) AND component[6] == -sin(pi/4) BIT-EXACTLY, plus |R| == 1. THREE CORRECTIONS to the originally proposed sweep, each measured: (1) Step in powers of TWO, not decades. With k = 10^e the reciprocal 1/norm rounds and k*(1/k) != 1.0, so strict == on component[6] fails for 49 of the 309 in-range decades EVEN ON A CORRECTLY REPAIRED IMPLEMENTATION \u2014 including k = 1e-11, one decade from the shipped threshold, off by 1 ULP. The decade form as written would be read as \"the repair did not work\". With k = 2^j the reciprocal is exact and the repaired implementation gives 0 failures in 1049, while the shipped code fails 498 (every j <= -40). (2) The sweep CANNOT bracket DBL_MIN and must not claim to: below j = -538 the square underflows to exactly 0 and BOTH the shipped and any sub-2.22e-162 threshold return the identity, so DBL_MIN, 1e-160 and \"norm == 0\" are mutually indistinguishable through axis scaling. What the sweep must bracket is the OBSERVABLE candidate set, and 2^j does: 1e-6 (2^-20), 1e-9 (2^-30), EPSILON_F64 = 1e-12 (2^-40), 1e-15 (2^-50), EPSILON_F64^2 = 1e-24 (2^-80), and the true 0/nonzero boundary at j = -538 vs -537. (3) EXTEND the top to j = +530 (the original stopped at 1e+150 and missed this): assert |R| == 1 there, because at j >= 512 the sum of squares overflows and the function returns a bivector of -0 with |R| = 0.7071 \u2014 a second live fabrication the DBL_MIN repair does not fix. Additionally assert with a 4-ULP tolerance (not ==) on a generic axis such as (0,0,1e-160) and (k,k,k) in the band 2.22e-162 <= |a| <= 1.49e-154, where the sum of squares is subnormal and sqrt loses up to ~11 bits \u2014 measured 4e-6 relative error on component[6] at k = 1e-160 \u2014 which is inherent to the unscaled norm and must not be conflated with the guard."
  },
  {
   "site": "src/lie.cyr:79",
   "fn": "su2_from_quat",
   "guarded_quantity": "norm = sqrt(qw^2+qx^2+qy^2+qz^2) \u2014 the Euclidean 4-norm of the caller's raw quaternion, read straight off HQuat_{w,x,y,z}(q) with no prior normalisation. Dimension: whatever units the caller's components carry; homogeneous of degree 1 in them (measured exactly so \u2014 norm(2^e * q) == 2^e * norm(q) bit-exactly for e = -537..511).",
   "protects": "the division inv = f64_div(F64_ONE, norm) on the next line, used to normalise the quaternion. It is a divide-by-zero guard, not a convergence tolerance and not a near-degeneracy test.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE GROUNDS; ALL FAILED. (1) Not dimensionless/bounded: norm is degree-1 homogeneous in the caller's raw components, while the function's contract \u2014 its own comment, \"Construct from raw quaternion components (w, x, y, z). Normalizes.\" \u2014 is degree-0. A degree-1 quantity compared against a fixed 1e-12 is the defining shape of this class. (2) Caller does not normalise: the ONLY in-tree caller is lie_ext.cyr:366 (se3_from_mat4), which builds q from a rotation matrix and so passes norm ~= 1; I traced the degenerate case too (an all-zero mat4 takes the r00-largest branch and yields q = (0.5,0,0,0), norm 0.5), so no in-tree path can reach the guard. That makes the defect INVISIBLE in-tree \u2014 it is the reason the suite is silent, not evidence the guard is correct. su2_from_quat is a public entry point in dist/hisab.cyr and its doc comment advertises accepting non-unit input, so the caller is arbitrary by design. (3) Not a convergence tolerance: it guards f64_div(F64_ONE, norm) three lines down; there is no iteration. (4) F64_TINY changes NO existing test \u2014 checked, not assumed: modules.tcyr:3626 (the only su2_from_quat call in tests/) feeds su2_to_quat(su2_from_axis_angle(...)), norm exactly 1.0; modules.tcyr:7688's se3_from_mat4 round-trip likewise; abuse.tcyr:2526 exercises su2_from_axis_angle's SEPARATE guard on `len`, a different quantity at a different site. No test input lands in the changed interval [DBL_MIN, 1e-12). (5) The scale_test DOES discriminate, but the version as filed is a BROKEN INSTRUMENT \u2014 see scale_test; it is rewritten rather than refuted. MEASURED, exact power-of-two scaling of q = (w = cos(pi/4), x = sin(pi/4), 0, 0): shipped EPSILON_F64 is bit-exact only on e = -39..+511, fabricating the identity for every e <= -40; F64_TINY is bit-exact on e = -537..+511 \u2014 1049 consecutive binary exponents, 315 decades, zero ulp deviation, exact even where the squares are subnormal. The repair widens the correct window by 498 binary exponents (~150 decades) and the division it protects fails only at norm = 0, another 537 exponents below where the shipped guard fires.",
   "fabricates": "_su2_alloc(F64_ONE, 0, 0, 0) \u2014 the identity element: a valid unit quaternion, rc = 0, no error channel, indistinguishable from a real answer. Worst-case plausible, because for a scaled +w-only quaternion the fallback happens to BE right, which is exactly how a suite can pass while the wrong rotation is returned. \u26a0 A SECOND, DISTINCT FABRICATION LIVES ON THE SAME LINE AND NEITHER THRESHOLD CAN REACH IT: for |q| >= 2^512 (~1.34e154) the squares overflow to +Inf, norm = +Inf, inv = f64_div(1, +Inf) = 0, and the function returns (0, 0, 0, 0) with rc = 0 \u2014 not a unit quaternion, not the identity, not any group element. `f64_lt(norm, T)` is false for +Inf under every T, so a threshold-only repair leaves it live. Measured: bit-exact through e = +511, all-zero from e = +512 (decades: correct through 1e154, zeros from 1e155).",
   "correct_threshold": "F64_TINY (DBL_MIN, 0x0010_0000_0000_0000) on `norm` \u2014 the smallest magnitude whose reciprocal is finite, i.e. precisely what makes f64_div(F64_ONE, norm) fail, and nothing more. The constant is already in the tree at src/error.cyr:45, whose own comment states this exact rule and names EPSILON_F64 as the wrong choice when the guard protects a division; quat.cyr:102/123 (hquat_normalize/hquat_inverse) is the same predicate on the same quantity already repaired this way in 2.11.1, and geo's _GEO_F64_TINY is the 2.10.2 repair of the same class. No overflow risk from the widening: norm >= DBL_MIN gives inv <= 4.49e307 (finite), and each component satisfies |q_i| <= norm so |q_i * inv| <= 1. Keep the identity fallback for the norm == 0 case \u2014 it matches hquat_normalize's convention. \u26a0 TWO SCOPE NOTES A THRESHOLD-ONLY REPAIR MUST NOT SILENTLY INHERIT: (a) F64_TINY is the right guard for the DIVISION, but it does not make the function scale-free to DBL_MIN \u2014 the squared-norm accumulation loses the input first, flushing to 0 at e <= -538 (|q| ~ 1e-162), where the identity is then returned for a reason that is no longer the guard's; (b) the +Inf/(0,0,0,0) path above is unguarded at any threshold and needs its own fix (test norm for non-finiteness, or scale by the max component before squaring). Fixing the threshold alone is correct but partial, and should be recorded as such.",
   "scale_test": "HOMOGENEITY OF DEGREE 0, SCALED BY POWERS OF TWO \u2014 NOT DECADES. su2_from_quat(k*q) must be BIT-IDENTICAL to su2_from_quat(q) for every k > 0. Fix q = hquat_new(sin(pi/4), 0, 0, cos(pi/4)) \u2014 a 90-degree rotation about x, so BOTH w and x are non-trivial (a w-only q cannot discriminate: the fabricated identity has the right shape and would let every mutant live). Take k = 2^e via ldexp/scalbn, e = -537..+511 (1049 exponents), asserting _su2_w == 0x3fe6a09e667f3bcd and _su2_x == 0x3fe6a09e667f3bcc at every one. \u26d4 THE FILED DECADE VERSION IS A BROKEN INSTRUMENT AND WOULD FAIL ON CORRECTLY REPAIRED CODE \u2014 three measured defects. (i) Its central claim, \"j = 0..12 all return 0x3fe6a09e667f3bcd/0x3fe6a09e667f3bcc, bit-identical across 13 decades\", is FALSE: 4 of those 13 are 1 ulp off (j=1 w+1, j=4 x+1, j=10 both +1, j=11 w-1), because 10^-j is not representable so k*q is itself a rounding \u2014 and WHICH decades fail depends on how you scale (multiplying by 10^-j fails {1,4,10,11}; dividing by 10^j fails {1,4,5,9,10,11}). Over j = 0..154 only 96/155 are bit-exact. A test asserting decade bit-identity fails at j=1 on BOTH shipped and repaired code, reads as \"the repair did not work\", and the obvious rescue \u2014 loosening to a tolerance \u2014 is exactly the move that makes an assertion vacuous. (ii) Its span is wrong: \"run to j=160, qw*qw underflows at j ~ 162\" \u2014 measured, under F64_TINY the decade results are already 148 ulp off at j=155, 10,847 at j=156, 1.05e8 at j=158, 3.5e10 at j=160 and 3.8e13 at j=161, because the squares go SUBNORMAL near j ~ 154 and bleed bits long before reaching zero; j=155..161 is a garbage zone belonging to neither leg. (iii) Its mirror leg's j <= 150 cap steps around the +Inf -> (0,0,0,0) fabrication, so a threshold-only repair would be declared complete with half the range still fabricating. Powers of two fix all three: the scaling is exact, so bit-identity is a TRUE property, and it stays exact even where the squares are subnormal (at e = -511 each square is 2^-1023 and the sum 2^-1022 \u2014 powers of two, so subnormal arithmetic is exact). MEASURED WINDOWS: shipped EPSILON_F64 bit-exact on e = -39..+511 only; F64_TINY bit-exact on e = -537..+511 with zero ulp deviation. BRACKETING (first exponent at which each candidate fires, norm = 2^e exactly): 1e-6 at e=-20; sqrt(DBL_EPSILON) 1.4901e-8 at e=-27; the shipped 1e-12 at e=-40; 1e-15 at e=-50; DBL_EPSILON 2.22e-16 at e=-53; 1e-100 at e=-333 \u2014 all strictly inside the leg, so e = -537..0 kills every one, and only F64_TINY survives (it cannot fire anywhere in range). Add TWO separately-labelled boundary assertions so the partial-repair scope is explicit: at e = -538 the result IS the identity and must be asserted to be reached because the squared-norm expression underflowed to 0 (not because the guard chose a scale); at e = +512 the result is currently (0,0,0,0) \u2014 assert the intended post-fix value there, and pin the CURRENT value as a known-defect marker until the overflow path is fixed."
  },
  {
   "site": "src/lie.cyr:100",
   "fn": "su2_from_axis_angle",
   "guarded_quantity": "len = f64_sqrt(ax^2 + ay^2 + az^2) at src/lie.cyr:99 \u2014 the Euclidean LENGTH of the caller's rotation axis, in the caller's own units. Dimension length^1, homogeneous of degree 1: writing the same axis in millimetres instead of metres multiplies it by 1000. It is not dimensionless, not bounded, and not a residual. The `angle` argument, which is the only place the rotation magnitude lives, is a separate parameter and is never inspected by the guard.",
   "protects": "the division `var inv = f64_div(F64_ONE, len)` at src/lie.cyr:102, which normalises the axis. f64_div is a cycc primitive with no internal guard of its own (checked: the only EPSILON_F64 division guard in f64_util.cyr is f64_fmod's, a different function), so the sole failure mode is len == 0 (or subnormal, where 1/len overflows to +Inf and ax*inv becomes NaN).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation avenues were tested and all five fail. (1) NOT dimensionless/bounded \u2014 `len` is |axis| in caller units, degree 1; error.cyr:40-44 states this repo's own rule verbatim (\"EPSILON_F64 remains right for a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, a parameter in [0,1]\"), and `len` is none of those. (2) NO caller normalises \u2014 grep over src/, tests/ and examples/ finds ZERO internal call sites (the only src/ hits besides the definition at :93 are three history comments); it is a public entry point whose own header says \"Automatically normalizes on construction\" and whose body comment is \"# Normalize axis\", i.e. arbitrary magnitude IS the contract. (3) NOT a convergence tolerance \u2014 it is three lines above the division it guards, one-shot, no loop. (4) The F64_TINY repair is test-neutral, MEASURED not assumed: a scratch tree with only line 100 changed to `f64_lt(len, F64_TINY)` runs hisab 416 / foundation 359 / modules 1827 / edge_cases 233 / abuse 776 = 3611 passed, 0 failed \u2014 BIT-IDENTICAL pass counts to the shipped tree built in the same directory. The one test that pins the fabrication path, abuse.tcyr:2525-2527 (`hvec3_zero()`), still passes: len is exactly 0, 0 < DBL_MIN, so the designed zero-axis identity return is preserved and only the fabrication BAND is removed. (5) The scale_test discriminates in substance but is WRONG as written \u2014 see scale_test below. Decisive precedent: `hquat_normalize` (src/quat.cyr:100-107) is the same three lines in the same shape (`len = length; if (len < T) return identity; inv = 1/len`) and was already moved to `_QUAT_F64_TINY` in 2.11.1; lie.cyr's su2 pair was simply missed by that sweep (`su2_from_quat`, src/lie.cyr:78, is the same defect on `norm` and should be repaired in the same edit).",
   "fabricates": "_su2_alloc(F64_ONE, 0, 0, 0) \u2014 the identity of SU(2), a valid unit quaternion every downstream consumer (su2_to_quat, su2_to_rotation_matrix, so3_from_quat, se3_new) accepts, returned with no error channel. MEASURED on the shipped binary: su2_from_axis_angle(hvec3_new(1e-13,0,0), pi/2) returns w = 0x3FF0000000000000 (1.0), x = 0x0, where a unit axis returns w = 0x3FE6A09E667F3BCD (0.7071067811865476), x = 0x3FE6A09E667F3BCC. Worse than \"a small rotation is dropped\": holding the axis at 1e-13 and sweeping the angle, EVERY angle from 1e-6 through pi returns (1.0, 0) \u2014 a full 180-degree rotation discarded because a direction vector was short. Under the F64_TINY repair the same k = 1e-13 returns, at angle = pi, w = 0x3C91A60000000000 (6.123e-17 = cos(pi/2)) and x = 0x3FF0000000000000 (1.0), bit-identical to the unit-axis answer.",
   "correct_threshold": "F64_TINY (error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN) on `len` \u2014 exactly what makes f64_div(F64_ONE, len) fail and nothing more. Use the existing constant, matching src/quat.cyr:102's already-repaired `hquat_normalize`; do not introduce a fourth local alias. Apply the identical change to su2_from_quat (src/lie.cyr:78), which guards `norm` with EPSILON_F64 for the same division at :82. NOTE, and file separately rather than folding in: the repair does NOT make the function scale-free at the extremes, because the SUM OF SQUARES overflows/underflows before the guard is reached \u2014 measured, k^2 -> +Inf gives x = 0 with w still correct from k = 1e155 up, and k^2 -> 0 gives the full identity from k = 1e-162 down. Only a max-component-scaled (hypot-style) length is scale-free everywhere; F64_TINY is the correct threshold for the guard as the guard is written.",
   "scale_test": "HOMOGENEITY OF DEGREE 0 IN THE AXIS, ANGLE HELD FIXED: su2_from_axis_angle(k*axis, angle) must equal su2_from_axis_angle(axis, angle) for every k > 0. \u26d4 THE ORIGINAL FORM OF THIS TEST WOULD FAIL ON CORRECTLY-REPAIRED CODE and must be sharpened \u2014 MEASURED: with the F64_TINY repair in place and k = 10^-j, _su2_x reads 0x3FE6A09E667F3BCB at j = 5, 9, 11, 18 and 0x3FE6A09E667F3BCC elsewhere, a 1-ULP wobble that is INHERENT to the normalisation (fl(sqrt(fl(k*k))) does not round-trip to k for a decimal k) and not a defect; a hand-normalised control shows the same wobble at the same j. Bit-exactness on `_su2_x` for decimal k is therefore an over-strict assertion. Sharpened, three legs: (A) LOW LEG, EXACT POWERS OF TWO \u2014 axis = hvec3_new(2^-j, 0, 0), angle = pi/2, j = 0..537: assert _su2_w == 0x3FE6A09E667F3BCD AND _su2_x == 0x3FE6A09E667F3BCC BIT-EXACTLY. k*k is exact for a power of two while 2j <= 1074, so the repaired path is bit-exact with no wobble at all \u2014 VERIFIED bit-exact at j = 0,17,...,527 and fabricating at j = 544 (2^-1088 underflows), so cap at 537. This spans 10^0 down to ~2.3e-162 and brackets every threshold a repair might plausibly pick: 1e-6 (fires j=20), 1.49e-8 (j=26), 1e-12 as shipped (j=40), 1e-15 (j=50), 2.2e-16 DBL_EPSILON (j=52), 1e-100 (j=333), 1e-150 (j=499). F64_TINY is the ONLY candidate that never fires in range. (B) HIGH MIRROR, j = 0..511 only (2^512 squared overflows to +Inf \u2014 that is the separate overflow defect noted above; capping keeps the two from being conflated). (C) DECIMAL / UNITS LEG, which is the one that speaks in the units the defect is about (metres -> millimetres is x1e3): k = 10^-j, j = 0..154, assert _su2_w BIT-EXACT (w = cos(angle/2) does not depend on the axis at all and IS bit-identical at every k \u2014 measured) and |_su2_x - 0x3FE6A09E667F3BCC| <= 2 ULP. (D) SECOND LEG, THE ONE THAT SEPARATES THIS FROM A SMALL-ANGLE COMPLAINT: hold k = 1e-13 and sweep angle over 1e-6, 1e-5, ... 1, and pi; assert each result is BIT-IDENTICAL to su2_from_axis_angle(hvec3_new(1,0,0), angle). MEASURED: shipped returns (1.0, 0) for all eight; repaired returns all eight bit-identical to the unit-axis reference. This is what proves an arbitrarily LARGE rotation is being thrown away by a guard on a quantity that does not carry it \u2014 and a unit-norm assertion would not discriminate here at all, since the fabricated identity is itself unit, so the assertion must check DIRECTION."
  },
  {
   "site": "src/lie.cyr:151",
   "fn": "su2_exp",
   "guarded_quantity": "norm = f64_sqrt(ox^2+oy^2+oz^2) = |omega|, the magnitude of the tangent vector, which by this function's own documented convention IS the rotation angle in radians. Dimensionless as a UNIT (an angle is invariant under any change of length/time units) but NOT bounded and NOT normalised: it is the caller's free scale parameter (a body rate times a timestep, a subdivision of a twist, a finite-difference step h). Measured range of the expression itself: it can only ever produce exactly 0.0 or a NORMAL double >= 2.2e-162 \u2014 f64_sqrt of the smallest positive subnormal (4.9e-324) is 2.2e-162, and the sum-of-squares underflows to 0 below that (verified: f64_mul(0x1,0x1) = 0x0, and su2_exp's own norm reads 0x0 from theta = 1e-162 down). The guarded quantity therefore has no subnormal regime at all.",
   "protects": "The division sinc = f64_div(f64_sin(half), norm), with half = norm/2 \u2014 i.e. the sinc term sin(|omega|/2)/|omega| that carries the tangent (x,y,z) part. Nothing else in the function divides; f64_cos(half) is total.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation routes were tried against measurement on cycc 6.6.2 and all five fail. (1) NOT dimensionless-and-bounded in the sense error.cyr:37-43 reserves EPSILON_F64 for (\"a normalised cosine, a barycentric weight, a parameter in [0,1]\"): |omega| is an unbounded free input and the guard fires on the SMALL side, where nothing whatsoever fails. (2) NOT caller-normalised: the only in-tree caller, se3_exp (lie_ext.cyr:443), pre-filters theta at the SAME 1e-12 and so never reaches this guard \u2014 but su2_exp is a public bundle export (dist/hisab.cyr:19939), called directly by tests, and reachable by every one of the ten SHA-locked consumers. (3) NOT a convergence tolerance: the division is perfectly conditioned everywhere the guard fires, and I measured that it is EXACT, not merely well-conditioned \u2014 f64_sin(half) returns half bit-for-bit for every theta <= 1e-8, so sinc = 0.5 exactly and _su2_x == theta/2 to the last bit for all 154 decades j=8..161 (first_inexact = -1, none). A Taylor switch would return (1, omega/2), which is precisely what the full formula already computes exactly; the shipped fallback returns (1, 0), the ZEROTH-order truncation, which is a different and strictly worse thing. (4) NOT covered by an existing test: I snapshotted src+tests, flipped line 151 to F64_TINY, and ran all five suites both ways \u2014 416/359/1827/233/776 = 3611 passed, 0 failed, IDENTICAL on both sides; no assertion in the repo touches the changed region (zero omega still returns identity, verified directly: w = 0x3ff0000000000000, x = 0x0). (5) The scale test DOES discriminate \u2014 measured first-collapse decade per candidate threshold, where \"collapse\" is the ratio 2*_su2_x/theta dropping below 0.5: 1e-6 -> j=7, sqrt(DBL_EPSILON) 1.49e-8 -> j=9, shipped 1e-12 -> j=13, 1e-15 -> j=16, DBL_EPSILON -> j=16, 1e-100 -> j=101, DBL_MIN -> never over j=0..161. The step is confirmed as a discontinuity, not rounding: theta=1e-12 gives _su2_x = 0x3d619799812dea12 (exactly theta/2), theta=1e-13 gives 0x0 where the truth 0x3d2c25c268497683 is an ordinary normal double. The broken covariance is not a change of units (an angle has none) \u2014 it is the exponential map's own one-parameter-subgroup homogeneity, deg-1 in the tangent part, the same property 2.10.0 used to catch geo_ray_sphere. TWO CORRECTIONS TO THE FILING'S OWN INSTRUMENT, both measured: (a) its assertion \"_su2_x == theta/2 to within 1 ulp for j = 0..160\" is FALSE ON CORRECT CODE for j=0..7 \u2014 at theta=1, sin(0.5)/1 = 0.479425538604203, a 4.1% deviation, and j=7 is still 3 ulp out; written as stated it would fail the repair and get the repair reverted; (b) its companion leg \"_su2_w == f64_cos(theta/2)\" is VACUOUS over the entire defect region \u2014 cos(theta/2) rounds to exactly 1.0 for every theta <= 1e-8, which is also what the fabricated identity returns, so the w leg cannot see this defect anywhere it exists. The tangent part is the only discriminator.",
   "fabricates": "su2_identity() = (1,0,0,0) \u2014 a valid, unit, plausible quaternion, returned as a normal pointer with no error channel and no way for a caller to tell it apart from a genuine near-identity result. It is unit length, so any \"is the result normalised\" assertion agrees with it; only DIRECTION discriminates. Composed N times it is still the identity, so an integrator taking 1e6 steps of 1e-13 rad ends exactly where it started instead of 1e-7 rad away, and a Lie-derivative or finite-difference probe with h < 1e-12 reads exactly zero, making exp/log non-invertible near 0.",
   "correct_threshold": "F64_TINY (DBL_MIN, 0x0010_0000_0000_0000, already defined at src/error.cyr:45) on `norm` \u2014 exactly what makes the sinc division fail and nothing more. Measured refinement: because f64_sqrt of the smallest positive subnormal is 2.2e-162 and the sum-of-squares underflows to 0 below that, this expression never yields a subnormal, so on THIS site F64_TINY is exactly equivalent to `norm == 0.0` \u2014 and at norm == 0 the identity fallback is not a fabrication but the true exponential, so the existing return stays. Verified non-breaking: 3611/3611 assertions across hisab/foundation/modules/edge_cases/abuse pass unchanged with the flip applied. Two scope notes that the repair must not be mistaken for: repairing line 151 does NOT repair the SE(3) path \u2014 se3_exp (lie_ext.cyr:443) short-circuits on its own EPSILON_F64 before ever calling su2_exp \u2014 and the sibling absolute guards on the same quantity in this pair of files (lie.cyr:79 su2_from_quat, :100 su2_from_axis_angle, :172 su2_log, :399 lorentz_boost, :440 lorentz_rotate; lie_ext.cyr:200 so3_exp, :215 so3_log, :443 se3_exp) are separate sites, of which so3_exp's must move with su2_exp's if the pinned double-cover invariant su2_to_rotation_matrix(su2_exp(w)) == so3_exp(w) is to stay exact below 1e-12.",
   "scale_test": "DEGREE-1 HOMOGENEITY OF THE TANGENT PART, in the corrected form (the filing's literal wording fails on correct code \u2014 see `why`). For n_hat = (1,0,0) and theta = 10^-j, j = 0..161: assert the RATIO r = 2*_su2_x(su2_exp(theta*n_hat))/theta stays near 1 \u2014 a plain `r > 0.5` is enough to discriminate every candidate and needs no closed form, since the fabrication drives r to exactly 0 while the truth is 1 - theta^2/24 (r = 0.9589 at j=0, and >= 1-1e-16 from j=8 down). Tighten the tail with the bit-exact leg, which measurement says holds with no slack at all: for j = 8..161, _su2_x MUST equal f64_mul(theta, F64_HALF) EXACTLY (measured: 0 mismatches over all 154 decades under DBL_MIN). Do NOT assert exactness for j = 0..7 (sinc genuinely differs there) and do NOT rely on the _su2_w leg at all (cos(theta/2) == 1.0 exactly for theta <= 1e-8, so it agrees with the fabricated identity). MEASURED first-collapse decade, which is the bracketing proof: 1e-6 -> j=7, 1.49e-8 -> j=9, 1e-12 (shipped) -> j=13, 1e-15 -> j=16, DBL_EPSILON -> j=16, 1e-100 -> j=101, DBL_MIN -> never; so the j=0..161 span brackets every threshold a repair might plausibly choose and only DBL_MIN survives it. Stop at j=161: at j=162 ox*ox underflows to 0 so norm reads 0x0 (measured), which is the norm expression's own limit, not this guard's. UNIT-FREE GROUP LEG (put this one in the suite, it needs no closed form) \u2014 but not as the filing writes it, which is both infeasible and false on correct code: use repeated SQUARING, q_k = su2_exp(omega/2^k) composed with itself 2^k times via k squarings, k = 0..520 (520 compositions total, versus the 10^12 su2_compose calls and 32 TB of never-freed 32-byte allocations the \"N = 10^j composed N times\" form literally demands), and assert |_su2_x(q_k) - _su2_x(su2_exp(omega))| < 1e-6, NOT equality. The tolerance is load-bearing and measured: composition rounding alone plateaus at 1.786e-9 for every k >= 30, so my own first run of this leg at a 1e-9 tolerance reported failure at k=26 for EVERY threshold including DBL_MIN \u2014 the instrument, not the code. At 1e-6 it brackets cleanly, first-failing k = 20 / 27 / 40 / 50 / 53 / 333 / never for 1e-6 / 1.49e-8 / 1e-12 / 1e-15 / DBL_EPSILON / 1e-100 / DBL_MIN, with the shipped guard deviating by 0.4794 (total collapse) against that 1.79e-9 floor."
  },
  {
   "site": "src/lie.cyr:172",
   "fn": "su2_log",
   "guarded_quantity": "|sin(ganita_f64_acos(w))| where w = f64_clamp(_su2_w(s), -1, 1), the scalar part of a unit quaternion. Dimensionless, bounded in [0,1], degree 0 \u2014 it does NOT scale with any unit choice, so no scale sweep applies. Mathematically it equals |v| = sqrt(x^2+y^2+z^2), but it is COMPUTED through acos(w), whose input has already lost the information: w = cos(theta/2) rounds to exactly +-1.0 for all theta within 2.1e-8 of 0 or 2*pi.",
   "protects": "the division sc = f64_div(theta, sin_half) at src/lie.cyr:176 (and notionally sin_half == 0 exactly at w = +1). Per src/error.cyr:40-45's own rule \u2014 F64_TINY when the guard protects a DIVISION whose only failure is a zero/subnormal denominator, EPSILON_F64 only for a genuinely dimensionless bounded quantity \u2014 this is the division case, and the bounded-quantity exemption does not rescue it because the bounded quantity is a destroyed proxy for the divisor that matters.",
   "verdict": "DEFECT_WRONG_QUANTITY",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FAILED, and the measurements strengthen the filing rather than weaken it. (1) \"Dimensionless/bounded, so an absolute epsilon is correct\" \u2014 the dimensional half is TRUE (degree 0, no scaling axis, so DEFECT_ABSOLUTE_ON_SCALING is the wrong label and a scale sweep is meaningless here) but it does not save the guard, because the number is not the variable. MEASURED on the shipped binary (probe3): sin(acos(w)) = 0.0 at w = +1, 1.2246063538223773e-16 at w = -1, 1.4901161193847656e-08 at w = 0x3FEFFFFFFFFFFFFF (largest double < 1), 1.490116131630829e-08 at w = 0xBFEFFFFFFFFFFFFF. The reachable set is {0} U {1.22e-16} U [1.49e-08, 1] \u2014 EPSILON_F64 = 1e-12 sits in the hole, so the guard fires IFF w == +-1.0 exactly, and every threshold in (1.23e-16, 1.49e-8) is behaviourally identical. (2) \"The caller normalises\" \u2014 it does, and that is the CAUSE, not the cure: unit-ness is exactly what forces w to round to +-1.0. (3) \"It is a convergence tolerance\" \u2014 no, it guards a divide at line 176. (4) \"The repair breaks an existing test\" \u2014 CHECKED, NOT ASSUMED (probe2, built against src/lie.cyr + src/lie_ext.cyr): modules.tcyr:5918-5921 su2_log(su2_exp(lw)), lw = (1/3,1/2,1/5), shipped x-bits 4599676419421066580 vs repaired ...582 (true value ...581) \u2014 1-2 ulp apart against LOOSE_TOL_M = 1e-8, eight orders of slack; modules.tcyr:5934-5938 se3_log's omega is bit-identical to that same pair; abuse.tcyr:2528-2529 identity returns (0,0,0) and a non-null pointer under BOTH. Nothing flips. (5) \"The scale_test does not discriminate\" \u2014 PARTLY TRUE, and it is the one real correction: the claimed mirror leg theta = pi - 10^-j probes w -> 0, the BEST-conditioned point, not w -> -1; MEASURED, shipped and repaired agree bit-for-bit at every j = 0..19 on that leg, and theta saturates at pi from j >= 16, so the leg kills zero mutants. The true w -> -1 mirror is theta = 2*pi - 10^-j (see scale_test). DECISIVE NEW EVIDENCE that the threshold is not the variable (probe4): with the guard DELETED \u2014 which is where \"lower EPSILON_F64 to DBL_MIN\" and every other threshold choice lands \u2014 q = su2_from_axis_angle(x_hat, 1e-8) returns NaN and q = su2_from_axis_angle(x_hat, 2*pi - 1e-8) returns 256538985.08980078 rad, while the shipped guard returns 0.0 and the truth is 1e-08 and 6.283185297179586. NO value of the threshold produces a correct answer at either input; the atan2(|v|, w) form returns both bit-exactly.",
   "fabricates": "hvec3_new(0,0,0) \u2014 \"this element is the identity, there is no rotation\" \u2014 with a valid Vec3 pointer and rc = 0, on BOTH branches, and the -1 branch is far worse than originally filed. w -> +1 branch: every rotation of magnitude <= 2.1e-8 rad about any axis (MEASURED: theta = 1e-8 about x_hat holds x = 5e-9, a normal double, and returns exactly 0x0). w -> -1 branch: every theta >= 2*pi - 2.1e-8, where the function's OWN convention (theta = 2*acos(w) in [0, 2*pi], which it honours at theta = 2*pi - 1e-7) says the answer is ~6.2832 \u2014 an absolute fabrication of a full 2*pi rad, i.e. relative error 1.0, MEASURED as ol=0x0 at j = 8..19 on the 2*pi leg. And on that same branch the acos path is already wrong BEFORE the guard ever fires, with no error channel: theta = 2*pi - 1e-6 returns 6.282905038986482 (4.4e-5 relative), theta = 2*pi - 1e-7 returns 6.356724824889811 \u2014 0.0735 rad, 4.2 DEGREES wrong \u2014 against a bit-exact repaired result. A pose solver measuring residual as ||su2_log(dR)|| reads exactly 0.0 (\"converged, bit-exact\") at up to 2.1e-8 rad of true residual, or reads 0.0 for a near-2*pi element; a finite-difference or Lie-derivative probe at the identity reads exactly zero gradient.",
   "correct_threshold": "Guard the divisor that actually carries the answer: vnorm = sqrt(x*x+y*y+z*z) compared against F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN \u2014 vnorm >= 0, so no f64_abs is needed), and recover the angle as theta = 2*ganita_f64_atan2(vnorm, w) instead of 2*ganita_f64_acos(w); then sc = theta/vnorm, which tends to 2 smoothly as vnorm -> 0 with the error in vnorm cancelling between numerator and denominator. THE REPAIR IS A CHANGE OF QUANTITY, NOT OF NUMBER: leaving acos in place and lowering EPSILON_F64 to DBL_MIN measurably makes it worse, not better (NaN on the +1 branch, 2.57e8 rad on the -1 branch \u2014 probe4). MEASURED behaviour of the repaired form: bit-exact round trip for theta = 10^-j at j = 0..161 (j = 162 returns 0 for both forms, because x*x underflows to +0 there \u2014 a separate limitation of the plain sum-of-squares norm, not of this guard); bit-exact on the whole theta = 2*pi - 10^-j leg, j = 0..19; branch convention preserved (atan2(vnorm, w) is in (pi/2, pi) for w < 0, so theta stays in (pi, 2*pi) exactly as 2*acos(w) did \u2014 j = 0 of that leg agrees bit-for-bit with shipped); within 1-2 ulp of shipped on every existing test input. The one remaining (0,0,0) return is vnorm == 0 exactly, i.e. q = +-1: correct for +1, and for -1 it is a genuine mathematical ambiguity (every axis at angle 2*pi maps there), which shipped and repaired answer identically. Keeping the f64_clamp on w is harmless \u2014 atan2 has no domain restriction \u2014 but is no longer load-bearing.",
   "scale_test": "\u26a0 A THRESHOLD SWEEP CANNOT DISCRIMINATE THIS ONE (the reachable set has a hole from 1.23e-16 to 1.49e-8 and 1e-12 lies inside it), and \u26a0 THE ORIGINALLY FILED MIRROR LEG IS WRONG AND MUST BE REPLACED: theta = pi - 10^-j drives w -> 0, the best-conditioned point, and MEASURED shipped == repaired bit-for-bit at every j (plus theta saturates at pi from j >= 16), so it kills nothing. Use TWO legs, both with a UNIT axis so su2_from_axis_angle's own len guard provably cannot fire and its components are exactly (cos(theta/2), sin(theta/2), 0, 0). Leg A (w -> +1): q = su2_from_axis_angle(hvec3_new(1,0,0), 10^-j), assert su2_log(q) == (10^-j, 0, 0) to 4 ulp RELATIVE, j = 0..161. Leg B (w -> -1, the true mirror): theta = 2*pi - 10^-j over j = 0..19, assert su2_log(q) == (theta, 0, 0) to 4 ulp relative. MEASURED TODAY on the shipped binary: leg A round-trips bit-exactly for j = 0..7 and returns exactly 0x0 for every j = 8..161; leg B is already 1.7e-10 relative off at j = 3, 2.9e-9 at j = 4, 4.4e-5 at j = 6, 1.17e-2 (4.2 degrees) at j = 7, then exactly 0x0 for j = 8..19 \u2014 so leg B fails even a LOOSE_TOL_M = 1e-8 ABSOLUTE assertion from j = 4, four decades above the cliff, which is the earliest available discriminator. Together the legs bracket every repair a maintainer might reach for, and every acos-based variant fails at the SAME j on leg A \u2014 shipped 1e-12, sqrt(DBL_EPSILON) = 1.49e-8, DBL_EPSILON = 2.2e-16, 1e-100 \u2014 which is the signature that says \"the threshold is not the defect\". The two mutants a narrow test would let live, both MEASURED: (i) guard deleted / threshold lowered to DBL_MIN returns NaN on leg A and 256538985.08980078 on leg B \u2014 note a mere \"result is not exactly zero\" assertion PASSES this mutant on leg B, so the assertion must be the relative-error one; (ii) any acos-based variant with a different constant, which is bit-identical to shipped. Only the atan2(vnorm, w) form with the guard on vnorm passes both legs. Stop leg A at j = 161: at j = 162 the x*x+y*y+z*z sum-of-squares underflows to +0 on its own (a scaled/hypot norm would carry it to ~1e-300), a separate limitation that must not be charged to this guard."
  },
  {
   "site": "src/lie.cyr:399",
   "fn": "lorentz_boost",
   "guarded_quantity": "norm = f64_sqrt(nx^2+ny^2+nz^2) \u2014 the Euclidean LENGTH of the caller's boost-direction Vec3 `dir`. Dimension: length^1, homogeneous of degree 1 in the caller's unit choice (mm vs m: x1000; AU vs m: x1.5e-11). REFUTATION ATTEMPT 1 FAILED: it is not dimensionless and not bounded \u2014 `dir` is a raw public-API parameter, and the function's own comment (lie.cyr:395, \"dir is a Vec3 (will be normalized)\") states that the function, not the caller, does the normalising, i.e. the magnitude is by construction arbitrary. REFUTATION ATTEMPT 2 FAILED: there are ZERO internal callers (`grep -n \"lorentz_boost(\" src/` returns only the definition), so no caller pre-normalises. REFUTATION ATTEMPT 3 FAILED: it is not a convergence tolerance \u2014 the guarded statement is one line above `var inv = f64_div(F64_ONE, norm);`.",
   "protects": "The division `inv = f64_div(F64_ONE, norm)` at lie.cyr:402, used to normalise the direction before building Lambda^0_i = n_i*sinh(eta) and Lambda^i_j = delta_ij + (cosh(eta)-1)*n_i*n_j.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "NOT REFUTED \u2014 confirmed by execution against the shipped source. The contract is scale-free (only the DIRECTION of `dir` is meaningful), yet the arbitrary magnitude is tested against a fixed absolute 1e-12 (EPSILON_F64 = 0x3D719799812DEA11, src/error.cyr:26), so the identical physical boost is honoured at |dir| = 1 and silently discarded at |dir| = 1e-13. MEASURED on the 2.13.0 tree under cycc 6.6.2 (probe.tcyr/probe2.tcyr, 22/22 assertions pass): lorentz_boost(hvec3_new(0x3D3C25C268497682 /* exactly 1e-13 */, 0, 0), eta=1) returns L00 = 0x3ff0000000000000 and L10 = 0x0, against the 0x3ff8b07551d9f550 (cosh 1) and 0x3ff2cd9fc44eb982 (sinh 1) the same call with dir = (1,0,0) returns \u2014 the claim's literal MEASURED string reproduces bit for bit. On an exact power-of-two ladder k = 2^-j the guard first fabricates at j = 40 (k = 9.09e-13) and only 40 of the 538 representable rungs are honoured; 498 rungs of perfectly well-determined direction are discarded. The division itself fails only at norm == 0 \u2014 and because norm is a SQRT, it can never even be subnormal (sqrt of the smallest positive subnormal is 2.2227587494850775e-162, a normal number; asserted directly as `f64_lt(f64_sqrt(1), F64_TINY) == 0`), so 1/norm is finite for every nonzero norm and the guard fires ~296 decades above anything that can fail. This is the same class the repo already repaired in `hvec3_angle` (src/vec3.cyr:198) under its own stated rule \u2014 \"guard exactly what makes the operation fail and nothing more; a threshold that needs a scale chosen for it IS the defect\" \u2014 with the constant already extracted as `F64_TINY` in src/error.cyr; lie.cyr was never swept. Sibling instances of the identical shape on the identical scale-free quantity: lie.cyr:441 (`lorentz_rotate`, same file, 42 lines down), lie.cyr:79 (`su2_from_quat`), lie.cyr:100 (`su2_from_axis_angle`). NOT a sibling: lie.cyr:151 (`su2_exp`), where `norm` is a rotation-vector ANGLE whose magnitude is meaningful \u2014 that one needs a separate small-angle analysis and must not be swept in with these.",
   "fabricates": "lorentz_identity() \u2014 a valid, exactly Lorentzian 4x4 matrix returned with no error channel. MEASURED: `lorentz_is_valid()` on the fabricated matrix returns 1, so the repo's own validity predicate cannot detect the substitution (probe assertion A5). It does not perturb the requested boost, it DISCARDS it: a rapidity-1 boost (v = 0.7616c) becomes \"no boost\". The magnitude is independent of eta \u2014 MEASURED: eta = 10 (gamma = 11013) is dropped just as silently (probe assertion D1). \u26a0 Separate, adjacent fabrication found while bracketing the upward leg and explicitly NOT scored against this guard: at |dir| >= 2^512 (1.34e154) the nx*nx+ny*ny+nz*nz sum OVERFLOWS to +Inf, so norm = Inf, inv = 0, n = (0,0,0), and the function returns L00 = cosh(eta) with a unit spatial block and zero off-diagonals \u2014 a matrix that is not even Lorentzian. That is a defect in the norm EXPRESSION (no hypot-style scaling), not in this threshold.",
   "correct_threshold": "F64_TINY \u2014 the constant ALREADY DEFINED at src/error.cyr (0x0010000000000000, DBL_MIN), the smallest magnitude whose reciprocal is still finite, i.e. exactly what makes `f64_div(F64_ONE, norm)` fail and nothing more. No new constant is needed and no `f64_abs` is needed (a sqrt result is never negative): `if (f64_lt(norm, F64_TINY) == 1) { return lorentz_identity(); }`. \u2b50 SHARPENED by measurement: because norm is a SQRT it is never subnormal, so on this function `norm < F64_TINY` is EXACTLY `norm == 0`, and every threshold at or below 2.2227587494850775e-162 is behaviourally indistinguishable (1e-300 also never fires). F64_TINY is therefore chosen by the project's stated rule and by consistency with vec3.cyr:198 / calc_ext.cyr:434 / autodiff.cyr:71 \u2014 NOT because the ladder singles it out. REFUTATION ATTEMPT 4 FAILED (checked, not assumed): the repair breaks no existing assertion. The only tiny-direction test in tests/*.tcyr is abuse.tcyr:2533, `assert_neq(lorentz_boost(hvec3_zero(), F64_ONE), 0, \"zero direction allocates\")` \u2014 with norm = 0 the F64_TINY guard STILL fires and still returns an allocated identity, so it still passes (asserted directly, probe E1); foundation.tcyr:1225 uses hvec3_unit_x() (norm = 1), unaffected. Secondary, and genuinely out of this guard's scope: the sum of squares underflows to 0 below |dir| ~ 1.11e-162 and overflows to +Inf above |dir| ~ 1.34e154; carrying the scale-free contract past those needs a scaled/hypot norm, not a different threshold.",
   "scale_test": "HOMOGENEITY OF DEGREE 0 IN THE DIRECTION: lorentz_boost(k*dir, eta) must be BIT-IDENTICAL to lorentz_boost(dir, eta) for every k > 0. \u26d4 THE ORIGINALLY-PROPOSED DECIMAL LADDER IS UNSOUND AS WRITTEN AND MUST NOT BE USED \u2014 MEASURED on the real tree: at k = 1e-5, 1e-9 and 1e-11 the guard does NOT fire (rungs are honoured) yet L10 comes back 0x3ff2cd9fc44eb981, one ULP BELOW sinh(1), because nx = k*fl(1/f64_sqrt(k*k)) is not exactly 1.0 for those k. A bit-exact L10 assertion on a decimal ladder therefore FAILS ON CORRECTLY REPAIRED CODE at j = 5, 9, 11 (probe assertions G1-G3). USE AN EXACT POWER-OF-TWO LADDER instead, built by repeated exact halving/doubling from 1.0 \u2014 MEASURED: L00 and L10 are bit-exact on every honoured rung of both legs (probe B3, C1, C2), so both assertions are sound. TEST: fix dir = hvec3_new(k,0,0), eta = 1. DOWNWARD leg k = 2^-j, j = 0..537 (538 rungs; stop at 537 \u2014 at j = 538, k = 1.11e-162 and nx*nx underflows to 0, which is the norm expression's limit, not this guard's). UPWARD leg k = 2^+j, j = 0..511 (512 rungs; stop at 511 \u2014 at j = 512, k = 1.34e154 and nx*nx overflows to +Inf, again the expression, not the guard). On all 1050 rungs assert m4_get(L,0,0) == 0x3ff8b07551d9f550 AND m4_get(L,1,0) == 0x3ff2cd9fc44eb982 bit-exactly. MEASURED against shipped code: downward leg honours only j = 0..39 and fabricates the identity on j = 40..537 (498 failures); upward leg is clean, which is what makes the downward leg's asymmetry the finding. BRACKETING (measured first-firing rung on the 2^-j ladder, so the ladder brackets every threshold a repair might plausibly choose): 1e-6 at j = 20, sqrt(DBL_EPSILON)=1.49e-8 at j = 27, the shipped 1e-12 at j = 40, 1e-15 at j = 50, DBL_EPSILON=2.2e-16 at j = 53, 1e-100 at j = 333 \u2014 and F64_TINY/DBL_MIN never fires anywhere before the norm expression itself underflows at j = 538. SECOND LEG (proves the fabrication is unbounded, not a perturbation): hold k = 1e-13 and sweep eta over 1e-3 .. 1e1; every one returns L00 = 0x3ff0000000000000. \u26a0 DO NOT assert `lorentz_is_valid(L) == 1` anywhere in this test \u2014 MEASURED (probe A5): the fabricated identity PASSES lorentz_is_valid, so that assertion discriminates nothing, exactly as asserting unit length failed to discriminate the fabricated quaternion identity in 2.11.1. The discriminating property is DIRECTION AND MAGNITUDE OF THE BOOST, i.e. the L00/L10 bit compare."
  },
  {
   "site": "src/lie.cyr:440",
   "fn": "lorentz_rotate",
   "guarded_quantity": "norm = f64_sqrt(nx^2+ny^2+nz^2) \u2014 the Euclidean LENGTH of the caller's rotation axis. Dimension: length^1, homogeneous of degree 1 in the axis; changes by 1000 between metres and millimetres. Not bounded, not dimensionless, not normalised anywhere upstream: `lorentz_rotate` has exactly ONE call site in the entire tree (tests/foundation.tcyr:1226) and is a public entry point of dist/hisab.cyr, so the magnitude is whatever the consumer hands in.",
   "protects": "the division inv = f64_div(F64_ONE, norm) on the very next line (src/lie.cyr:443), used to normalise the axis. It is a divide-by-zero guard, not a convergence tolerance \u2014 there is no iteration in this function.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FIVE FAIL, THREE OF THEM BY MEASUREMENT ON THE REAL BINARY (cycc 6.6.2, probe under scratchpad/eps/refute/, built against src/lie.cyr unmodified). (1) Bounded/dimensionless? No \u2014 `norm` is length^1 and the comment at src/lie.cyr:436 states the contract, \"axis is Vec3 (normalized internally)\", so only the direction carries meaning and the magnitude is arbitrary by construction. (2) Caller normalises? No \u2014 one call site tree-wide, and it is a bundled public API. (3) Convergence tolerance? No \u2014 it guards a reciprocal two lines below. (4) Would F64_TINY break an existing test? MEASURED NO: the only assertion in tests/ that reaches this function is `assert_eq(lorentz_is_valid(lorentz_rotate(hvec3_unit_z(), F64_PI_2)), 1)` and hvec3_unit_z() has norm exactly 1.0, far above both thresholds. Worse, that assertion is non-discriminating: measured, lorentz_is_valid(lorentz_identity()) = 1 AND lorentz_is_valid(lorentz_rotate(1e-13*x_hat, pi/2)) = 1, so the tree's lone test returns 1 for the fabrication and could never have caught this. (5) Does the scale_test discriminate the thresholds? Yes for the thresholds \u2014 but the claim's ASSERTION FORM is unsound and its RANGE is scoped to miss a second fabrication; both are corrected below. The claimed measurement itself is CONFIRMED exactly: k=1 gives m4_get(R,2,2) = 0x3c91a60000000000 and m4_get(R,3,2) = 0xbff0000000000000; the guard fires from k < 1e-12 (first identity at j=13) returning bit-exact 0x3ff0000000000000 / 0x0. (My own first instrument was the wrong one and I checked it before believing it: a libm C simulation gave cos(pi/2) = 0x3c91a62633145c07, disagreeing with the claim \u2014 because hisab's f64_cos is a cycc polyfill, not libm. The claim's hex was right and my sim was measuring a different function.) Non-axis-aligned confirmation: axis = (1e-13, 1e-13, 1e-13), norm = 1.73e-13, also returns the bit-exact identity, so this is not an artifact of a single-component axis.",
   "fabricates": "lorentz_identity() \u2014 a valid Lorentz matrix, rc = 0, no error channel; MEASURED lorentz_is_valid() = 1 on it, so the repo's own validity predicate certifies the fabrication. A requested 90-degree spatial rotation becomes no rotation at all, and the fabrication is independent of `angle`, so a pi rotation is dropped as silently as a small one. \u26a0 A SECOND, DISTINCT FABRICATION LIVES AT THE OTHER END AND THE CLAIM MISSED IT: when the SUM OF SQUARES overflows while no component does \u2014 measured at axis = (1e200, 1e200, 0), and on a single component from k >= 1e155 \u2014 norm = +Inf, `f64_lt(norm, EPSILON_F64)` is FALSE so the guard does not fire, inv = 1/Inf = 0, the axis normalises to (0,0,0), and the function returns diag(1, cos t, cos t, cos t): R22 = 0x3c91a60000000000, R32 = 0x0. That is neither the identity nor a rotation; lorentz_is_valid() = 0 on it. Swapping the threshold does NOT fix this arm.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) on `norm` \u2014 the constant this repo already established for exactly this rule at src/geo.cyr:26 (_GEO_F64_TINY) and src/autodiff.cyr:17 (_AD_F64_TINY): guard exactly what makes the division fail and nothing more. It is the right bound in both directions \u2014 norm >= DBL_MIN implies 1/norm <= 4.49e307 < DBL_MAX, so the reciprocal cannot overflow either. \u26a0 BUT F64_TINY ALONE IS AN INCOMPLETE REPAIR, and this is the sharpening the original claim needs: it leaves the +Inf arm above entirely live (measured: axis = (1e200,1e200,0) still returns a non-Lorentzian diag(1,cos t,cos t,cos t) with rc = 0). The guard must be `norm` finite AND >= F64_TINY, i.e. reject non-finite norm explicitly \u2014 or, better and fixing both ends at once, hoist by m = max(|nx|,|ny|,|nz|) before squaring, so the sum of squares can neither overflow nor underflow and the only unrecoverable case is m == 0. Without the hoist there remains an honest floor no threshold can lift: at axis = (1e-200,1e-200,0) the sum of squares underflows to +0 before the guard is reached, so norm == 0 and every threshold including DBL_MIN fires (measured: identity, valid = 1). Note also lorentz_boost (src/lie.cyr:399) and lie.cyr:79/100/151 carry the identical shape and must be repaired in the same pass.",
   "scale_test": "\u26d4 THE CLAIM'S SCALE_TEST IS DEFECTIVE AS WRITTEN AND MUST NOT BE USED \u2014 MEASURED. It asserts m4_get(R,3,2) is BIT-IDENTICAL to 0xbff0000000000000 for every k, and that assertion FAILS ON THE SHIPPED CODE at scales the guard never touches: R32 comes back 0xbfefffffffffffff (1 ULP below -1.0) at k = 1e-5, 1e-9, 1e-11 and at 22 of the 151 positive decades (1e11, 1e21, 1e26, 1e30, 1e37, 1e38, 1e42, 1e58, 1e65, 1e88, 1e99, 1e104, 1e111, 1e121, 1e128, 1e136, 1e141, 1e148, 1e150, 1e151, 1e152). Cause: nx_hat = k * fl(1/fl(sqrt(fl(k*k)))) rounds 1 ULP low; it is normalisation rounding, not the guard, and a correct F64_TINY repair does not change it. WHICH decades fail depends on how k is generated (repeated /10 gave j=5,10; ganita_f64_pow gave j=5,9,11), so the failure set is not even stable. As written the test reports ~25 false failures on BOTH sides of the repair and cannot distinguish the guard defect from rounding. CORRECTED TEST \u2014 HOMOGENEITY OF DEGREE 0 IN THE AXIS. Fix axis = hvec3_new(1,0,0), angle = F64_PI_2. Per k, assert TWO legs: (A) m4_get(R,2,2) == 0x3c91a60000000000 BIT-EXACT. This entry is cos(angle) + (1-cos)*ny*ny with ny = 0, so it is mathematically independent of k and MEASURED bit-stable across all 341 decades j = -170..+170 whenever the guard does not fire; the fabricated identity gives 0x3ff0000000000000, so leg A is exactly discriminating and needs no tolerance. Never assert on R11 or on R22-of-the-rotation-axis, which agree with the identity and let every mutant live. (B) m4_get(R,3,2) within 1 ULP of -1.0, i.e. bits in {0xbfefffffffffffff, 0xbff0000000000000, 0xbff0000000000001} \u2014 a set membership, NOT a bit-equality. Leg B is what kills the +Inf arm, where R32 = 0x0. RANGE: k = 10^-j for j = 0..161 and k = 10^+j for j = 0..170 \u2014 the claim capped the positive leg at 150 and thereby scoped the test so it could not see the overflow fabrication at all; it must run past 1e154. MEASURED bracketing of every threshold a repair might plausibly choose: 1e-6 fails at j=6, 1.49e-8 at j=8, the shipped 1e-12 at j=13, 1e-15 at j=15, 2.2e-16 at j=16, 1e-100 at j=100 \u2014 all on leg A; and the unguarded +Inf arm fails on leg B from j=+155. Only \"finite AND >= F64_TINY\" survives both legs over the whole range. Stop the negative leg at j=161: at j >= 162 the sum of squares underflows to +0 (measured norm = 0 exactly), so no threshold below ~1.5e-162 is distinguishable through this entry point \u2014 state that limit rather than papering over it. Third leg, mixed magnitudes, which single-component scaling cannot reach: assert leg A + leg B on axis = (1e200, 1e200, 0) (currently returns the non-Lorentzian diag, lorentz_is_valid = 0) and record axis = (1e-200, 1e-200, 0) as the known underflow floor. Fourth leg: hold k = 1e-13 and sweep angle over 1e-3 .. pi, asserting the result is bit-identical to lorentz_identity() at every angle, to show the whole rotation is discarded rather than perturbed. \u26a0 Do NOT gate any of this on lorentz_is_valid(): measured 1 for the fabricated identity, so it is blind to the defect that matters."
  },
  {
   "site": "src/lie_ext.cyr:119",
   "fn": "so3_from_axis_angle",
   "guarded_quantity": "norm = f64_sqrt(nx*nx + ny*ny + nz*nz), the Euclidean LENGTH of the caller's axis vector (dimension length^1, in the caller's own units). Unbounded dynamic range in both directions, and its magnitude carries zero information: lines 122-123 compute inv = 1/norm and divide it straight back out. Only the DIRECTION survives into the Rodrigues formula.",
   "protects": "the single division inv = f64_div(F64_ONE, norm) at src/lie_ext.cyr:122 (one division, then three multiplies that consume it).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE GROUNDS; ALL FAIL. (1) NOT BOUNDED: the quantity is a raw length with free scale. The file makes the bounded-quantity argument explicitly for a DIFFERENT constant 20 lines earlier -- _SO3_ORTHO_TOL, lie_ext.cyr:26-31: \"Every entry of a rotation matrix is bounded by 1, so an ABSOLUTE tolerance is the right scale here -- there is no dynamic range to normalise against.\" That sentence is exactly what line 119 cannot say. (2) NO CALLER NORMALISES: the only in-tree call site is so3_exp (lie_ext.cyr:203), which passes the UN-normalised rotation vector omega with theta = |omega|; and so3_exp guards theta against the same 1e-12 first, so line 119's guard never fires on that path -- the only way to reach it with a nonzero axis is a DIRECT public call. so3_from_axis_angle is exported in dist/hisab.cyr to 10 SHA-locked consumers with no unit-axis precondition documented anywhere in the file. (3) NOT A CONVERGENCE TOLERANCE: one-shot division, no iteration. (4) DOES NOT BREAK AN EXISTING TEST -- CHECKED, NOT ASSUMED: sandbox copy, line 119 mutated EPSILON_F64 -> F64_TINY, mutation proven installed by grep, all five suites run: modules 1827/1827, abuse 776/776, edge_cases 233/233, hisab 416/416, foundation 359/359 = 3611 passed, 0 failed. The one test that touches this function (tests/modules.tcyr:7611) uses hvec3_unit_z(), |axis| = 1; abuse.tcyr:2530-2531 exercises so3_exp(hvec3_zero()), where norm = 0 and BOTH thresholds fire. (5) THE SCALE TEST IS FLAWED, BUT AGAINST THE FILING, NOT THE DEFECT -- see scale_test below. MEASURED ON THE SHIPPED TREE (sandbox copy of src/, unmutated, cyrius 6.6.2), axis (0,0,2^k), angle 0.5, correct R[0][1] = sin(0.5) = 0x3fdeaee8744b05f0 and R[0][0] = cos(0.5) = 0x3fec1528065b7d50: k = 0, -10, -20, -26, -30, -39 all bit-exact; k = -40 (9.0949e-13) and every k below it return R01 = 0x0, R00 = 0x3ff0000000000000, i.e. the identity. The cliff sits exactly on 1e-12. UNIT-FREE REACHABILITY (no exotic units needed, measured): build the axis the textbook way, as cross(a,b) of two UNIT vectors misaligned by eps -- |a x b| = sin(eps). eps = 1e-6, 1e-8, 1e-10, 1e-12 all return the correct sin(0.5); eps = 1e-14 gives |axis| = 1e-14 and returns the IDENTITY, for an axis whose direction (+z) is exact and whose reciprocal is finite. Every input on that path is dimensionless. The units argument is a second, independent route: a cross-product axis scales as length^2, so 1 mm edges give |a x b| ~ 1e-6 in metres (accepted, exact) and ~1e-12 in kilometres (on the cliff; halve the edge and it is 2.5e-13, rejected) -- same geometry, different answer. Nothing about 1e-12 relates to what fails: 1/norm is finite for every norm >= DBL_MIN.",
   "fabricates": "TWO different fabrications, and the second is worse. LOW END (norm < 1e-12): m3_identity() -- a structurally valid rotation, returned with no rc and no error channel, so a caller cannot distinguish \"no rotation was asked for\" from \"the axis was shorter than a hard-coded constant\". It propagates into so3_compose / so3_log / se3_* as a genuine rotation and passes so3_from_mat3's membership check. HIGH END (|axis| >= 2^512 ~ 1.34e154, MEASURED, and NOT fixed by the threshold change): nx*nx overflows to +Inf, norm = +Inf, inv = 1/Inf = 0, so nx = ny = nz = 0 and the Rodrigues formula returns cos(0.5) * I -- measured R00 = 0x3fec1528065b7d50, R01 = 0x0. That is not the identity and not a rotation: det = cos^3(0.5) ~ 0.674, so it is the one fabrication so3_from_mat3 WOULD reject, silently manufactured by the constructor. The filing missed it because its sweep stops at 1e+150 < 2^512.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) -- the project constant that exists for exactly this rule, whose own comment states it: \"the right threshold when the guard protects a DIVISION and the only thing that can actually fail is a zero (or subnormal) denominator\", as against \"EPSILON_F64 remains right for a genuinely dimensionless, bounded quantity\". VERIFIED BY MUTATION (sandbox copy, mutant proven installed by grep at :119): with EPSILON_F64 -> F64_TINY the probe returns bit-identical sin(0.5) AND cos(0.5) for every exact power of two from 2^-537 to 2^511 -- 1049 consecutive binades, including k = -40, -50, -100, -511, -537 where the shipped code returns the identity -- and all 3611 assertions across the five suites still pass. \u26a0 THE THRESHOLD IS NECESSARY BUT NOT SUFFICIENT: the guard fixes the low end only, and only down to where the NAIVE sum-of-squares survives. Measured limits of the F64_TINY repair: 2^-538 still returns the identity (correctly -- (2^-538)^2 = 2^-1076 is below the smallest subnormal 2^-1074, so Sum x^2 flushes to 0 and norm really is 0), and 2^512 still returns cos(0.5)*I ((2^512)^2 = 2^1024 overflows to +Inf). Completing the repair means computing norm with a max-component prescale (hypot-style: divide by max|x| before squaring), which extends exactness to roughly [2^-1074, 2^1023] and removes the high-end fabrication as well. Guard first, prescale second; they are separable and the guard is the one this site is about.",
   "scale_test": "\u26d4 THE FILING'S PROPOSED TEST IS WRONG AND WOULD REJECT A CORRECT REPAIR -- REPLACE IT. Its \"decimal half-decades over 1e-150...1e+150, R[0][1] BIT-IDENTICAL to the s=1 result\" fails at ORDINARY scales for a reason that has nothing to do with the guard: normalisation rounding. MEASURED on the shipped tree with correctly-rounded decimal literals -- s = 1e-5 (0x3ee4f8b588e368f1), 1e-9 (0x3e112e0be826d695) and 1e-11 (0x3da5fd7fe1796495) ALL return R01 = 0x3fdeaee8744b05ef, one ulp below the s=1 answer 0x3fdeaee8744b05f0, because s*(1/sqrt(s*s)) = 0x1.fffffffffffffp-1 rather than 1.0. Simulating the filing's own sweep: 85 of its 601 samples are 1 ulp low, at scales up to 1e+42 and as high as 1e-2.5 -- seven or more orders above every candidate threshold. A DBL_MIN repair leaves all 85 exactly as they are, so the test fails a correct fix at 14% of its samples and reports a \"defect\" that is not one. CORRECT TEST -- EXACT POWERS OF TWO, where s*(1/sqrt(s*s)) is 1.0 by construction so the ONLY thing that can move the answer is the guard: for every integer k in [-537, +511], build the axis as the exact bit pattern (1023+k)*2^52 (i.e. 2^k), call so3_from_axis_angle(hvec3_new(0,0,2^k), 0.5) and assert BIT-EXACT R[0][1] == 0x3FDEAEE8744B05F0 and R[0][0] == 0x3FEC1528065B7D50 -- a bit compare, never f64_to and never a tolerance. 1049 samples, 3.32 per decade over 315 decades. IT BRACKETS EVERY THRESHOLD A REPAIR MIGHT PLAUSIBLY CHOOSE, each killed at the first power of two below it: 1e-6 at 2^-20 = 9.5e-7, sqrt(eps) = 1.49e-8 at 2^-27 = 7.5e-9, 1e-9 at 2^-30 = 9.3e-10, the CURRENT 1e-12 at 2^-40 = 9.09e-13 (measured: shipped code passes 2^-39 and returns identity at 2^-40), eps = 2.22e-16 at 2^-53 = 1.11e-16, 1e-100 at 2^-333, 1e-300 at 2^-997. Only a threshold at or below 2^-538 survives the whole sweep, and every such threshold is behaviourally identical here, so DBL_MIN is the honest one. MIRROR UPWARD: the sweep already runs to 2^511 = 6.7e153, which kills any repair that clamps large axes -- and it stops one binade short of the overflow rather than at the filing's arbitrary 1e+150, so it also DOCUMENTS the high-end fabrication instead of hiding it (2^512 returns cos(0.5)*I; assert that separately as a known-open second defect, or extend the sweep past 2^511 once the hypot prescale lands). BOTH BOUNDS ARE DERIVED, NOT FUDGED, unlike the filing's empirical 1e-150: 2^-537 squared is 2^-1074, exactly the smallest subnormal; 2^-538 squared is 2^-1076, which flushes to zero, so a correct guard legitimately returns the identity there and the sweep must not reach it. 2^512 squared is 2^1024, which overflows to +Inf."
  },
  {
   "site": "src/lie_ext.cyr:200",
   "fn": "so3_exp",
   "guarded_quantity": "theta = |omega|, the rotation angle in radians of the caller-supplied so(3) element. Dimensionless \u2014 unchanged by any change of LENGTH units \u2014 but unbounded downward and not clamped to [0,pi]. Compared against EPSILON_F64 = 1e-12 (error.cyr:26).",
   "protects": "Nothing at this site. so3_exp performs no division; it returns m3_identity() or tail-calls so3_from_axis_angle(omega, theta). The only division on the path is inv = 1/norm at :119, whose owner carries an IDENTICAL guard on the IDENTICAL quantity at the IDENTICAL threshold. MEASURED, not inferred: build B (\":200\" mutated to F64_TINY, \":119\" left at EPSILON_F64) produces output byte-identical to the shipped build across all 305 sweep points and all 7 spot values. The filed site is therefore unobservable in isolation \u2014 :119 is the site that binds.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "SUBSTANCE CONFIRMED, SCOPE AND EVIDENCE CORRECTED. I could not refute the core: the guard pair discards ~296 decades of exactly representable rotation and fabricates a plausible answer, which is the class error.cyr:28 itself defines (EPSILON_F64 is for a \"dimensionless, BOUNDED quantity\"; theta is dimensionless but unbounded downward, and the only thing that can actually fail is the 1/norm division at norm == 0, i.e. DBL_MIN). It is the same shape as 2.11.1's hquat_inverse/hquat_normalize repair. Measured on cyrius 6.6.2, omega = (0,0,t), reading m3_get(R,0,1) which must be sin(t): SHIPPED gives exactly 0 for t = 1e-13, 1e-20, 1e-100, 1e-150 and 276 of 305 half-decade points from 1e+2 down to 1e-150; with BOTH guards at F64_TINY the same sweep gives EXACTZERO = 0 \u2014 the general Rodrigues path is capable across the whole range, so the guard is the sole cause. FOUR CORRECTIONS to the filing. (1) The class label is a stretch and the filing admits it: theta does not move under a change of units, so this is not the geo.cyr scaling family; it is an absolute epsilon on an unbounded-range quantity whose divisor fails only at zero. (2) The motivation is arithmetically INVERTED: the guard cuts at 1e-12 rad = 0.206 microarcsecond, and 1 uas = 4.85e-12 rad, so every discarded rotation is SMALLER than a microarcsecond, not \"two orders of magnitude larger\". The sound motivation is the second one \u2014 a sub-stepped angular-velocity integrator (omega*dt < 1e-12) loses each step entirely, and the group law confirms the repaired path accumulates correctly (10 composed 1e-13 steps == so3_exp(1e-12) bit-exactly, diff = 0). (3) The stated round-trip impact is MISATTRIBUTED (see fabricates). (4) The repair cannot be applied at this site alone: on build C (so3 pair repaired, su2_exp untouched) the documented, test-pinned cover invariant su2_to_rotation_matrix(su2_exp(w)) == so3_exp(w) \u2014 asserted at modules.tcyr:5909 and stated at lie.cyr:147 \u2014 HOLDS today at w = (0,0,1e-13) because both sides fabricate identity, and is VIOLATED after the narrow repair (su2 gives 0, so3 gives 1e-13). The existing test samples only |w| ~ 0.63 and would not announce it. No existing test blocks the repair: abuse.tcyr:2530-2532's zero generator still returns a non-null identity with m3_get(Z,0,0) bit-equal to f64_from(1) on build C (0 < DBL_MIN), and modules.tcyr's so3/se3/su2 tests all use angles >= 0.2 rad under LOOSE_TOL_M.",
   "fabricates": "m3_identity() \u2014 no rc, no error channel; the caller reads back \"no rotation\". \u26a0 THE FILING'S ROUND-TRIP EVIDENCE IS MISATTRIBUTED AND MUST NOT BE USED AS THE ORACLE: so3_log(so3_exp((0,0,1e-13))).z == 0 is true, but it is NOT caused by this guard and is NOT fixed by repairing it. Measured on the FULLY REPAIRED build C, so3_log returns exactly 0 for every t from 1e-8 down to 1e-14 (last nonzero: t = 3.16e-8), because so3_log's own path takes theta = acos((tr(R)-1)/2) and tr(R) = 3 - theta^2 rounds to exactly 3 below theta ~ 1e-8, then hits its own EPSILON_F64 gate at :215. So the round trip reports 100% absent at 1e-9, 1e-10 and 1e-11 too \u2014 where :200 never fires \u2014 and still does after the repair. That is a separate defect at lie_ext.cyr:215/:220 (a trace-based log is the wrong formula at small angle; the unskew branch is the fix), and folding it into this finding overstates what repairing :200/:119 buys.",
   "correct_threshold": "F64_TINY (DBL_MIN, error.cyr:47) \u2014 but the repair is a SET, not this line. Minimum correct change: (a) so3_from_axis_angle:119 -> F64_TINY, since that is the divisor's owner and the only site where anything can actually fail; (b) so3_exp:200 deleted outright (0 still routes to :119 and returns identity) or lowered to F64_TINY, otherwise it becomes the binding constraint the moment :119 is fixed; (c) su2_exp lie.cyr:151 and se3_exp lie_ext.cyr:443 -> F64_TINY in the same commit, or the cover invariant at lie.cyr:147 silently becomes false below 1e-12. No small-angle series is needed (unlike se3_exp's V matrix, which divides by theta^2/theta^3): measured bit-exact-or-1-ulp down to 1e-150. Do NOT extend below that in one step \u2014 below ~1e-154 the sum of squares in the norm goes subnormal, which is a real and separate limitation of the norm, not of the guard. Leave lie_ext.cyr:215/:220 (so3_log) OUT of this repair and file it separately \u2014 it needs a formula change, not a threshold change.",
   "scale_test": "\u26a0 THE FILED TEST DOES NOT DISCRIMINATE AT THIS SITE AND FALSE-FAILS ON A CORRECT REPAIR \u2014 both measured. (i) It cannot see the filed site: every threshold at or below 1e-12 installed at :200 (DBL_MIN, 1e-100, 2.22e-16, deletion) yields output byte-identical to the shipped 1e-12, because :119 fires first at the same value. Mutants MUST be installed at :119, or at both sites, and the harness must grep-prove the mutant is in place before believing a count. (ii) Bit-exact is too tight: on build C, 49 of the 305 half-decade points are exactly 1 ulp BELOW f64_sin(s) \u2014 s*(1/s) evaluates to 0.99999999999999989 in the axis renormalisation, so the entry is sin(s)*(1-1ulp). The filing's own measurement paragraph shows this (t=1e-11 -> 9.999999999999998e-12, labelled \"1 ulp\") and then specifies an assertion that rejects it. CORRECTED TEST: for s over every half-decade from 1e+2 to 1e-150 with omega = (0,0,s), assert |bits(m3_get(so3_exp(omega),0,1)) - bits(f64_sin(s))| <= 2 (ULP compare on the raw i64 patterns, never f64_to, which maps every one of these to 0), and assert the entry is NOT exactly 0 for any s in range \u2014 the not-zero clause is what kills the mutants, the 2-ulp clause is what stops the sweep false-failing. Bracketing is confirmed: a guard at 1e-6, 1.49e-8, 1e-9, 1e-12, 2.22e-16 or 1e-100 each produces an exact 0 inside this range and dies; only DBL_MIN and deletion survive (EXACTZERO = 0 measured). Keep the group law \u2014 so3_exp((0,0,1e-13)) composed 10 times equals so3_exp((0,0,1e-12)) with diff = 0 bits on build C, and yields 0 vs 1e-12 on the shipped tree, so it discriminates and pins the accumulation case the astrometry framing got wrong. ADD the cover invariant at tiny angle, absent from the filing and the reason a partial repair regresses silently: for the same s sweep, su2_to_rotation_matrix(su2_exp((0,0,s)))[0][1] must equal m3_get(so3_exp((0,0,s)),0,1) to <= 2 ulp \u2014 it passes vacuously on the shipped tree (both 0) and FAILS on a so3-only repair, which is exactly the mutant no existing test catches. DO NOT use so3_log as the oracle anywhere in this sweep: it returns exactly 0 for all |theta| <= 1e-8 even after a correct repair, so any so3_log-based assertion fails on the fix and passes on the defect."
  },
  {
   "site": "src/linalg_ext.cyr:88",
   "fn": "csr_from_dense (counting pass; coupled twin at :107)",
   "guarded_quantity": "|A_ij| \u2014 f64_abs of one dense matrix entry read via ganita_mat_get, compared against EPSILON_F64 (= 1e-12, src/error.cyr:26) with f64_gt. Dimensions: whatever the caller's entries carry (stiffness N/m, compliance m/N, covariance, metres). Degree 1 in the problem scale: not squared, not normalised, not bounded, not a ratio.",
   "protects": "Nothing that can fail. There is no division, sqrt, acos or iteration in this function \u2014 it is a sparsification DROP-tolerance choosing which entries are \"structurally zero\" (header comment, line 75). It cannot even be defended as protecting the allocation: it only makes count SMALLER, and the nnz = 0 / alloc(0) path is already exercised by tests/abuse.tcyr:915.",
   "protects_note": "verified by reading the whole function body (lines 77-121): no arithmetic other than the compare itself.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED by measurement; every refutation avenue was tried and closed. (1) Not dimensionless: the quantity is a raw caller matrix entry, degree 1 in the caller's unit, so re-expressing the same matrix in mm rather than m moves every entry by 1e3 and moves the SPARSITY PATTERN across a fixed 1e-12. (2) Not normalised by a caller: `csr_from_dense` has ZERO internal callers \u2014 grep over all of src/ finds the name only at its own definition line 77 \u2014 so it is a bare public entry point in dist/hisab.cyr reached directly by consumers, with no normalisation anywhere in the call chain. (3) Not a convergence tolerance: there is no loop to converge, no iterate, no residual; it is a one-shot data-structure conversion. (4) Measured on the shipped tree with a 4x4 fixture M0 (five dyadic nonzeros 1.0, 2^-10, 2^-5, 2^-10, 1.0; k = 5) scaled by c = 2^p: nnz is 5 at p = 0, +-20, -29 and +1000, falls to 3 at p = -30, and is 0 at every p <= -40 \u2014 a matrix whose entries are all ~1e-12..1e-13 converts to a structurally EMPTY CSR returned as a valid non-null handle. csr_spmv on it then returns exactly +0.0 for row 0 (bits 0) where the truth is 4427042831751708672 = 9.10e-13, with no error channel anywhere. (5) A NaN entry is silently dropped too (measured: f64_gt(f64_abs(NaN), EPSILON_F64) == 0, so a 2x2 with one NaN yields nnz = 1) \u2014 poisoned data is converted into a clean finite product. Because nothing here divides, there is no scale this guard could legitimately own; the only scale-free predicate for \"structural zero\" is exact equality with zero. Secondary notes, both verified: (a) the line-75 comment says \"Drops entries with |value| < EPSILON_F64\" while the code keeps only |v| > EPSILON_F64, i.e. it also drops |v| == EPSILON_F64; (b) the predicate is triplicated, not merely doubled \u2014 :88 (count) and :107 (emit) size and then fill the same alloc(count*8) buffers so any repair MUST change both in one edit or the emit pass writes past them (CWE-787), and csr_add:202 applies the IDENTICAL absolute drop to the merged sum while its two drain loops (lines 213-226) apply no drop at all, so csr_add is internally inconsistent about the very tolerance it claims \u2014 direct evidence this is not a principled sparsification.",
   "fabricates": "A silently smaller matrix presented as a valid one: real entries omitted, and in the limit nnz = 0 handed back as a normal non-null CSR pointer so csr_spmv / csr_add / csr_transpose all answer the zero vector. Measured: csr_spmv row 0 returns bit-exact +0.0 where the correct value is 9.10e-13 (bits 4427042831751708672), a 100% relative error with rc/no error channel. Also fabricates finiteness: a NaN entry is dropped, so a poisoned matrix produces a clean finite product (measured nnz 1 instead of 2).",
   "correct_threshold": "Exact zero, written as `f64_eq(v, f64_from(0)) == 0`, applied IDENTICALLY at :88 and :107 in one edit (and at csr_add:202). Three near-miss formulations are measurably wrong and must not be used: (i) `|v| >= DBL_MIN` \u2014 NOT equivalent to v != 0; it drops subnormals, measured nnz 5 -> 3 at c = 2^-1013 and 2 at c = 2^-1022 where the exact predicate keeps all 5; (ii) `f64_gt(f64_abs(v), f64_from(0))` \u2014 drops NaN (measured f64_gt(f64_abs(NaN), 0) == 0), converting poisoned data into a structural zero; (iii) a raw `load64(...) != 0` bit compare \u2014 KEEPS -0.0 (bits 0x8000000000000000) as a stored nonzero, whereas f64_eq(-0.0, 0.0) == 1 correctly drops it (both measured; this is exactly the negative-zero trap 2.13.0 already found in an antiparallel cross product). Verified non-breaking: ganita_mat_new zero-fills explicitly (lib/ganita.cyr:78-82), so the modules.tcyr 3x3 fixture has exactly 5 entries that are not exactly zero (test asserts 5, \"not 9\") and the abuse.tcyr all-zero 2x2 has 0 (test asserts 0) \u2014 the exact-zero predicate changes NO existing assertion. If a drop tolerance is genuinely wanted it must be RELATIVE and caller-visible: a pre-pass computing scale = max|A_ij| over the whole matrix, then `var tol = f64_mul(EPSILON_F64, scale)` \u2014 the idiom this repo already uses at src/linalg_precision.cyr:442 \u2014 with the SAME tol value reused by both passes; a per-row scale in one pass and a global scale in the other desynchronises count from emit and overruns alloc(count*8).",
   "scale_test": "Fixture M0 (4x4): entries (0,0)=1.0, (0,2)=2^-10, (1,1)=2^-5, (2,0)=2^-10, (3,3)=1.0, remaining 11 cells exact +0.0; k = 5. x = (1,1,1,1). \u26a0 M0's entries and x MUST be exact powers of two \u2014 the original filing's \"entries spanning 1.0 down to 2^-10\" does not guarantee that, and a general 53-bit mantissa loses bits once c pushes products subnormal, which breaks assertion (2) for a CORRECT implementation. Sweep c = 2^p for p in {0, +-20, -29, -30, -40, -41, -42, -43, -53, -200, -322, -400, -1000, -1012, -1013, -1022, -1064, -1069, -1074, +400, +1000}. Assert (1) csr_nnz(csr_from_dense(c*M0)) == (the number of entries of c*M0 that are not exactly zero) \u2014 COMPUTED FROM THE SCALED MATRIX, not fixed at k. \u26a0 This is the original filing's one real error: it asserts nnz == k at every c, and at p = -1074 the 2^-10 and 2^-5 entries genuinely underflow to exact zero, so a measured correct repair returns 2 there and the filing's own test would report the correct repair as FAILING (measured exact-zero column: 5 through p = -1064, 3 at -1069, 2 at -1074). Assert (2) csr_spmv(csr_from_dense(c*M0), x)[i] == c * csr_spmv(csr_from_dense(M0), x)[i] BIT-EXACTLY, restricted to |p| <= 1000 where no product is subnormal \u2014 beyond that gradual underflow breaks exact covariance for any implementation. Threshold bracketing, MEASURED on the shipped tree rather than estimated (columns: shipped 1e-12 / DBL_EPSILON / 1e-100 / DBL_MIN / exact-zero): shipped is intact at p = -29 and already loses 2 of 5 at p = -30, total loss (nnz = 0) from p = -40 on; a DBL_EPSILON repair is intact at -41, loses 2 at -42/-43, total at -53; a 1e-100 repair is intact at -200 and total by -400 (analytic crossing p ~ -322, NOT the -290 originally claimed); a DBL_MIN repair is intact at -1012, loses 2 at -1013, keeps 2 at -1022, total at -1064; the exact-zero repair keeps all 5 through -1064 and thereafter loses only what truly underflowed. Every candidate a repair might plausibly choose is therefore separated at a distinct, identifiable c: -30/-40 kills the shipped guard, -41 vs -53 separates DBL_EPSILON, -200 vs -400 separates any arbitrary small constant, -1012 vs -1013/-1022 separates DBL_MIN from exact zero. Control that kills a guard-deleted (keep-everything) mutant: M0's 11 exact-0.0 cells must still be omitted, i.e. nnz must be 5 and never 16, at every c \u2014 measured on the modules.tcyr fixture as 5, not 9. Second control, for the coupling: after the repair, run tests/modules.tcyr:7392-7418 and tests/abuse.tcyr:904-923 unchanged (they must stay green), and mutate ONLY :88 to the new predicate while leaving :107 shipped \u2014 the emit pass must then be shown to overrun alloc(count*8), proving the two sites are load-bearing together."
  },
  {
   "site": "src/linalg_ext.cyr:107",
   "fn": "csr_from_dense (emitting pass)",
   "guarded_quantity": "|A_ij| \u2014 the same dense matrix entry as line 88, re-tested to decide which entries are written into col_idx/values. Degree 1 in the problem scale; units are whatever the caller's matrix is in. MEASURED: csr_from_dense has ZERO internal callers tree-wide (grep over src/, examples/, docs/ finds only tests/modules.tcyr:7392 and tests/abuse.tcyr:904-915), so it is a pure public entry point and nothing normalises the entries before the call.",
   "protects": "Nothing that can fail \u2014 there is no division, sqrt or acos anywhere in the function. It is the emit half of the same drop-tolerance. Its ONLY hard requirement is that it accept exactly the same set as line 88, because line 88 computed `count` and lines 94-96 sized col_idx/values at count*8. Note csr_new is then handed `count` (pass 1), NOT `pos` (pass 2) \u2014 measured to diverge 4 vs 16 under de-sync.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Refutation attempted on all five avenues and FAILED on all five; the verdict stands on measurement, not argument. (1) Not dimensionless/bounded: probe built the SAME 2x2 [[1,2],[3,4]] in eight unit systems (scale factors 1e0, 1e-9, 1e-11, 1e-12, 1e-13, 1e-16, 1e-100, 1e-320 as exact f64 bit patterns) and got nnz = 4, 4, 4, 3, 0, 0, 0, 0. A scale-free predicate gives 4 in all eight. (2) No caller normalises \u2014 there are no callers at all. (3) Not a convergence tolerance: no iteration, no residual; it is a sparsification policy threshold, and the filing is correct that it protects nothing that can fail. (4) The proposed repair does NOT break an existing test: with `f64_gt(f64_abs(v), 0)` installed at BOTH sites (install proven by grep: 2 occurrences of the new predicate, 0 residual) all five suites are green and byte-identical to baseline \u2014 hisab 416/416, foundation 359/359, modules 1827/1827, edge_cases 233/233, abuse 776/776. The converse is the damning half: the guard can be anything in [0, 1e-12] and all 3611 assertions stay green, so the suite has no opinion on this threshold. (5) The scale_test avenue is where the filing was genuinely weak, but weak in a way that UNDER-states the fix, not one that refutes it \u2014 see the sharpened fields. Two corrections to the filing's own reasoning: the boundary is `>=`, not `>` as documented \u2014 shipped code uses strict `f64_gt`, so |v| == EPSILON_F64 exactly is DROPPED, measured as nnz = 3 (not 4) at scale 1e-12, which also makes the line-75 doc comment \"Drops entries with |value| < EPSILON_F64\" wrong at the boundary; and DBL_MIN is the WRONG threshold to name here \u2014 src/error.cyr's own freshly-landed F64_TINY comment states the doctrine, \"This is the right threshold when the guard protects a DIVISION\", and this guard has no denominator.",
   "fabricates": "Measured on shipped code. At scale 1e-12: nnz = 3 of 4 \u2014 a silently WRONG sparsity pattern that still looks healthy (one entry vanished, no error). At 1e-13 and below: csr_from_dense returns a non-null, valid-looking CSR handle with nnz = 0, nrows = 2, ncols = 2, and NULL col_idx/values pointers (alloc(count*8) with count = 0 hits `if (size <= 0) { return 0; }` in lib/alloc.cyr:224) \u2014 the entire matrix destroyed, reported as a successful conversion; csr_spmv on it then returns an all-zero y. If ever de-synchronised from line 88 in the RELAXING direction the failure mode splits by fixture, and only one half is silent: with count = 0 the buffers are NULL so the first store64 faults at address 0 (loud SIGSEGV, measured); with count > 0 it is genuine silent CWE-787 (measured: 4x4, 4 diagonal entries above eps and 12 below, runs to completion with no error, reports nnz = 4 while row_ptr[4] = 16, i.e. 16 entries written into 32-byte col_idx and values buffers \u2014 a 96-byte overrun of each, and since they are consecutive bump allocations col_idx's overrun lands inside values).",
   "correct_threshold": "Exact non-zero, written as `f64_gt(f64_abs(v), 0)` \u2014 a one-token change from EPSILON_F64 to 0, using only helpers already in scope. Do NOT write it as `|v| >= DBL_MIN`: the filing offered `v != 0.0` and `|v| >= DBL_MIN` as if equivalent and they are not, measured \u2014 a DBL_MIN build returns nnz = 0 for the same matrix at scale 1e-320, so it still destroys the whole matrix, just three hundred decades further down. src/error.cyr's F64_TINY comment states the rule this violates: DBL_MIN is the threshold for a guard protecting a DIVISION, and this guard has no denominator. `f64_gt(f64_abs(v), 0)` is also the negative-zero-correct spelling: f64_abs(-0.0) = +0.0 which is not > 0, so -0.0 is dropped, matching IEEE `v != 0.0` and avoiding the -0.0 trap 2.13.0 found. Change BOTH sites in ONE edit, ideally by extracting `_csr_keep(v)` \u2014 line 202 is the third instance, so the project's own third-instance rule fires (note 202 in csr_add has NO safety coupling: it writes into an upper-bound allocation of nnz_a + nnz_b and reports `pos`, so csr_add is already structured safely and csr_from_dense is the lone exact-count site). Two structural fixes belong in the same edit, because a shared helper only prevents drift it is actually used by: bound the EMIT loop by `count` so an over-emit cannot write past the allocation at all (this is the mutation-proof half \u2014 2.12.0 proved the tempting other half leaves the silent write intact), and hand csr_new `pos` rather than `count` so nnz and row_ptr[nrows] cannot disagree. Fix the line-75 doc comment too: it says `<` and the code is `<=`.",
   "scale_test": "Two parts, both corrected from the filing \u2014 its version does not discriminate as written. PART A, the scale sweep, must NOT be delegated to line 88's test and must reach SUBNORMALS: assert nnz == 4 for the same dense [[1,2],[3,4]] built at scale factors 1e0, 1e-9, 1e-11, 1e-12, 1e-13, 1e-16, 1e-100 and 1e-320 (exact f64 bit patterns; the matrix is one problem in eight unit systems). Measured discrimination \u2014 shipped fails 5 of 8 (nnz 4,4,4,3,0,0,0,0); a `>= DBL_MIN` repair fails ONLY the 1e-320 row; the correct `> 0` repair passes all 8. So a sweep bottoming at 1e-13, which is all the filing's \"entries in (0, 1e-12]\" fixture reaches, PASSES the DBL_MIN mutant and brackets nothing below it. The 1e-12 row is load-bearing on its own: it is the only one that catches the strict-vs-non-strict boundary (nnz 3, not 0). PART B, the de-synchronisation control, needs its OWN fixture \u2014 the filing's cannot express it. Measured: installing the de-sync (line 88 kept at EPSILON_F64, line 107 relaxed, install proven by `sed -n 88p` / `sed -n 107p` before running) and running all five existing suites gives 3611/3611 PASSING, not because the overrun is undetected but because no existing fixture holds a single entry in (0, 1e-12], so the divergent branch is never taken \u2014 mutating the existing suite proves nothing here. And the filing's proposed M0 (all entries in (0, 1e-12]) is the wrong fixture: count = 0 makes alloc(0) return NULL and the first store64 faults at address 0 \u2014 a loud SIGSEGV any harness catches. The fixture that exhibits the SILENT overrun must have count > 0: a 4x4 with 4 diagonal entries above eps and 12 entries at 1e-13, which runs to completion with no error and reports nnz = 4 while row_ptr[4] = 16 (16 entries into 32-byte buffers). Assert `row_ptr[nrows] == csr_nnz(c)` on THAT matrix \u2014 it is the assertion that fails on the de-sync and passes on both the shipped and the repaired build. Prove the mutant is installed with a grep before believing any of these counts, and beware `$?` after a pipe: in this session a `cyrius test` that SIGSEGV'd reported `harness exit: 0` because the exit code came from grep."
  },
  {
   "site": "src/linalg_ext.cyr:202",
   "fn": "csr_add",
   "guarded_quantity": "|s| where s = a_ij + b_ij \u2014 the magnitude of the SUM of two stored matrix entries at a coinciding column, computed at line 201 and tested at 202 as `f64_gt(f64_abs(s), EPSILON_F64) == 1` (EPSILON_F64 = 1e-12, src/error.cyr:26). Dimensions: the entries' own units, degree 1 in the problem scale. Unbounded above and below.",
   "protects": "Nothing that can fail. There is no division, sqrt, acos or normalisation anywhere in csr_add \u2014 s is stored straight into out_v. It is a pure drop-tolerance on a returned value, and the function's own header comment (line 149) states the contract as \"Drops zeros\", not \"drops small entries\", so the tolerance is undocumented as well as absolute.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation routes were tried and each is closed by measurement (probes under scratchpad/eps/refute/, run against the shipped tree). (1) NOT dimensionless/bounded: s is a user-supplied matrix entry sum. Measured sweep over c = 2^p on a fixture whose merged row is {overlap sum = c, A-only = c, B-only = c}: 10 of 23 scales lose the overlap entry, first bad at p = -40. nnz is not invariant in c and csr_spmv is not covariant in c. (2) NO caller normalises: `grep -rn csr_add src` returns only the definition \u2014 csr_add has ZERO internal callers; it is a public entry point taking any CSR, and csr_new (also public) imposes no invariant on values. (3) NOT a convergence tolerance: no iteration, no residual; the value is emitted, not iterated. Contrast the genuine tolerance uses at lines 741 and 858 in the same file. (4) The proposed exact repair changes NO existing test \u2014 measured, not assumed: the only value-bearing fixture is modules.tcyr:7392-7413 (A = [[1,0,2],[0,3,0],[4,0,5]], A+A), every entry of whose sum is > 1e-12, so nnz = 5 and the spmv row2 = 18 assertion are identical under `s != 0.0`; abuse.tcyr:908-911 checks only the 0 / non-0 return. (5) The scale_test does discriminate, but had two defects of its own, corrected below. The project's own doctrine already decides this case at src/error.cyr:40-45: EPSILON_F64 \"remains right for a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, a parameter in [0,1]\". A matrix-entry sum is none of those. Two sub-claims of the original filing, adjudicated: the drain-loop inconsistency is REAL and measured (at c = 2^-41 the three output entries have IDENTICAL magnitude 2^-41 and the shipped code emits 2 of them), though reaching it requires csr_new-built input, since csr_from_dense's own guards at lines 88/107 pre-filter operands to |v| > 1e-12; the value destruction, however, is reachable with NO csr_new anywhere \u2014 A = [[1.0]] and B = [[-(1 - 2^-41)]] both built by csr_from_dense, both entries far above the drop threshold, true sum exactly 2^-41, and csr_add returns nnz = 0.",
   "fabricates": "A CSR that is not A+B, returned as a normal valid handle (csr_add's only failure channel is the designed 0 for dimension mismatch, not raised here). Measured on the public dense-only path: csr_nnz(csr_add(A,B)) = 0 and csr_spmv(...)[0] = +0.0 where the true value is 2^-41 = 4.547473508864641e-13 \u2014 a structurally missing entry and a fabricated zero, silent. On the scale sweep, 10 of 23 scales return the wrong nnz and the wrong spmv.",
   "correct_threshold": "Exact `s != 0.0` \u2014 and explicitly NOT `|s| >= DBL_MIN` / F64_TINY, which the original filing offered as equivalent. They are not: measured, s = 2^-1030 is a genuine nonzero sum for which `f64_gt(f64_abs(s), F64_TINY)` returns 0, so a DBL_MIN guard fabricates a missing entry too, just 300 decades lower down. F64_TINY is the right threshold only where the guard protects a DIVISION and the failing thing is a zero/subnormal denominator (src/error.cyr:39-45 says exactly this); csr_add divides by nothing, so the only scale-free test is exact zero. The exact test is also what the function's own comment already promises (\"Drops zeros\") and is self-consistent with the two unfiltered drain loops at lines 213-226. If a noise-drop is genuinely wanted it must be relative to the operands actually summed \u2014 `EPSILON_F64 * (|a_ij| + |b_ij|)` \u2014 and must then be applied to the drain loops too and to csr_from_dense:88/107, or the inconsistency merely moves.",
   "scale_test": "Build A0 and B0 with `csr_new` and hand-built row_ptr/col_idx/values arrays \u2014 NOT csr_from_dense. This is the first correction: csr_from_dense (lines 88, 107) carries the IDENTICAL absolute `|v| > EPSILON_F64` drop, and measured, a 1x3 dense fixture at scale 2^-40 comes back with nnz = 0, so a from_dense-built sweep fails identically no matter what line 202 does and blames the wrong guard. Fixture (1x3 suffices; 4x4 adds nothing): A = {col0: 3c, col1: 1c}, B = {col0: -2c, col2: 1c}, so the merged row is {col0: c (overlap sum), col1: c (A-drain), col2: c (B-drain)} \u2014 all three output magnitudes identical by construction, and 3c - 2c = c is bit-exact for every c = 2^p, verified down to p = -1070. Assert for every p: (1) csr_nnz(csr_add(cA,cB)) == 3, (2) csr_spmv(...)[0] == 3c bit-exactly. Exponents p in {0, \u00b120, \u00b140, \u00b141, \u00b153, \u00b160, \u00b1100, \u00b1200, \u00b1400, \u00b1600, \u00b1800, \u00b11000, \u22121030, \u22121040, \u22121060, \u22121070}. Bracketing, with s0 = 1 so the break point is log2(threshold): shipped 1e-12 breaks from p = \u221240 (measured: 10 of 23 fail, first bad \u221240); DBL_EPSILON at p = \u221253; 1e-100 at p = \u2212333 (log2(1e-100) = \u2212332.19 \u2014 the original filing's \"p \u2248 \u2212290\" is arithmetically wrong), caught by \u2212400; 1e-300 at p = \u2212997, caught by \u22121000; F64_TINY/DBL_MIN at p = \u22121023 \u2014 which the original exponent list, bottoming at \u22121000, does NOT reach. That is the second correction: the four subnormal exponents are what bracket a DBL_MIN repair, and without them a wrong repair passes. Exact `s != 0.0` and relative `EPSILON_F64*(|a|+|b|)` pass throughout. Second, SCALE-INDEPENDENT assertion at c = 2^-41 alone: the three output entries have identical magnitude, so nnz must be 3 or 0, never 2 \u2014 shipped returns 2, which discriminates a drop-tolerance from an exact test without any scaling argument at all. Third, a reachability assertion that uses no csr_new: A = [[1.0]] and B = [[-(1 - 2^-41)]] via csr_from_dense (both stored, both >> 1e-12), true sum exactly 2^-41; assert csr_nnz == 1 and csr_spmv[0] == 2^-41 \u2014 shipped returns 0 and +0.0."
  },
  {
   "site": "src/linalg_ext.cyr:471",
   "fn": "solve_gmres (Arnoldi / modified Gram-Schmidt inner loop)",
   "guarded_quantity": "wj_norm = _lext_norm(wj, n) \u2014 the Euclidean LENGTH (already un-squared; _lext_norm takes the sqrt at src/linalg_ext.cyr:312) of the Arnoldi vector wj = A*v_j - sum_i h_ij*v_i after modified Gram-Schmidt. Every v_i is unit-length, so wj carries the units of A's ENTRIES: degree 1 in ||A||, i.e. ||A|| times a dimensionless quantity, bounded above only by ||A||_2. REFUTATION ATTEMPTED AND FAILED: it is not dimensionless, not bounded by 1, and no caller normalises \u2014 solve_gmres is a public entry point shipped in dist/hisab.cyr (grep: only tests/*.tcyr call it in-tree), and A reaches it only as the opaque callback A_fn, so hisab never sees, let alone rescales, the operator.",
   "protects": "The division f64_div(f64_from(1), wj_norm) at line 472 that normalises the next Krylov basis vector. It is NOT a convergence tolerance \u2014 tol is a separate parameter, tested at lines 411, 518 and 557; this branch body does nothing but the reciprocal and the scaling loop.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED BY MEASUREMENT, not by argument (probe: scratchpad/eps/refute/tree/tests/{scale,probe}.tcyr, cycc 6.6.2; the repo tree was copied to scratch and left byte-identical, md5 572273090611bc28f557dabcf03d9eb2). GMRES is exactly covariant under (A,b,tol) -> (cA,cb,c*tol): the Krylov basis and the returned x are UNCHANGED and H scales by c, so with c a power of two every intermediate scales by an exact factor and x must come back BIT-IDENTICAL. The only scale-breaking comparisons in the whole function are the three absolute guards at 471, 499 and 536. Measured on a well-conditioned 8x8 SPD tridiagonal(-1,4,-1), b=1, x0=0, tol0=1e-12, m=8, budget=64: x is bit-identical for c=2^p over p in [-38,+509] and BREAKS AT p=-39 (c~1.8e-12), exactly where wj_norm crosses 1e-12. It returns 0.428571428571 (=3/7) at p=-39..-41 and 2^p (~2.8e-14 at p=-45) from p<=-45, against the true 0.366013071895 \u2014 and it reports CONVERGENCE while doing so, because the fabricated Givens rotation (cs=1, sn=0 from the sibling guard at 499) makes g[k] exactly 0, which returns x at line 558. \u26d4 AND THE DEFECT IS NOT ONLY ABOUT UNITS: it is reachable at c = 1 with no rescaling at all. A = diag(2,3,5,7,11,13,17,19), b = e1 + 1e-14*e2, tol = 1e-18 gives wj_norm ~ 1e-14 at j=0; the shipped gate fires, v_1 is stored unnormalised, and x comes back with x1 = 1e-14 instead of 1e-14/3 and relative residual 5e-15 against the 1e-18 the caller asked for. tests/hisab.tcyr:2544 already passes tol = 1e-18 to this function, so that regime is not hypothetical. The branch body is wrong independently of the threshold whenever the guard fires on a NONZERO wj_norm: line 479 stores wj into v_basis[j+1] unconditionally, so the 'orthonormal' basis contains a vector of length ~1e-14 and every later h_ij = <w,v_i> and the back-substitution are computed against it.",
   "fabricates": "An x built on a non-orthonormal Krylov basis, returned through the normal success path with the solver reporting convergence. Measured, c = 2^-45: x = (2.8e-14, ...) where the answer is (0.366013071895, ...) \u2014 thirteen orders of magnitude wrong; c = 2^-40: 3/7 instead of 0.366013071895; c = 1 near-breakdown: relative residual 5e-15 for a requested 1e-18. solve_gmres's only failure channel is the designed 0 from the two allocation guards (n > ALLOC_MAX/8, m+1 > (ALLOC_MAX/8)/m), so the caller gets a wrong solution with no error signal at all. \u26a0 CORRECTION to docs/audit/2026-08-11-v2.11.0-full.md, which records this function's fabrication as 'the initial guess': the initial guess here was 0 and the returned vector is not it \u2014 what comes back is an arbitrary wrong vector, which is strictly harder to notice than an unchanged x0.",
   "correct_threshold": "`f64_gt(wj_norm, F64_TINY)` \u2014 DBL_MIN, src/error.cyr:45, the smallest magnitude whose reciprocal is still finite; guard exactly what makes the DIVISION fail and nothing more. It is earned, not merely conservative: at c = 2^-534/2^-535 a deleted-guard mutant returns NaN (1/subnormal -> inf -> 0*inf) where the DBL_MIN gate returns finite, so the guard has a real job at a real input. \u2b50 THE REPAIR MUST COVER ALL THREE SIBLING GUARDS, measured: repairing 471 alone only moves the scale-sweep kill from p=-39 to p=-42, because line 499 (r_val, the Givens radius) and line 536 (|hii|, the back-substitution pivot) are the same quantity class and fabricate an identity rotation and a skipped divide. With all three on F64_TINY the sweep is bit-identical across p in [-509,+509]. \u26a0 AND THE 'RELATIVE DEFLATION TEST BESIDE IT' IS NOT A DROP-IN: measured, a relative gate `wj_norm > EPSILON_F64 * r_norm` with the SHIPPED branch body fails the c=1 near-breakdown control identically to the shipped absolute gate (same wrong x1 = 1e-14, same 5e-15 residual). A deflation test is only a repair if firing BREAKS the inner loop at k = j+1; as a threshold swap it reproduces the defect. \u26a0 Existing tests do not constrain the choice \u2014 measured, not assumed: with F64_TINY installed at all three sites (installation proven by grep) all five suites pass with counts identical to the pristine tree (hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776, 0 failed both ways). The suite is blind to this. \u26a0 ADJACENT, OUT OF THIS GUARD'S SCOPE BUT FOUND BY THE SAME PROBE: `inv_r = f64_div(f64_from(1), r_norm)` at line 416 has NO guard at all; with tol <= 0 and an exact happy breakdown, `f64_lt(r_norm, tol)` is false at r_norm = 0 and every gate choice returns NaN through that site.",
   "scale_test": "PRIMARY (scale sweep, measured to bracket): fix the 8x8 SPD tridiagonal(-1,4,-1) with b0 = 1, x0 = 0, restart m = 8, max_iter = 64, tol0 = 1e-12. For c = 2^p assert solve_gmres(c*A0*, c*b0, x0, 8, c*tol0, 64, 8) is BIT-IDENTICAL to the c=1 run (x is invariant, not covariant, so no rescaling of the compared value and the compare is exact). Measured first-failing p, all three guards set to the same rule: shipped 1e-12 -> p = -39; DBL_EPSILON -> -51; 1e-100 -> -331; F64_TINY/DBL_MIN, a relative rule, and no guard at all -> survive to -509. USABLE WINDOW p in [-509, +509]: outside it _lext_norm's sum of SQUARES overflows (upward, entries 4c, (4c)^2 >= 2^1024 at p = +510) or goes subnormal (downward, p = -510), so the test would measure the HELPER. p in [-500, +500] is safely inside and brackets every plausible repair. \u26d4 TWO CORRECTIONS TO THE ORIGINAL TEST DESIGN, both measured. (1) THE SWEEP DOES NOT ISOLATE LINE 471 \u2014 it is a whole-function property; a 471-only repair still fails at p = -42, so a repairer must patch 471, 499 and 536 together or will read a real fix as a failure. (2) THE PROPOSED EXACT-BREAKDOWN CONTROL DOES NOT DISCRIMINATE AND MUST BE REPLACED. Measured with A = diag(2,3,5,...), b = e1 (and the 2-D variant b = e1+e2): wj_norm is exactly 0, which forces sn_j = 0, hence g[k] = +-0, which trips the |g[k]| < tol break at line 518; v_basis[j+1] is written but the x-update loops jj < k and never reads it. All six gate choices \u2014 INCLUDING deleting the guard entirely \u2014 return the exact 0.5 (and (0.5, 1/3, 0...)) with identical callback counts, so the 'delete-the-guard mutant produces inf/nan and dies' prediction is refuted. Setting tol = 0 to defeat the break does not rescue it: then all six return NaN via the unguarded division at line 416, i.e. the control measures a different site. \u2b50 REPLACEMENT CONTROL, needs no rescaling and discriminates every candidate: A = diag(2,3,5,7,11,13,17,19), b = e1 + 1e-14*e2, x0 = 0, tol = 1e-18, m = 8, max_iter = 64; assert relative residual ||b - Ax||/||b|| == 0 exactly. Measured: F64_TINY, DBL_EPSILON, 1e-100 and no-guard give 0; the shipped 1e-12 AND a naive relative gate both give 5e-15 with x1 = 1e-14 instead of 1e-14/3. Pair it with a subnormal control at c = 2^-534, where no-guard returns NaN and F64_TINY returns finite, to prove the guard is still doing work."
  },
  {
   "site": "src/linalg_ext.cyr:499",
   "fn": "solve_gmres (Givens rotation construction)",
   "guarded_quantity": "r_val = sqrt(hjj^2 + hj1j^2) \u2014 the Givens rotation RADIUS, the 2-norm of a two-element column segment of the Hessenberg after the previous j rotations have been applied. The sqrt IS taken, so this is a length, not a squared quantity; the class is scaling, not squared-vs-unsquared. Degree 1 in ||A||: the v_i are unit vectors by construction, so H entries are v_i^T\u00b7A\u00b7v_j and carry the units of A's entries. Measured on A0 = tridiag(1,4,1) scaled by c: r_val at j=0 is exactly (16/3)\u00b7c, and the guard's firing point tracks c to the decade (see scale_test bracket).",
   "protects": "The two divisions `f64_div(hjj, r_val)` and `f64_div(hj1j, r_val)` on lines 500-501 forming the rotation cosine and sine. Note r_val >= max(|hjj|, |hj1j|) by construction, so BOTH quotients are bounded by 1 in magnitude and neither can overflow or lose precision at any scale \u2014 the one and only failure mode is 0/0 when both operands are exactly zero. That confirms this is a pure divide-by-zero guard, not a convergence tolerance (the happy-breakdown test on the subdiagonal is a separate guard, line 471), and it is why the correct magnitude is DBL_MIN and not any tolerance.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED as a scaling defect, but the filing's reasoning is wrong in three places and its probe would not have shown any of this. All numbers below are measured on pristine `git show HEAD` sources, cycc 6.6.2, mutants grep-verified installed before each run.\n\n(1) The defect is real and I isolated it. Line 499 cannot be exercised alone: line 507 OVERWRITES H[j][j] with r_val, so the `hii` tested at line 536 (back-substitution) IS r_val, compared against the SAME EPSILON_F64 \u2014 the two guards fire in lockstep on the same quantity, and line 471 (wj_norm, also EPSILON_F64) fires earlier still. Repairing 471+536 and leaving 499 shipped isolates it cleanly: bit-exact correct down to p = -42, then FLAT WRONG from p = -43 to p = -500 \u2014 2 matvecs of a 30 budget (i.e. convergence declared after one Arnoldi step) and a true relative residual of 8.813e-2. Repairing all three restores bit-exact covariance across the entire p \u2208 [-500, +500] window.\n\n(2) REFUTED \u2014 \"the branch body is correct at r_val == 0 ... so ONLY the threshold is wrong.\" Measured false. Singular fixture A0 = [[0,0],[0,1]], b0 = e_0 makes r_val EXACTLY 0 at j=0 at every scale. Shipped, DBL-MIN-at-499, 471+536-repaired and all-three-repaired are IDENTICAL on it: 2 matvecs of a 50 budget, x = [1,0], true residual 1.0 \u2014 the MAXIMUM achievable, since A is singular and b is outside range(A) \u2014 reported as converged. The else-branch writes g[j+1] = -0\u00b7g[j] = 0, so the line-518 residual test reads exactly 0 whenever g[j] != 0. The threshold is NECESSARY BUT NOT SUFFICIENT; the fallback fabricates convergence at r_val == 0 too.\n\n(3) REFUTED \u2014 \"DBL_MIN and relative-to-||A||_F survive the window.\" Not as scoped. DBL_MIN at line 499 ALONE never produces the right answer at any p <= -42: it only changes the failure mode (30 matvecs = budget exhausted at -42..-100, then back to 3 matvecs with a 100% residual at p <= -300, where line 471's unnormalised basis vector cascades into an exact-zero r_val and the fabricating else-branch of (2) takes over).\n\n(4) REFUTED \u2014 the proposed control does not kill a guard-deleted mutant (see scale_test).\n\n(5) CONFIRMED, and the filing understates it: NO existing test constrains this guard. On HEAD's 3574 assertions (776/233/351/416/1798), the DBL_MIN repair is green AND deleting the guard outright is green. Nothing in five suites reaches it.",
   "fabricates": "A converged-looking x, with no error channel \u2014 solve_gmres returns only the solution array and has no rc. The identity rotation sets g[j+1] = -sn\u00b7g[j] = 0, so the residual estimate |g[k]| that line 518 tests is exactly 0 and the routine returns the current iterate as an exact solution. Measured with line 499 isolated (471/536 repaired), A0 = tridiag(1,4,1), b0 = (1,1,1), c = 2^-43: returns after 2 matvecs of a 30 budget with a true relative residual of 8.813e-2, flat at that value all the way to c = 2^-500. In the fully shipped configuration the co-firing line-536 guard skips the back-substitution division too and the true relative residual is 1.000 \u2014 a 100%-wrong answer reported as exact.",
   "correct_threshold": "`f64_gt(r_val, F64_TINY)` (error.cyr's DBL_MIN, equivalently r_val != 0) \u2014 the quotients are bounded by 1 by construction, so zero is the only thing that can fail. But the threshold is NOT the whole repair, and shipping it alone is measurably worthless:\n (a) line 536's `f64_abs(hii) > EPSILON_F64` tests the SAME quantity (line 507 stores H[j][j] = r_val) against the same absolute epsilon and must move with it, or x stays wrong at every scale where 499 fires;\n (b) line 471's `wj_norm > EPSILON_F64` is the same class in the same loop and binds FIRST (it, not 499, is what breaks p = -42);\n (c) the else-branch must stop fabricating convergence: at a genuinely zero column the minimum residual over the Krylov space is unchanged, so g[j+1] must carry |g[j]| forward rather than being zeroed \u2014 otherwise the singular fixture in `why` (2) still reports convergence with a 100% residual, identically before and after the threshold change.\nOnly all three together restore bit-exact scale covariance (measured: p \u2208 [-500, +500], 13 scales, x bit-identical, 3 matvecs at every one). Caveat outside the window: r_val is computed as a naive sum of squares, so it is a FALSE zero once |hjj|, |hj1j| < ~1.5e-154 (their squares underflow); below p \u2248 -513 a DBL_MIN threshold is unreachable and only a hypot-style computation would help.",
   "scale_test": "The filed test does not discriminate \u2014 it fails identically for every candidate threshold \u2014 and its control kills nothing. Four measured corrections:\n\n(1) tol MUST be scaled: tol = c\u00b7tol0. With tol fixed, line 413's `f64_lt(r_norm, tol)` short-circuits and solve_gmres returns the UNTOUCHED initial guess. Measured: x bits = 0x0 at every p <= -42 under shipped, DBL-MIN-at-499, and all-three-repaired alike. The assertion fails everywhere for every threshold and brackets nothing.\n\n(2) The sweep MUST be run with lines 471 and 536 already repaired, or line 499 is invisible behind them: shipped, m499, and 471+499 all fail identically at p <= -43. This is the only configuration in which the guard is observable.\n\n(3) Bracket, measured in that isolated configuration on A0 = tridiag(1,4,1), b0 = (1,1,1), x0 = 0, restart 3, budget 30, c = 2^p, asserting x bit-identical to the c = 1 run (r_val at j=0 = (16/3)\u00b7c, so the firing point is T\u00b73/16):\n    1e-12 (shipped): last good p = -42, first bad p = -43\n    DBL_EPSILON:     last good p = -54, first bad p = -55\n    1e-100:          last good p = -334, first bad p = -335\n    DBL_MIN:         survives to p = -500 (and +500)\n  A step-50 grid over [-500, +500] does bracket all three losers (-50, -100, -350 respectively); the filing's extra p = \u00b140, \u00b141, \u00b153 points are mis-chosen \u2014 none of them is the edge. Keep the window at \u00b1500: the `_lext_norm` sum-of-squares underflows below p \u2248 -513 (measured: all-three-repaired is bit-exact at -500 and breaks at -510) and overflows above p \u2248 +511.\n\n(4) Add the second observable the filing is right to want, but make it the MATVEC COUNT via a counting callback, not the true residual: the fabrication announces itself as an early return (3 matvecs when correct, 2 when it falsely converges), and that count is what separates line 499 from line 536 \u2014 536 corrupts x without changing the iteration count.\n\n(5) CONTROL \u2014 the filed one is refuted. A0 = I, b0 = e_0 does NOT kill a guard-deleted mutant: measured bit-identical 0x3ff0000000000000 with and without the guard, no NaN, because GMRES converges and RETURNS at j = 0 (r_val = 1, 2 matvecs); step j = 1, where the filing expects hjj = hj1j = 0, is never reached. Use instead the singular fixture A0 = [[0,0],[0,1]], b0 = e_0, n = 2, budget 50: r_val is exactly 0 at j = 0 at every scale, and with the guard deleted x0 comes back 0xfff8000000000000 (NaN) after burning all 50 matvecs, versus 0x3ff0000000000000 with it. Assert on that fixture BOTH that x is not NaN and that the routine does not report convergence (matvecs must reach the budget) \u2014 the second half is what fails on the threshold-only repair and is the assertion that would have caught defect (2) in `why`."
  },
  {
   "site": "src/linalg_ext.cyr:536",
   "fn": "solve_gmres (back-substitution)",
   "guarded_quantity": "|H[i][i]| \u2014 but specifically it is the value stored at line 509, i.e. r_val = sqrt(hjj^2 + hj1j^2) computed at line 498, the Givens-rotated R diagonal. Units of A's entries: H[i][j] = <A\u00b7v_i, v_j> on UNIT Arnoldi basis vectors, so degree 1 in ||A|| and degree 0 in b. A plain magnitude, not squared. f64_abs is redundant on it (sqrt is non-negative).",
   "protects": "The division `yi = f64_div(yi, hii)` on line 537 in the upper-triangular back-substitution H\u00b7y = g. f64_div is a raw IEEE intrinsic, not a safe-div (lib/ganita.cyr:660 uses f64_div(0,0) deliberately to manufacture a NaN), so an unguarded hii == 0 yields inf/nan.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FAILED, TWO CAME BACK SUPPORTING. (a) Not dimensionless: nothing normalises A \u2014 A_fn is an arbitrary caller callback, and normalising r into v0 rescales g, not H, so hii is degree 1 in ||A||. (b) No caller normalisation exists; b is not normalised either. (c) Not a convergence tolerance: tol is a separate caller parameter (lines 411, 517, 556); line 536 runs after the Arnoldi loop has ended and guards a division only. (d) No existing test blocks the repair \u2014 11 solve_gmres assertions (abuse.tcyr:951-996, hisab.tcyr:2548-2559) use fixtures 2I (_ab_matvec) and tridiag(1,4,1) (_gmb_A) at unit scale, giving hii = 2 and 4.24, so the guard is never reached; and every one of those assertions is on the returned POINTER or the matvec count, never on x's values, so y_i = 0 cannot flip any of them. (e) The scale_test partially fails to discriminate \u2014 see scale_test; that sharpens the test, it does not save the guard. \u26d4 WHAT THE REFUTATION PASS ADDED: hii is not merely \"an R diagonal\", it is EXACTLY the r_val tested three lines earlier at 499 against the IDENTICAL EPSILON_F64, and r_val >= |H[j+1][j]| = wj_norm tested at 471 against the same constant. So line 536 can NEVER fire without 499 and 471 having already fired at that same step j, and since the 499 else-branch sets sn = 0, g[k] = -0 and the solver then takes the `f64_lt(f64_abs(g[k]), tol)` exit at 556 \u2014 the fabricated answer is returned through the success path AND labelled converged. One defect, three sites. \u26d4 And it is reachable at c = 1 with no scaling at all: A = 0 with b = [1,2,3] gives hii == 0 exactly, and the shipped code returns x = b = [1,2,3] reporting convergence for a system with no solution; the nilpotent shift [[0,1,0],[0,0,1],[0,0,0]] with b = [1,0,0] returns [1,0,0] (residual 1.0) and reports converged while a zero-residual solution [t,1,0] exists. [measured: python replica of lines 400-556, scratchpad sim3/sim4, 2026-09-09]",
   "fabricates": "y_i left undivided \u2014 the numerator returned as if the pivot were 1.0 \u2014 so x is short of the true step by exactly the factor |hii| (a ratio of 1/|hii| >= 1e12 at the threshold). Measured on A = tridiag(1,4,1)\u00b72^p, b = [1,2,3]\u00b72^p, x0 = 0: x(1) = [0.2222, 0.1111, 1.4444], x(2^-45) = [2.84e-14, 5.68e-14, 8.53e-14]. Returned through the normal success path AND flagged converged (the companion guard at 499 zeroes sn, hence g[k], hence the line-556 early return), so nothing downstream notices. Its only failure value is the designed 0 from the allocation guards, which this path never takes.",
   "correct_threshold": "`|hii| >= DBL_MIN` for the division (f64_abs is redundant \u2014 hii is a sqrt), and y_i = 0 when it genuinely is zero, never y_i = numerator: at hii = 0 the true y_i is unbounded, so the numerator is the one value guaranteed wrong. \u26a0 SHARPENED: the repair is not well-defined at this site alone. Because hii IS the r_val tested at 499 and dominates the wj_norm tested at 471, all three must move to DBL_MIN in one change or the solver stays scale-dependent \u2014 measured, a 536-only DBL_MIN repair still returns a WRONG (though now scale-invariant) rank-1 answer x = (g0/r_val)\u00b7v0 = [0.2646, 0.5292, 0.7937] for every p <= -40, versus the correct [0.2222, 0.1111, 1.4444]. With 471/499/536 all at DBL_MIN the returned x is bit-identical across p in [-500, +500]. The alternative relative form `|hii| > EPSILON_F64 * |H[0][0]|` also survives the sweep, but DBL_MIN is preferable here: it is the only threshold that needs no scale chosen for it, and only DBL_MIN + y_i = 0 gives an honest answer at exact rank deficiency.",
   "scale_test": "\u26a0 THE PROPOSED SWEEP DOES NOT DISCRIMINATE AS WRITTEN \u2014 two measured defects, both fixed below. (1) It leaves tol unscaled. With tol fixed at 1e-10 and (A,b) -> (cA,cb), a FULLY REPAIRED implementation still fails 12 of 18 sweep points: p <= -39 returns x0 through the line-411 `r_norm < tol` early exit (r_norm = c\u00b7||b||, and tol is itself an absolute), and p >= +40 differs in the last ulp because a fixed tol buys extra outer cycles. Scale it: tol' = c\u00b7tol. (2) It attributes the failure to line 536, but 536 cannot fire without 471 and 499 firing on the same quantity at the same p, so a 536-only repair still fails at p = -40 (measured above). \u2705 THE TEST THAT DOES DISCRIMINATE: sweep c = 2^p, p in [-500, +500] step 50 plus -39, -40, -41, -50, -53, -332, -333 (the sum-of-squares in _lext_norm caps the usable window at |p| ~ 511), with (A, b, tol) -> (cA, cb, c\u00b7tol), x0 = 0, and assert x(c) bit-identical to x(1) \u2014 a firing guard is orders of magnitude off, never a ulp, so no tolerance can hide it. Bracketing measured with 471/499/536 moved together: shipped 1e-12 first breaks at p = -40, DBL_EPSILON at p = -53, 1e-100 at p = -332, 1e-150 at p = -500, DBL_MIN and the relative form bit-exact across the whole window. \u2705 EXACT-ZERO CONTROL, CORRECTED: the claim's \"A0 singular, b in range\" does NOT reach the guard \u2014 diag(1,1,0) with b = [1,2,0] gives hii = 1 and all three variants return [1,2,0] identically; with b out of range it gives hii = 1.24e-16, which separates shipped from repaired but NOT DBL_MIN from a deleted guard (both divide). Use a control where hii is EXACTLY 0: A = 0 (3x3), b = [1,2,3], c = 1 \u2014 shipped returns x = [1,2,3] \"converged\", DBL_MIN+y_i=0 returns [0,0,0], a guard-deleted mutant returns [inf,inf,inf]; three distinguishable outcomes, no scaling required. Add the nilpotent shift [[0,1,0],[0,0,1],[0,0,0]] with b = [1,0,0] as a second: shipped returns [1,0,0] with residual 1.0 and claims convergence though an exact solution exists, and the mutant returns [inf,nan,nan]."
  },
  {
   "site": "src/linalg_ext.cyr:720",
   "fn": "solve_pgs",
   "guarded_quantity": "|A[i][i]| \u2014 a diagonal entry of the caller-supplied system matrix, read via ganita_mat_get(A, i, i). Units of A's entries (for a contact solver, an effective-mass / inverse-mass term); degree 1 in the problem scale. Compared against EPSILON_F64, declared at src/error.cyr:26 as 0x3D719799812DEA11 = 1e-12 exactly (decoded, confirms the constant gate).",
   "protects": "The division f64_div(sigma, aii) on line 732 \u2014 the Gauss-Seidel update for row i. Nothing else in the row uses aii, so the guard has exactly one job.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Verdict survives every refutation avenue I could construct. (1) NOT dimensionless: aii is a raw entry of a caller-owned matrix; the header comment (lines 704-712) states only \"A: stdlib matrix (n x n)\" \u2014 no normalization contract, no unit convention, no bound. (2) NO caller normalises: grep over src/, tests/, examples/ finds ZERO in-repo callers of solve_pgs \u2014 it is a public library entry point reached only by tests/hisab.tcyr:1576/1605 and tests/abuse.tcyr:940-948. collision_core's contact path uses sequential_impulse, not this. Nothing can normalise A before the call. (3) NOT a convergence tolerance: line 741 IS the convergence tolerance and uses the SAME constant \u2014 that is the confusion, not a defence. Line 720 sits immediately in front of a division and has no other consumer. (4) Covariance verified numerically, not argued: PGS is invariant (not covariant) in x under (A,b) -> (cA,cb) with lo/hi fixed, and for c a power of two the invariance is BIT-EXACT \u2014 a 4x4 fixture reproduces the identical x for p in [-1019, +1021] with the guard removed entirely. So aii scales by c exactly while 1e-12 does not, and the same contact problem in kg vs tonnes crosses the threshold at a different point. Measured on a 4x4 with diag (4,3,2,1): at c = 2^-40 row 3 alone is skipped and x[3] stays frozen at the initial 0.0 where the answer is 0.29166666666666663 \u2014 the rest of the vector solved around it; at c = 2^-45 every row is skipped, max_change is never written (the skip path at 721-723 does not touch it), the line-741 early-out fires on the FIRST sweep, and the function returns rc = 0 with x still the caller's x0. Documented as \"Returns: 0 on success\", and solve_pgs has no error channel at all (both returns are literal 0).",
   "fabricates": "Uniformly small-scale system: the caller's own initial guess x0, unmodified, returned as rc = 0 (measured: c = 2^-45 returns [0,0,0,0] for a well-conditioned SPD system whose answer is [0.25, 0.5833\u2026, 0.29166\u2026, 0.29166\u2026]). Single small diagonal: that one component frozen at its initial value while every other row is solved around it \u2014 a plausible-looking partial answer (measured at c = 2^-40). Additionally, if the guard is loosened without an error channel, a subnormal aii against an O(1) sigma divides to \u00b1inf, and f64_clamp (lib/math.cyr:431-435 \u2014 `if x<lo return lo; if x>hi return hi`) converts that into hi or lo, i.e. a fabricated BOUND value rather than an inf the caller could detect.",
   "correct_threshold": "For the division proper the exactly-right condition is `aii != 0.0` \u2014 that is the only input on which f64_div fails to produce the best available answer; a subnormal aii yields a correctly-rounded quotient (or an honest \u00b1inf on genuine overflow). `|aii| >= DBL_MIN` is one notch conservative and acceptable, but it is NOT behaviourally identical and must be chosen deliberately: it is discriminated from `aii != 0` at aii in the subnormal range (see the n=1 companion in scale_test, where DBL_MIN dies at p <= -1023 and `aii != 0` survives to 2^-1074). If a small-pivot POLICY is wanted at all it must be relative to the row: `|aii| > EPSILON_F64 * sum_j |A[i][j]|` \u2014 dimensionless by construction, invariant under (A,b) -> (cA,cb). \u26a0 CHECKED, NOT ASSUMED \u2014 the threshold change alone breaks NO existing test: tests/hisab.tcyr's two PGS fixtures have diagonals (4,3) and (1,1); tests/abuse.tcyr:948 passes an all-zero matrix whose aii = 0 fails `>= DBL_MIN` and `!= 0` alike, so the skip and the rc = 0 both survive. \u26d4 But the ERROR-CHANNEL half of this repair DOES break a currently-passing assertion: tests/abuse.tcyr:948-949 asserts `solve_pgs(ganita_mat_new(3,3), \u2026) == 0` with the message \"all-zero diagonal skipped, not divided by\" \u2014 that assertion PINS the silent-skip-returns-success contract this finding calls a defect. Returning a nonzero code for a structurally singular row requires updating that assertion in the same change; it is not collateral damage, it is the test having pinned the fabrication.",
   "scale_test": "A0 = 4x4 SPD, diag (4,3,2,1), off-diagonals O(1) powers of two (1, 0.5, 0.25, 0.125); b0 = (1,2,1,0.5); x0 = 0; lo/hi = \u00b11e6. SET max_iter = 1 so the line-741 early-out cannot influence the result \u2014 with max_iter = 1 the loop exits after one sweep either way, isolating this guard from the tolerance at 741. Assert x after the single sweep is BIT-EXACTLY x(A0,b0) for c = 2^p. \u26d4 TWO CORRECTIONS to the proposed sweep, both measured: (1) the claimed \"full double range is usable\" is FALSE, and the limit is not sums of squares \u2014 it is SUBNORMAL PRODUCTS A_ij\u00b7x_j. With the guard deleted entirely (pure arithmetic control), exact covariance holds at p = -1019 and BREAKS at p = -1020, while holding to at least p = +1021. Since DBL_MIN fires only near p <= -1024 for an O(1) diagonal, the proposed p = -1022 / -1074 points lie inside the broken window: the recommended repair \"fails\" there for reasons that are not the guard, and the sweep as written cannot bracket DBL_MIN at all. So restrict the 4x4 bit-exact sweep to p in {0, \u00b120, \u00b140, \u00b141, \u00b153, \u00b160, \u00b1100, \u00b1200, \u00b1400, \u00b1600, \u00b1800, \u00b11000} and REQUIRE the guard-deleted control run to establish that window rather than assuming it. (2) Add an n = 1 COMPANION case to reach the subnormal end exactly: A = [[c]], b = [c], x0 = 0, lo/hi = \u00b11e6, expect x = 1.0 bit-exact. With no off-diagonal products there is nothing to underflow, and c/c = 1.0 is exact even at the minimum subnormal. Measured brackets on that companion: shipped 1e-12 dies at p <= -41; DBL_EPSILON at p <= -53; 1e-100 at p <= -334; DBL_MIN at p <= -1023; `aii != 0` and the row-relative form survive to p = -1074. \u26d4 CONTROL REPLACED \u2014 the proposed one is VACUOUS: \"A0 with A0[2][2] = 0.0 at c = 1 must not produce nan\" PASSES on the guard-deleted mutant, so it kills nothing. sigma is nonzero there, so sigma/0 = +inf, and f64_clamp maps +inf to hi: measured mutant output is x = [0.25, 0.5833\u2026, 1000000.0, -249999.635\u2026] \u2014 finite, no nan anywhere. (This also disproves the test's own premise that bounds \"wide enough\" never bind: \u00b1inf binds against ANY finite bound.) Use instead: with A0[2][2] = 0.0 at c = 1, assert x[2] is bit-exactly its initial 0.0 AND x[3] == 0.3645833333333333 \u2014 the mutant misses both by six orders of magnitude, because the fabricated hi propagates into the next row."
  },
  {
   "site": "src/linalg_ext.cyr:741",
   "fn": "solve_pgs",
   "guarded_quantity": "max_change = max_i |x_i^new \u2212 x_i^old| over one sweep \u2014 the sup-norm change in the SOLUTION vector. Units of x (an impulse in a contact solver), degree 1 in the SOLUTION scale. \u26a0 Degree 0 in the SYSTEM scale: under (A,b) \u2192 (cA, cb) the solution is invariant, so max_change is bit-invariant too (measured). It scales only when b (or lo/hi) scales relative to A.",
   "protects": "Nothing. It is the convergence early-out for the sweep loop (\"Early out if converged (use EPSILON_F64 as tolerance)\", line 740). No division, sqrt or domain is protected \u2014 the divide at line 732 is guarded separately at line 720.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "VERDICT HELD, BUT THE FILING'S OWN TEST WAS REFUTED. All five refutation paths were run against a built probe (replica of lines 713\u2013746, instrumented to return the sweep count; A0=[[4,1],[1,3]], b0=[1,2], x0=0, lo/hi=\u00b12^1000). (1) Dimensionless/bounded \u2014 REFUTED: dimensioned in x; with b scaled by 2^-40 the shipped guard returns after ONE sweep at 175.0% relative error on x0 with rc = 0, where the unit-scale run takes 13 sweeps. (2) Caller normalises \u2014 REFUTED: `grep` finds NO in-tree caller; solve_pgs is a public entry point and A, b, lo, hi are all caller-supplied, with lo/hi (\u00b11e6 in the test) carrying the solution scale independently of A. (3) \"It is a tolerance, not a divide-guard\" \u2014 TRUE as stated and it is why this is milder than the ten division guards, but it does not make an ABSOLUTE tolerance correct: a stopping criterion that accepts an iterate with zero correct digits is not a tolerance, it is a tolerance in the wrong units. (4) Proposed repair breaks a passing test \u2014 REFUTED BY MEASUREMENT, checked not assumed: the relative form returns BIT-IDENTICAL x and the same sweep count on both hisab.tcyr fixtures (line 1566 unbounded, 13 sweeps, x0 = 0x3fb745d1745d1c4c; line 1590 clamped, 2 sweeps, x = [2,2]) and leaves the abuse.tcyr rc = 0 paths unchanged (zero-solution and n = 0 both still terminate on sweep 1 with a DBL_MIN floor). (5) scale_test discriminates \u2014 CONFIRMED BROKEN; see that field. TWO OVERSTATEMENTS IN THE ORIGINAL CORRECTED: the \"compounds line 720\" claim is wrong about consequence \u2014 when every diagonal is skipped, x is never written at all, so firing on sweep 1 changes the RETURNED VALUE by nothing and only saves work; and the `why` argued b-only scaling while the `scale_test` scaled A and b jointly, which is the error that made the test vacuous. \u26a0 INCIDENTAL, worth its own filing: with the early-out removed the solver reaches the bit-exact 1/11 and 7/11 (0x3fb745d1745d1746 / 0x3fe45d1745d1745d); the shipped early-out stops 1286 ulp short (~2.0e-13 relative), and hisab.tcyr:1586 passes only because `assert_f64_eq` is LOOSE_TOL_H \u2248 1e-8, not a bit-exact compare \u2014 so 2.13.0's bit-exact migration did not reach this site.",
   "fabricates": "An under-converged iterate returned with rc = 0 (\"Returns: 0 on success\"). MEASURED: A0=[[4,1],[1,3]] with b = 2^-40\u00b7[1,2] returns after ONE sweep with x0 175.0% relative error against the exact 1/11 scaling \u2014 no correct digits \u2014 while the identical system at unit b takes 13 sweeps and lands 2e-13 from exact. The severity stays below the division guards only because the max_iter path returns 0 as well, so the caller could never distinguish converged from not-converged in any case.",
   "correct_threshold": "Not DBL_MIN \u2014 nothing fails here. Relative, or caller-supplied as solve_gmres already takes a `tol`. Measured working form: `max_change < max(EPSILON_F64 * max_i |x_i|, DBL_MIN)`. The floor is load-bearing and must be a value an exactly-zero max_change still passes: with the DBL_MIN floor the all-zero-solution and n = 0 fixtures still fire on sweep 1 (0 < DBL_MIN), so abuse.tcyr:942\u2013949's rc = 0 assertions are untouched; a floor of 0, or a `<=`-free formulation, would instead burn all max_iter sweeps on every frozen system. In-tree precedent: src/linalg_precision.cyr:442 `var tol = f64_mul(EPSILON_F64, scale)`. \u26a0 Known limit of the relative form, measured: eps\u00b7max|x| denormalizes once max|x| < ~2.2e-296, so bit-exact scale invariance holds only for |p| <= ~800 in b = 2^p\u00b7b0 \u2014 at p = -1000 the correct repair itself takes 8 sweeps and is not bit-exact. State that band in the repair's comment rather than claiming unconditional invariance.",
   "scale_test": "\u26d4 THE ORIGINAL scale_test IS REFUTED \u2014 IT PASSES ON THE SHIPPED DEFECT. Scaling A AND b by c leaves x = A\u207b\u00b9b invariant, hence max_change bit-invariant, hence line 741 UNREACHABLE by that perturbation: measured on the shipped tree, sweeps = 13 and x bit-identical to the c = 1 run at every p in {0, \u00b120, \u00b140, -41, +53, +100}. The only failures it produces (p <= -42: 1 sweep, x = 0x0, the untouched initial guess) are LINE 720 firing on |c\u00b74| < 1e-12 \u2014 the guard it explicitly claims to have isolated away, so its one failure signal is misattributed. Its premise is unsatisfiable besides: c\u00b7A0's diagonal IS c\u00b74, so \"A0's diagonal held at unit magnitude\" cannot hold while A is scaled. Its bracketing predictions (-40 / -53 / -332 knees) are predictions about b-only scaling, which is not what it scales. And its success criterion is wrong: a correct relative repair does NOT hold across \u00b11000 (see correct_threshold), so it would REJECT a correct repair. REPLACEMENT (measured, discriminating): hold A = A0 = [[4,1],[1,3]] FIXED (diagonal 4 and 3 \u2014 720 provably silent, which is what actually isolates 741), scale b ONLY: b = 2^p\u00b7[1,2], x0 = 0, lo/hi = \u00b12^1000, max_iter = 2000. x then scales exactly by 2^p. Assert (a) x(2^p)/2^p bit-equal to x(1), (b) the sweep count invariant in p, and (c) the relative error of x0 against 1/11 stays at 0. Measured sweep counts [p: shipped-1e-12 | DBL_EPSILON-abs | 1e-100-abs | relative] \u2014 1000: 17|17|17|13, 300: 17|17|17|13, 100: 17|17|17|13, 20: 17|17|17|13, 0: 13|16|17|13, -20: 7|11|17|13, -39: 2|5|17|13, -40: 1|5|17|13, -51: 1|2|17|13, -52: 1|1|17|13, -100: 1|1|17|13, -200: 1|1|17|13, -331: 1|1|2|13, -332: 1|1|1|13, -400: 1|1|1|13, -800: 1|1|1|13. Every absolute candidate has its own collapse-to-one-sweep knee (1e-12 at p = -40, DBL_EPSILON at p = -52, 1e-100 at p = -332) AND over-iterates at large scale (17 vs 13), while only the relative form is flat \u2014 so p in {0, \u00b120, \u00b140, \u00b152, \u00b1100, \u00b1332, \u00b1400, \u00b1800} brackets all three candidates in both directions. CAP AT |p| <= 800. The headline assertion is the value, not the count: at p = -40 the shipped code returns 175.0% relative error on x0 with rc = 0."
  },
  {
   "site": "src/linalg_ext.cyr:886 (the division it protects is at :893, not :892; the sibling absolute epsilon is set at :858 and applied at :900 \u2014 NOT :872, which is `i = i + 1;`)",
   "fn": "eigen_power",
   "guarded_quantity": "|max_val| \u2014 largest-magnitude component of w = A\u00b7v, where v is the previous iterate renormalised by its own max component (v = e_0 initially). Degree 1 in ||A||, degree 0 in the scale of v: a plain matrix-entry-scale magnitude, not squared, not bounded, not dimensionless. \u26a0 The finding's premise \"v's largest component is exactly 1\" is NOT true by construction \u2014 line 896 multiplies by the reciprocal, and x*(1/x) != 1 for 27,022 of 200,000 random x (13.5%). It happens to be exactly 1 for the suite's inputs (hisab.tcyr:1510 asserts that bit-exactly and passes). Covariance is unaffected: under A -> cA with c = 2^p every mantissa is bit-identical and only the exponent moves, so max_val(cA) == c\u00b7max_val(A) EXACTLY \u2014 which is what makes a power-of-two sweep a clean bisection.",
   "protects": "The reciprocal `inv_max = f64_div(f64_from(1), max_val)` at :893 and the renormalising multiply `w[i] * inv_max` at :896. It is a divide/overflow guard, not the convergence test \u2014 the convergence test is a separate comparison at :900 against `tol` (:858), and both were given the same EPSILON_F64.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "NOT REFUTED \u2014 the class holds, and every refutation angle was tried and failed. The quantity is degree 1 in ||A||: the caller does not normalise (eigen_power is a public entry point, exported at dist/hisab.cyr:17124; the 17 test call sites all pass raw matrices), the guard is not the convergence tolerance (that is :900, on a different quantity), and the reciprocal at :893 fails only at max_val = 0 or below ~1/DBL_MAX. Measured on [[2,1],[1,2]] scaled by c = 2^p, the shipped 1e-12 returns NO_CONVERGENCE from p = -41 down for a matrix with a cleanly separated dominant eigenvalue. BUT the finding is materially wrong in four measured particulars, and acting on it as written would make the function WORSE. (1) Its value assertion fails on SHIPPED code with no repair at all: at c = 1 the returned lambda is 2.9999999999997375, relative error 8.749e-14 \u2014 outside the proposed 1e-14 \u2014 and on the matrix the suite actually uses the error is 2.5e-13, which tests/hisab.tcyr:1504 already records in its own comment (\"measured error is 2.5e-13\"). eigen_power's accuracy is set by its stopping rule, not by this guard, so a 1e-14 assertion fails for every candidate threshold at every scale and brackets nothing. (2) The top-end caveat is false: measured p = +16 \u2026 +1000 all return rc = 0 with relative error EXACTLY 0.0 in 35 iterations, for every candidate threshold including shipped \u2014 because an absolute tolerance on a huge magnitude cannot fire until the f64 iteration reaches an exact fixed point, which yields the correctly-rounded eigenvalue. Nothing fails at the top for :900's reason or any other. (3) \"Hold the tolerance constant\" is precisely the condition under which the proposed DBL_MIN repair FABRICATES: with :900 left absolute, DBL_MIN at :886 returns rc = 0 and out_val = 2c (the first column's max) where the truth is 3c \u2014 a 33% error, silently \u2014 at p = -41, -60, -100, -400, -1000 and -1022, i.e. over 980+ binades. Repairing this guard alone is strictly worse than shipping it. (4) The silent-wrong band already exists ABOVE the guard: on shipped code p = -20 returns 2.9999995818c (rel 1.4e-7), p = -30 returns 1.0e-4 off, and p = -39/-40 return 2.5c \u2014 rel error 1.67e-1, the WRONG eigenvalue, with rc = 0. So :900's `|dlambda| < EPSILON_F64` is the same defect class on the same covariant quantity and is the LARGER of the two, because it fabricates where :886 merely errors. :886 is real but is the second defect in this function, and the two must be repaired together or not at all. Also corrected: the finding's parenthetical that \"even DBL_MIN is slightly permissive for a RECIPROCAL\" is backwards \u2014 1/DBL_MIN = 4.4942e307 is finite by construction, so no is_inf check is needed; measured, DBL_MIN is slightly CONSERVATIVE, rejecting p = -1024 (max_val = 2^-1023, subnormal, 1/it = 2^1023 finite) where a nonzero-only guard returns the exactly right answer.",
   "fabricates": "As shipped: none \u2014 it reports HSB_ERR_NO_CONVERGENCE and writes neither out_val nor out_vec. The error is nonetheless FALSE (the matrix is well-conditioned and 1/1e-13 = 1e13 is an ordinary double), and it is unit-dependent rather than iteration-dependent, so a caller reading NO_CONVERGENCE as \"no dominant eigenvalue\" is misled. \u26a0 But this field is only true because the second absolute epsilon at :900 is standing behind it: repair :886 to DBL_MIN alone and the function starts fabricating out_val = 2c (33% wrong, rc = 0) at every p <= -41 \u2014 measured. And on shipped code the fabrication already occurs just ABOVE the guard, at p = -39/-40 (returns 2.5c for a true 3c, rc = 0, out_vec written).",
   "correct_threshold": "PAIRED \u2014 either both or neither. (a) :886 guard: replace the multiply-by-reciprocal at :893/:896 with a direct divide `store64(v + i*8, f64_div(load64(w + i*8), max_val))`, after which the only correct guard is `max_val != 0`. Measured: this is exact at every scale from p = +1000 down to p = -1030, removes the reciprocal-overflow mode entirely, and makes v's max component exactly 1.0 by construction \u2014 which tests/hisab.tcyr:1510 asserts bit-exactly today and currently gets only by luck (x*(1/x) != 1 for 13.5% of x). If the reciprocal is kept for speed, `f64_abs(max_val) >= DBL_MIN` is correct and needs NO is_inf check (1/DBL_MIN = 4.4942e307 is finite); it is conservative by ~2.4 binades, not permissive. A genuinely zero max_val does mean A\u00b7v = 0 and NO_CONVERGENCE is the right report. (b) :858/:900 stopping test, which must move in the same commit: `|new - old| < EPSILON_F64` is the same defect on the same degree-1 quantity and is the one that FABRICATES. Make it relative: `f64_abs(f64_sub(new_eigenvalue, eigenvalue)) < f64_mul(tol, f64_abs(new_eigenvalue))`. Verified against the existing suite: all eigen_power value assertions go through `f64_approx_eq(..., EP_TOL = 1e-11)` / `_AB_TOL`, and the relative rule moves [[2,1],[1,3]] from 3.6180339887501418 to 3.618033988751586 and the 3x3 from 5.9999999999993179 to 5.999999999994543 \u2014 both still inside 1e-11, so no existing assertion in tests/*.tcyr breaks. The one bit-exact assertion (:1510, v[1] == 1.0) is made safer, not riskier, by the direct divide.",
   "scale_test": "A0 = [[2,1],[1,2]] (lambda_1 = 3, lambda_2 = 1, ratio 1/3), n = 2, max_iter = 500, c = 2^p so the scaling is EXACT (mantissas bit-identical, exponent only). p in {+1000, +400, +100, +20, 0, -20, -30, -39, -41, -54, -100, -334, -400, -1000, -1022, -1024, -1030} \u2014 note -1024 and -1030 are ADDED; the finding's list stops at -1022 and therefore cannot separate DBL_MIN from a nonzero-only guard at all (measured: DBL_MIN and nonzero-only agree at -1000, -1010 and -1022, and first differ at -1024). TWO assertions per scale, both required: (i) rc == 0, and (ii) out_val within RELATIVE 1e-11 of 3c \u2014 1e-14 is unsatisfiable and fails on shipped code at c = 1 (measured rel err 8.749e-14; the suite's own EP_TOL is 1e-11 for exactly this reason). Assertion (ii) is the half the finding omitted the force of and is what makes the sweep discriminate, because rc alone REWARDS the fabricating repair. Never compare through f64_to. Measured brackets, first p at which rc != 0 (relative stopping rule in place): shipped 1e-12 -> -41; DBL_EPSILON -> -54; 1e-100 -> -334; DBL_MIN -> -1024; nonzero-only/direct-divide -> never in range (entries underflow to 0 at p <= -1075); relative (EPSILON_F64 * ||A||_inf) -> never. Every neighbouring pair is >= 12 binades apart except DBL_MIN vs nonzero-only, which is ONE binade (-1024) \u2014 so that pair is separated only by including p = -1024, not by sweep width. Measured brackets on assertion (ii), which is what exposes the fabrication and what the finding missed entirely: with :900 left absolute, out_val/c = 3.0 only for p >= -10; it is 2.9999995818 at p = -20, 2.999695 at p = -30, and 2.5 at p = -39 and -40 \u2014 all with rc = 0 on SHIPPED code \u2014 and 2.0 at every p <= -41 once :886 is repaired alone. With the paired repair, out_val/c = 2.999999999999213 at every p from +1000 to -1030, identical to 16 digits. The p > 0 half fails nothing (rel err exactly 0.0 at p = +16 \u2026 +1000 for every threshold) so it is a covariance control, not a discriminator; keep it as the control that proves the sweep did not simply shift the answer. Guard-deleted mutant control: A = the 2x2 zero matrix must report NO_CONVERGENCE with out_vec untouched \u2014 pre-fill out_vec with a sentinel and assert the sentinel survives, because deleting the guard makes the ABSOLUTE :900 test fire at iteration 1 (|0 - 0| < tol) and return rc = 0, out_val = 0, out_vec = NaN. \u26a0 That control stops killing the mutant once :900 is relative (0 < tol*0 is false, so it spins to max_iter and returns NO_CONVERGENCE either way); under the paired repair the mutant must instead be killed by a subnormal case \u2014 entries ~1e-320 with the guard deleted and the reciprocal kept overflows 1/max_val to +inf and writes a non-finite v \u2014 so assert both the zero-matrix sentinel AND that no scale in the sweep ever writes a non-finite out_vec component."
  },
  {
   "site": "src/linalg_ext.cyr:900 (the finding said 872; 872 is `i = i + 1;` inside the matvec loop. The `var tol = EPSILON_F64;` alias is line 858, the convergence test is line 900.)",
   "fn": "eigen_power (convergence test at line 900; reached via the `tol` alias at 858, so the task's grep for a literal EPSILON_F64 comparison does not report it)",
   "guarded_quantity": "|lambda_new - lambda_old| \u2014 the absolute change in the dominant-eigenvalue estimate between successive power iterations, where lambda_k is the max-|component| of w = A*v. Carries A's units; degree 1 in ||A||. Compared against a dimensionless-in-name-only constant EPSILON_F64 = 1e-12.",
   "protects": "Nothing. It is purely the stopping criterion that decides when to write out_val/out_vec and return 0. No division, sqrt or acos depends on it (the only division in the loop, `1/max_val`, is guarded separately and absolutely at line 886).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED by measurement on cycc 6.6.2; all five refutation routes were tried and failed. (1) Not dimensionless: an eigenvalue is degree 1 in ||A||, so scaling A by an exact power of two scales |dlambda| identically while the 1e-12 threshold stays put. (2) No caller normalises: `eigen_power` has ZERO in-tree callers \u2014 it is a public entry point reached only from tests/. (3) \"It is a convergence tolerance, not a divide-by-zero guard\" is true as a description and does not exonerate it: an ABSOLUTE tolerance on a covariant quantity makes the routine's answer unit-dependent in both directions, and it reports rc = 0 while doing so. Measured on the suite's OWN fixture A1 = [[2,1],[1,3]] (hisab.tcyr:1502, abuse.tcyr:865), c = 2^p, max_iter = 500: p = 0 relerr 6.8e-14; p = -10 1.5e-10; p = -20 1.3e-7; p = -30 1.1e-4; p = -40 out/truth = 0.6909830056250525, i.e. 30.9% LOW with rc = 0. Top end: p = 0..+11 fine, p = +12 and every p above through +300 return rc = -8 (HSB_ERR_NO_CONVERGENCE) \u2014 at p = +12 the matrix entries are 8192 and 12288, an utterly ordinary magnitude, and lambda's ulp (2.2e-9) is three orders above the 1e-12 stop, so the iterate limit-cycles between adjacent floats and no max_iter can help. The finding UNDER-claimed the top end (it said \"about +40\" and hedged \"input-dependent\"); it is +12 on a fixture already in the suite. (4) The proposed relative repair breaks NO existing test: patching line 900 to `f64_mul(tol, f64_abs(new_eigenvalue))` and re-running the three suites that include linalg_ext.cyr gives 416 + 776 + 1827 = 3019 passed, 0 failed, identical to baseline \u2014 the suite compares through EP_TOL = 1e-11, so a 3.6x-looser stop is invisible to it. (5) The scale_test WAS wrong, in three measurable ways, and is rewritten below rather than being grounds to drop the verdict. NOTE the shared floor: even repaired, every candidate threshold dies at p <= -41 on both fixtures, because line 886's `|max_val| < EPSILON_F64` is a SECOND absolute-on-scaling guard at a separate site; repairing 900 alone leaves eigen_power returning a false NO_CONVERGENCE for any ||A|| below ~2^-41.",
   "fabricates": "Small ||A||: a materially wrong dominant eigenvalue written to out_val with rc = 0 (success), plus a matching under-converged eigenvector. Measured at c = 2^-40 \u2014 A1 = [[2,1],[1,3]] returns 69.1% of the true eigenvalue (30.9% low); A0 = [[2,1],[1,2]] returns 83.3% (16.7% low, out/truth = 0.8333333333333334, i.e. it stopped at 2.5 where the answer is 3). Large ||A||: a FALSE HSB_ERR_NO_CONVERGENCE, from c = 2^12 upward on A1 (verified through 2^300), on a perfectly well-conditioned matrix at any max_iter. Which end bites is fixture-dependent: a matrix whose max-component-normalised fixed point is exactly representable (A0 = [[2,1],[1,2]], A2 = [[1,2],[3,4]]) reaches dlambda == 0 exactly and never fails at the top; one whose fixed point is irrational (A1) fails at both ends.",
   "correct_threshold": "Relative: `if (f64_lt(f64_abs(f64_sub(new_eigenvalue, eigenvalue)), f64_mul(tol, f64_abs(new_eigenvalue))) == 1)`, with `tol = EPSILON_F64` kept as the relative factor. VERIFIED: bit-identical out_val/c and out_vec at every p from -40 to +300 on all three fixtures (A0 out/truth 0.9999999999997376 at every scale; A1 1.0000000000004674 at every scale; A2 1.0000000000000133), and 3019/3019 existing assertions still pass. `new_eigenvalue` cannot be near zero here \u2014 line 886 already rejects |max_val| < 1e-12 before this point \u2014 so no separate zero-divisor case arises. \u26a0 The CONSTANT is not load-bearing, only the RELATIVITY: a relative DBL_EPSILON (2.22e-16) behaves identically to a relative EPSILON_F64 across the whole sweep (same first-bad p, same clean top end), so a mutation on the constant alone is not a discriminating mutant \u2014 mutate the *form* (relative -> absolute). Not DBL_MIN: nothing divides at this site. A caller-supplied `tol` parameter is a strictly better API (solve_gmres in this same file already takes one) but is a breaking change; the relative form is the minimal correct repair. \u26a0 INCOMPLETE ALONE: line 886's `|max_val| < EPSILON_F64` is the same class at the same function and floors every repair at p = -41; fix both or the routine is still scale-bound at the bottom.",
   "scale_test": "\u26d4 THE FINDING'S PROPOSED SWEEP DOES NOT DISCRIMINATE \u2014 measured, three ways. (a) Its fixture A0 = [[2,1],[1,2]] has NO top-end failure for any threshold: shipped code returns rc = 0 with relerr EXACTLY 0.0 from p = +10 through +300, because a geometrically convergent iteration in binary FP DOES land on an exact fixed point where dlambda == 0 and every positive absolute tolerance passes \u2014 the finding's stated rationale (\"never underflows to an exact fixed point\") is backwards, and the top end is precisely where it claimed its discrimination. (b) Its assertion \"out_val == c*3.0 to a RELATIVE 1e-14\" is unsatisfiable by ANY threshold: shipped A0 at p = 0 is relerr 8.75e-14, and the finding's OWN proposed repair is 2.62e-13 at every scale \u2014 sweeping p in [-60,+60] the proposed repair is \"bad\" at 121 of 121 points under that assertion, i.e. the test kills the repair harder than the defect. 1e-14 is below what a 1e-12 stopping rule can deliver at any scale. (c) On A0, an absolute 1e-100 is INDISTINGUISHABLE from the relative repair (both first-bad at p = -41, the floor set by line 886), so A0 fails to bracket the very candidate the finding named. \u2705 CORRECTED TEST: fixture A1 = [[2,1],[1,3]] \u2014 dominant lambda = 3.618033988749895, ALREADY in hisab.tcyr:1502 and abuse.tcyr:865 \u2014 n = 2, max_iter = 500, c = 2^p. Sweep p at UNIT steps over [-40, +60] (not a sparse decade ladder: both shipped edges, +12 and -8, sit inside |p| < 20 and the finding's {0,\u00b120,\u00b140,...} ladder mis-locates both). Bound the bottom at p >= -40 (or repair line 886 first) so kills are attributed to line 900, not 886. Assert two things per p: (1) SCALE COVARIANCE \u2014 `out_val / c` is BIT-IDENTICAL to the p = 0 result, and out_vec bit-identical (this is the load-bearing assertion; it is exact under a relative rule and cannot be satisfied by an absolute one); (2) an accuracy FLOOR at relative 1e-11, the suite's own EP_TOL, not 1e-14. Measured first-bad-p under that assertion, which is the bracket: SHIPPED abs 1e-12 -> bad going UP at p = +12 and DOWN at p = -8 (fails BOTH ends); absolute DBL_EPSILON -> bad at p = 0 (500 iterations exhausted, never converges at any scale); absolute 1e-100 -> bad at p = 0 (same); relative EPSILON_F64 -> clean through +300, bottom floor -41; relative DBL_EPSILON -> clean through +300, bottom floor -41. Only the two RELATIVE forms survive, and they survive identically \u2014 so the test brackets every plausible repair and isolates relativity as the property under test. \u26a0 An f64_to compare would be vacuous here and an ABSOLUTE compare would itself be the scale-dependence under test; the covariance assertion sidesteps both."
  },
  {
   "site": "src/linalg_ext.cyr:1044",
   "fn": "lyapunov_max",
   "guarded_quantity": "r00 = |R[0][0]| from ganita_mat_qr of Z = A*Q \u2014 the growth factor of the dominant direction over one step. Q is orthonormal every iteration, so r00 = ||A*q1||, carrying the units of A's entries: degree 1 in ||A||, a plain magnitude, not squared. MEASURED, not argued: with the guard neutralised, out(c*A0) - out(A0) reproduces ln(c) to <= 1.4e-15 relative at every p in {0, +-20, -39..-60, +-100, -200, -332, +-400, +-600, +-800, +-1000, -1010, -1022}, which is only possible if r00 scales exactly linearly with c.",
   "protects": "The logarithm domain of f64_ln(r00) on line 1045 (ln is undefined at 0, -inf at +0), plus the sample counter `count` that follows it and becomes the divisor at line 1055.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Refutation attempted on all five avenues and all five fail against measurement. (1) NOT dimensionless: r00 has the units of A and scales exactly with it (above). (2) NO normalising caller: grep finds zero callers of lyapunov_max in src/; it is a public entry point shipped in dist/hisab.cyr:17300, so the caller's units are whatever the caller's Jacobian has. (3) NOT a convergence tolerance: it gates a log accumulation and a sample count inside a fixed-trip-count loop, with no early exit \u2014 contrast line 741 in the same file, which is a real convergence test. (4) The proposed repair breaks NO existing test \u2014 measured, not assumed: with the guard replaced by `r00 > 0.0`, the three suites that include linalg_ext.cyr return hisab 416/416, abuse 776/776, modules 1827/1827, byte-identical to the shipped baseline. The two lyapunov tests are unreachable by the change (identity gives r00 = 1; iterations = -5 never enters the loop). (5) The scale test DOES discriminate, with every boundary measured (see scale_test). ln has no difficulty with a small positive argument \u2014 ln(1e-300) = -690.8 \u2014 so the only thing this guard must exclude is r00 <= 0; instead it excludes everything below 1e-12. \u2b50 The chain around it has already made this correction: ganita 1.2.4 replaced its OWN absolute 1e-12 reflector test in ganita_mat_qr with a relative `f64_mul(LINALG_EPS, _linalg_mat_scale(m))` for exactly this failure, and _linalg_norm2 is a scaled two-norm with no over/underflow. r00 is therefore scale-covariant by construction into the subnormal range, and hisab's caller-side absolute EPSILON_F64 is the last absolute threshold left in the chain \u2014 the one that fabricates.",
   "fabricates": "Two distinct fabrications, both measured on the shipped tree. (a) TOTAL firing: every iteration skipped, count == 0, the else-branch at 1054-1055 stores exactly +0.0 (bit pattern 0x0) and returns rc = 0 \u2014 a Lyapunov exponent of zero, i.e. 'marginally stable', for a system contracting at -27.7 nats/step. Measured at p = -40 and every p below, down to -1022. (b) PARTIAL firing, which is subtler, worse, and NOT bit-identifiable: with a non-normal fixture whose transient r00 straddles 1e-12, only the contracting iterations are dropped and the mean is taken over the biased survivors. A0 = [[0.5, 0],[10, 0.9]] at p = -40 returns -26.351213 where the truth is -27.725887 \u2014 off by +1.374674 nats \u2014 and at p = -42 by +2.358899, always biased UPWARD, i.e. a contracting system reported as less stable than it is, with rc = 0 and no indication samples were dropped. A test that only checks 'is the result exactly 0.0' passes on this.",
   "correct_threshold": "`r00 > 0.0` \u2014 guard exactly the ln domain and nothing more. \u26a0 The filing's parenthetical 'equivalently r00 >= DBL_MIN' is REFUTED by measurement and must be dropped: a DBL_MIN variant fabricates 0x0 at p = -1022, where `r00 > 0.0` returns -708.396418532 correct to 1.4e-15 relative. Subnormal r00 is a perfectly good ln argument and must not be excluded. Second half of the repair, which the threshold alone does not fix: a dropped sample must not silently shrink the divisor. Either return an error code (or -inf) when any iteration is skipped, or divide by `iterations` rather than `count` \u2014 otherwise the partial-firing bias above survives any threshold, including a relative one, whenever the dominant direction genuinely collapses on some steps. A truly zero r00 means the dominant direction collapsed exactly; the honest report is -inf or an error, never a smaller sample count.",
   "scale_test": "TWO fixtures, both required; measured on cyrius 6.6.2, ganita 1.2.4. Build c = 2^p exactly as bits (1023+p) << 52 so the scaling is bit-exact, n = 2, iterations = 64. FIXTURE A (total firing, upper triangular) A0 = [[0.9, 0.2],[0.0, 0.5]], lambda0 = ln(0.9) = -0.105360515658. Sweep p in {0, +-20, -39, -40, -41, -45, -52, -53, -60, +-100, -200, -332, -333, +-400, +-600, +-800, +-1000, -1010, -1022} and assert out(c*A0) - out(A0) == p*ln(2) to 1e-12 relative (absolute at p = 0). MEASURED collapse boundaries, which is the bracket: shipped 1e-12 is last-good at p = -39 and returns 0x0 from p = -40 down; a DBL_EPSILON repair is last-good at p = -45 and fabricates from p = -52; a 1e-100 repair is last-good at p = -332 and fabricates from p = -333 (caught by the -400 sample); a DBL_MIN repair is last-good at p = -1010 and fabricates at p = -1022; `r00 > 0.0` satisfies the identity at EVERY p listed, worst residual 1e-12 absolute on a 708-magnitude value. So the sweep separates all four candidates from each other and from the correct repair, and -1022 is load-bearing \u2014 without it DBL_MIN and r00>0 are indistinguishable. The positive p arm is a CONTROL, not a discriminator: measured, all variants agree at p up to +1000 (ln(r00) ~ +693, no overflow, since _linalg_norm2 scales the max out first), so it proves only that no repair clamps from above. FIXTURE B (partial firing, non-normal \u2014 the filing omitted this and its bit-check cannot see the failure) A0 = [[0.5, 0.0],[10.0, 0.9]], same eigenvalues 0.9/0.5 but r00 ~ 10 on the first iterations and 0.9 asymptotically, lambda0 = -0.055065580894. Sweep p in {-36, -38, -40, -42, -44, -46, -50, -60} and assert the SAME identity. Measured on the shipped tree: p = -38 correct, p = -40 returns -26.351213 vs -27.725887 (+1.374674), p = -42 returns -29.112182 + 2.358899, and only from p = -44 does it collapse to 0x0. The `r00 > 0.0` repair reproduces the identity at all eight. Do NOT rely on 'the result is not exactly 0.0' as the failure signal \u2014 at p = -40 and -42 the shipped code returns a plausible non-zero number and that check passes."
  },
  {
   "site": "src/linalg_ext.cyr:1150",
   "fn": "cmat_inverse",
   "guarded_quantity": "max_norm = max over rows col..n-1 of cx_norm_sq(aug[i][col]) = re^2 + im^2 (src/complex.cyr:79) \u2014 the SQUARED modulus of the largest-magnitude candidate pivot in the current column of the augmented [M | I]. Dimensions: the matrix entries' units SQUARED, degree 2 in the problem scale. Verified degree 2 at EVERY column, not just column 0: after eliminating column 0 the left-half entries are a_ij \u2212 a_i0\u00b7a_0j/a_00, still degree 1 in c, so every later pivot search is degree 2 as well; the right half (degree \u22121) is never scanned. No caller normalises \u2014 `grep -rn cmat_inverse src tests examples` finds exactly one definition site and four test call sites, no in-tree caller at all, so it is a bare public entry point.",
   "protects": "The pivot reciprocal `cx_inv(pivot_val)` at src/linalg_ext.cyr:1163 (which divides by cx_norm_sq of the same pivot) and, through it, the row scaling and the whole Gauss-Jordan elimination.",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE ANGLES; ALL FAILED, three by direct measurement (probes at /tmp/claude-1000/-home-macro-Repos-hisab/68915bf3-8a70-4292-b984-a0be0a4996e8/scratchpad/eps/refute/probe{,2}.tcyr, cyrius 6.6.2). (1) Dimensionless/bounded: no \u2014 degree 2 in the entries at every column, shown above. (2) Caller normalises: no \u2014 zero in-tree callers; public entry point in dist/hisab.cyr. (3) Convergence tolerance: no \u2014 it is a one-shot pivot test whose only downstream consumer is a division; the file's own convergence tolerances are elsewhere (:741 max_change, :858 tol). (4) Breaks an existing test: CHECKED, not assumed \u2014 the only in-tree cmat_inverse assertions are tests/abuse.tcyr:347 (m=0), :353 (capped cmat_new), :420 (non-square), tests/modules.tcyr:8749 (m=0) and tests/hisab.tcyr:1430 (250x500 over cap). All four reach an EARLIER guard (m==0 / non-square / aug==0) and none constructs a small-but-nonzero pivot, so no proposed threshold changes any passing assertion. (5) Test doesn't discriminate: this is the one place the claim was WRONG, but it was wrong in a way that makes the finding stronger, not weaker \u2014 see scale_test. \u2b50 THE DECISIVE NEW MEASUREMENT IS AT UNIT SCALE, so the squaring error needs no unit change to expose at all: `diag(1, 2^-20)` \u2014 largest entry EXACTLY 1, condition number 1.05e6, inverse `diag(1, 2^20)` exactly representable \u2014 is returned as 0 (\"singular\"). Measured first-rejected k = 20 in a sweep of diag(1, 2^-k), k = 0..60. With matrix scale pinned at 1, absolute and relative thresholds COINCIDE, so the \"conditioning is relative\" defence cannot apply and only the squaring remains: sqrt(1e-12) = 1e-6 is the real cutoff on |pivot| where the constant reads 1e-12. Scale sweep confirms the second, compound half: cmat_inverse(c\u00b7I_3) returns 0 from c = 2^-20 (9.54e-7) down, measured, while c = 2^-19 inverts and satisfies c\u00b7inv[0][0] == 1 bit-exactly. Compound: even un-squared, an absolute pivot threshold on a degree-1 quantity is still unit-dependent, and cx_inv only actually fails when norm_sq underflows to 0 or the reciprocal overflows \u2014 DBL_MIN territory, not 1e-12.",
   "fabricates": "On the low side: none \u2014 it reports an error, but the error is FALSE and its channel is overloaded. cmat_inverse returns the designed 0, the SAME value it returns for a null argument, a non-square argument and an over-cap augmented alloc, so a caller cannot distinguish \"you passed garbage\" from \"hisab decided your cond-1e6 matrix was singular\"; a caller that stores through it null-derefs, one that checks reports a nonexistent singularity. \u26a0 CORRECTION TO THE CLAIM: on the HIGH side it does fabricate. Measured, cmat_inverse(c\u00b7I) first returns a non-null WRONG matrix at c = 2^512 (1.34e154): max_norm overflows to +inf, `inf < 1e-12` is false so this guard passes, and cx_inv then computes d = inf and returns cx_new(re/inf, ...) = exactly zero, so the pivot row is annihilated and the ZERO MATRIX is returned with no error. That fabrication is cx_inv's squaring, not this guard's \u2014 but it is on the same path and bounds the test window (q <= 511 all correct, measured).",
   "correct_threshold": "Un-square first, then make it relative, AND floor it \u2014 three parts, the third of which the original claim omitted and a probe caught: reject when `max_norm < DBL_MIN` (0x0010000000000000, the `_GEO_F64_TINY` constant 2.10.2 settled on, src/geo.cyr:26) OR `sqrt(max_norm) <= EPSILON_F64 * scale`, where scale = max |entry| of the current column or of m. \u26a0 The claim's bare relative form `sqrt(max_norm) < EPSILON_F64 * max|entry|` REGRESSES a behaviour measured green today: for the all-zero matrix, max|entry| = 0, so the threshold is 0, `0 < 0` is false, the guard does not fire, cx_inv(0) fabricates cx_zero and the function returns a zero MATRIX where it currently returns 0 (probe2 control (A): all-zero 3x3 -> 0, passes on HEAD). The DBL_MIN floor and the `<=` both fix that. \u26d4 AND THE REPAIR IS NOT LOCAL: cx_inv (src/complex.cyr:92-95) has its OWN squared-vs-squared guard that returns cx_zero() for |z| < 1e-12 \u2014 measured to fire at exactly 2^-40 (9.09e-13) and to be clean at 2^-39. Repairing line 1150 alone converts a false \"singular\" into a SILENT zero matrix for every pivot in (1e-154, 1e-12), i.e. trades a wrong error for wrong data. Either repair cx_inv in the same change (guardless reciprocal / Smith's algorithm, guarding only what makes the division fail) or scale the pivot row with a division that does not pre-flush. Prior art for the relative form is in-tree at src/linalg_precision.cyr:442 (`var tol = f64_mul(EPSILON_F64, scale);`) and :534-536 (`threshold = EPSILON_F64 * (|d_i| + |d_i+1|)`) \u2014 both confirmed read.",
   "scale_test": "\u26a0 THE CLAIM'S BRACKET IS WRONG AND MUST BE REPLACED \u2014 measured, not argued. Its stated window p in [-500,+500] with five distinct kill points (shipped p\u2248-20, un-squared p\u2248-40, DBL_EPSILON p\u2248-53, DBL_MIN p\u2248-505, relative never) does not exist: cx_inv's own 1e-12 flush at p = 40 (measured) means EVERY candidate repair of line 1150 fails from p = 40 down \u2014 the un-squared-absolute by returning 0, the DBL_MIN and relative forms by returning the all-zero matrix \u2014 so the sweep's real discriminating window is p in [20, 39] (c in 9.1e-13 .. 9.5e-7), SIX decades, not 485, and it separates shipped-from-repaired only, never repaired-from-repaired. Use this instead, in three parts. (A) FIXED-SCALE KILL, no units change needed and immune to the conditioning defence: for k = 0..60, M_k = 2x2 complex diag(1, 2^-k) \u2014 matrix norm exactly 1, cond 2^k, inverse exactly representable \u2014 assert cmat_inverse(M_k) != 0 and inv[1][1] == 2^k bit-exactly. Shipped fails first at k = 20 (MEASURED); an un-squared absolute repair first fails at k = 40; DBL_EPSILON at k = 53; DBL_MIN/relative never within k <= 60. Four candidates, four distinct kill points, all inside one 60-step loop, and NOTHING in it depends on cx_inv until k >= 40. (B) SCALE COVARIANCE, run only after cx_inv is repaired as above, otherwise it measures cx_inv: M0 = 3x3 identity (cond 1), c = 2^p, assert cmat_inverse(c*M0) != 0 and c * inv[i][j] == M0inv[i][j] bit-exactly (the inverse is covariant as 1/c, so the compared value must be rescaled by c; powers of two make it exact, no tolerance needed). Sweep p in [-1000, +511]: on HEAD this fails from p = -20 (MEASURED) and, at the top, first returns a non-null WRONG (all-zero) matrix at p = +512 (MEASURED, norm_sq overflow inside cx_inv) \u2014 so +511 is the hard ceiling of the window, not +500 by estimate. With cx_inv repaired the candidates re-separate: un-squared-absolute kills at p = -40, DBL_EPSILON at p = -53, DBL_MIN-on-norm_sq at p = -512 (norm_sq underflow), relative never. (C) CONTROLS killing a guard-deleted mutant, both of which the repair must keep green: the all-zero 3x3 must still return 0 (passes on HEAD, MEASURED \u2014 and is exactly what the claim's bare relative form breaks), and a 3x3 with an exactly-zero first column and unit diagonal elsewhere must return 0 rather than nan/zero entries."
  },
  {
   "site": "src/linalg_precision.cyr:240",
   "fn": "_lp_bidiagonalize",
   "guarded_quantity": "vtv = v^T v for the left Householder vector after v[0] += sign(v0)*||x||, i.e. exactly 2*||x||*(||x|| + |x0|), bounded in [2||x||^2, 4||x||^2] where x is the tail of column k (rows k..m) of the working matrix W. Dimension: LENGTH SQUARED, degree 2 in matrix entries. Measured on A(c) = diag(1) (+) c*[[1,2,3],[4,5,6],[7,8,10]]: the k=1 reflector has vtv = 148*c^2, k=2 has ~6*c^2, k=3 (length-1 tail) has 0.174*c^2 \u2014 pure c^2, confirming degree 2. Reachable range is bounded BELOW by the :225 norm gate: norm_l > 1e-12 forces vtv > 2e-24 whenever line 240 is reached at all.",
   "protects": "The division two_over_vtv = f64_div(F64_TWO, vtv) on line 241, and the reflector application W -= (2/vtv)*v*(v^T W) plus the Vt/U accumulations that use it. Nothing else in the branch has a floor.",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "A squared quantity is compared against the unsquared EPSILON_F64 = 1e-12, so the effective cut is on ||x|| ~ 7.07e-7, six decades above the 1e-12 the sibling gate at :225 applies to the UNSQUARED norm of the same vector \u2014 the pair is self-inconsistent by construction, the class repaired at six sites in geo.cyr in 2.10.1/2.10.2. 2/vtv is finite down to vtv ~ 1.1e-308 and :225 already guarantees vtv > 2e-24, so no division can fail here: the guard is mathematically redundant and only its wrong threshold has any effect. All four refutation routes were tested and failed. (a) NOT dimensionless: measured to scale as c^2 exactly. (b) The caller DOES normalise \u2014 svd_golub_kahan:787 balances by _lp_pow2_floor(_lp_mat_max_abs(A)) \u2014 but that bounds only the GLOBAL max; a block-scaled/graded matrix has max = 1, sc = 1, and its column tails are still O(c), so balancing does not reach this quantity. (c) NOT a convergence tolerance: it guards a division one line below. (d) NO existing test constrains it: with :240 changed to vtv > 0, tests/hisab 416, foundation 351, modules 1798, edge_cases 233, abuse 776 = 3574/3574 pass, and the three-site repair passes 3574/3574 too. The positive proof is a one-line mutation on a pristine `git archive HEAD` tree: at c = 1e-7 the shipped code returns HSB_ERR_NO_CONVERGENCE and the same tree with line 240 reading `f64_gt(vtv, f64_from(0))` returns HSB_ERR_NONE with S/c = 17.412505 / 0.875161 / 0.196867, every value correct. Internally the effect is larger: with the guard and its two siblings repaired, _lp_bidiagonalize is bit-stable scale-covariant (d_i/c and f_i/c constant) from c = 1e-1 to c = 1e-12, while the shipped code perturbs the bidiagonal at c = 1e-6, loses the k=2 reflector at 1e-7, and collapses to the raw matrix entries at 1e-8. [measured: 2026-09-09, cycc 6.6.2]",
   "fabricates": "The FILING'S fabrication claim is wrong about the entry point and is corrected here. What _lp_bidiagonalize itself returns is HSB_ERR_NONE with a W that is NOT bidiagonal: the reflector is skipped, lv_tv[k] stays 0, and the extraction at :387 reads only two bands, so the below-diagonal mass is DISCARDED \u2014 silent, and this is the only caller-visible product of the guard. Through the public entry, in the decade this guard alone owns (c = 1e-7 of the 4x4 fixture) the shipped code returns HSB_ERR_NO_CONVERGENCE (-8) with out_S untouched \u2014 LOUD, not a fabricated answer; one decade higher (c = 1e-6) it returns rc = 0 with a silently sign-flipped bidiagonal that happens not to move S. The rc = 0 / S[1]/c = 10.000 regime the filing measured at c <= 1e-13 is NOT this guard: repairing :240 (and even :240+:301+:360) leaves those rows byte-identical to shipped. That regime belongs to the :225 unsquared-norm gate plus the ABSOLUTE EPSILON_F64 block-finder at :580/:590. Worst measured silent output actually attributable to this class is from the half-fix: repairing :240 alone leaves :360 (the U-accumulation gate on the same stored vtv) firing, so the reflector enters W but not U and ||A - U*S*Vt||_F/||A||_F goes 5.0e-13 -> 4.2e-7 at c = 1e-6 with rc = 0.",
   "correct_threshold": "vtv > 0 (equivalently >= DBL_MIN, the _GEO_F64_TINY constant 2.10.2 settled on) \u2014 that is exactly what makes 2/vtv fail, and it also catches the real underflow case where the sum of squares flushes to 0. \u26a0 SHARPENED: the change is only correct applied to ALL THREE sites that gate on this same squared quantity \u2014 :240 (left reflector), :301 (vtv_r, right reflector) and :360 (bvtv, the backward U accumulation, which re-reads the vtv stored at :385). Repairing :240 alone is a MEASURED REGRESSION, not a partial fix: the reflector is then applied to W and to Vt but dropped from U, and the 4x4 fixture's reconstruction error at c = 1e-6 goes from 5.0e-13 (shipped) to 4.2e-7 with rc = 0 (3x3 fixture: 0 -> 8.0e-7). The three-site change passes 3574/3574 assertions and makes _lp_bidiagonalize exactly scale-covariant over 12 decades. Repairing :240 without :360 is the tempting half-fix of 2.12.0's zeroing-loop lesson and must be mutation-refused.",
   "scale_test": "\u26d4 THE FILING'S 150-DECADE SWEEP IS REFUTED BY MEASUREMENT AND MUST NOT BE USED: with `vtv > 0` at :240 the sweep still fails at every c <= 1e-8 (NO_CONVERGENCE 1e-8..1e-12, rc = 0 with S/c = 10/5/1 below), so it passes for NO candidate threshold and discriminates nothing; and `vtv > 1e-24` (EPSILON_F64^2, the obvious squared repair, absent from the filing's threshold list) is BIT-IDENTICAL to `vtv > 0` on every row of two fixtures \u2014 the :225 gate forces vtv > 2e-24 whenever :240 is reached, so no input can bracket any threshold at or below 2e-24. Replace with two assertions. (A) PUBLIC, one decade, the only one this guard alone owns: A = diag(1) (+) c*[[1,2,3],[4,5,6],[7,8,10]] (4x4), c = 1e-7 \u2014 assert svd_golub_kahan returns HSB_ERR_NONE, |S[i]/c - sigma_{i-1}| <= 1e-9*sigma for sigma(M) = (17.412505166808593, 0.87516135, 0.19686652), AND ||A-U*S*Vt||_F/||A||_F <= 1e-12. Shipped: rc = -8. The reconstruction clause is what kills the :240-only half-fix (4.2e-7 at c = 1e-6). (B) INTERNAL, the real bracketing: call _lp_bidiagonalize(A(c), 4, 4, d, f, U, Vt) directly and assert exact scale covariance \u2014 d_i(c)/c and f_i(c)/c equal their c = 1e-1 values to 1e-12 relative \u2014 for c = 1e-1 .. 1e-12, one per decade. Shipped breaks at 1e-6 (sign of d3), 1e-7 (d2, d3) and 1e-8 (total collapse to d/c = 1,5,10 and f/c = 2,6); the three-site repair is bit-stable across all 12. This brackets every candidate T on vtv in (2e-24, 1e-12], since each reflector breaks at c ~ sqrt(T/(2*||x_hat||^2)) and the three reflectors have ||x_hat|| = 8.124, 1.77, 0.209, giving a staircase: T = 1e-12 breaks 1e-6/1e-7/1e-8, T = 2.2e-16 breaks ~1e-8/1e-9, T = 1e-18 breaks ~1e-9/1e-10. Floor at c = 1e-12: at c ~ 1.2e-13 the :225 absolute NORM gate takes over (a different guard) and below ~1.5e-154 the squares underflow, so failures there are not this threshold's artifacts. State plainly that T <= 2e-24 is UNREACHABLE while :225 stands \u2014 it is not a gap in the test, it is a fact about the guard."
  },
  {
   "site": "src/linalg_precision.cyr:301",
   "fn": "_lp_bidiagonalize",
   "guarded_quantity": "vtv_r = v_r^T v_r after the sign shift = 2*||x||*(||x|| + |x1|), where x = W[k, k+1..n-1] is the row-k tail. Dimension: LENGTH SQUARED (degree 2) in the entries of the BALANCED matrix As = A / 2^e. Since 2*norm^2 <= vtv_r <= 4*norm^2, the guard's effective cutoff on ||x|| is 5e-7 .. 7.07e-7, i.e. sqrt(EPSILON_F64), not EPSILON_F64.",
   "protects": "f64_div(F64_TWO, vtv_r) on :302 (and its two reuses in the W and Vt_acc application loops).",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "SURVIVES, but the original reasoning is wrong in its operative half and I have replaced it. (1) The scaling argument is REFUTED by the caller. _lp_bidiagonalize has exactly one caller in the tree (svd_golub_kahan:790), which balances first: sc = _lp_pow2_floor(_lp_mat_max_abs(A)), As = A/sc, so ||As||_max is in [1,2) and the divisor is an exact power of two. Measured on the isolating matrix below: rescaling A by 2^-80, 2^-10, 2^+40, 2^+200 gives singular-value ratios that are BIT-IDENTICAL (max deviation 0.000e+00) and a reconstruction error identically 1.000e-07 at every scale. A units change therefore cannot reach this guard at all, and post-balancing the absolute EPSILON_F64 at :288 is a relative-to-||A||_max drop policy that is defensible on its own terms. (2) What is NOT defensible is the squaring, and that is the whole defect: :288 declares the policy \"an entry below 1e-12*||A|| is negligible\" on a degree-1 quantity, and :301 applies the SAME constant to a degree-2 quantity three lines later, which moves the identical policy's cutoff to sqrt(1e-12) = 7.07e-7*||A|| -- a silent five-to-six-order inflation of the accepted backward error, with rc = HSB_ERR_NONE. Dead band (balanced units): ||x|| in (1e-12, 5e-7..7.07e-7), where :288 says \"keep\" and :301 says \"skip\". The division itself cannot fail there: :288 having passed implies vtv_r >= 2e-24, and 2/vtv_r stays finite down to vtv_r = 1.11e-308, so the guard protects nothing and only its threshold acts. The same file already states the correct principle at :424 (\"The zero test must be RELATIVE to the block\"), and the only relative reference available here is ||As||_max ~ 1, which :288 uses correctly and :301 squares.",
   "fabricates": "Corrected -- the original overstated it. Skipping the reflector leaves W non-bidiagonal to the right of the superdiagonal; the extraction at :387 silently drops those entries, and svd_golub_kahan returns HSB_ERR_NONE. Measured with :301 isolated (no other guard firing): the reconstruction A = U*S*Vt is wrong by exactly the dropped magnitude -- ||U*B*Vt - As||_max / ||As||_max = 1.00e-07 at delta = 1e-6, 1.00e-12 at delta = 1e-11, versus 1.8e-16 with the guard repaired -- and Vt_acc never receives the reflector, so Vt is no longer the right factor. But the SINGULAR VALUES are NOT \"of a different matrix\": a firing :301 means the whole row-k tail INCLUDING the superdiagonal slot is ~0, so the bidiagonal nearly splits and the dropped entries are off-block, killing the first-order perturbation -- 7.4e-13 relative sigma error at delta = 1e-6 in the isolating matrix, worst case 9.10e-07 over a 4000-case near-degenerate search. The damage is bounded by the dropped magnitude (<= ~7e-7 relative), never O(1). ALSO MEASURED AND IMPORTANT: in every failing case ||U^T U - I|| = 2.2e-16, ||Vt*Vt^T - I|| = 5.6e-16, and |sum(sigma^2) - ||A||_F^2| / ||A||_F^2 = 5.6e-16 -- the orthogonality check AND the Frobenius identity the suite already uses for the m &lt; n finding are both BLIND to this defect. Only the reconstruction sees it.",
   "correct_threshold": "vtv_r &gt;= DBL_MIN (_GEO_F64_TINY's rule: guard exactly what makes the DIVISION fail and nothing more). NOT \"vtv_r &gt; 0\" -- the original object called those equivalent and they are not: a subnormal vtv_r makes 2/vtv_r overflow (measured: 2.0/5e-320 = +Inf; the exact finiteness bound is 2/DBL_MAX = 1.11e-308). With :288 standing, the repaired guard is provably dead (vtv_r &gt;= 2*norm_r^2 &gt; 2e-24), which is the point: the negligibility POLICY must live only at :288, where a degree-1 quantity meets a degree-1 constant, and :301 must be a pure divide-by-zero guard. If the policy is instead wanted at both sites it must be squared to match the quantity (vtv_r &gt; EPSILON_F64*EPSILON_F64 = 1e-24), which is behaviourally identical to DBL_MIN here because :288 dominates. VERIFIED NOT TO BREAK EXISTING TESTS, not assumed: I traced all 11 svd matrices in tests/ (lps_A, lpo_A @1e200, lpu_A @1e-200, @1e-10, rank-1 A1, A5 4x3, A7 5x4, An diag(1,2,3), tA 4x3, diag(3,2), lpo_E) -- neither vtv guard fires on any of them, so the repair cannot change a currently-passing assertion.",
   "scale_test": "THE PROPOSED TEST IS REFUTED BY MEASUREMENT -- it does not discriminate, and I ran the exact family given. A(c) = diag(1) (+) c*M, M = [[1,2,3],[4,5,6],[7,8,10]], max relative sigma error, four configurations {shipped, :301 repaired, :240 repaired, both}: c &gt;= 1e-6 passes in ALL four (3e-15); c = 1e-7 gives 5.441e-01 shipped and 5.441e-01 with :301 repaired -- IDENTICAL -- while repairing :240 alone passes (2.7e-15), so the one decade the claim points at attributes entirely to the WRONG guard; c in [1e-8, 1e-12] fails both before (4.279) and after (3.156) the :301 repair, passing only when :240 is also repaired; and c &lt;= 1e-13 fails 4.279 in all four configurations INCLUDING both-repaired, because :225/:288's degree-1 norm guards drop the block outright -- that is the declared policy, not this defect, so roughly 137 of the proposed 150 decades assert something FALSE after a correct repair. Not one decade flips on the :301 repair alone. (Its parenthetical is wrong too: at k=0 row 0's tail is exactly zero, so :288 fires, not :301; :301 first fires at k=1.) REPLACEMENT, measured to discriminate: because the caller's power-of-two balancing makes a units change provably inert (bit-identical sigma over 2^-80..2^+200), the sweep variable must be the INTERNAL RATIO, not the units. Use A(d) = [[1,0,d,d],[0,1,2,3],[0,4,5,6],[0,7,8,10]] -- every left reflector is healthy (column norms 1.25e-1, 1.02, 2.21e-1, 2.61e-2 balanced) and the k=0 right tail is the only quantity in the band, so a failure can only be :301. Assert (a) the reconstruction ||U*S*Vt - A||_max &lt;= 1e-13 * ||A||_max and (b) sigma to 1e-9 relative, for d = 1e-4 down to 1e-11 one decade at a time. Measured: rec/||A|| = 2.7e-16, 6.2e-16, 1.0e-07, 1.0e-08, 1.0e-09, 1.0e-10, 1.0e-11, 1.0e-12 shipped versus &lt;= 3.6e-16 repaired -- six consecutive decades flip, with d &gt;= 1e-5 as the passing control on both sides and d &lt;= 1e-12 as the control proving :288's policy is untouched (1.0e-13 on both sides). BRACKETING: balanced norm_r = 0.1768*d, so the ladder spans 1.77e-05 down to 1.77e-12, bracketing the shipped cutoff 7.07e-7 (d ~ 4e-6), a repair that reaches for machine epsilon 2.22e-16 (cutoff ~1.05e-8, d ~ 6e-8), and :288's own 1e-12 floor; extend to d = 1e-20, 1e-60, 1e-160 as a no-NaN/no-Inf tail proving DBL_MIN and 1e-24 repairs are indistinguishable and neither divides by zero. Add the near-degenerate sigma variant A[0][0] = 0.196866521137 (matching M's smallest singular value) at d = 9.77e-7, where the sigma-only assertion also discriminates (9.10e-07 shipped vs 1.41e-15 repaired) -- without that variant a sigma-only test is second-order blind, and orthogonality / sum-of-squares assertions are blind at every d."
  },
  {
   "site": "src/linalg_precision.cyr:580",
   "fn": "_lp_bidiag_qr",
   "guarded_quantity": "|superdiag[p_hi-1]|, one bidiagonal off-diagonal entry. Dimension: MATRIX ENTRY (degree 1). REFUTATION ATTEMPTED AND FAILED: svd_golub_kahan:789 does balance the input (sc = _lp_pow2_floor(_lp_mat_max_abs(A)); As = A/sc), so ||A||_max is pinned to [1,2) \u2014 but that bounds only the GLOBAL max, not the scale of a sub-block. The entry compared here belongs to whatever unreduced sub-block it sits in, and a matrix with a unit-norm block beside a c-scaled block leaves this entry at O(c) with sc == 1.0 exactly.",
   "guarded_quantity_note": "deflation / block-bound test, not a divisor",
   "protects": "No division. It is the loop bound deciding where the active unreduced block ends, i.e. which superdiagonal entries count as converged-to-zero. Confirmed by reading: nothing between :579 and :582 divides.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "CONFIRMED, and causally isolated by a control build. Three lines above, the deflation loop at :543 tests THE SAME ENTRIES against the relative threshold EPSILON_F64*(|d_i| + |d_{i+1}|); this loop tests them against the bare absolute EPSILON_F64. They disagree exactly when EPSILON_F64*(|d_i|+|d_{i+1}|) <= |f_i| < EPSILON_F64, i.e. for any sub-block with |d_i|+|d_{i+1}| < 1 \u2014 the relative test correctly KEEPS a coupling this loop then walks past, leaving a superdiagonal entry that is still nonzero and now never gets zeroed or chased. Verbatim the defect repaired in the sibling _lp_tridiag_qr in 2.7.0, documented at :1064 ('a second loop whose test was an ABSOLUTE EPSILON_F64, inconsistent with the RELATIVE threshold the deflation test above uses ... the block bounds and the deflation test could contradict each other'); the SVD path never received it. All five refutation avenues were tested and closed: (1) the quantity is degree 1, not dimensionless; (2) the caller DOES normalise but only the global max \u2014 measured, the probe defeats it with sc == 1.0 exactly; (3) it being a convergence test rather than a divide guard is the reason an absolute epsilon is wrong, not a defence; (4) the repair breaks NO existing test \u2014 all five suites, 3611 assertions, pass identically on the shipped tree, the :580-repaired tree, and the fully-repaired tree (so the suite is also blind to the defect); (5) CAUSALITY ISOLATED: a control tree that makes the Givens guards at :650/:692 scale-free while leaving :580 absolute is correct for every c down to 1e-12 and silently wrong from c = 1e-13 \u2014 the cliff sits at exactly EPSILON_F64.",
   "fabricates": "The raw bidiagonal DIAGONAL, returned as singular values with rc = HSB_ERR_NONE. Measured on the shipped tree (probe1, A = diag(1) (+) c*[[1,1],[0,1]], already bidiagonal so every Householder reflector is a no-op and :225/:240 are out of the causal chain): svd_golub_kahan returns rc = 0 with S[1]/c = S[2]/c = 1.000000000 for every c from 1e-13 down to 1e-169, where the truth is 1.618033989 and 0.618033989 \u2014 +61.8% and -38.2%, silently, over ~150 decades. On the causality-control tree (Givens guards scale-free, :580 left absolute) the identical fabrication appears with the cliff at exactly c = 1e-13, confirming this guard alone produces it.",
   "correct_threshold": "The same relative threshold the deflation loop at :543 already computes: EPSILON_F64 * (|diag[p_hi-1]| + |diag[p_hi]|), and zero the entry when the test passes (as the repaired sibling does at :1076) so the block bound and the array agree. NOTE \u2014 since :543 runs first on every iteration and has ALREADY zeroed every relatively-negligible entry, the strictly minimal correct test at :580 is `== 0` (equivalently `< DBL_MIN`); the relative form is behaviourally identical and self-documenting, so either is correct and both are indistinguishable by any test. SCOPE CORRECTION (measured, and the filing was wrong about this): this repair alone makes the failure LOUD, not CORRECT \u2014 it converts the silent rc = 0 into HSB_ERR_NO_CONVERGENCE across c = 1e-13..1e-161, because :650 and :692 compare the Givens radius r = sqrt(y^2+z^2) \u2014 a DEGREE-2 quantity in the shift branch, y = d^2 - mu, z = d*f \u2014 against the same absolute EPSILON_F64 and co-fire on the very inputs this guard was hiding. The two must be repaired together (r needs only `< DBL_MIN`, since r >= 0 and the sole failing case is exactly 0) for the values to come back right.",
   "scale_test": "CORRECTED \u2014 the filed test was unpassable and the wrong shape; both errors measured. (a) A pure UNITS change does not discriminate at all: svd_golub_kahan(c*A) is bit-exactly invariant for power-of-two c because of the pow2_floor balancing at :789, and tests/hisab.tcyr:2102/2122/2179 already sweep 1e200 / 1e-200 / 1e-10 uniformly and pass on the defective tree. The discriminating variable is the RATIO BETWEEN SUB-BLOCKS, not the units. (b) The filed 1e-150 endpoint is unreachable by ANY threshold repair: the Wilkinson shift forms det = a11*a22 - a12^2, DEGREE 4 in the block scale, so c^4 goes subnormal near c = 1e-80. Measured on the fully scale-free tree: exact to c = 1e-79, degraded at 1e-80 (1.618021672), HSB_ERR_NO_CONVERGENCE for 1e-81..1e-161, silently wrong below 1e-162. A fully correct repair would be REPORTED AS FAILING by the filed sweep. USE INSTEAD: A = diag(1) (+) c*B with B = [[1,1],[0,1]] (3x3, already bidiagonal, and the unit entry pins _lp_mat_max_abs at 1 so sc == 1.0 exactly and the balancing cannot mask the sub-block scale). sigma = {1, 1.618033989*c, 0.618033989*c} exactly for c < 0.6. Sweep c = 1e-1 .. 1e-79, ONE ASSERTION PER DECADE, asserting the no-confident-lie property that interrogates THIS guard alone: rc != HSB_ERR_NONE || (|S[1]/c - 1.618033989| <= 1e-9 && |S[2]/c - 0.618033989| <= 1e-9). Measured: FAILS on the shipped tree at every decade 1e-13..1e-79 (rc = 0 with 1.000/1.000); PASSES on the :580-only repair (rc = -8 is permitted) and on the :580 + :650/:692 repair (exact values). 79 decades bracket every threshold a repair might plausibly choose above DBL_MIN \u2014 1e-6 (sqrt EPSILON_F64, in case a repair mistakes the class), 1e-12 (EPSILON_F64, where the measured cliff actually sits), 1e-14, 1e-15, 2.2e-16 (DBL_EPSILON) \u2014 and deliberately stops short of the c^4 underflow so the assertion cannot be failed by a mechanism this guard does not control. Start at 1e-1, not 1e-0: at c = 1 the sorted order ties (S = {1.618, 1, 0.618}) and S[1]/c = 1.0 legitimately. Add a COMPANION assertion demanding rc == HSB_ERR_NONE over the same range to hold the :650/:692 repair, so the two findings cannot be closed by half a fix."
  },
  {
   "site": "src/linalg_precision.cyr:650",
   "fn": "_lp_bidiag_qr",
   "guarded_quantity": "r = sqrt(y*y + z*z), the norm of the RIGHT Givens vector. On the first pass of every chase (k == p_lo) y = d[p_lo]^2 - mu and z = d[p_lo]*f[p_lo] -- entries of B^T*B - mu*I, dimension MATRIX-ENTRY SQUARED (degree 2). On every later pass y = superdiag[k], z = -sn*f[k+1], degree 1. The file proves this against itself: on later passes that same r is stored straight into superdiag[k-1] at :658, i.e. it IS a matrix entry there, while on the first pass :657's `k > p_lo` suppresses that store because r is not one. One variable, two dimensions, one absolute threshold.",
   "protects": "Nothing. cs = f64_div(y, r) and sn = f64_div(-z, r) are computed at :648-649, ABOVE the test, and merely overwritten at :651-652; the division is never gated. The guard only substitutes the identity rotation (cs=1, sn=0). It is not the deflation/convergence criterion either -- that is :543, and it is already correctly written relative to |d[i]|+|d[i+1]|.",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "CONFIRMED, and the squared-vs-unsquared dimension is now proven QUANTITATIVELY rather than argued. Substituting cs=1,sn=0 makes the implicit-shift step the identity (bulge = -sn*d[k+1] = 0), the block is reproduced unchanged, and the outer loop spins to max_iter -- the same failure the file already documents at :406 for the zero-diagonal case, reached by the guard instead of by a zero. Isolation: swapping ONLY this one constant to F64_TINY (DBL_MIN, already defined at src/error.cyr:45) recovers the entire 1e-7..1e-11 band on the shipped tree, so the band attributes to this line alone. Dimension proof: with r = 1.9021*c^2 on the first pass, the last-clean decade must go as sqrt(threshold). Measured over four thresholds spanning 10 decades -- 1e-6 -> 1e-3, 1.4901e-8 -> 1e-4, 1e-12 (shipped) -> 1e-6, 2.2204e-16 -> 1e-7 -- all 4 match the sqrt law exactly, where a degree-1 quantity would have given 1e-6 / 1e-8 / 1e-12 / 1e-15, off by up to 8 decades. \u26a0 TWO CORRECTIONS TO THE FILING, BOTH MEASURED. (1) The caller DOES normalise, and the filing's scale-test rationale is wrong because of it: svd_golub_kahan:789 balances by _lp_pow2_floor(_lp_mat_max_abs(A)), an exact power-of-two divisor, so UNIFORM rescaling is exactly invariant -- probe B measured c*[[1,1,0],[0,1,1],[0,0,1]] over 20 decades and got identical singular values at every one, which is also why the existing suite's 1e-200 / 1e+200 / 1e-10 tests pass. A true \"same problem in different units\" test can NEVER reach this guard. The quantity that scales is the ACTIVE BLOCK's magnitude relative to ||A||_max -- the condition number, not the unit -- so the discriminating input must be MIXED-scale. The filing's matrix is right; its stated reason is not. (2) The filing's 150-decade span is not satisfiable by ANY repair at :650: below c = 1e-12 the independent ABSOLUTE EPSILON_F64 superdiagonal tests at :557/:580/:590 take over (measured: patching those as well moves the wrong-value onset from 1e-13 to 1e-12), so those decades test other guards and do not attribute. Refutation avenues checked and all closed: the quantity is not bounded (:658 proves it), the normalisation does not reach it, it is not a convergence tolerance, and the proposed repair breaks no existing test -- 3611/3611 assertions pass across all five suites both before and after, identical counts.",
   "fabricates": "HSB_ERR_NO_CONVERGENCE (-8) with out_S never written -- confirmed by pre-filling out_S with a sentinel and reading it back untouched, so a caller that zero-filled out_S reads all zeros and a caller that did not reads uninitialised memory. Measured on the shipped tree with A = diag(1) (+) c*[[1,1],[0,1]]: correct through c = 1e-6, rc = -8 for c = 1e-7 .. 1e-12 (five full decades of ordinary conditioning, cond(A) ~ 1.6e7 at the onset), then from 1e-13 down a DIFFERENT, silent fabrication belonging to :557/:580/:590 -- rc = 0 with S = (1, c, c), the raw diagonal, where the truth is (1, 1.618c, 0.618c).",
   "correct_threshold": "F64_TINY (= DBL_MIN, src/error.cyr:45) -- i.e. guard exactly what makes the division fail and nothing more, the same rule and the same named constant 2.10.2 settled on for _GEO_F64_TINY. |y|/r and |z|/r are both bounded by 1 so no overflow is possible; only r == 0 can fail. MEASURED, not proposed: this one-constant change recovers c = 1e-7..1e-11 in full and is behaviour-neutral on the entire existing suite (3611/3611 both sides). \u26a0 Do NOT take the filing's relative alternative -- EPSILON_F64*(|d[p_lo]|^2+|mu|) on the first pass and EPSILON_F64*(|d[k]|+|f[k]|) later. It is unnecessary (DBL_MIN already recovers the whole reachable band) and it would require two separate tests inside one loop body for no measured gain. The sibling guard at :692 has the same shape but is degree 1 on every pass (its r is stored into diag[k] at :697), so it is a smaller, separate instance and F64_TINY is right there too. SEPARATELY FILED, not folded in: :557, :580 and :590 test |superdiag| against an ABSOLUTE EPSILON_F64 and are the reason nothing below c = 1e-12 can be tested through this entry point; they are genuine convergence criteria and need a threshold relative to the block (:543 already computes one), NOT DBL_MIN -- moving them to DBL_MIN removes convergence detection outright and reintroduces sporadic rc = -8 at 1e-16 and 1e-20.",
   "scale_test": "A CONDITIONING sweep, not a unit sweep -- the balancing at :789 makes a unit sweep vacuous, so state that in the test comment or the next reader will write the wrong one. A = diag(1) (+) c*B with B = [[1,1],[0,1]] (already bidiagonal, so :225/:240/:288/:301 are no-ops and the sweep attributes to _lp_bidiag_qr). sigma = {1, 1.618033988749895c, 0.618033988749895c} exactly. Assert per decade: rc == HSB_ERR_NONE, S[0] == 1, and S[1]/c, S[2]/c within 1e-9 of 1.618033989 / 0.618033989. SPAN c = 1e-1 .. 1e-11, ELEVEN decades -- NOT the filed 150. Below 1e-12 the absolute-EPSILON_F64 tests at :557/:580/:590 fabricate S = (1,c,c) with rc = 0 under any repair to :650, so a longer sweep fails for a reason that is not this guard; extend it to 1e-15 only in the same change that makes those three relative. Eleven decades is enough because it BRACKETS every candidate, each with a distinct measured onset: 1e-6 = sqrt(EPSILON_F64) fails from 1e-4, 1.4901e-8 = sqrt(DBL_EPSILON) from 1e-5, 1e-12 = EPSILON_F64 (shipped) from 1e-7, 2.2204e-16 = DBL_EPSILON from 1e-8, and F64_TINY = DBL_MIN is clean throughout -- so a mutant carrying any of the four dies, and the four onsets also fit onset = sqrt(T/1.9021) 4-for-4, which is itself the discriminating evidence that the quantity is squared. \u26a0 ONE PAIR IT CANNOT SEPARATE: sqrt(DBL_MIN) = 1.4917e-154 and DBL_MIN are both clean across all eleven decades, because separating them needs c near 1e-78 and the co-defects block everything below 1e-12. Record that as a known gap rather than claiming full coverage; separating that pair requires probing _lp_bidiag_qr directly with a hand-built diag/superdiag, or repairing :557/:580/:590 first."
  },
  {
   "site": "src/linalg_precision.cyr:692",
   "fn": "_lp_bidiag_qr",
   "guarded_quantity": "r = sqrt(y*y + z*z) for the LEFT Givens rotation, y = diag[k] (the value just stored at :670, new_dk = cs*dk - sn*fk) and z = bulge = -sn*d[k+1] (:667). Dimension: MATRIX ENTRY, degree 1 \u2014 it is the 2-norm of rows (k,k+1) of column k of the working bidiagonal after the right rotation. Because svd_golub_kahan divides A by _lp_pow2_floor(max|A|) at :789 before bidiagonalising, r is effectively a FRACTION OF ||A||_max, not an absolute physical magnitude; the filing's \"it scales with the active block\" is right in spirit but wrong in mechanism (see why).",
   "protects": "cs = f64_div(y, r) at :690 and sn = f64_div(f64_neg(z), r) at :691 \u2014 both computed ABOVE the guard and then overwritten, so only the identity substitution is gated. Confirmed: the divisions execute unconditionally. Note :697 `store64(diag + k*8, r)` is also unconditional, so when the guard fires diag[k] is set to the sub-threshold r while superdiag[k]/diag[k+1] are left untouched by cs=1, sn=0 \u2014 the block is reproduced exactly.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "NOT REFUTED, but three of the filing's claims are refuted by measurement and are corrected here. (1) The quantity is degree 1 (a matrix entry of the balanced bidiagonal); the threshold is the absolute EPSILON_F64 = 1e-12 (src/error.cyr:26). The division fails only at r = 0; cs, sn are bounded by 1, so there is no overflow path \u2014 1e-12 is a chosen scale, which by the 2.10.2 rule is itself the defect. (2) REFUTED \u2014 the filing's causal story (\"for a sub-block at scale c, r ~ c, so the guard fires once c < 1e-12\"). Instrumented counters on a copy of the module (guard-fire counters at :650 and :692, at the relative deflation :543, at the ABSOLUTE p_hi loop :580, and at _lp_split_zero_diag) over the filing's OWN sweep, c = 1e-0 .. 1e-20: g692 = 0 at every one of 21 decades. The c in [1e-7, 1e-12] non-convergence is :650 alone (g650 = 270 = max_iter = 30n^2, rc = -8), and the silent 1.000/1.000 at c <= 1e-13 the filing attributes to THIS guard is the absolute superdiagonal test at :580 (phi_defl = 1, g692 = 0, g650 = 0) \u2014 the block is deflated away before the chase is ever entered. (3) REFUTED \u2014 a units-only scale test cannot discriminate this guard at all, because the caller DOES normalise: _lp_pow2_floor makes A and 2^k*A bit-identical (48/48 rows of a x2^+40 / x2^-40 control identical in rc, S and every counter), and a non-power-of-2 x10^-j rescale moves the fired r only within [9.4e-15, 1.6e-14] \u2014 the balancing residue in [1,2) \u2014 never changing the outcome. What the balancing CANNOT remove is INTERNAL grading, and that is the real mechanism: on 11000 random n=2..6 matrices with per-entry exponents uniform in [-E,E], the guard first fires at E = 4 and fires on 484 of 11000 overall (by E: 1/12/45/59/83/70/67/70/77 for E = 4..20). It is decisive and it is NOT shadowed by :650: on a differential run of the identical RNG stream against a copy with only :692 changed to F64_TINY, 218 trials change (rc, S), 14 are materially worse on shipped (>=10x and >1e-8 relative reconstruction error), and 11 of those 14 have g650 = 0 with split = 0 and phi_defl = 0 \u2014 this guard firing alone. Reachability mechanism: r^2 = (cs*dk - sn*fk)^2 + (sn*dk1)^2 can collapse mid-chase even though _lp_split_zero_diag guarantees every diag >= 1e-12*||block|| BEFORE the sweep, because dk1 is rewritten at :672/:704 during the chase and the split test never runs again.",
   "fabricates": "Measured, and NARROWER than the filing claims. The observed mode is the STALL branch only: cs = 1, sn = 0 leaves superdiag[k] and diag[k+1] unchanged while :697 writes the sub-1e-12 r into diag[k], so the sweep reproduces the block and fires again on every one of the 30*n^2 sweeps (fixture: g692 = 480/480 for n = 4, 1080/1080 for n = 6), after which svd_golub_kahan returns HSB_ERR_NO_CONVERGENCE at :798 with out_S NEVER WRITTEN \u2014 the caller keeps whatever its buffer held (all zeros in the probe, and svd_golub_kahan's own tests at tests/hisab.tcyr:2197-2200 already record that this makes \"s == 0\" assertions vacuous). 14 of 14 materially-wrong shipped trials had exactly this shape. The filing's SECOND branch \u2014 \"mid-chase, a dropped bulge with rc = HSB_ERR_NONE and singular values of a matrix that is not A\" \u2014 was NOT OBSERVED: of the 484 firing trials, 0 returned HSB_ERR_NONE with a wrong answer attributable to this guard alone (all 10 fire-alone trials returned -8). So the failure is LOUD, not silent \u2014 which lowers the severity the filing assigned it, and the filing's attribution of probe2's silent 1.000/1.000 region to this guard is measured false.",
   "correct_threshold": "r >= F64_TINY (DBL_MIN, error.cyr:45 \u2014 the same constant geo.cyr already uses as _GEO_F64_TINY), i.e. guard exactly what makes the division fail and nothing more. Measured against the alternatives on the 11000-trial graded corpus, bad trials (rc != 0 or reconstruction rel err > 1e-8): 1748 at the shipped 1e-12, 1743 at 1e-14, 1742 at 2.22e-16, 1741 at DBL_MIN \u2014 monotone, with 7 trials fixed and 0 broken by DBL_MIN versus shipped, and the candidates differing pairwise on 54-112 trials, so the corpus ranks them rather than merely tying them. A RELATIVE alternative (EPSILON_F64 * (|diag[k]| + |diag[k+1]|), consistent with :543) is NOT justified here and should not be adopted: the caller has already balanced, r is the very quantity the division consumes, and the identity rotation is mathematically correct only at r = 0. VERIFIED SAFE, not assumed: patching only :692 to F64_TINY in an isolated tree passes all five suites unchanged \u2014 416/359/1827/233/776 = 3611 assertions, 0 failed, byte-identical pass counts to a control tree cut from the same snapshot with the line reverted. TWO HONEST CAVEATS. (a) The repair is not monotone at the individual-trial level: idx 6873 (n = 6, E = 12) goes from rc = 0 with 3.82e-8 error to rc = -8 under EVERY candidate threshold including 1e-14. (b) Removing the fabrication does not make the graded case correct \u2014 1741 of 11000 trials are still bad after the repair, so :692 is one defect in a sweep that also has :650 (a squared/unsquared guard on the degree-2 shift quantity) and the ABSOLUTE superdiagonal tests at :580/:590; fixing this one alone must not be reported as fixing graded SVD.",
   "scale_test": "The filing's proposed sweep is REFUTED and must be replaced \u2014 svd_golub_kahan(diag(1) (+) c*[[1,1],[0,1]]) over c = 1e-1..1e-150 never reaches this guard (g692 = 0 at all 21 decades measured), so it discriminates :650 and :580 and says nothing about :692. Use three parts. (A) REGRESSION, deterministic, cheap: the measured 4x4 fixture A = [[-3.39e6, -2.18e-9, -7.45e-3, 3.27e-7], [1.30e-6, 5.09e-9, -9.90e-7, 885.0], [1.59e-4, 8.20e-8, -30.0, 4.63e-9], [7.15e5, -7.05e-8, 6.23e3, 7.41e-5]] (exact bits c149dd1800000001 be22b9dee6015fa7 bf7e83e425aee632 3e95f1d135899c18 / 3eb5cf751db94e6b 3e35dc83a1a72e7f beb09c0482f18c75 408ba80000000000 / 3f24d72799a1fd16 3e7602ff4171c2ee c03e000000000000 3e33e2bd24654675 / 4125d1f000000000 be72ecb91dbac85f 40b8560000000000 3f136cc44e7909d7). Shipped: rc = HSB_ERR_NO_CONVERGENCE, out_S untouched, :692 fires 480/480 sweeps at r = 1.4678e-14, :650 fires 0 times, split 0, phi 0. Assert rc == HSB_ERR_NONE and ||A - U*S*Vt||_max / ||A||_max < 1e-6; reference spectrum (numpy) 3.464582e6, 6.095960e3, 885.0, 8.1657e-8, repaired code returns 3.464582e6, 6.095960e3, 885.0, 3.096e-8 at 3.04e-12 reconstruction. A second fixture at 7e28 dynamic range (bits 3ea28e3bdc3df2c4 bc7fb49140a16450 bfee8f5c28f5c28f 415ab3f000000001 / 425da0b0e8800000 bda5982b259b8f51 3de68a3cad82d3e7 bc67fb1390c48b2b / bc76b669ea57b01f 3cff13288bb5747e bf4797cc39ffd60e bfea45a1cac08312 / beae667b11ccb9b5 4265465fd9c00000 bc867b6257b8427a 4023147ae147ae14) behaves identically: shipped -8, repaired 7.310e11, 5.090e11, 7.000e6, 7.2011e-4 matching numpy. (B) BRACKET \u2014 a single fixture fires at r ~ 1.5e-14 and therefore CANNOT separate 1e-14 from 2.22e-16 from DBL_MIN; the corpus does. 11000 matrices, n = 2..6, per-entry value = mantissa in [-1,1] (3 digits) times 10^e with e uniform in [-E,E] for E = 0,2,...,20, 200 per (n,E), from a fixed LCG so it is deterministic; assert rc == HSB_ERR_NONE and reconstruction < 1e-8, and record the bad-trial count. It brackets all four candidates (1748 / 1743 / 1742 / 1741) and every pair of repairs differs on 54-112 trials. (C) THE CONTROL THAT MUST NOT MOVE, and which the filing got backwards: the SAME problem in different UNITS. Multiply either fixture by 2^+40 and 2^-40 and assert bit-identical rc, S, U, Vt (measured: 48/48 rows identical); by 10^-j for j = 0..14 and assert the outcome is unchanged (measured: r at firing stays in [9.4e-15, 1.6e-14], rc = -8 throughout). This is a real property of the entry point \u2014 _lp_pow2_floor balancing \u2014 and its presence is exactly why the discriminating grading must be INTERNAL to the matrix, not applied to all of it."
  },
  {
   "site": "src/linalg_precision.cyr:936",
   "fn": "_lp_tridiagonalize",
   "guarded_quantity": "vtv = v^T v after the same-sign Householder shift. _lp_sign returns +1 for x >= 0, so the shift never cancels and vtv = 2*||x||*(||x|| + |x1|) exactly, in [2||x||^2, 4||x||^2]. Degree 2 in the trailing column x = W[k+1..n, k]. CORRECTION to the filing: it is NOT an absolute length squared. eigen_qr:1245 balances first (sc = _lp_pow2_floor(_lp_mat_max_abs(A)); As = A/sc), so vtv is measured in units of max|A_ij|^2 and is therefore DIMENSIONLESS -- but it is the SQUARE of a dimensionless ratio (the trailing column's magnitude relative to the largest entry anywhere in A, i.e. the matrix's dynamic range), while :922 tests the FIRST power of that same ratio against the same constant. Degree 2 vs degree 1 against one shared 1e-12 is the defect; \"different physical units\" is not.",
   "protects": "two_over_vtv = f64_div(F64_TWO, vtv) on :937, and the reflector's whole symmetric rank-2 update plus the Q accumulation built from it. It is a divide-by-zero guard, not a convergence tolerance -- the division is on the very next line, and the convergence tolerances in this file (:1050, :1076) are separate and correctly RELATIVE.",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "REFUTATION ATTEMPTED ON FIVE FRONTS; THE VERDICT SURVIVES, THE scale_test DOES NOT. (1) \"Dimensionless/bounded so absolute is fine\" -- half true and it does not save the guard. The balancing at eigen_qr:1245 does bound vtv above (max|A_ij| in [0.5,1) after _lp_pow2_floor, so vtv <~ 4n), which is why the existing suite's \"eigen_qr @1e200\" test (tests/hisab.tcyr:2139) passes and why the routine is scale-invariant under a UNIFORM rescale. But balancing normalises the GLOBAL scale, not the trailing column's: a matrix with an O(1) entry anywhere keeps its small sub-blocks small after balancing, so vtv still ranges over the full dynamic range of the input, and a degree-2 quantity against 1e-12 fires 5-6 decades before the degree-1 gate at :922 that guards the same reflector. (2) \"The caller normalises\" -- it does, and I MEASURED that it is not enough: shipped code, A(c) = diag(1) (+) c*S3, n=4, returns the WRONG spectrum with rc = HSB_ERR_NONE on 12 of 45 sweep points (c = 3e-7 through 7e-12). (3) \"Convergence tolerance\" -- no, it guards f64_div on :937. (4) \"The repair breaks an existing test\" -- CHECKED, not assumed: with `if (f64_gt(vtv, f64_from(0)) == 1)` installed, all five suites pass, 416 + 359 + 1827 + 233 + 776 = 3611 assertions, 0 failures. (5) \"The scale_test does not discriminate\" -- THIS ONE LANDED, TWICE, and the finding is corrected below rather than withdrawn. The guard is in fact unreachable-false: :922 guarantees norm > 1e-12, and vtv >= 2*norm^2 > 2e-24, so 2/vtv is finite by construction and only the threshold ever acts. Measured onset brackets the prediction exactly: vtv = 6.828c^2 crosses 1e-12 at c = 3.827e-7, and the sweep is correct at 7e-7 and wrong at 3e-7.",
   "fabricates": "The eigenvalues of a DIFFERENT tridiagonal matrix, with rc = HSB_ERR_NONE. The skipped reflector leaves W[k+2..n, k] nonzero and the extraction at :1024 reads only the two bands, discarding it. MEASURED on the shipped 2.13.0 tree, A(c) = diag(1) (+) c*S3 with n = 4 and max_iter = 100000: lambda/c = (1.414213562, -1.414213562, 0.000000000) where the truth is (2, -1, -1) -- 29% wrong on the dominant eigenvalue and an eigenvalue reported as 0 that is really -c. That is the spectrum of [[0,c,0],[c,0,c],[0,c,0]], the matrix that survives the skipped reflector. CORRECTION to the filing's extent: the fabricating window is bounded on BOTH sides and is about 5.7 decades, not open-ended. Correct at 7e-7; wrong-with-rc-0 at 3e-7, 1e-7, 3e-8 ... 7e-12 (12 consecutive sweep points); and from c = 1e-13 downward it stops fabricating and returns HSB_ERR_NO_CONVERGENCE (rc = -8) all the way to 1e-160, because the degree-1 gate at :922 (norm = 1.414c vs 1e-12) fires there and the Wilkinson-shift guards at :1098/:1103 then stall the iteration. Loud failure below the window, silent wrong answers inside it.",
   "correct_threshold": "vtv > 0 (equivalently >= DBL_MIN) -- exactly what makes 2/vtv fail and nothing more, the _GEO_F64_TINY = DBL_MIN rule from 2.10.2. SHARPENED by measurement: at this site `vtv > 0`, `vtv >= DBL_MIN` and `vtv > EPSILON_F64*EPSILON_F64` are not three candidate repairs to choose between, they are the SAME repair -- provably (the upstream :922 gate forces norm > 1e-12, and the same-sign shift makes vtv >= 2*norm^2 > 2e-24 > EPSILON_F64^2) and measurably (three separately compiled builds classify identically on all 45 sweep points). The guard is therefore dead code that can only misfire, so deleting it outright is equally correct; `vtv > 0` is the defence-in-depth form and is what I verified against the suites. Do NOT reach for a norm^2-relative threshold: it is unnecessary here and would only re-introduce a tuned scale.",
   "scale_test": "THE FILED scale_test IS WRONG TWICE AND MUST NOT BE USED AS WRITTEN -- both refutations measured, not argued. (A) \"The SAME problem in different units\" is VACUOUS on this routine. eigen_qr's first act is to balance (:1245, sc = _lp_pow2_floor(_lp_mat_max_abs(A)); As = A/sc), so a uniform rescale is undone before _lp_tridiagonalize ever sees it: A = c*S3 alone (n=3, eig = 2c,-c,-c) returns the exact spectrum on the SHIPPED, DEFECTIVE code at every decade from c = 1e-1 to 1e-300 (measured, 33 points, all correct). Anyone re-deriving the test from that phrase writes a sweep that passes on the bug. The discriminating variable is DYNAMIC RANGE, not units: an O(1) anchor must PIN the balance factor while the block shrinks. The filing's own matrix happens to do this (the leading 1 holds max_abs at 1) but its rationale does not say so. (B) The 150-decade range FAILS 138 of its 150 points WITH THE PROPOSED REPAIR INSTALLED. Measured: with `vtv > f64_from(0)` at :936, the sweep is correct on c = 1e-1..1e-12 and returns HSB_ERR_NO_CONVERGENCE on every decade from 1e-13 to 1e-160 -- because four OTHER absolute-EPSILON_F64 guards floor it, none of them this one: :922 (norm > EPSILON_F64, norm = 1.414c, fires at c <= 7.07e-13), and the Wilkinson-shift pair :1098/:1103 (|delta| and |denom| vs EPSILON_F64, both degree 1) which strip the shift and stall unshifted QR on the repeated eigenvalue -c. Freeing all five (adding :1117, r > EPSILON_F64) makes the identical sweep correct down to 1e-154 and only then failing on c^2 underflow. A test floored at 1e-150 would read as \"the repair does not work\" and get the repair reverted. THE CORRECTED TEST: A(c) = diag(1) (+) c*S3, n = 4, S3 = [[0,1,1],[1,0,1],[1,1,0]], eig(S3) = (2,-1,-1) exactly, so eig(A) = {1, 2c, -c, -c}. S3 is load-bearing: it makes the k=1 reflector's v = c*[1,1] have BOTH components nonzero, where a single-nonzero column is a sign flip whose skip is harmless and would pass vacuously. Sweep c = 1e-1 down to 1e-12, one assertion per decade, PLUS 7e-7 and 3e-7 to bracket the crossing at vtv = 6.828c^2 = EPSILON_F64 (c = 3.827e-7); assert rc == HSB_ERR_NONE and (lambda1,lambda2,lambda3)/c == (2,-1,-1) to 1e-9 absolute. Cap at c <= 1e-1 (at c = 0.7, 2c exceeds the anchor 1 and the descending-|lambda| sort permutes the output slots -- a probe artifact that reported \"wrong\" for every threshold including the correct one, caught only because all five builds agreed on it). FLOOR at 1e-12 and say why in the test comment, or file :922/:1098/:1103 as the sibling defects of the same class and extend to 1e-154 only after they are repaired too. DISCRIMINATION over that range, measured on five separately compiled builds: shipped EPSILON_F64 wrong on 12 points; DBL_EPSILON (2.22e-16) wrong on 6 points (3e-9..7e-12) -- killed; sqrt(DBL_EPSILON) (1.49e-8) would be wrong from c <= 4.7e-5 -- killed with a decade to spare; `vtv > 0`, `>= DBL_MIN` and `> EPSILON_F64^2` correct on all 45 and mutually indistinguishable, which is a theorem about :922 rather than a gap in the sweep."
  },
  {
   "site": "src/linalg_precision.cyr:1103",
   "fn": "_lp_tridiag_qr",
   "guarded_quantity": "|denom| where denom = delta + sign(delta)*sqrt(delta^2 + e2^2), delta = (d1-d2)/2. Dimension: MATRIX ENTRY (degree 1) in the entries of the BALANCED tridiagonal T; with the correct sign |denom| >= |e2|. Refined against the caller: eigen_qr divides A by _lp_pow2_floor(_lp_mat_max_abs(A)) at :1243 before tridiagonalizing, so the quantity is INVARIANT to the caller's units (a power-of-two rescale is bit-exact; any rescale moves it by <2x) and scales only with the ACTIVE SUB-BLOCK's magnitude relative to the balanced whole. Measured on the shipped tree: the same problem at s = 1e-200, 1e-200... 1, 3, 1024 gives an identical pass/fail structure \u2014 the failure boundary tracks the sub-block ratio alone.",
   "protects": "f64_div(e2sq, denom) in the Wilkinson shift at :1104. With the correct sign the division cannot fail: |e2sq/denom| <= |e2|, and denom == 0 implies e2 == 0, where the fallback mu = d2 is the right answer anyway. The one path that can reach a true 0 with e2 != 0 is the sign-forcing branch at :1098 (sgn := +1 whenever |delta| < EPSILON_F64, even for delta < 0), where delta + sqrt(delta^2+e2^2) cancels; that is why a zero guard is still warranted, and it is the ONLY thing this guard legitimately protects.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "Degree-1 quantity, absolute threshold (EPSILON_F64 = 1e-12), on a value that scales with the sub-block, not with the balanced whole. None of the refutation routes hold: the quantity is not dimensionless; the caller's balancing normalises only the WHOLE-matrix scale (measured invariant over 400 decades of units) and does nothing to the sub-block ratio, which is the axis that actually reaches the guard; the guard is a divide-by-zero guard, not a convergence tolerance (the deflation tolerances at :1050 and :1076 are relative and separate); and the proposed threshold breaks no existing test \u2014 all five suites are 3574/3574 with it installed. Instrumented counters confirm the guard fires on well-posed input: on a 4x4 with an O(1) leading block and a trailing 2x2 of scale t it fires 3 of 4 shift computations for every t <= 1e-13, permanently substituting mu = d2 (Rayleigh) for the Wilkinson shift. BUT TWO OF THE FILING'S OWN CLAIMS ARE WRONG, BOTH FOUND BY MUTATION RATHER THAN BY ARGUMENT. (1) THE EVIDENCE IS MISATTRIBUTED: on the dense fixture at c = 1e-12 the instrumented shipped build fires this guard ZERO times (g1103f=0, g1103p=31) \u2014 the silent rc = 0 answer wrong by 0.98% is produced entirely by the OTHER absolute epsilon in the same loop, `f64_gt(r, EPSILON_F64)` at :1117, which fires 30 times; repairing :1117 alone makes that whole sweep exact to 1e-20, and repairing :1103 alone leaves it bit-identical to shipped. (2) EACH GUARD MASKS THE OTHER, so the site is real but not independently observable: on the antisymmetric fixture, shipped fails from 1e-13, fix-1103-only fails from 1e-13 (identical output), fix-1117-only fails from 1e-13 (identical output), and only fix-BOTH converges \u2014 because with mu = d2 = 0 the rotation radius is r = c and with mu = -c it is sqrt(2)c, and both are below 1e-12 exactly where denom is, so :1117 zeroes the rotation before the shift quality can matter. On a generic tiny block (nonzero diagonal) the Rayleigh fallback still converges and the isolated effect of this guard is 1-2 ULP; the consequential effect is confined to the zero-diagonal cycle, where it is total. So: real defect, correctly diagnosed as an absolute epsilon on a degree-1 quantity, with a repair that is a measured NO-OP unless :1117 is repaired in the same change.",
   "fabricates": "Once :1117 is repaired (and only then), HSB_ERR_NO_CONVERGENCE with out_vals never written \u2014 the caller's zero-filled buffer reads as an all-zero spectrum, i.e. a plausible \"this matrix is singular\" answer for a matrix whose true spectrum is {1, +c, -c}. Measured (fix-1117-only build): eigen_qr(diag(1) (+) c*[[0,1],[1,0]]) returns rc = -8 with all three eigenvalue slots left at 0.0 for every c <= 1e-13, and rc = 0 with bit-exact +-1 ratios for every c >= 1e-12. On the shipped tree it fabricates NOTHING ON ITS OWN: the two rc = -8 / silent-0.98%-error observations in the original filing both reproduce with this guard hard-wired open, so they are :1117's fabrications, not this one's. On a tiny block with a nonzero diagonal (Rayleigh fallback still converges) the isolated damage is 1-2 ULP in the returned eigenvalues, rc = 0.",
   "correct_threshold": "denom != 0 (equivalently |denom| >= DBL_MIN; error.cyr now carries F64_TINY = 0x0010_0000_0000_0000 for exactly this). A relative alternative EPSILON_F64 * (|d1| + |d2| + |e2|), matching the deflation threshold this same function uses at :1050 and :1076, also passes the whole sweep. \u26a0 LAND IT TOGETHER WITH :1117 (`f64_gt(r, EPSILON_F64)` -> `r != 0`, r = hypot(x,z), also degree 1): measured, this repair alone changes nothing on either fixture in the filing, and a reviewer who lands it alone, sees no change, and reverts it will have \"refuted\" a real defect. Neither half is redundant \u2014 :1117 alone fixes the dense/silent-wrong case and :1103 alone fixes nothing, but only both together fix the zero-shift cycle. Verified non-regressing: 416+351+1798+233+776 = 3574/3574 assertions pass with :1103 alone repaired and with both repaired.",
   "scale_test": "PAIRED-MUTANT SWEEP; the unpaired form is measured not to discriminate. Fixture (correct as filed): eigen_qr(diag(1) (+) c*[[0,1],[1,0]], n=3) \u2014 spectrum {1, +c, -c} EXACTLY for every c, and the block is the classical zero-shift cycle, so it is the one family where losing the Wilkinson shift is fatal rather than merely 1-2 ULP. Sweep c = 1e-1 down to 1e-150, one assertion per decade: rc == HSB_ERR_NONE, lambda[1]/c == +1 and lambda[2]/c == -1 to 1e-12. Run it against FOUR builds and require all four rows, or it proves nothing: shipped -> rc=-8 from 1e-13 (measured); :1103 repaired alone -> rc=-8 from 1e-13, byte-identical to shipped (measured); :1117 repaired alone -> rc=-8 from 1e-13, byte-identical to shipped (measured); BOTH repaired -> rc=0 with bit-exact +-1 through 1e-150 (measured; convergence actually holds to 1e-157, degrades 1e-158..1e-161 and dies at 1e-162 where e2*e2 underflows into the subnormals). Add the dense control, which separates the two sites: diag(1) (+) c*[[4,1,0],[1,3,1],[0,1,2]], n=4, asserting lambda/c == {4.732050808, 3, 1.267949192} \u2014 shipped is SILENTLY wrong at c = 1e-12 (4.685623325, rc = 0) and repairing :1103 alone does not move it by one ULP, while repairing :1117 alone makes it exact at every decade to 1e-20. \u26a0 DO NOT write this as a units test: eigen_qr balances by _lp_pow2_floor(_lp_mat_max_abs(A)), so \"the same problem in different units\" is measured invariant (s = 1e-200 .. 1e+200, powers of two bit-exact) and such a test passes vacuously on the shipped tree; the discriminating axis is the sub-block ratio c. Brackets 1e-12 (EPSILON_F64), 2.2e-16 (DBL_EPSILON), 1e-14/1e-15 and 1e-6 (sqrt EPSILON_F64, for a repair that mis-reads the dimension). It does NOT bracket DBL_MIN \u2014 the e2*e2 underflow floor at ~1e-161 makes every threshold below that unreachable through this entry point, so `|denom| >= DBL_MIN` and `denom != 0` are indistinguishable here and both are acceptable; the original filing's claim that 150 decades brackets 2.2e-308 is false."
  },
  {
   "site": "src/linalg_precision.cyr:1117",
   "fn": "_lp_tridiag_qr",
   "guarded_quantity": "r = sqrt(x*x + z*z), the Givens radius of the bulge-chasing rotation. At k == p_lo, x = diag[p_lo] - mu and z = offdiag[p_lo+1]; at k > p_lo (where it actually fires, measured) x = offdiag[k] as just rewritten and z = sn*offdiag[k+2], the bulge. Dimension: MATRIX ENTRY (degree 1), scaling with the active deflated block -- and, decisively, with the QR iteration's own convergence tail, since driving these offdiagonals to zero IS the algorithm.",
   "protects": "cs = f64_div(x, r) and sn = f64_div(z, r). The guard is correctly PLACED (before the divisions, unlike :650/:692), so it does gate them; only its threshold is wrong.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "The defect is real and I isolated it, but the filing's stated ARGUMENT for it is refuted and its scale_test proves nothing about this site. (1) REFUTED -- the units framing. _lp_tridiag_qr has exactly ONE caller, eigen_qr:1251, and it balances immediately before at :1243 (sc = _lp_pow2_floor(_lp_mat_max_abs(A)); As = A/sc, an exact power-of-two divide), so max|entry| of the matrix reaching this guard is always in [1,2). Measured: a PURE units change, c*[[4,1,2],[1,3,0],[2,0,6]] over c = 10^-330..10^+300 (631 decades), is correct at every one of the 609 normal decades, and the 22 subnormal decades (c <= 1e-309) fail IDENTICALLY in shipped code and in all four repaired variants -- so that tail is a different guard (pow2_floor returns 1.0 for a subnormal max), not this one. A change of units cannot reach this guard. (2) REFUTED -- the filing's scale_test. On its own family, diag(1) (+) c*[[0,1],[1,0]], I measured ship, repair-:1103-only and repair-:1117-only ALL returning rc = -8 from c <= 1e-13; only repairing both passes. So that sweep attributes nothing to :1117. Its proposed separator (2x2 with b = 1e-3*|a-d|) does not separate either: for a 2x2, denom = delta + sgn*sqrt(delta^2+e2^2) ~ c(a-d) and r ~ |d0 - mu| ~ c(a-d) are the same magnitude by construction. (3) NOT REFUTED, and now isolated. Replacement family diag(1) (+) c*[[4,1,0],[1,3,1],[0,1,2]] (n = 4): repairing :1103 alone is bit-identical to ship and fixes NOTHING; repairing :1117 alone is correct at every c from 1e-1 to 1e-30. (4) SHARPENED mechanism -- I instrumented the guard. It does not fire on the first Givens. At c = 1e-12 it fires 30 times at k = 2 > p_lo on the CONVERGENCE TAIL, with r decaying geometrically 5.45e-13 -> 2.36e-13 -> ... -> 3.34e-17, while the true rotation is an utterly ordinary cs = 0.756, sn = 0.655, and the file's own relative threshold EPSILON_F64*(|d_k|+|d_k+1|) at those same points is 4.3e-24 -- eleven orders BELOW r. Balancing pins the GLOBAL max to [1,2); it does not pin the scale of a deflated sub-block, and nothing pins the convergence tail, which by construction runs to zero. So an absolute floor under r stops the iteration rotating the moment the active block's scale falls under ~1e-12. The divisions fail only at r = 0 and |x|/r, |z|/r <= 1 gives no overflow path, so r > 0 is the whole requirement and 1e-12 is a scale chosen for it -- the same relative-vs-absolute inconsistency the comment at :1064 says 2.7.0 repaired in this very function's p_lo loop, with the deflation tests at :1050 and :1076 relative 60 lines above. Also checked and refuted: this is not a convergence tolerance (it fabricates a 0.98%-wrong eigenvalue with rc = 0 while the true rotation is well-conditioned), and it is not squared-vs-unsquared (r and EPSILON_F64 are both degree 1).",
   "fabricates": "cs = 1, sn = 0 -- the IDENTITY rotation, in place of a well-conditioned one (measured cs = 0.756, sn = 0.655). Two outcomes, both measured on diag(1) (+) c*[[4,1,0],[1,3,1],[0,1,2]], n = 4: at c = 1e-12, rc = HSB_ERR_NONE with a SILENTLY partially-converged spectrum -- lambda = 4.68562332476e-12 against a true 4.732050808e-12, 0.98% wrong on that eigenvalue and 3.39% max relative error over the block (the filing's 0.98% figure reproduced exactly); at every c <= 1e-13, HSB_ERR_NO_CONVERGENCE with out_vals never written, i.e. an all-zero spectrum to a caller who zero-filled. Repairing :1117 alone removes both across all 18 decades tested; repairing :1103 alone changes neither.",
   "correct_threshold": "r > 0 -- exactly what makes x/r and z/r fail, and nothing more. NOTE the filing's parenthetical \"(equivalently >= DBL_MIN)\" is WRONG: for 0 < r < DBL_MIN both |x| <= r and |z| <= r, so x/r and z/r are finite and in [-1,1] and a DBL_MIN floor still fabricates the identity. I could not construct a REACHABLE subnormal-r case -- the block invariant r >= |z| >= EPSILON_F64*(|diag[p_lo]|+|diag[p_lo+1]|) would need |diag[p_lo]|+|diag[p_lo+1]| <= 2.2e-296 -- so on every input I measured the two are indistinguishable; > 0 is still what the division requires and costs nothing. The relative alternative EPSILON_F64*(|diag[k]|+|diag[k+1]|), consistent with :1050/:1076, also repairs every case I measured, and at k = p_lo it is provably equivalent to r > 0 because its firing region is exactly the region the deflation test has already zeroed. Verified no regression: r > 0 and r > F64_TINY each leave all five suites at 776/233/359/416/1827, identical to shipped -- the filing's correct_threshold breaks no existing test.",
   "scale_test": "REPLACE the filing's family; it cannot discriminate this site (measured above). Use eigen_qr(diag(1) (+) c*[[4,1,0],[1,3,1],[0,1,2]], n = 4) swept over c = 1e-1 .. 1e-320, asserting rc == HSB_ERR_NONE and, RELATIVELY, lambda_i/c == {4.732050807568877, 3, 1.2679491924311228} to 1e-10 with lambda_0 == 1. Three properties the filing's family lacks: (a) it ISOLATES -- the Wilkinson denom stays 1.618c so :1103 never fires, and mutation-proven, repairing :1103 alone leaves the sweep failing while repairing :1117 alone passes it; (b) the assertion must be on lambda/c, not on absolute error -- an absolute tolerance scaled to the matrix norm silently accepts the c = 1e-12 case, which is the exact trap my own first comparison fell into and the reason this defect is invisible to a suite; (c) it BRACKETS every threshold a repair might plausibly choose, each failing at its own c and separating from the correct one: 1e-6 fails below ~1e-6, EPSILON_F64 = 1e-12 below 1e-12, 1e-14 below ~1e-14, DBL_EPSILON = 2.2e-16 below ~1e-16, while r > 0, r > DBL_MIN and the relative EPSILON_F64*(|d_k|+|d_k+1|) all pass the full sweep. Pair it with the PURE-UNITS CONTROL that must stay green -- c*[[4,1,2],[1,3,0],[2,0,6]] over c = 10^-308..10^+300 -- which is what proves the failure is the deflated block's scale and the convergence tail, not the input's units, and which stops the next reader re-deriving the balancing at :1243 for themselves."
  },
  {
   "site": "src/linalg_precision.cyr:1449",
   "fn": "cqr_decompose",
   "guarded_quantity": "vhv = v^H v after the phase shift = 2*||x||*(||x||+|x1|), where x is column k below the diagonal OF THE INTERNALLY BALANCED matrix (out_R holds A/sc, sc = _lp_pow2_floor(_lp_cmat_max_abs(A)), :1352). So it is NOT a caller-units length-squared: it is degree 2 in the DIMENSIONLESS ratio ||x||/||A||_max, and degree 0 in the caller's units.",
   "protects": "two_over_vhv = f64_div(F64_TWO, vhv) at :1450, used by both reflector application loops.",
   "verdict": "DEFECT_SQUARED_VS_UNSQUARED",
   "why": "Two corrections to the filing, both measured, neither of which saves the guard. (1) The 'different units' half of the class is REFUTED: cqr_decompose balances on the way in and un-balances R at the end, so vhv is unit-invariant. The identical failing fixture (grading c = 3e-7) rescaled uniformly by s = 1e-100, 1e-8, 1, 1e8, 1e100 produces bit-identical output (all correct), and the shipped `cqr @1e200` test in tests/hisab.tcyr:2152 is that same control, passing. The guarded quantity is a dimensionless ratio, so an absolute constant is legitimate IN KIND. (2) It is the wrong absolute constant, and the evidence is the exponent, not the units: :1424 tests the degree-1 ratio ||x||/||A|| against EPSILON_F64 and :1449 tests the degree-2 ratio against the SAME constant 25 lines later, so the effective cutoff on the ratio is sqrt(EPSILON_F64/31.38) = 1.785e-7 instead of 1e-12 -- 5.6 decades coarser than its own sibling, on a column that is perfectly representable and 9 decades above round-off. Measured onset (sub-decade scan, c = k*1e-8, k = 10..20): correct at k >= 18, fabricated at k <= 17, i.e. the crossing sits at 1.78e-7, exactly where vhv = 31.38c^2 crosses 1e-12 and nowhere near :1424's own crossing at c = 3.0e-13. This is the identical class cx_div was repaired for in 2.6.14 (complex.cyr:57-62 names it and the effective 1e-6 cutoff it produced); cx_div at this very call site is now correct (it compares |z|^2 against EPSILON_F64^2 and is consistent with the :1429 gate, so it cannot confound the attribution). vhv >= 2*||x||^2 > 2e-24 whenever :1424 passed, so the guard protects no division: 2/vhv <= 1e24, finite, and the NaN/Inf paths behave identically under any threshold (f64_gt is false for NaN either way; 2/Inf = 0 is a no-op reflector).",
   "fabricates": "rc = HSB_ERR_NONE with the reflector skipped outright. TWO modes, the second missed by the filing. (a) R not upper triangular: measured on diag(1) (+) c*[[1+i,2],[3,4-i]], |R[2][1]|/c = 3.000000000 (the untouched input entry) and |R[1][1]|/c = 1.414213562 (|x1|, not ||x|| = 3.316624790) for every c <= 1.7e-7, with Q the identity on that column. (b) WORSE, and invisible to the filing's decade grid: when reflector k is skipped but reflector k+1 is applied, the R-update loop starts at j = k+1 while the Q-update loop runs over all m columns, so column k of R is never reflected and the returned pair is NOT A FACTORIZATION -- Q*R != A with rc = 0. Measured on the ungraded fixture at c = 1.3e-7 .. 1.7e-7 (the window between the two reflectors' onsets, 1.213e-7 .. 1.785e-7, only 0.17 decades wide, which the filing's 1e-6/1e-7 grid steps over): max|Q*R - A| = 6c, up to 1.02e-6 ABSOLUTE against ||A||_max = 1, i.e. 4.6e9 * DBL_EPSILON.",
   "correct_threshold": "vhv > 0. DBL_MIN and EPSILON_F64^2 are equally correct and BEHAVIOURALLY INDISTINGUISHABLE from it here -- both were built and swept and their output is byte-identical (diff -q), necessarily, because :1424 forces vhv > 2e-24 at every reachable evaluation. Measured with `vhv > F64_TINY` installed (mutant grep-proven at :1449): six decades, c = 1e-7 .. 1e-12, flip from fabricated to correct (|R21|/c = 0, |R11|/c = 3.316624790), and all five suites stay green -- hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776, 0 failed, identical to the shipped control -- so no existing test pins the current behaviour. Two riders. (i) Better than re-thresholding: decide reflect/skip ONCE, at :1424, since vhv <= 0 is unreachable there; a skip that happens between the two gates is what produces the Q*R != A mode. (ii) Repairing :1449 alone leaves the same class alive one guard over -- :1424's own absolute 1e-12 on the degree-1 ratio (crossing c = 3.0e-13) and :1429's phase fallback (`phase := 1` when |x1| <= EPSILON_F64, crossing c = 7.07e-13), the latter building a reflector that does not zero the subdiagonal at all.",
   "scale_test": "The filing's sweep must be REPLACED, not floored differently: measured, `vhv > DBL_MIN` makes it pass c = 1e-1..1e-12 and still FAIL 1e-13..1e-150 (137 of its 150 assertions), because below 7.07e-13 the :1429 phase fallback fabricates on its own (|R21|/c = 0.694987437, |R11|/c = 3.242991283, |Q*R - A| = 1.29c) and below 3.0e-13 :1424 skips -- repairing :1424 too (measured) does not rescue it. As written it would read as \"the repair did not work\". Use instead: (1) CONTROL, to prove the fixture varies the right thing -- the same matrix scaled uniformly by s = 1e-100, 1e-8, 1e8, 1e100 must give IDENTICAL results, because the internal balancing makes vhv unit-invariant; the varying parameter is the GRADING ratio, not units. (2) FIXTURE A(c,K) = diag(1) (+) [[c(1+i), 2c],[3c, K*c*(4-i)]] with K = 1e6, which spreads the two reflectors' onsets apart so the Q*R != A window is six decades wide instead of 0.17. One assertion per decade, c = 1e-1 down to 1e-12 (floor at 1e-12: below 7.07e-13 no repair of :1449 can pass), plus sub-decade points {1,2,5}x10^-e and the explicit 1.3e-7 .. 1.7e-7 band on the K = 1 fixture. Three assertions per point: (i) ||Q*R - A||_max <= 1e-13 * ||A||_max -- norm-relative and NOT vacuous, it fails on shipped code at six decades with residual 6e-7 down to 6e-12; (ii) |R[2][1]| <= 1e-10 * c; (iii) |R[1][1]|/c == 3.3166247903554 to 1e-9 relative, which is what discriminates the three states from each other: correct 3.316624790, reflector skipped 1.414213562, phase fallback 3.242991283. Brackets crossed: 1.785e-7 (the shipped onset, sqrt(EPSILON_F64/31.38)), 8.4e-9 (DBL_EPSILON on vhv), 1.5e-8 (sqrt DBL_EPSILON), 3.0e-13 and 7.07e-13 (the two sibling guards, which the test must ATTRIBUTE rather than trip). It deliberately does NOT try to separate DBL_MIN from EPSILON_F64^2 from 0 -- measured byte-identical, and provably so given :1424."
  },
  {
   "site": "src/mat3.cyr:123",
   "fn": "m3_inverse (inherited unguarded by m3_normal_matrix at src/mat3.cyr:186, `m3_transpose(m3_inverse(m))` \u2014 the only in-tree caller, verified by grep over src/ and tests/)",
   "guarded_quantity": "|det(M)| for a general 3x3, computed by m3_determinant (src/mat3.cyr:110-118) as the full cofactor expansion over the raw entries. Dimensions are (entry)^3 \u2014 if entries carry length, det carries length^3. Unbounded and fully scale-covariant: under M -> s*M, det -> s^3*det. Nothing normalises it: m3_determinant reads m3_get directly, and m3_inverse's only caller (m3_normal_matrix) passes the caller's matrix through untouched. NOT dimensionless, NOT bounded, NOT a cosine/barycentric/[0,1] parameter \u2014 i.e. not the case error.cyr:41-44 reserves EPSILON_F64 for.",
   "protects": "The division on the next line, src/mat3.cyr:124: `var inv_det = f64_div(F64_ONE, det);`. That is the only division in the function; the nine cofactor stores are multiplies by inv_det. No sqrt, no acos, no loop bound, no iteration \u2014 so it is a divide-by-zero guard, not a convergence tolerance (refutation avenue \"convergence tolerance\" checked and rejected: there is no loop and no residual anywhere in the function).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "A length^3 quantity compared against a dimensionless absolute 1e-12 (EPSILON_F64 = 0x3D719799812DEA11, decoded and confirmed = 1e-12, src/error.cyr:26), so the threshold moves by the CUBE of any unit change. REPRODUCED ON THE REAL TOOLCHAIN (cyrius 6.6.2), not argued: a probe over M_k = 2^k*I built in a scratch copy of the tree returns the true inverse at k = -13 (det = 2^-39 = 1.82e-12) and the FABRICATED identity at k = -14 (det = 2^-42 = 2.27e-13) \u2014 the pass/fail boundary sits exactly where 2^(3k) crosses 1e-12, which is the signature of the class. Over the full sweep k in [-341,+341], 328 of 683 samples are wrong, every one returning I. Condition number of every one of them is exactly 1, so the guard is measuring MAGNITUDE, not singularity. What actually makes line 124 fail is |det| < ~1/DBL_MAX = 2^-1024 = 5.5627e-309, i.e. k <= -342 \u2014 296 orders of magnitude below where the guard fires. ALL FIVE REFUTATION AVENUES WERE TESTED AND FAILED: (1) not dimensionless/bounded \u2014 it is a raw degree-3 cofactor expansion; (2) no caller normalises \u2014 m3_normal_matrix is the sole in-tree caller and does nothing to the operand, and the probe confirms the fabrication propagates through it (m3_normal_matrix(2^-20*I) returned 1.0 where 2^20 is correct: the identity is a fixed point of transposition); (3) not a convergence tolerance \u2014 single division, no loop; (4) NO EXISTING TEST CHANGES BEHAVIOUR \u2014 checked by running, not assuming: with the guard changed to F64_TINY, all five suites pass, 416+359+1827+233+776 = 3611 assertions, 0 failures. The three test sites that reach the guard (abuse.tcyr:1469-1470 via m3_new(), foundation.tcyr:1105 via a 4x4 read as a 3x3) all have det EXACTLY +0, which is still below DBL_MIN, so the documented identity-on-singular contract is preserved verbatim; the one invertible fixture (modules.tcyr:5881-5885) has det = 8; (5) the scale_test DOES discriminate \u2014 every candidate threshold's first-failing k was computed and each is distinct and inside the swept range. \u26a0 THE SWEEP ALSO FOUND A SECOND, SEPARATE FABRICATION AT THE OTHER END that no threshold repair can touch: at k >= +342 the DETERMINANT ITSELF overflows to +Inf, |Inf| < eps is false so the guard does not fire, inv_det = 1/Inf = +0, and m3_inverse returns the ZERO matrix for a matrix whose true inverse 2^-342*I is exactly representable (measured: got 0, expected 2^-342). Related: the same overflow makes the exactly-singular fixture's det NaN at k = +341, and the guard does not fire on NaN either. File separately; the sweep must exclude it or it will fail against a correctly-repaired threshold and look like a regression.",
   "fabricates": "m3_identity() \u2014 a full-rank, orthogonal, unit-determinant matrix in the slot where the inverse belongs. m3_inverse returns a bare pointer with no rc channel, so the caller cannot distinguish \"here is your inverse\" from \"I refused\"; nothing returned is NaN or Inf or otherwise self-announcing. Measured propagation: the identity is a fixed point of transposition, so m3_normal_matrix returns it unchanged \u2014 a small-scale model matrix silently yields an identity normal matrix, and unrotated normals look plausible rather than broken. For the 1e-5 (metres->micrometres) case the answer is wrong by 1e5 on every diagonal entry. Companion fabrication at the opposite extreme (separate defect, det overflow rather than threshold): the ZERO matrix, measured at k >= +342.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) \u2014 already extracted in this tree as the shared constant for exactly this rule, and mat3.cyr can name it directly (same-bundle global; no per-module alias needed, unlike geo/autodiff/quat which aliased it before extraction). Measured kill matrix over the 683-sample sweep, one build per candidate on cyrius 6.6.2: EPSILON_F64 -> 328 wrong; F64_TINY -> 1 wrong; guard-only-exact-zero (0x1) -> 0 wrong. \u26a0 SHARPENED BY MEASUREMENT: F64_TINY is not exactly \"what makes the division fail\" \u2014 it over-refuses by a factor of 4 in det (2^(2/3) = 1.587x in uniform entry scale). The true overflow bound is |det| < ~1/DBL_MAX = 2^-1024; at k = -341 det = 2^-1023 is subnormal but 1/det = 2^1023 is finite and the exact inverse 2^341*I is representable, and the F64_TINY repair fabricates the identity there (measured: the single residual failure). This also means src/error.cyr:29's own header line \u2014 \"The smallest MAGNITUDE whose reciprocal is still finite: DBL_MIN\" \u2014 is off by a factor of 4; the smallest such magnitude is ~2^-1024. Recommendation: ship F64_TINY for consistency with the extracted constant and the 22 existing use sites, and make the test STATE the k = -341 over-refusal as accepted rather than quietly stopping the sweep above it (a sweep whose floor is chosen to avoid the residual is the same shortcut 2.14.0 recorded against the 10-decade geo sweep). A genuine SINGULARITY report is a separate concern: it must be relative and degree-3 to be scale-free \u2014 |det| < rel_tol * ||c0||*||c1||*||c2|| (Hadamard bound, also length^3, so the ratio is dimensionless) \u2014 and delivered on an rc out-parameter, never by returning a plausible matrix. That is an API change and should be filed separately from the threshold repair.",
   "scale_test": "DEGREE -1 HOMOGENEITY OF THE INVERSE, swept over 683 powers of two, written in the house idiom already in tests/modules.tcyr:10291 (`_p2(e) = (1023 + e) << 52`; bit-exact `!=` compares on raw f64 bit patterns, NEVER through f64_to, which truncates \u2014 2.13.0). ALL NUMBERS BELOW ARE MEASURED ON cyrius 6.6.2, not derived on paper. (A) PRIMARY SWEEP: for k in [-341, +341], M_k = 2^k*I. Entries, det = 2^(3k) and the true inverse 2^-k*I are all exactly representable, so the compare is legitimately bit-exact: assert m3_get(inv,0,0) == _p2(-k) and every off-diagonal == +0. Verified exact for all 683 samples with the guard disabled. Truth: the division only overflows at k <= -342, so a mathematically exact guard refuses nothing in the swept range. MEASURED RESULTS: shipped EPSILON_F64 fails 328 of 683 (every k <= -14, all returning I); F64_TINY fails exactly ONE (k = -341, det = 2^-1023 subnormal) \u2014 assert that sample explicitly as the accepted over-refusal with a comment naming it, so a future tightening to 1/DBL_MAX is not mistaken for a regression and the sweep is not vacuous at its own floor. DISCRIMINATION (each candidate's first failing k, all distinct, all inside the range): 1e-4 (eps^(1/3)) k<=-5; 1e-9 k<=-10; shipped 1e-12 k<=-14 (confirmed against the real binary at the k=-13/-14 pair); 1e-15 k<=-17; DBL_EPSILON k<=-18; 1e-36 k<=-40; DBL_MIN k<=-341; 1/DBL_MAX k<=-342. \u26a0 CORRECTED UPPER BOUND: stop the truth assertion at k = +341. At k >= +342 the determinant itself overflows to +Inf and m3_inverse returns the ZERO matrix (measured) \u2014 a SEPARATE defect the threshold cannot repair, so including it makes the sweep fail against correct code. Add it as its own named assertion documenting current behaviour, not as part of clause (A). (B) ANTI-MUTANT (so that DELETING the guard does not pass): M = 2^k * [[1,2,3],[2,4,6],[7,8,9]], row 2 = 2*row 1. \u26a0 CORRECTED RANGE: k in [-341, +340], not [-341,+341] \u2014 measured, det is bit-exactly +0 across that range but is NaN at k = +341 (the triple product overflows, Inf - Inf), and f64_lt(f64_abs(NaN), t) is false for every t, so the \"must be refused\" assertion fails against correct code at +341. Within [-341,+340] all three candidate guards refuse correctly (verified: 0 bad), while a deleted guard yields 1/0 = +Inf and NaN entries. (C) NON-UNIFORM, ILL-CONDITIONED BUT INVERTIBLE, pinning the shipped threshold from BOTH sides on a matrix that is not a multiple of I: M = 2^k * [[1,1,0],[0,2^-27,0],[0,0,1]], det = 2^(3k-27). Confirmed numerically: k = -5 gives det = 2.2737e-13, just BELOW 1e-12; k = -4 gives 1.8190e-12, just ABOVE \u2014 two adjacent samples straddling the shipped constant. Assert the relative residual ||M*inv(M) - I||_F / (||M||_F * ||inv(M)||_F) <= 8*DBL_EPSILON; the fabricated identity fails it by O(1) at k = -5 and passes at k = -4. (D) THE ONE-LINE UNIT-CHANGE ASSERTION FOR THE CHANGELOG: m3_inverse(s*M) == (1/s)*m3_inverse(M) elementwise and bit-exactly for s = 2^20 and s = 2^-20 over a handful of well-conditioned M whose scaled det stays inside [2^-900, 2^900] (power-of-two rescaling commutes exactly with rounding, so bit-exactness is legitimate only while nothing over/underflows \u2014 state that precondition). Today the micrometre side returns the identity and the metre side the true inverse, so a change of units alone violates the identity that defines the operation. (E) WRAPPER: run (A) and (D) against m3_normal_matrix(M_k) too \u2014 truth 2^-k*I for symmetric M_k. Not optional: the fabrication is a fixed point of transposition, measured to survive the wrapper unchanged, so a test that only exercises m3_inverse leaves the reachable path unproven."
  },
  {
   "site": "src/mat4.cyr:150",
   "fn": "m4_transform_point",
   "guarded_quantity": "|pw| \u2014 the absolute value of the homogeneous w component of M\u00b7(v,1). Confirmed against the source: m4_mul_vec4 computes r = \u03a3_k v[k]\u00b7col_k with column k at m+32k, so pw = x\u00b7c0w + y\u00b7c1w + z\u00b7c2w + 1\u00b7c3w. DIMENSIONS: not dimensionless, not bounded. For an affine M (m4_from_srt:305 and m4_look_at_rh:365 both set [3][3]=F64_ONE, [i][3]=0) it is exactly 1.0 and the guard never fires. For a perspective M it is -z_eye, a LENGTH in the caller's units: m4_perspective_rh:335 sets m4_set(m,2,3,-1) and never writes [3][3], leaving it 0; m4_perspective_reverse_z:347-348 has col2=(0,0,0,-1) and col3=(0,0,near,0), also [3][3]=0. For a general 4x4 it is an arbitrary linear form in the input coordinates. Homogeneous coordinates are additionally defined only up to overall scale, so pw has no intrinsic magnitude at all \u2014 (kx,ky,kz,kw) denotes the same point as (x,y,z,w) for every k != 0.",
   "protects": "The division `var inv_w = f64_div(F64_ONE, pw)` on the literal next line (mat4.cyr:151, the perspective divide). Nothing else \u2014 the guarded value is the divisor itself and is used nowhere before the division. That reciprocal is finite for every |pw| >= DBL_MIN; it overflows to +/-Inf only in the subnormal floor.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FIVE CLOSED AGAINST THE REFUTATION. (1) Dimensionless/bounded? No \u2014 read the source: m4_perspective_rh sets [2][3]=-1 with [3][3]=0, so pw = -z_eye, a length. Only the affine case is pinned at 1. (2) Caller normalises? No \u2014 `grep -rn m4_transform_point src/` returns ONLY the definition at mat4.cyr:147. Zero internal callers; it is a public library entry point reached directly by consumers, so no normalisation can be assumed. (3) Convergence tolerance? No \u2014 it guards the division on the next line and the quantity is the divisor. (4) Does F64_TINY change an input an existing test passes? CHECKED, NOT ASSUMED \u2014 all five call sites in tests (hisab.tcyr:163, foundation.tcyr:946/954/962/976/1039/1045) build M via m4_from_srt or m4_look_at_rh, both of which set [3][3]=F64_ONE, so pw is bit-exactly 1.0 \u2014 a factor of 1e12 above the current threshold and ~4.5e307 above the proposed one. No test transforms a point through a projective matrix at all; abuse.tcyr:1474 merely asserts m4_perspective_rh(0,0,0,0) allocates. The repair is behaviour-neutral on the entire existing suite. (5) Does the scale_test discriminate? Here the refutation LANDED \u2014 but against the finding's test, not the finding. See scale_test: the submitted test A fails on CORRECT code and test B fails to bracket the deep end. Both are replaced below. The underlying claim stands: the quantity is a length (or an arbitrary homogeneous scale factor) and the threshold is a fixed 1e-12 with no scale attached \u2014 exactly the shape 2.10.2's rule names as the defect, and exactly the case error.cyr:40-45 already adjudicates in this repo's own words (\"NOT A REPLACEMENT FOR EPSILON_F64 ... EPSILON_F64 remains right for a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, a parameter in [0,1]\"). pw is none of those three. 1/pw is perfectly finite at 1e-13, at 1e-100 and at 1e-300. \u26a0 NEW, and it raises the priority: this site is not one instance but the PATTERN SOURCE. src/transforms.cyr:169 (world_to_screen) is the identical guard on the identical quantity \u2014 `if (f64_lt(f64_abs(cw), EPSILON_F64) == 1) { return hvec3_zero(); }` before `f64_div(F64_ONE, cw)` \u2014 and docs/audit/2026-04-15.md:82 explicitly ordered the copy: \"Fix: Guard w==0 like m4_transform_point does.\" The copy fabricates WORSE than the original: hvec3_zero() is the top-left screen pixel, an entirely plausible in-range viewport coordinate, whereas mat4's undivided clip coords are at least anomalously large. Repair both sites in one change, and delete the exemplar advice from the audit doc so it stops propagating. NOTE the guard is not testing the wrong quantity \u2014 pw IS the divisor; only the threshold is wrong.",
   "fabricates": "Silently returns hvec3_new(px, py, pz) \u2014 the UNDIVIDED clip-space coordinates \u2014 as if they were the projected point. There is no error channel at all: the return type is a plain hvec3, there is no rc, and the comment above the function (mat4.cyr:146, \"Mat4 * Vec3 as point (w=1, perspective divide)\") does not mention the guard, so a caller cannot distinguish a projected point from an unprojected one. Measured (float64 reimplementation of m4_mul_vec4's exact accumulation order, scratchpad probe): camera fov 60deg, aspect 16/9, near 0.1\u00b7s, far 100\u00b7s, point (1,2,-5)\u00b7s. ndc_x = 0.19485571585149875 at s = 1, holds to within 2 ulp for every s down to 1e-12, then at s = 1e-13 the guard fires and it returns 9.742785792574936e-14 \u2014 wrong by twelve orders of magnitude on a dimensionless output that must not move at all. (Minor correction to the original filing: the figure it quoted as the correct value, 0.1948557158514987, is the s=1e-12 sample; the s=1 value is 0.19485571585149875. The two differ in the last digit, which is why test B below must be ulp-based rather than bit-exact.) The sibling site transforms.cyr:169 fabricates hvec3_zero(), i.e. screen pixel (0,0) with depth 0 \u2014 indistinguishable from a legitimate top-left hit.",
   "correct_threshold": "F64_TINY (error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) \u2014 the smallest magnitude whose reciprocal is finite, exactly as _GEO_F64_TINY is used at the 15 geo.cyr sites. Guard the division and nothing more. The constant already exists, is already the documented answer for this exact situation in its own comment block (error.cyr:40-45), and needs no new declaration, so the constant gate is unaffected. Apply the same substitution at src/transforms.cyr:169 in the same change. \u26a0 CORRECTION to the original filing's second-order note, which overclaimed: it asserted that even under a DBL_MIN guard `x\u00b7(1/pw)` can overflow to Inf where `x/pw` is representable. I searched the overflow boundary for such a case (pw at and just above DBL_MIN, px swept down from DBL_MAX\u00b7pw) and FOUND NONE \u2014 with pw >= DBL_MIN the reciprocal is finite and the product overflows only where the true quotient does too, so that ground does not hold and should not be cited. The accuracy ground DOES hold and is worth the same edit: reciprocal-then-multiply rounds twice. Measured on 200,000 random (px,pw) pairs, px\u00b7(1/pw) differs from px/pw in 54,049 cases \u2014 27.0%, worst 1 ulp. So divide each component directly (f64_div(HVec4_x(p), pw)) for the last ulp, not to avert an overflow. A point exactly on the w = 0 plane is a genuine point at infinity and +/-Inf is the honest answer for it, not a fabricated finite triple.",
   "scale_test": "\u26a0 THE ORIGINAL FILING'S TEST A IS BROKEN AND WOULD FAIL ON CORRECTLY REPAIRED CODE; TEST B DOES NOT BRACKET. Both measured, both replaced. WHY A FAILS: k = 10^j is not exact in binary, so fl(k\u00b7m_ij) rounds each of the 16 entries independently and the projective quotient is NOT reproduced bit-for-bit. Measured with a perfect DBL_MIN guard, k = 10^j over j = -300..300 step 10: 55 of 61 decades FAIL the bit-exact assertion (first failure j = -300, 0.19485571585149872 vs 0.19485571585149875). A test that red-flags the correct repair is worse than no test. The fix is one character class: use k = 2^j. Exact power-of-two scaling makes k\u00b7m_ij, the accumulation, and the quotient all exact, and the same sweep then passes 0/201. WHY B DOES NOT BRACKET: measured over its stated +/-140 decades, a 1e-300 guard fails 0 decades and a 1e-306 guard fails 0 decades \u2014 B alone waves both through, because s = 1e-140 only drives pw to 5e-140. USE THESE THREE INSTEAD, all bit-exact except (B). (A') HOMOGENEOUS-RESCALE INVARIANCE, exact binary scaling: M = m4_perspective_rh(60deg, 16/9, 0.1, 100), v = (1,2,-5); for k = 2^j over j = -1000..1000 step 10, assert m4_transform_point(k\u00b7M, v) is bit-identical to m4_transform_point(M, v) in all three components (true by definition \u2014 k\u00b7M is the same projective map). Measured failing-decade counts: 1e-12 guard 96, F64_EPS 95, 1e-100 67, 1e-300 1, DBL_MIN 0. (C) THE STRONGEST AND CHEAPEST \u2014 PINHOLE RAY INVARIANCE, and the one to write first: keep M FIXED and scale the POINT along its own ray, v = 2^j\u00b7(1,2,-5) for j = -1020..1020, asserting ndc_x and ndc_y are bit-identical to the j = 0 values. Moving a point along the ray from the eye cannot change where it lands on the image plane, since ndc_x depends only on x/(-z); power-of-two scaling keeps every operand exact, and the matrix entries never move so nothing goes subnormal. Measured failing-j counts over the 2041 exponents: 1e-12 guard 978, F64_EPS 966, 1e-100 686, 1e-300 22, 1e-306 2, DBL_MIN 0. This brackets EVERY plausible repair threshold including 1e-306, which neither submitted test reaches. \u26a0 Honest limit, stated rather than overclaimed: at j = -1020 pw is 4.45e-307, and min|M entry| = 0.2002 means entries go subnormal below about j = -1019, so exact arithmetic bottoms out there \u2014 this sweep cannot separate DBL_MIN from a hypothetical ~1e-307 guard. Nothing below 1e-306 is a threshold a repair would plausibly choose, and the componentwise-division form makes the distinction moot. (B') UNIT-CHANGE INVARIANCE, the physically meaningful form, kept from the filing and confirmed sound: for s = 10^j over j = -140..140 step 10, build m4_perspective_rh(60deg, 16/9, 0.1\u00b7s, 100\u00b7s) and the point (1,2,-5)\u00b7s, and assert ndc_x, ndc_y are within 4 ulp of their s = 1 values (0.19485571585149875 and 0.692820323027551) \u2014 NDC is dimensionless so it is exactly s-invariant. Measured worst deviation under a DBL_MIN guard: 2 ulp, 0 decades exceeding the 4-ulp bound, so the tolerance is right. It kills 1e-12 (13 decades), F64_EPS (13) and 1e-100 (4) but NOT 1e-300/1e-306 \u2014 keep it for the physical statement, rely on (C) for the bracket. Assert x and y only, never z: pz/pw = (m22\u00b7s\u00b7z + m32)/(-s\u00b7z) is genuinely not s-invariant. Finally, mutant-check the range itself: a 6-decade sweep (the 2.11.1 quaternion mistake) passes a 1e-12 guard and a DBL_MIN guard identically and discriminates nothing. Add the same three sweeps against transforms.cyr's world_to_screen, whose fabricated hvec3_zero() is harder to spot downstream."
  },
  {
   "site": "src/mat4.cyr:252",
   "fn": "m4_inverse",
   "guarded_quantity": "|det| \u2014 the absolute value of the full 4x4 determinant built from the s0-s5 / c0-c5 2x2 minors. A degree-4 homogeneous form in the matrix entries: det(k\u00b7M) = k^4\u00b7det(M) exactly (verified bit-exactly for k = 2^j over j = -250..+250). If the entries carry a length unit the determinant carries length^4, so a metre\u2192millimetre change of units moves it by 1e12. Nothing in the function normalises, rescales, or bounds it, and the only other guard in the file (m4_transform_point's |w| at :150) is on a different quantity.",
   "protects": "The division on the very next line, `var inv_det = f64_div(F64_ONE, det)`. Nothing else \u2014 every subsequent use of det is through inv_det. That reciprocal is finite for every |det| >= DBL_MIN; measured at the counter-example, 1/1.25e-13 = 8e12 (0x429d1a94a2000000), an ordinary normal double.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation routes were tried against the built library on cycc 6.6.2 and all five failed. (1) NOT dimensionless/bounded: det is the single most scale-sensitive quantity in the file, degree 4, and the guard is a bare absolute 1e-12 (EPSILON_F64, error.cyr:26) \u2014 whose own comment at error.cyr:40-45 restricts it to \"a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, a parameter in [0,1]\" and names F64_TINY as the constant for a guard that protects a division. The site violates the rule written 6 lines above the constant it uses. (2) NO caller normalises: m4_inverse is a public bundle export (dist/hisab.cyr, ten SHA-locked consumers), and the one in-tree caller, transforms.cyr:89 t3d_inverse_matrix, hands it t3d_to_matrix(t) with the caller's scale unmodified \u2014 measured, a t3d with scale (5e-5,5e-5,5e-5), a 45-degree Y rotation and translation (1,2,3) comes back as the exact identity, [3][0] = +0 where the scale-1 control gives 1.4142 (0x3ff6a09e667f3bce). The fabrication silently discards the rotation AND the translation through that second public entry point. (3) NOT a convergence tolerance: it is a straight-line Cramer's-rule evaluation with no iteration. (4) The accuracy defence fails on measurement: at the counter-example the cofactor path is not merely usable, it is EXACT \u2014 with the threshold swapped to F64_TINY, all 16 entries of M\u00b7M^-1 are bit-exact (16/16 identical to 1.0/0.0), while the shipped guard leaves 3 of 16 wrong. The guard cannot tell ill-conditioning from small units, and here there is no ill-conditioning: cond(M) = 1. (5) NO existing test blocks the threshold swap \u2014 checked, not assumed: I copied the tree, changed line 252 to F64_TINY, and ran every suite: hisab 416, foundation 359, modules 1827, edge_cases 233, abuse 776 = 3611/3611 passed, 0 failed, and `cyrius fuzz` PASS. The two tests that look like blockers are not: edge_cases.tcyr:163-170 and abuse.tcyr:1472 both feed m4_new(), whose det is exactly +0, and 0 < DBL_MIN is still true, so the identity fallback still fires for them. The fuzz target (tests/hisab.fcyr:236) only checks M\u00b7M^-1 when |det| > 1e-8, four decades above the guard, so it cannot see the change at all. Reproduction of the claimed anchor is exact: m4_from_srt(scale 5e-5, identity rotation, zero translation) gives det = 1.25e-13 (0x3d419799812dea11), the shipped function returns m4_identity(), and M\u00b7M^-1 reads 5e-5 (0x3f0a36e2eb1c432d) on the first three diagonals instead of 1.0.",
   "fabricates": "m4_identity() \u2014 a well-formed, unit-determinant, perfectly legal-looking matrix, returned through a bare matrix handle with no rc and no error channel. Measured downstream consequence: m4_mul(M, m4_inverse(M)) evaluates to M, not I (three diagonals read 5e-5), so the fabrication propagates as a silent no-op transform rather than a detectable NaN/Inf, and through t3d_inverse_matrix it also silently drops the translation. No consistency check on the RESULT ALONE can catch it, because the identity is a valid inverse of a valid matrix \u2014 only a check that compares against the input's scale can.",
   "correct_threshold": "F64_TINY (error.cyr:45, DBL_MIN = 0x0010_0000_0000_0000) on |det| \u2014 guard exactly the reciprocal and nothing more; below it 1/det is not finite and there is genuinely nothing to return. Measured: this swap alone is behaviour-preserving for the whole existing suite (3611/3611 + fuzz) because every singular fixture in the tree has det == +0, which still trips a DBL_MIN guard. \u26a0 TWO SHARPENINGS the original proposal got wrong. (a) Do NOT bundle the \"return \u00b1Inf / an error channel instead of the identity\" change into this repair: edge_cases.tcyr:166-170 asserts the identity fallback bit-exactly on five entries and abuse.tcyr:1467-1472 documents it as the contract, so that is a separate, test-visible API decision (add a rc-returning `m4_try_inverse`, or update those five assertions deliberately) \u2014 the threshold is the defect, the fallback is a policy. (b) If the scale-free relative form is chosen instead \u2014 |det| against the Hadamard bound (product of the four column 2-norms, also degree 4, so the ratio is invariant under M -> k\u00b7M) \u2014 it MUST be written `<=`, or floored at DBL_MIN: measured, a strict `f64_lt(|det|, 1e-12 * hadamard)` never fires on the zero matrix (the bound is 0 and 0 < 0 is false), inv_det becomes +Inf and the result is NaN (0xfff8000000000000), which breaks those same five assertions. The relative form does pass the full covariance sweep (0 failures over 501 scales) and correctly declines to fire on the cond = 1 counter-example (returns 20000.0), so it is a legitimate repair once that boundary is fixed.",
   "scale_test": "EXACT RECIPROCAL COVARIANCE, powers of two so the scaling introduces zero rounding, in the house 2.14.0 style (modules.tcyr:10264-10289: bit-exact, never tolerance-based \u2014 \"an assertion that accepts a range accepts a fabrication that lands in the range\"). Build M = m4_from_srt(scale (1,1,1), a fixed unit quaternion, translation (3,-4,5)); form k\u00b7M by scaling ALL SIXTEEN entries (the bottom row too \u2014 m4_from_srt with a small scale only touches the 3x3 block and gives det = s^3, which is a weaker, different test), for k = 2^j, j = -250, -240, ..., +250; assert m4_inverse(k\u00b7M) equals (1/k)\u00b7m4_inverse(M) BIT-EXACTLY in all 16 entries (identically true in IEEE-754: cofactors scale by exactly k^3, det by exactly k^4, and rounding commutes with exact power-of-two scaling). MEASURED KILL MATRIX, one build per candidate guard, lowest and highest failing j on a step-1 grid: shipped EPSILON_F64 1e-12 fails j <= -10 (241 of 501 scales); F64_EPS 2.22e-16 fails j <= -13 (238); a 1e-100 guard fails j <= -84 (167 \u2014 note: -84, not the -83 estimated); F64_TINY/DBL_MIN 0 failures; relative 1e-12\u00b7Hadamard 0 failures. So the step-10 grid samples a killing point for every rejected candidate (-10, -20, -90) while both legitimate repairs survive, and the -20 -> -90 gap must not be closed up. The +250 half is load-bearing, not decoration: 0 failures there proves the repair introduced no upper clamp and that det overflow behaves identically at both ends. THREE NON-PARAMETRIC ANCHORS beside the sweep, each a readable bug report: (1) M = m4_from_srt(scale (5e-5,5e-5,5e-5), identity rotation, zero translation), det = 1.25e-13 \u2014 assert all 16 entries of m4_mul(M, m4_inverse(M)) are BIT-EXACTLY I (measured 16/16 exact under DBL_MIN; today 3 of 16 are wrong, the diagonals reading 5e-5 \u2014 assert bit-exact, not \"within 8 ulp\", because the exact result is measured and a ulp window would let a future fabrication through). (2) t3d_inverse_matrix(t3d_new(pos (1,2,3), rot_y(pi/4), scale (5e-5,5e-5,5e-5))) \u2014 assert [3][0] != 0 (today +0, identity fabricated; scale-1 control gives 0x3ff6a09e667f3bce), which pins the PUBLIC transforms.cyr entry point the sweep does not reach. (3) REGRESSION ANCHOR against the boundary the repair can break: m4_inverse(m4_new()) must still return identity[0][0] == 1.0 bit-exactly \u2014 this is what fails if the relative form is written with a strict `<` (measured NaN), and it is what edge_cases.tcyr:166-170 already pins."
  },
  {
   "site": "src/num.cyr:50",
   "fn": "num_newton",
   "guarded_quantity": "|f'(x)| \u2014 `f64_abs(dfx)` where `dfx = fncall1(df, x)` (src/num.cyr:48), the caller's derivative callback at the current iterate. Dimensions [f]/[x]: residual units over unknown units. Unbounded, un-normalised, and supplied entirely by an arbitrary caller function pointer. Confirmed by reading the body: the sibling test one line group above (:44) compares |f(x)| against `tol`, a CALLER-SUPPLIED parameter that can carry the caller's units; :50 is the only hardcoded scale in the function. EPSILON_F64 = 1e-12 (error.cyr:26, 0x3D719799812DEA11 decoded).",
   "protects": "The division at src/num.cyr:54, `x = f64_sub(x, f64_div(fx, dfx))`. It is a divide-by-zero guard, not a convergence test: the convergence test is the separate `|f(x)| < tol` at :44 (its own parameter, its own return HSB_ERR_NONE at :46), exhaustion is HSB_ERR_NO_CONVERGENCE at :57, and this branch returns HSB_ERR_DIVISION_BY_ZERO with the comment \"Check derivative not zero\". The header at :37 enumerates the two failure codes separately.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "UPHELD. All five refutation routes were attempted against the real source and all five died; three died on measurement rather than on reading.\n\n(1) DIMENSIONLESS/BOUNDED \u2014 no. `df` is a raw `fnptr` invoked via `fncall1`; nothing in the function touches its magnitude before comparing it.\n\n(2) CALLER NORMALISES \u2014 dead by grep, not assumption: `grep -rn \"num_newton(\" src/` returns ZERO internal callers. It is a pure public entry point (roadmap.md:695 lists it in the public API table). Every argument, including the derivative callback, is the caller's.\n\n(3) IT IS REALLY A CONVERGENCE TOLERANCE \u2014 refuted by the structure above; `tol` is a distinct parameter tested at :44 and the two error codes are distinct.\n\n(4) THE REPAIR BREAKS AN EXISTING TEST \u2014 checked, does not. All 8 `num_newton` call sites in tests/ enumerated and every fixture body read: modules.tcyr:95-102 (`_tn_df`=2x from x0=1, `_tn_cube_df`=3x^2 from x0=3, `_tn_lin_df`=1, `_tn_sin_df`=cos from x0=0.5 converging to 0 so |cos|\u21921), hisab.tcyr:565 (`_nr_df`=2x), examples/basic_math.cyr:23 (same). Minimum |f'| over every run is O(1), far above BOTH thresholds \u2014 unchanged. The only two fixtures that reach the guard return a LITERAL zero (edge_cases.tcyr:44 `_ec_zero_df(x){return 0;}`, abuse.tcyr:197 `_ab_dg_dy(t,y){return 0;}` \u2014 note it is a 2-param function called through `fncall1`, still returns 0), and `f64_abs(0.0) < DBL_MIN` is true, so both keep returning HSB_ERR_DIVISION_BY_ZERO. Zero shipped assertions change; the repair is strictly a narrowing.\n\n(5) THE SCALE TEST DOES NOT DISCRIMINATE \u2014 I modelled the loop body verbatim in IEEE double and measured. It discriminates exactly as claimed: 6 of 15 k values flip (k <= 1e-13, split at 2k < 1e-12, since min|f'| over the run is 2k at x0=1), and under DBL_MIN all 15 return HSB_ERR_NONE with the BIT-IDENTICAL root 0x3ff6a09e667f57db (1.4142135623746899) at iteration 4. My a-priori objection to leg (b) \u2014 that k = 1e300/1e-305 are not powers of two so the scaling rounds, and that at k=1e-305 the terminating residual is SUBNORMAL (~4.5e-317, with tol_k = 1e-315 itself subnormal) \u2014 was tested and is refuted: per-iterate step error stays ~6 orders below ulp(x4), so bit-identity is structural, not luck.\n\nThe two scaling arguments themselves hold as stated: range scaling f -> k*f leaves every Newton iterate bit-identical (measured, above) while moving |f'| by k; domain scaling u = x/s moves |f'| by s with no change to the problem (measured, 7 values of s, s*root bit-identical). So \"the derivative is too small\" is not a scale-free statement and 1e-12 is a scale chosen for a quantity that has none \u2014 2.10.2's definition of the defect, and the thirty-third instance of the class recorded at complex.cyr:58.\n\nSECONDARY (unchanged): what actually makes the DIVISION fail is a zero or subnormal denominator, nothing more. Modelled at k=1e-13: fx=-1e-13, dfx=2e-13, fx/dfx=-0.5, an ordinary finite division that this guard refuses.\n\nCORROBORATION, not evidence: docs/audit/2026-09-09-roadmap-verification.md:491 already lists `num_newton` in the open epsilon tier (\"num_newton/num_tridiag_solve -> early exit\"), and docs/audit/2026-08-11-v2.11.0-full.md:54 files it as \"DIVISION_BY_ZERO on well-conditioned input\". It is scheduled, not repaired.",
   "fabricates": "none - it reports an error. Verified by reading :51: the branch is a bare `return HSB_ERR_DIVISION_BY_ZERO;` with no `store64`, so `*out` is left UNWRITTEN (it is a raw `alloc(8)` at every call site \u2014 uninitialised, not zeroed). A caller that checks rc gets a false \"your derivative is zero\" on a well-conditioned problem; a caller that ignores rc reads heap garbage. FALSE NEGATIVE, not the fabricate-a-plausible-answer shape of eigen_qr / m3_inverse / cx_div \u2014 less dangerous than those, still wrong: the function refuses a problem whose Newton step is exact. Note the repair does not introduce a fabrication either: at the genuinely pathological end the repaired guard yields HSB_ERR_NO_CONVERGENCE instead (measured), never a written wrong root.",
   "correct_threshold": "`F64_TINY` (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) \u2014 the constant this repo extracted in 2.10.2 for exactly this rule, currently aliased by geo (15 uses), autodiff (4) and quat (3) and used ZERO times in num.cyr. Guard: `if (f64_lt(f64_abs(dfx), F64_TINY) == 1)`. The smallest magnitude whose reciprocal is still finite: exactly what makes the division fail, and nothing more.\n\nMEASURED PROPERTIES OF THE REPAIR (all four verbatim-loop simulations):\n- Keeps abuse.tcyr:2391 and edge_cases.tcyr:381 green (both fixtures return literal 0.0; abs(0.0) < DBL_MIN).\n- Also fires on a SUBNORMAL denominator, which is intended and is what separates it from \"no guard\": at x0 = 1e-308, dfx = 2e-308 < DBL_MIN -> DIV0; at x0 = 5e-324 (min subnormal), dfx = 1e-323 -> DIV0. With the guard DELETED both of those spin to NO_CONVERGENCE instead, so DBL_MIN is behaviourally distinct from deletion on its own fixture, without leaning on abuse.tcyr.\n- Do NOT substitute the \"relative\" alternative `|dfx| < |fx|/DBL_MAX`. It is not a stricter version of DBL_MIN, it is LOOSER: with |fx| = 2 it evaluates to 1.1e-308, below DBL_MIN, so it admits subnormal denominators DBL_MIN rejects (x0 = 1e-308 then takes a finite -1e308 step and reaches NO_CONVERGENCE by callback overflow). It also costs a division to compute a division guard. Prefer the constant the project already settled on.\n- And do not \"fix\" it by making the threshold relative to |fx| with a chosen epsilon (`|dfx| < |fx|*1e-12`): that is scale-free and therefore survives the ENTIRE k sweep, yet still refuses a convergent unscaled problem \u2014 see leg (d), which exists to kill it.",
   "scale_test": "FIVE LEGS. Legs (a)-(c) are the claim's, confirmed by measurement; (d) and (e) are added because (a)-(c) leave a wrong repair alive. All numbers below are from a verbatim transcription of :39-56 in IEEE double, not estimated.\n\nLEG (a) \u2014 RANGE COVARIANCE. f_k(x) = k*(x*x - 2), df_k(x) = k*(2x), x0 = 1.0, max_iter = 100, tol_k = k*1e-10 (tol carries the units of f). For k in {1e300, 1e200, 1e100, 1e20, 1e12, 1e6, 1e0, 1e-6, 1e-12, 1e-13, 1e-20, 1e-100, 1e-200, 1e-300, 1e-305} assert rc == HSB_ERR_NONE for every k. MEASURED: shipped 1e-12 returns HSB_ERR_DIVISION_BY_ZERO for the last SIX (k <= 1e-13); DBL_MIN returns NONE for all 15.\n\nLEG (b) \u2014 BIT-EXACT INVARIANCE. Assert load64(out) is bit-identical across all 15 k, comparing raw patterns (never through f64_to, which truncates \u2014 2.13.0). MEASURED under DBL_MIN: one distinct pattern, 0x3ff6a09e667f57db, at iteration 4 for all 15.\n  \u26a0 tol_k = k*1e-10 IS LOAD-BEARING and the claim did not say so. Measured tolerance sweep: k*1e-10 and k*1e-14 both give 1 distinct pattern; at k*1e-16 and k*1e-18 ALL FIFTEEN return NO_CONVERGENCE \u2014 Newton's residual floor on x^2-2 is ~4e-16 \u2014 so a \"tighter is safer\" edit turns the test into a uniform failure that proves nothing about the guard.\n\nBRACKETING, MEASURED per candidate against leg (a) (first k that fails): 1e-3 fails at k=1e-6; 1e-6 and 1e-9 at k=1e-12; 1e-12 (shipped) at k=1e-13; 1e-15 and 1e-18 at k=1e-20; 1e-100 at k=1e-200; 1e-300 at k=1e-305. Only DBL_MIN and \"deleted\" pass all 15. \u26a0 The TAIL ENTRIES ARE LOAD-BEARING: drop 1e-300/1e-305 from the k list and a 1e-300 threshold survives the whole sweep. 605 decades of |f'| (2e300 down to 2e-305) comfortably clears the 2.11.1 quaternion lesson (12 decades killed 1 of 3 mutants, 20 killed all 3).\n\nLEG (c) \u2014 DELETION KILL. A df callback returning exactly 0.0 must still return HSB_ERR_DIVISION_BY_ZERO (this is abuse.tcyr:2391 unchanged). MEASURED: every absolute threshold from 1e-3 to 1e-300 AND DBL_MIN pass it; only the deleted guard fails, spinning to NO_CONVERGENCE. So leg (c) discriminates deletion and NOTHING about scale \u2014 it is necessary but must not be mistaken for coverage.\n\nLEG (d) \u2014 MANDATORY, ADDED. NO unit scaling at all (k = 1): the textbook fixture already in hisab.tcyr:564-565 with a different start. f(x) = x*x - 2, df(x) = 2x, tol = 1e-10, max_iter = 100, x0 in {1e-13, 1e-20}. Assert rc == HSB_ERR_NONE and the root bits. MEASURED: shipped 1e-12 returns DIVISION_BY_ZERO at iteration 0 for both; DBL_MIN converges in 48 iterations to 0x3ff6a09e667f3bcd (x0=1e-13) and 71 to 0x3ff6a09e667f3bf5 (x0=1e-20).\n  WHY IT IS MANDATORY: (i) it removes the only serious objection to legs (a)-(b) \u2014 \"nobody expresses a residual in nanonewtons\" \u2014 by exhibiting a false negative on an UNSCALED problem the suite already ships; (ii) it kills the one wrong repair that survives (a)+(b)+(c): `|dfx| < |fx|*1e-12` is scale-free, passes all 15 k and passes leg (c), and still returns DIVISION_BY_ZERO here (MEASURED at both x0). A leg-(a)-only sweep would bless it.\n  \u26a0 Keep x0 >= ~1e-150. At x0 = 1e-200 the first step is 1/x0 = 1e200 and the callback's next x*x overflows to +Inf, so the run is NO_CONVERGENCE under EVERY threshold including deletion (measured, still NOCONV at 1200 iterations) \u2014 that failure belongs to the fixture, not the guard. x0 = 1e-100 does converge but needs max_iter = 400 (337 iterations).\n\nLEG (e) \u2014 SUBNORMAL BOUNDARY, ADDED. x0 = 5e-324 with the same f/df: assert HSB_ERR_DIVISION_BY_ZERO. MEASURED: DBL_MIN gives DIV0 (dfx = 1e-323 is subnormal), the deleted guard gives NO_CONVERGENCE. A second, self-contained kill of the deletion mutant that does not depend on abuse.tcyr's fixture surviving unedited.\n\nSECOND AXIS (optional, and it is the axis CLAUDE.md states in words \u2014 \"millimetres instead of metres\"). DOMAIN scaling: F_s(u) = (s*u)^2 - 2, dF_s(u) = 2*s*(s*u), u0 = 1/s, root sqrt(2)/s.\n  \u26a0 CORRECTION TO THE CLAIM: tol must be FIXED at 1e-10, NOT scaled \u2014 F_s(u) = f(s*u) is unchanged in RANGE, only the domain moves; the claim left tol unstated.\n  \u26a0 CORRECTION 2: the claim's s list {1e12, 1e6, 1, 1e-6, 1e-12, 1e-100, 1e-200} jumps straight from 1e-12 to 1e-100, so the shipped guard first fails at s = 1e-100 there, not at the split. Since dF at u0 is exactly 2s, add s = 1e-13 to place the boundary (2s < 1e-12) precisely. MEASURED on the 7-value list: shipped passes down to s = 1e-12 and returns DIVISION_BY_ZERO at 1e-100 and 1e-200; DBL_MIN returns NONE for all 7.\n  \u26a0 CORRECTION 3: assert BIT-EXACT, not \"relative 1e-12\". MEASURED: s*load64(out) is 0x3ff6a09e667f57db for all 7 s under DBL_MIN \u2014 a tolerance here is precisely the shape 2.13.0 showed goes vacuous."
  },
  {
   "site": "src/num_ext.cyr:1118",
   "fn": "num_tridiag_solve (n == 1 fast path)",
   "guarded_quantity": "|b[0]| \u2014 the single diagonal coefficient of a 1x1 system, read raw from the caller's array at src/num_ext.cyr:1117 (`var b0 = load64(b);`) with no normalisation and no subtraction before it. Dimensions: an entry of A in Ax = d, i.e. units of [d]/[x]. Degree-1 homogeneous: scale the system (A -> sA, d -> sd, x unchanged) and this quantity scales by exactly s.",
   "protects": "The division `f64_div(load64(d), b0)` on the next line (src/num_ext.cyr:1121) \u2014 x[0] = d[0]/b[0]. Nothing else: no sqrt, no acos, no loop bound, no allocation in this branch (the alloc pair is at :1130-1133, after the early return).",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "VERDICT SURVIVES ADVERSARIAL REFUTATION; all five routes checked against the source, four dead, the fifth produced sharpenings rather than a refutation. (1) BOUNDED/DIMENSIONLESS \u2014 dead. b0 is a raw caller pointer read; there is no normalisation in the function and none is documented. It carries the units of A, so a 1-D operator discretised in millimetres instead of metres moves a second-difference diagonal by 1e6, and a caller formulating in compliances or carrying a 1e-9 diffusion coefficient in the coefficient lands under 1e-12 on a system whose exact solution is an ordinary O(1) number. Measured: b0 = 4*2^-45 with d = 12*2^-45 divides to a BIT-EXACT 3.0, and the guard refuses it. (2) CALLER NORMALISES \u2014 dead by grep: `num_tridiag_solve` has ZERO internal call sites (the only other src/ hit is the module header list at num_ext.cyr:12). It is a public entry point on raw f64 pointers; there is no caller to normalise. (3) CONVERGENCE/CONDITIONING TOLERANCE \u2014 this is the strongest defense available and it dies exactly at this site. For n > 1 the same EPSILON_F64 guards `denom = b[i] - a[i]*c'[i-1]` (:1157), a catastrophic-cancellation quantity where a stability argument at least has a shape \u2014 though even there an absolute 1e-12 is not a conditioning test; a conditioning test is RELATIVE (|denom| < tol*|b[i]|). At line 1118 there is no subtraction at all, no elimination has run, and the function's own doc comment states the intent verbatim: \"HSB_ERR_DIVISION_BY_ZERO if pivot is zero\". 1e-12 is not a zero test on a quantity with units. (4) BREAKS AN EXISTING TEST \u2014 dead by measurement, not assumption. No test in tests/*.tcyr calls this function with n == 1 at all: modules.tcyr:5985 uses n = 3 (b = 1.0), :6025 n = 5 (b = 2.0), :6057 n = 4 (b = 9.0); abuse.tcyr:1139-1142 uses n = 0 and n = -4 (both caught by the n <= 0 guard at :1114) and :2669 a huge n (caught by HSB_ERR_ALLOC). The entire n == 1 fast path is uncertified today, and an F64_TINY repair at :1118 changes the outcome of ZERO existing assertions. (5) SCALE TEST DOESN'T DISCRIMINATE \u2014 not a refutation, but it found a real gap; see the sharpened scale_test. The filed ladder is arithmetically correct (every k in the list divides to a bit-exact 3.0; k=-45 kills 1e-12, -60 kills DBL_EPSILON, -110 kills 1e-30, -510 kills 1e-150, -1000 kills 1e-300 at b = 3.73e-301) \u2014 but it does NOT pin its own recommended constant: an exact `b0 == 0` repair passes every k AND the b = 0 firing assertion, so the sweep as filed cannot tell F64_TINY from `== 0`. Two additions fix that, plus a boundary case (k = -1024, where b is bit-identical to DBL_MIN) that pins the threshold from above in a single assertion. ADJACENCY NOW VERIFIED (the filing said it could not read f64_lt's NaN semantics): lib/math.cyr:449-455 documents that f64_lt/f64_gt/f64_eq all return 0 for any NaN operand, IEEE-754 unordered, true since 6.2.41. So a NaN pivot passes this guard and every sibling guard in the function and yields NaN output with rc = HSB_ERR_NONE. That is a SECOND, independent defect at the same site \u2014 the guard is wrong in both directions, rejecting solvable systems and accepting NaN \u2014 and note an `== 0` repair would inherit the NaN hole unchanged, so it wants its own filing, not a merge into this verdict.",
   "fabricates": "none - it reports an error. rc = HSB_ERR_DIVISION_BY_ZERO and `out` is untouched (the store at :1121 is after the guard). Confirmed by reading the branch: this guard is NOT a member of the fabricate-a-plausible-answer class the 2026-08-11 audit catalogued (m3_inverse -> identity, cx_div -> zero); the caller gets a real error channel and cannot mistake it for success, so the defect is a FALSE REJECTION of a solvable, perfectly conditioned system \u2014 strictly less severe than the fabricating tier. The diagnosis is still a misstatement: at b0 = 1e-13 the pivot is not zero. \u26a0 Separately, and NOT part of this verdict: on a NaN pivot the function DOES fabricate \u2014 verified via lib/math.cyr:449-455, f64_lt returns 0 for NaN, so control falls through to the store and the caller gets NaN with rc = 0. A NaN is at least self-announcing, unlike an identity matrix, but the rc = 0 is a lie.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308 \u2014 both values decoded and confirmed). This is the constant error.cyr's own comment nominates for precisely this case (\"the right threshold when the guard protects a DIVISION and the only thing that can actually fail is a zero (or subnormal) denominator\"), it is the single constant-gate-checked definition the three former module-local aliases collapsed into, and it matches geo.cyr's 31 repaired sites. It needs no scale chosen for it. \u26a0 SHARPENED \u2014 record the residual honestly rather than claiming \"nothing more\": F64_TINY is NOT literally minimal. Measured, b0 = 4*2^-1025 (subnormal, 0x0008000000000000) with d = 12*2^-1025 divides to a bit-exact 3.0, and so does every k down to -1070; F64_TINY rejects all ~52 subnormal binades of them. The literal minimum is `f64_eq(b0, 0)`. F64_TINY is still the right choice, for three reasons that are measurements rather than taste: it is the project's settled, gate-checked constant (one definition, one literal to verify); a subnormal pivot against a normal RHS overflows rather than solving (12 / 2^-1040 = +Inf, verified), which the exact-zero repair would return with rc = 0; and the residual band is 52 binades of pure subnormal, where the quotient's own precision is already degraded. As filed, this does not prevent d[0]/b[0] overflowing to +/-Inf for a huge d over a small-but-normal b \u2014 same trade geo.cyr accepted in its reciprocal repair, where an Inf propagates honestly and is detectable while a wrongly-rejected solve is not. Do NOT fold the NaN hole into this constant: no threshold repair closes it, because f64_lt is unordered-false on NaN by design; that needs an explicit `f64_eq(b0, b0) == 0` test and is a separate filing.",
   "scale_test": "SCALE COVARIANCE, BIT-EXACT ON THE RAW i64 \u2014 never through f64_to (2.13.0), and never through f64_approx_eq/TOL_1EM12, which is the comparison the two existing tridiag fixtures use (tests/modules.tcyr:6033, :6062) and which would let a repair that perturbs the quotient pass unseen. Fix the 1x1 system b = [4], d = [12], x = 3 exactly; scale BOTH sides by s = 2^k so the scaling is exact in binary FP and the quotient is bit-identical to 3.0 at every k (verified in python for every value below). PASSING SWEEP \u2014 assert rc == HSB_ERR_NONE AND load64(out) bit-identical to the k = 0 result, for k in {+1000, +500, +100, +40, 0, -45, -60, -110, -260, -510, -1000, -1024}. Bracketing (b = 4*2^k): k=-45 -> 1.14e-13 kills the shipped EPSILON_F64 (fires below 1e-12 at k <= -42); k=-60 -> 3.47e-18 kills a DBL_EPSILON (2.22e-16) repair; k=-110 -> 3.08e-33 kills a 1e-30 repair; k=-510 -> 1.19e-153 kills a 1e-150 repair; k=-1000 -> 3.73e-301 kills a 1e-300 repair and, in fact, EVERY threshold above 3.73e-301. \u2b50 ADDED, AND IT IS THE STRONGEST SINGLE ASSERTION IN THE LIST: k = -1024 makes b = 4*2^-1024 BIT-IDENTICAL to DBL_MIN (0x0010000000000000, verified) with d = 3*2^-1022 still normal and the quotient still exactly 3.0 \u2014 it must SUCCEED, which kills every threshold strictly above DBL_MIN in one shot, including a 2*DBL_MIN or 1.1*DBL_MIN fudge that the k-ladder alone cannot see. The ladder LOCATES; this one PINS from above. The k >= 0 half is the CONTROL: it proves the sweep detects a threshold and not merely underflow, and catches a repair that accidentally introduces an upper bound. Span 2024 binades ~ 609 decades, against the 20 decades 2.11.1's quaternion work established as the minimum that kills all plausible mutants. FIRING ASSERTIONS \u2014 the filing had ONE and it is not enough; four are needed and each kills a different mutant: (i) b = [+0.0], d = [12] -> HSB_ERR_DIVISION_BY_ZERO, kills guard DELETION (without it a deleted guard returns +Inf with rc = 0 and the whole passing sweep survives); (ii) b = [-0.0] -> HSB_ERR_DIVISION_BY_ZERO, proves the compare goes through f64_abs and not a signed `f64_lt(b0, EPS)`, which would pass every negative pivot including -0.0; (iii) \u2b50 b = [4*2^-1025] = 0x0008000000000000 (the largest magnitude strictly below DBL_MIN) with d = [12*2^-1025] -> HSB_ERR_DIVISION_BY_ZERO. This is the ONLY assertion that separates the recommended F64_TINY from an exact `f64_eq(b0,0)` repair \u2014 without it BOTH pass the entire sweep and every other firing case, i.e. the test as filed does not pin its own recommended constant. Note honestly in the fixture comment that this case's true quotient IS exactly 3.0, so the assertion encodes the deliberate 52-binade over-rejection documented on F64_TINY, not a mathematical necessity; (iv) b = [2^-1040], d = [12] -> HSB_ERR_DIVISION_BY_ZERO, the case where F64_TINY earns its keep over `== 0` (the exact-zero repair returns +Inf with rc = 0; verified 12 / 2^-1040 = inf). Together (i)+(ii) kill deletion and sign errors, (iii)+k=-1024 pin the threshold to (2^-1023, 2^-1022] = DBL_MIN exactly. \u26a0 Prove each mutant is INSTALLED with a grep before believing any kill count (2.13.0's harness lesson \u2014 a mutation that silently fails to apply reports \"0 failing\", which reads identically to \"the suite is blind\"). \u26a0 NOT COVERED BY ANY THRESHOLD REPAIR, do not expect this sweep to reach it: b = [NaN] passes every guard (lib/math.cyr:449-455) and returns NaN with rc = 0."
  },
  {
   "site": "src/num_ext.cyr:1137",
   "fn": "num_tridiag_solve (forward sweep, first pivot, n > 1)",
   "guarded_quantity": "|b[0]| \u2014 the first diagonal coefficient of the n x n tridiagonal matrix, loaded raw at :1136 (`var b0 = load64(b);`) with no normalisation, no subtraction and no reference to any other entry. Units of an entry of A, i.e. [d]/[x]; degree-1 homogeneous in a rescaling of the system. CONFIRMED BY MEASUREMENT, not only by dimensional argument: scaling ONLY row 0 (b[0], c[0], d[0]) by t = 2^m is a pure row operation that leaves the exact solution untouched \u2014 c'[0] = (t*c[0])/(t*b[0]) = -0.5 bit-exactly for every t \u2014 and the shipped guard's verdict nevertheless flips with t. A quantity whose guard verdict changes under a transformation that provably does not change the answer is degree-1, not dimensionless.",
   "protects": "The two divisions immediately below it at :1142 and :1144 \u2014 c'[0] = c[0]/b[0] and d'[0] = d[0]/b[0]. Both are plain f64_div. There is no iteration, no cancellation and no other failing operation between the guard and the divisions, so the only thing that can actually fail is a zero (or subnormal-driven overflow) denominator. NOT redundant with the :1157 loop guard: :1157 runs first at i = 1, after both of these divisions have already executed.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FIVE CLOSED, AND THE DEFECT REPRODUCES.\n\n(1) Dimensionless/bounded? No. b[0] is a raw A entry. Proven operationally by the row-scaling result above, not merely asserted.\n(2) Caller normalises? No caller exists. `grep -rn num_tridiag_solve src/` returns only the header comment at :12 and the definition at :1113 \u2014 it is a public entry point with zero internal callers, so there is no normalisation step anywhere to appeal to.\n(3) Convergence tolerance? No. Straight-line code; the guarded value is consumed by two divisions three lines later.\n(4) Would F64_TINY change an existing passing test? No. All six call sites in tests/ were enumerated: modules.tcyr:6054 (b = 1.0), :6094 (b = 2.0), :6126 (b = 9.0), abuse.tcyr:1139 (n = 0) and :1141 (n = -4) \u2014 both return HSB_ERR_INVALID_INPUT at :1114 before the guard \u2014 and abuse.tcyr:2669 (n = 3e8, returns HSB_ERR_ALLOC at :1132 before the guard). Not one has |b[0]| in [DBL_MIN, 1e-12). The repair is behaviour-preserving for the whole suite.\n(5) Does the scale test discriminate? PARTIALLY \u2014 and this is the one place the filing is wrong; see scale_test. The uniform sweep it proposes does NOT isolate this guard.\n\nMEASURED REPRODUCTION (probes compiled against the real src/num_ext.cyr, cyrius 6.6.2). Uniform-scaled n=4 Poisson tridiag(-1,2,-1), d = (0,0,0,5*s), exact x = (1,2,3,4): rc = HSB_ERR_NONE with bit-identical out at k >= -40, and rc = -4 (HSB_ERR_DIVISION_BY_ZERO) at every k in -41..-1021. The boundary sits exactly where |b[0]| = 2*2^k crosses 1e-12 (2^-39 = 1.82e-12 passes, 2^-40 = 9.09e-13 refused). Row-0-isolated sweep (see scale_test): shipped code refuses at m <= -41 down to m = -1021, i.e. ~295 decades of false rejection on a system whose four output words are bit-identical to the unscaled result at every one of those scales.\n\nWith BOTH guards moved to F64_TINY, the full uniform sweep over k = 1023..-1022 (2046 scales) gives 0 rc failures and bit-identical out at 2044 of 2046; the two exceptions are k = 1022 and 1023, where the FIXTURE overflows (5*2^1022 > DBL_MAX) \u2014 my instrument's limit, not the guard's.",
   "fabricates": "none - it reports an error. rc = HSB_ERR_DIVISION_BY_ZERO with `out` untouched (verified: `out` is written only in back-substitution at :1176 and :1184, both far below this return). This is a FALSE REJECTION with a working error channel, not the fabricate-a-plausible-answer class \u2014 same characterisation as :1118. Confirmed by measurement: every refused scale returned -4 and the caller's out buffer was never written. The two alloc()'d workspaces cp and dp are abandoned on this return path, but the success path abandons them too (this file frees nothing; the project runs a bump allocator that never frees), so it is not a leak introduced by the guard.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308). b[0] arrives by a raw load with no subtraction, so there is no cancellation to reason about and no stability argument in play; the only thing that can fail is a zero or subnormal denominator, which is exactly what F64_TINY names, and error.cyr:41-44 already states this is the constant for a guard that protects a DIVISION.\n\nMEASURED SUFFICIENCY, not just defensibility: with the guard on F64_TINY the row-isolated fixture returns rc = 0 and bit-identical out down to m = -1021, where b[0] = 4.45e-308 \u2014 a factor of exactly 2 above DBL_MIN \u2014 with c[0] = 2.2e-308 also normal and no subnormal anywhere in the computation. So DBL_MIN is not merely the dimensionally-correct floor, it is empirically reachable: the largest scale-free threshold this fixture can still exonerate is within one binade of it.\n\n\u26a0 SCOPE NOTE FOR WHOEVER APPLIES THIS: repairing :1137 alone is correct and is provable in isolation (row-scaled fixture below), but it does NOT make a UNIFORMLY-scaled system solvable \u2014 the :1157 loop guard, whose denominators here are 1.5s, 1.333s and 1.25s, refuses the same inputs. Measured: with F64_TINY at :1137 and EPSILON_F64 left at :1157, the uniform sweep still returns -4 at k = -45, -60, -110, -510 and -1000. The two guards are one repair for the caller even though they are two findings.",
   "scale_test": "\u26d4 THE FILING'S PROPOSED TEST IS WRONG AND MUST BE REPLACED. A uniform rescaling of tridiag(-1,2,-1) drives b[0] = 2s and the later pivots 1.5s / 1.333s / 1.25s down TOGETHER \u2014 all four inside one binade \u2014 so it cannot attribute a rejection to :1137. MEASURED: applying the F64_TINY repair to :1137 ONLY and re-running the filing's own sweep still yields rc = -4 at k = -45, -60, -110, -510, -1000. As written the test reports a CORRECT repair of this guard as a failure, and passes only when :1157 is repaired too. Keep it as a joint end-to-end check, but it is not this guard's discriminator.\n\n\u2705 USE ROW-0 SCALING INSTEAD \u2014 it isolates :1137 exactly. Same system, n = 4, a = c = (-1,-1,-1), b = (2,2,2,2), d = (0,0,0,5), exact x = (1,2,3,4) (2*1-2=0; -1+4-3=0; -2+6-4=0; -3+8=5). Now multiply ONLY row 0 \u2014 b[0], c[0] and d[0] \u2014 by t = 2^m. This is a row operation on Ax = d, so x is unchanged EXACTLY, and c'[0] = (-t)/(2t) = -0.5 bit-exactly for every t, which leaves denom[1..3] at 1.5, 1.333, 1.25 \u2014 O(1), so :1157 CANNOT fire and every verdict is attributable to :1137 alone.\n\nAssert for m in {+1000, +500, 0, -40, -41, -45, -60, -110, -260, -510, -1000, -1021}: rc == HSB_ERR_NONE AND all four out[] words bit-identical to the m = 0 result.\n\nBRACKETING (b[0] = 2*2^m): m = -41 kills the shipped 1e-12 (b[0] = 9.09e-13); -60 kills 2.22e-16; -110 kills 1e-30; -260 kills 1e-78; -510 kills 1e-150; -1000 kills 1e-300; -1021 kills anything above 4.45e-308, i.e. every plausible threshold down to within a factor of 2 of DBL_MIN. F64_TINY survives all twelve. MEASURED on the real tree: shipped returns -4 at m <= -41 through -1021; the :1137-only repair returns rc = 0 with bitdiff = 0 at all twelve, and no value in the computation is subnormal at any of them, so a pass is a real pass. Control: the m >= 0 half (0, +500, +1000) must also be bit-identical \u2014 it is.\n\n\u26a0 FIRING ASSERTION REQUIRED, or deleting the guard survives the whole sweep: b = (0.0, 2, 2, 2) must return HSB_ERR_DIVISION_BY_ZERO at every m (verified: shipped and repaired both return -4; with the guard deleted, c'[0] = -1/0 = -Inf and d'[0] = 0/0 = NaN, so out fills with NaN at rc = 0).\n\n\u26a0 INSTRUMENT CAVEAT \u2014 my own probe was wrong before the code was. Build 2^k as (1023 + k) << 52 and stay inside k in [-1022, 1021]. At k = -1040 that expression underflows the exponent field into a huge NEGATIVE f64, the guard passes, and the row scores a bogus PASS; at k >= 1022 the fixture's own 5*2^k overflows to +Inf and scores a bogus FAIL. Both were observed. Check the probe before believing the probe."
  },
  {
   "site": "src/num_ext.cyr:1157",
   "fn": "num_tridiag_solve (Thomas elimination loop, interior pivot)",
   "guarded_quantity": "|denom| = |b[i] - a[i]*c'[i-1]|, the Thomas-eliminated pivot. DIMENSIONS: c'[i-1] = c[i-1]/denom[i-1] is a ratio of two A-entries and is therefore DIMENSIONLESS, so a[i]*c'[i-1] carries the units of A and denom carries the units of A. Degree-1 homogeneous, exactly like b[0] \u2014 NOT squared. MEASURED, not argued: the identical 2x2 matrix at scale 2^0 returns rc = 0 and at scale 2^-20 returns rc = -4 on the shipped tree with b[0] provably above the other guard's threshold, so the guarded quantity demonstrably tracks the units.",
   "protects": "Two divisions in the loop body: c'[i] = c[i]/denom (when i < n-1) and d'[i] = (d[i] - a[i]*d'[i-1])/denom. It also transitively protects the next iteration, since denom feeds c'[i] which feeds denom[i+1]. It is a straight-line, non-iterative algorithm \u2014 there is no convergence quantity anywhere in the function, and the function's own contract comment at :1105 says \"HSB_ERR_DIVISION_BY_ZERO if pivot is zero\".",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "All five refutation avenues were tried against the running code and every one failed. (1) NOT dimensionless/bounded \u2014 measured on the SHIPPED tree with NO mutation: A = s*[[1,-1],[-1,1+2^-20]], d = s*(0, 2^-20), exact solution x = (1,1), condition number ~2^21 (about 2.1e6, six digits of sixteen). At s = 2^0 and s = 2^-10 it returns rc = 0 with out bit-exactly 0x3FF0000000000000, 0x3FF0000000000000; at s = 2^-20 and s = 2^-30 it returns rc = -4 with `out` untouched. b[0] = s = 9.5e-7 and 9.3e-10 there, both four to six orders ABOVE the 1e-12 that :1137 tests, and n = 2 so :1118 is unreachable \u2014 :1157 is the only guard that can fire, and a one-line mutation of :1157 alone (EPSILON_F64 -> F64_TINY, grep-proven installed) flips exactly those two rows to rc = 0 with bit-exact (1,1) and moves nothing else. The same matrix, only the units changed, decided differently. (2) NO caller normalisation \u2014 it is a public entry point taking raw a/b/c/d pointers; there is no other in-repo caller, and nothing scales the arrays. (3) NOT a convergence tolerance \u2014 the guard sits immediately before two divisions by `denom` in a direct O(n) elimination with no iteration count and no residual. (4) The proposed correct_threshold breaks NO existing test \u2014 measured: with :1157 at F64_TINY (mutant grep-proven installed) all five suites return identical counts, 416/359/1818/233/776, 0 failed, rc 0. (5) The scale_test DOES discriminate \u2014 measured below. \u26d4 The absolute 1e-12 gets both directions wrong, and the second direction is now measured too: a denom of 2^-39 = 1.8e-12 PASSES the shipped guard, c'[i] = 2^1000/2^-39 overflows to +Inf, and the function returns rc = HSB_ERR_NONE with NaN (0xFFF8000000000000) in out[0] and out[1]. So the shipped threshold rejects clean small-scale systems AND accepts systems it turns into NaN with a success code. It is not a stability check by any reading; the CORRECT_TOLERANCE reading is refuted by measurement, not only by argument. \u26a0 Prior art: docs/audit/2026-08-11-v2.11.0-full.md:54 already lists `num_newton` / `num_tridiag_solve` \u2014 \"DIVISION_BY_ZERO on well-conditioned input | pivot 1e-12\" \u2014 as an instance of the 24-member class. This is a re-confirmation of an open, never-acted-on finding, and docs/audit/2026-09-09-roadmap-verification.md:488 records that no commit in 2.11.3/2.11.4/2.11.5 touched an EPSILON_F64 occurrence.",
   "fabricates": "none - it reports an error. rc = HSB_ERR_DIVISION_BY_ZERO (-4); `out` is untouched (verified: pre-zeroed out stayed 0 across the whole declining sweep) because back-substitution is the only writer and sits past the loop. cp/dp hold partial results but are workspace the caller never sees. A FALSE REJECTION with an intact error channel, not the fabricate-a-plausible-answer class \u2014 at denom = 1.25*2^-45 the pivot is not zero. \u26a0 MEASURED COMPANION, attributable to the same wrong threshold but to its non-firing half: when the guard passes a denom whose quotient overflows (denom = 2^-39, c = 2^1000) the function DOES fabricate \u2014 rc = HSB_ERR_NONE with NaN in out. That is the guard failing to protect the division it exists for, so the repair must be validated with a no-NaN assertion on `out`, not only with a return code.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, 0x0010_0000_0000_0000 = DBL_MIN = 2.2250738585072014e-308) for the divide guard \u2014 exactly what makes f64_div undefined, per 2.10.2, and exactly what error.cyr:40-44's own comment already prescribes (\"the right threshold when the guard protects a DIVISION and the only thing that can actually fail is a zero (or subnormal) denominator\"; EPSILON_F64 is reserved there for \"a genuinely dimensionless, bounded quantity\", which denom is not). MEASURED SAFE: installing it changes no existing assertion (3602 pass, 0 fail, identical per-suite counts) and preserves genuine singular detection at every scale (the exactly-zero-pivot fixture still returns -4 at k = +1000 down to -1022). If a conditioning report is genuinely wanted it must be a SEPARATE, NAMED, RELATIVE test \u2014 |denom| < tol * (|b[i]| + |a[i]*c'[i-1]|), relative to the magnitudes that cancelled to produce it \u2014 returning HSB_ERR_SINGULAR_PIVOT, which ALREADY EXISTS as -9 at src/error.cyr:17 and is unused on this path. That is a different contract with a different error code and must be documented as such, not smuggled in as an absolute epsilon on a dimensional quantity. \u26a0 Neither threshold prevents the quotient from overflowing (measured above: the shipped 1e-12 lets denom = 1.8e-12 through and the function returns NaN with rc = 0), so the relative test is not optional polish \u2014 it is the half that catches the case the absolute epsilon was wrongly credited with catching.",
   "scale_test": "THREE PARTS, ALL EXECUTED AGAINST THE RUNNING CODE ON cyrius 6.6.2; results quoted are measured, not predicted.\n\n(A) MUTATION-FREE WITNESS \u2014 the primary assertion, needs NO isolation, so it cannot be dismissed as an artefact of the harness. n = 2, a = c = [-s], b = [s, (1+2^-20)*s], d = [0, 2^-20*s], exact solution x = (1,1), condition number ~2^21. Because b[0] = s while denom[1] = 2^-20*s, guard :1137 is silent over a 20-binade window that :1157 alone occupies. Assert rc == HSB_ERR_NONE and out[0] == out[1] == 0x3FF0000000000000 BIT-EXACTLY at k = 0, -10, -20, -30. MEASURED on the shipped tree: k = 0 -> rc 0, (1,1); k = -10 -> rc 0, (1,1); k = -20 -> rc -4, out untouched; k = -30 -> rc -4, out untouched. With :1157 alone at F64_TINY: all four -> rc 0, bit-exact (1,1). This single fixture reproduces AND attributes the defect with one mutated line.\n\n(B) DEEP SCALE COVARIANCE, BIT-EXACT, WITH PER-GUARD ISOLATION \u2014 for bracketing. Fixture n = 4, a = c = [-1,-1,-1], b = [2,2,2,2], d = [0,0,0,5], x = (1,2,3,4), scaled by s = 2^k; assert rc == HSB_ERR_NONE and all four out words BIT-IDENTICAL to k = 0 (0x3FEFFFFFFFFFFFFE, 0x3FFFFFFFFFFFFFFE, 0x4007FFFFFFFFFFFF, 0x4010000000000000) for k in {+1000, +500, +100, +40, 0, -45, -60, -110, -260, -510, -1000, -1015, -1020, -1021, -1022}. \u26a0 ISOLATION IS LOAD-BEARING AND WAS VERIFIED: b[0] = 2*2^k trips :1137 at k <= -41, so on the shipped tree the whole low half fails for the WRONG reason \u2014 measured, repairing :1157 alone leaves every one of those rows at -4. Run with :1118 and :1137 already at F64_TINY and ONLY :1157 reverted. MEASURED in that configuration: shipped EPSILON_F64 gives rc 0 for k >= 0 and rc -4 for every k from -45 to -1022; F64_TINY gives rc 0 and bit-identical output at all fifteen rungs. BRACKETING against min|denom| = 1.25*2^k (measured values): k=-45 -> 3.55e-14 kills 1e-12; -60 -> 1.08e-18 kills 2.22e-16; -110 -> 9.63e-34 kills 1e-30; -260 -> 6.75e-79; -510 -> 3.73e-154 kills 1e-150; -1000 -> 1.17e-301 kills 1e-300; -1022 -> 2.78e-308 kills everything down to within 1.25x of DBL_MIN. Only thresholds inside [2.225e-308, 2.78e-308) survive, i.e. the sweep brackets every threshold a repair could plausibly choose. 2045 binades / ~616 decades against the 20-decade floor 2.11.1 established. \u26a0 TRAP, HIT AND CORRECTED IN THIS RUN: the natural helper `_p2(e) = (1023 + e) << 52` silently produces a large NEGATIVE value for e < -1022 (e = -1030 yields -2^1018, not 2^-1030), and that bogus rung \"passed\" under the shipped guard and read as evidence of subnormal tolerance. -1022 is the deepest rung this construction can express; anything deeper needs a subnormal-safe constructor. Check the probe before believing the probe.\n\n(C) FIRING ASSERTION, verified to kill a deletion mutant. a = c = [-s,-s,-s], b = [s, s, 2s, 2s], d = [s,s,s,s]: c'[0] = -1 exactly and denom[1] = s - s = EXACTLY 0 at every scale, with a perfectly healthy b[0] = s. Assert HSB_ERR_DIVISION_BY_ZERO and `out` untouched at k in {+1000, +100, 0, -45, -110, -510, -1000, -1022} (run under the same isolation, since b[0] scales too and :1137 would otherwise answer with the same code). MEASURED under the F64_TINY repair: -4 at all eight rungs, out untouched. MEASURED with the guard DELETED: rc 0 and out = 0xFFF8000000000000 (NaN) at all eight \u2014 mutant killed. \u26d4 AND THE EXISTING SUITE DOES NOT: with :1157 deleted outright, all five suites still pass 416/359/1818/233/776, 0 failed. Nothing in 3602 assertions tests this guard in either direction today.\n\n(D) NO-FABRICATION ASSERTION, from the measured second failure mode: n = 3, a = [-1,-1], b = [1, 1+2^-39, 1], c = [-1, 2^1000], d = [1,1,1]. denom[1] = 2^-39 = 1.8e-12 passes BOTH the shipped 1e-12 and F64_TINY; measured result on the shipped tree is rc = HSB_ERR_NONE with out[0] = out[1] = NaN. Assert that `out` contains no NaN whenever rc == HSB_ERR_NONE. This is the assertion that forces the relative HSB_ERR_SINGULAR_PIVOT test to be written rather than assumed, and it fails identically before and after the threshold change \u2014 proving the absolute epsilon never bought the protection it is credited with."
  },
  {
   "site": "src/ode.cyr:251",
   "fn": "ode_dopri45_trajectory",
   "guarded_quantity": "f64_sub(tf, t) \u2014 the remaining span of the INDEPENDENT VARIABLE between the current step time and the requested final time. Dimensions: time^1, degree 1, entirely caller-scaled (t0/tf/dt0 are raw caller arguments; the function nondimensionalises nothing). Seconds\u2192milliseconds multiplies it by 1000. CONFIRMED: it is neither dimensionless nor bounded, so it is outside the class error.cyr's own F64_TINY comment reserves EPSILON_F64 for (\"a genuinely dimensionless, bounded quantity \u2014 a normalised cosine, a barycentric weight, a parameter in [0,1]\"). ode.cyr's own Newton solvers already use the relative form EPSILON_F64*(1+|z|) at lines 346/395/500; line 251 is the module's lone absolute comparison against a caller-scaled quantity.",
   "protects": "Not a division \u2014 it is the loop's arrival test, above `if (f64_gt(t, tf))`. CORRECTED from the filing: the stated mechanism (the clamped final step h = tf - t falling below ulp(t)) is NOT reachable. When t and tf are same-sign and within a factor of 2 \u2014 always true at the end of a run \u2014 Sterbenz makes `tf - t` exact and `t + (tf - t)` is then exactly tf, a representable value, so the clamped step always lands on tf. Measured: the `t + h == t` condition never fired on ANY row of either sweep for a legitimate integration. The real non-advancement path is the rejection-driven shrink (dt0 = 0, tol = 0 in abuse.tcyr), and this guard never sees it \u2014 `tf - t` is O(1) there and the 100000-step budget is what stops the spin. So the guard protects nothing that would otherwise fail; it only decides when to stop, and an absolute constant makes that decision wrong at small time scales.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "DEFECT CONFIRMED at small scale, REFUTED at large epoch. Reproduced on the real code (cycc 6.6.2, y' = y/T on [0,T], y0 = 1, dt0 = T/10, tol = 1e-8; exact y(T) = e for every T): T = 1e6/1e3/1/1e-3/1e-6/1e-9 all give n = 12, y_end = 2.718281834, rc = 0 \u2014 then T = 1e-11 gives n = 11, y_end = 2.634101143; T = 1e-12 gives n = 2, y_end = 1.105170918; T = 1e-13 through 1e-300 give n = 1, y_end = 1.000000000 \u2014 every one with rc = HSB_ERR_NONE. The crossover sits exactly on EPSILON_F64, and it is a unit choice, not a physics choice, that crosses it. REFUTED HALF: the filing's large-epoch evidence does not exist. At t0 = 1e15 the shipped code returns n = 9, y_end = 2.171795922, span = 1.0 exactly (it DOES reach tf), rc = 0 \u2014 no NaN, no 11741 samples; and an absolute-DBL_MIN mutant produces BYTE-IDENTICAL sweep-B output, so that error is ambient epoch resolution (dt0 = 0.1 < ulp(1e15) = 0.125, so t advances in 0.125 quanta while the integrator steps with h = 0.1) and is not attributable to this threshold. At t0 >= 1e16, tf = t0 + 1 rounds to t0 (measured: both 0x4341c37937e08000), so returning 1 sample is CORRECT for every candidate threshold including `tf - t <= 0`. The guard is too LARGE at small scale only. Refutation avenues checked and closed by measurement, not assumption: the four abuse.tcyr trajectory assertions all still pass under the repair, and hisab/abuse/modules re-run with the repaired module give 416/776/1827 passed, 0 failed \u2014 identical to the shipped module.",
   "fabricates": "HSB_ERR_NONE (rc = 0) plus a plausible short trajectory. Measured, tol = 1e-8, dt0 = T/10: T <= 1e-13 \u2192 n = 1, out_ys holds only y0, y_end = 1.000000000 where the answer is e \u2014 a completely un-integrated trajectory reported as success; T = 1e-12 \u2192 n = 2, y_end = 1.105170918, 59% short; T = 1e-11 \u2192 n = 11, y_end = 2.634101143, still 3% off. The filing's \"y_end = NaN after 11741 duplicate-t samples at t0 = 1e15\" is NOT reproducible and is struck \u2014 that row returns n = 9, y_end = 2.171795922, and is threshold-independent.",
   "correct_threshold": "Two tests, neither needing a scale chosen for it. (1) Arrival: `f64_le(f64_sub(tf, t), 0)` \u2014 replaces both this guard and the `f64_gt(t, tf)` line below it; preserves abuse.tcyr's \"tf < t0 returns immediately\" (measured rc = 0, n = 1). (2) Non-advancement, immediately after the overshoot clamp: `if (f64_eq(f64_add(t, h), t) == 1) { return HSB_ERR_NO_CONVERGENCE; }` \u2014 the literal condition that makes the loop fail, scale-free by construction, no constant. It must NOT return HSB_ERR_NONE; the contract already declares HSB_ERR_NO_CONVERGENCE. STRUCK from the filing: the \"equivalent closed-form relative bound\" `tf - t <= |t|*DBL_EPSILON` is NOT equivalent and is itself a fabricating guard \u2014 measured at t0 = 1e15 it stops at span 0.875 (0.125 short of tf) with y_end = 1.972024457 vs e and rc = 0, strictly worse than shipped. Measured effect of the pair: sweep A correct (n = 12, y_end = 2.718281834) at every T from 1e6 to 1e-300; sweep B unchanged from shipped at every t0; the dt0 = 0 abuse case now returns rc = -8 at n = 1 instead of after 100001 wasted pushes (both existing assertions still pass); hostile inputs (tf/t0/dt0/tol = NaN, \u00b1inf) return the same rc as shipped, several of them sooner; all three ode-including suites 0 failed.",
   "scale_test": "Sweep B is DROPPED as a discriminator \u2014 measured, it kills nothing: shipped and absolute-DBL_MIN give byte-identical output on every row. Two sweeps replace it. SWEEP A (scale invariance, e-oracle): y' = y/T on [0, T], y0 = 1, dt0 = T/10, tol = 1e-8, exact y_end = e for every T. T = 10^k for k in {6, 3, 0, -3, -6, -9, -11, -12, -13, -15, -20, -30, -60, -150, -300}. Assert rc == HSB_ERR_NONE implies vec_len(out_ts) >= 3 AND |y_end - e| <= 1e-6*e. STOP AT 1e-300: below it the ORACLE overflows, not the guard \u2014 y' = y/T needs 1/T, and at T ~ 1e-308 the stage coefficient _DP_A52 = -11.59 times 1e308 goes to -inf, so EVERY variant including the correct repair returns NaN (measured, n = 4, y_end = -nan, rc = 0). The filing's request to run this family to DBL_MIN would have produced a NaN misread as a guard kill. SWEEP A' (coverage; the sweep that actually kills absolute DBL_MIN, which sweep B claimed to and does not): y' = y, y0 = 1 \u2014 derivative bounded by e for ANY span, so nothing overflows \u2014 t0 = 0, tf = T, dt0 = T/10; assert `rc == HSB_ERR_NONE implies t_end == tf` bit-exactly. T = 1e0, 1e-12, 1e-20, 1e-300, and the bit patterns 0x0010000000000000 (DBL_MIN), 0x0008000000000000 (1.1e-308), 0x0000000000010000 (3.2e-319), 0x0000000000000010 (7.9e-323), 0x0000000000000001 (1 ulp). Do NOT build these with pow10 \u2014 ganita's pow underflows 1e-310 to exactly 0.0, which makes tf == t0 and every variant look correct. MEASURED MUTANT KILLS: shipped 1e-12 dies at A/T=1e-11 and A'/T=1e-12; absolute 2.2e-16 survives A to 1e-13, dies at A/T=1e-15 (n = 10, y = 2.391804783) and A/T<=1e-20; absolute DBL_MIN survives all of A and all of A' down to 1e-300, and dies at A'/T=DBL_MIN (n = 2, t_end != tf) and below; relative |t|*DBL_EPSILON passes A and A' but dies on the retained large-epoch CONTROL below; the `tf-t<=0` + `t+h==t` pair passes A and A' everywhere and at the 1-ulp span returns rc = HSB_ERR_NO_CONVERGENCE rather than a false success. CONTROLS that must not move: A/T=1 and A'/T=1 give n = 12, y_end = 2.718281834; and keep exactly two sweep-B rows as controls \u2014 y' = y on [t0, t0+1], t0 = 0 (n = 12, y_end = 2.718281834) and t0 = 1e15 (n = 9, y_end = 2.171795922, span = 1.0) \u2014 the second being the row that kills the relative-|t| candidate."
  },
  {
   "site": "src/transforms.cyr:121",
   "fn": "hisab_inverse_lerp",
   "guarded_quantity": "`denom = f64_sub(b, a)` under `f64_abs` \u2014 the WIDTH of the caller's input interval, in the caller's own units (metres, seconds, kelvin, pixels). Dimensions: length^1 (or whatever unit the caller supplies), strictly LINEAR in the problem scale. Re-express the same problem in millimetres instead of metres and the quantity is multiplied by 1000. Not dimensionless, not bounded, not squared. Confirmed linear by measurement, not by argument: on the small side the shipped guard first fabricates at decade 13 (s = 1e-13, i.e. exactly one decade past the 1e-12 literal), and `hisab_remap`, whose interval is 10s rather than s, first fabricates exactly one decade later at 14 \u2014 the offset a linear quantity predicts.",
   "protects": "The division on the next line, `f64_div(f64_sub(value, a), denom)`. Nothing else in the function can fail \u2014 there is no iteration, no sqrt, no acos domain, no convergence criterion. `hisab_lerp` above it and `hisab_remap` below it are pure add/mul/forwarding.",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FIVE CLOSED BY MEASUREMENT ON THIS TREE (probes under scratchpad/eps/refute, built against src/transforms.cyr at HEAD, cyrius 6.6.2).\n\n(1) DIMENSIONLESS/BOUNDED? No. The RESULT is a dimensionless fraction of the interval, so the same geometry must give the same number in any units \u2014 and it does not. Measured bit-exactly: hisab_inverse_lerp(0, s, s/2) == 0.5 for s = 1e0 through 1e-12 and flips to 0.0 at s = 1e-13. Identical code with the guard at F64_TINY stays bit-exact 0.5 through s = 1e-307. A guard that makes the same problem answer differently in different units is a statement about units, not arithmetic.\n\n(2) CALLER NORMALISES? No. `grep -rn 'inverse_lerp\\|hisab_remap' src/` returns exactly two lines tree-wide: the definition (transforms.cyr:119) and `hisab_remap` (transforms.cyr:126), which forwards `in_lo`/`in_hi` verbatim. It is a public entry point with no normalising wrapper anywhere in the 35 modules.\n\n(3) CONVERGENCE TOLERANCE? No. Three lines, one division, no loop.\n\n(4) WOULD F64_TINY BREAK AN EXISTING PASSING TEST? Checked, not assumed. There are exactly two test call sites tree-wide: tests/hisab.tcyr:296 `hisab_inverse_lerp(0, 10, 5)` and tests/foundation.tcyr:1088 `hisab_remap(5, 0, 10, 0, 100)`. Both re-run through an F64_TINY-guarded copy return bit-identical values (0.5 and 50). The degenerate case `hisab_inverse_lerp(3, 3, 7)` still returns 0 under F64_TINY (denom is exactly 0, which is < DBL_MIN), so the documented degenerate behaviour is preserved. Baseline is green for the comparison to mean anything: hisab 416/416, foundation 359/359.\n\n(5) DOES THE SWEEP DISCRIMINATE? Yes, and the bracketing is now measured rather than argued. Every threshold a repair might plausibly choose dies strictly inside 0..307: 1e-9 at decade 10, 1e-15 at 16, DBL_EPSILON (2.22e-16) at 16, 1e-100 at 101, 1e-200 at 201, shipped 1e-12 at 13. A wrong-quantity mutant guarding the NUMERATOR instead of `denom` dies at 12. The correct repair passes all 308 decades and first fails at 308.\n\nThe original triage was right on every load-bearing point. What it did NOT know is in `correct_threshold` and `scale_test` below: the depth is confirmed (not merely derived), the a = 0 origin is FORCED rather than lazy, one whole class of repair is invisible to the covariance sweep and is killed only by the boundary probe, and the proposed large-side mirror kills nothing.",
   "fabricates": "Exactly 0.0 (measured bit pattern 0, not merely \"approximately zero\"), and 0.0 is ALSO the legitimate answer for value == a, so the caller cannot distinguish the fabrication from a real result. There is no rc, no out-param, no sentinel \u2014 the function's only channel is the f64 it returns. `hisab_remap` (transforms.cyr:125) inherits it whole and silently returns `out_lo`: measured, hisab_remap(5s, 0, 10s, 0, 100) returns exactly 0 instead of 50 from s = 1e-14 downward. Both fabrications are silent and rc-less; nothing in either function can report.",
   "correct_threshold": "F64_TINY (src/error.cyr:45, `0x0010_0000_0000_0000` = DBL_MIN). geo.cyr:26, autodiff.cyr:17 and quat.cyr:12 already alias it for exactly this class of guard, so the repair adds no constant and needs no new entry in scripts/check-constants.sh.\n\n\u26a0 SHARPENED \u2014 DBL_MIN is the right choice here for CONSISTENCY with the rule error.cyr:45 already settled, but it is NOT the unique arithmetic failure boundary, and the repair comment must not claim it is. Measured, three variants side by side over 340 decades: shipped 1e-12 is bit-exact through decade 12; F64_TINY through 307; a minimal `denom == 0`-only guard through 308. So DBL_MIN gives up exactly ONE decade (s = 1e-308) in which the division would still have returned a bit-exact 0.5 \u2014 a subnormal denominator divides fine. And in the other direction DBL_MIN does not buy what a careless reader assumes: it bounds the RECIPROCAL, not the quotient, so a large `value - a` over a denom just above DBL_MIN still overflows to \u00b1Inf. The honest statement is \"the smallest denominator magnitude this project guards divisions at, one decade conservative against the true limit\", not \"exactly what makes the division fail\". Anything above DBL_MIN \u2014 1e-12 included \u2014 is a scale chosen for the caller, which is the 2.10.2 rule's definition of the defect.",
   "scale_test": "SCALE COVARIANCE, depth 307, MEASURED not argued \u2014 plus one boundary probe that turns out to carry more weight than the 616 sweep assertions.\n\nCORE SWEEP (small side, the only side that discriminates). For e in 0..307 with s = 10^-e by repeated f64_div by 10, assert BIT-EXACTLY (never through f64_to, never through LOOSE_TOL): hisab_inverse_lerp(0, s, s/2) == 0.5 and hisab_inverse_lerp(0, s, s) == 1.0. Assert the run counter == 308 (a sweep that silently stops running is indistinguishable from one that passes \u2014 the 2.10.2 lesson). Add the inheriting caller: hisab_remap(5s, 0, 10s, 0, 100) == 50 over the same 0..307. Measured baselines for the fixture comment: shipped first wrong at e=13 (remap e=14), F64_TINY-guarded first wrong at e=308 (remap e=309, one deeper because remap's interval is 10s), so 0..307 is the exact window in which the correct repair passes and every wrong threshold fails.\n\nBRACKETING \u2014 MEASURED, EACH MUTANT RUN. 1e-9 dies at decade 10, 1e-15 at 16, DBL_EPSILON at 16, 1e-100 at 101, 1e-200 at 201, shipped 1e-12 at 13, a numerator-guarding wrong-quantity mutant at 12. All strictly inside 0..307; DBL_MIN itself sits at 308, one past the last asserted decade. This is the 2.11.1 lesson applied \u2014 a 12- or 20-decade sweep would leave the 1e-100 and 1e-200 mutants alive, the same way the quaternion sweep killed only 1 of 3. foundation.tcyr:1146 derives 154 for the QUADRATIC hvec3_angle case by this argument; linear means 308 here, and 308 is now confirmed by running it, not inferred.\n\n\u26d4 THE COVARIANCE SWEEP KILLS EVERY ABSOLUTE MUTANT AND ZERO RELATIVE ONES \u2014 SO THE BOUNDARY PROBE IS LOAD-BEARING, NOT DECORATION. Measured: a repair using a RELATIVE threshold, `|b-a| < EPSILON_F64 * max(|a|,|b|)`, survives all 308 decades of the sweep untouched (first-wrong: never), because with a = 0 the threshold is 1e-12*s and |denom| = s is never below it. It is killed only by the boundary probe, and only if that probe is asserted bit-exactly: with a = 0, b = DBL_MIN/1000 (subnormal), value = b/2, the relative repair returns 0.5 (0x3FE0000000000000) where F64_TINY returns 0. So assert BOTH sides of the boundary explicitly \u2014 hisab_inverse_lerp(a, a, v) == 0 for a truly zero denom, and hisab_inverse_lerp(0, sub, sub/2) == 0 for a subnormal denom below DBL_MIN. A covariance sweep proves the guard is silent where it should be; only these two prove it still EXISTS and sits where it is claimed to.\n\n\u26a0 THE a = 0 ORIGIN IS FORCED, NOT LAZY \u2014 DO NOT \"IMPROVE\" IT WITH AN OFFSET FIXTURE. The obvious hardening (make a != 0 so the sweep proves the guard is on the DIFFERENCE rather than an endpoint) does not survive contact: a = s, b = 3s, value = 2s fails bit-exactly at decade 1 on the SHIPPED code AND on the correct F64_TINY code, because 3s and 2s each round independently. Even the power-of-two form a = s, b = s+s, value = s+s/2 fails at decade 1 on both \u2014 1.5*s needs 54 mantissa bits. Only a = 0 keeps numerator and denominator exact at every scale (halving is exact, s/s is exact), which is precisely why the fixture must be written that way. Anyone who adds an offset variant will get a red suite and blame the repair.\n\n\u26a0 THE PROPOSED LARGE-SIDE MIRROR KILLS NOTHING \u2014 TRIM IT OR LABEL IT DOCUMENTATION. Measured over e in 0..307 with s = 10^+e: both the shipped code and the F64_TINY repair are correct at every decade (first-wrong: never). It was proposed to catch \"a two-sided mutant\", but the only realistic sibling \u2014 a reversed comparison, f64_gt instead of f64_lt \u2014 already dies on the small side at decade 0. Roughly 300 assertions with no discriminating power over the small side; keep them only as an explicit record that the fabrication is one-sided (which is the evidence the quantity is linear rather than reciprocal), and do not count them toward mutation coverage."
  },
  {
   "site": "src/transforms.cyr:169",
   "fn": "world_to_screen",
   "guarded_quantity": "|w| of the clip-space vector clip = MVP * (point, 1). It is the homogeneous scale factor of the clip vector: degree-1 in the MVP's overall scale and degree-1 in the world scale. For any perspective matrix m4_perspective_rh builds (src/mat4.cyr:335 sets col 2 / row 3 to -1) it is exactly -z_view, i.e. a LENGTH in view units, dimension length^1, unbounded above and below. Not dimensionless, not bounded, not squared, not a normalised cosine or a barycentric weight. (For an orthographic MVP m4_ortho_rh sets w = 1 exactly \u2014 MEASURED at t = 1e0/1e-13/1e-100/1e-300, w == 1.0 bit-exactly at every one, ship and repair both returning 600.0 \u2014 so the guard is inert on that path and any ortho-only fixture is vacuous. The defect is reachable only through the perspective/scaled path, which is what the function exists for.)",
   "protects": "The reciprocal f64_div(F64_ONE, cw) on the next line (transforms.cyr:170) and the three perspective divides x*inv_w, y*inv_w, z*inv_w that consume it. Provenance is explicit and rules out any tolerance reading: docs/audit/2026-04-15.md H5 records this guard's origin as \"Division by zero in world_to_screen ... Fix: Guard w==0 like m4_transform_point does\", and docs/development/threat-model.md:41 lists it as \"Perspective divide by w=0 -> Returns hvec3_zero()\".",
   "verdict": "DEFECT_ABSOLUTE_ON_SCALING",
   "why": "REFUTATION ATTEMPTED ON ALL FIVE AVENUES; ALL FIVE FAIL, EACH BY MEASUREMENT RATHER THAN ARGUMENT. (1) DIMENSIONLESS/BOUNDED? No. w is a view-space depth for every perspective MVP, and independently of units the map the function computes, (x/w, y/w, z/w), is degree-0 homogeneous in the clip vector while |w| is degree-1 \u2014 so MVP and c*MVP are the same projection and must give the same pixel. Reproduced both ways in a bit-faithful IEEE-754 simulation of m4_perspective_rh, m4_mul_vec4 (col-scale-and-accumulate order, no FMA) and world_to_screen: base screen = (520.7106781186548, 300.0, 0.9676343009676343), bits_x = 408045af78026cd7; the shipped guard first returns a wrong answer at decade e = 13 in the UNIT sweep and at e = 13 in the unit-free HOMOGENEITY sweep, the same decade from two independent premises. (2) CALLER NORMALISES? No. grep over src/, tests/, examples/ finds no in-tree caller other than the abuse assertion; it is a public entry point taking a caller-supplied MVP, and there is no normalisation of a projective matrix to appeal to. (3) CONVERGENCE TOLERANCE? Refuted by the repo's own record \u2014 audit H5 above names it a divide-by-zero guard, and src/error.cyr:40-44 states the discriminating rule verbatim: F64_TINY \"is the right threshold when the guard protects a DIVISION and the only thing that can actually fail is a zero (or subnormal) denominator. EPSILON_F64 remains right for a genuinely dimensionless, bounded quantity - a normalised cosine, a barycentric weight, a parameter in [0,1]\". cw is none of those, so the project's own written rule selects F64_TINY. (4) BREAKS AN EXISTING TEST? CHECKED, NOT ASSUMED. The only occurrence of world_to_screen in tests/*.tcyr is tests/abuse.tcyr:1496, assert_neq(world_to_screen(hvec3_zero(), m4_identity(), 0, 0), 0, \"0x0 viewport returns a vector\") \u2014 an identity MVP gives clip = (0,0,0,1), so cw = 1.0, the guard is inert, and the assertion is a non-null pointer check that hvec3_zero() would also satisfy. Changing EPSILON_F64 -> F64_TINY changes zero assertions in all five suites. (5) TEST DOESN'T DISCRIMINATE? It does; bracketing measured and tabulated below. The comment \"# Point at camera\" additionally names an intent the expression does not implement \u2014 \"at the camera\" is w small RELATIVE to the rest of the clip vector or to the near plane, and neither quantity appears here \u2014 so it is also a wrong-quantity guard, but the taxonomy's primary fit remains ABSOLUTE_ON_SCALING because the guarded quantity plainly scales. NOTE THE CLASS, NOT JUST THE SITE: m4_transform_point (src/mat4.cyr:150) carries the byte-identical `f64_lt(f64_abs(pw), EPSILON_F64)` guard on the same homogeneous w and fabricates DIFFERENTLY (returns the undivided (x,y,z)), and screen_to_world_ray (transforms.cyr:187-188) divides by HVec4_w with no guard at all \u2014 three sites, three different answers to one question.",
   "fabricates": "hvec3_zero() = (0, 0, 0) \u2014 a perfectly legitimate screen position: x = 0 is the left edge, y = 0 is the top edge, z = 0 is the near plane. The function's only channel is the returned Vec3; there is no rc and no sentinel, and the sole existing test (assert_neq(..., 0)) accepts it, so a caller drawing at the returned pixel silently draws in the top-left corner. MEASURED at world scale 1e-13: shipped returns (0.0, 0.0, 0.0) where the correct answer is (520.7106781186548, 300.0, 0.9676343009676343) \u2014 the x error is 520.7 px on an 800 px viewport, i.e. 65% of the frame.",
   "correct_threshold": "F64_TINY \u2014 DBL_MIN, 0x0010_0000_0000_0000 \u2014 already defined at src/error.cyr:45 with a comment (lines 40-44) that selects it for exactly this case. It is the smallest magnitude whose reciprocal is finite, and a zero or subnormal cw is the only thing that makes 1/cw fail. \u26a0 SHARPENED BY MEASUREMENT: a bare `cw == 0` exact-compare is NOT an adequate repair and must be excluded explicitly, because subnormal w still overflows the reciprocal \u2014 at t = 1e-309..1e-323 the measured w is 3.0e-309 .. 3e-323 and `== 0` yields screen_x = +inf at every one, while F64_TINY yields the documented hvec3_zero(). Retain hvec3_zero() as the fired-guard return so the published contract (threat-model.md:41) is unchanged; only the threshold moves. If a real \"point at camera\" PREDICATE is wanted in addition it must be RELATIVE and must name what it is relative to \u2014 |w| < rel * max(|x|,|y|,|z|) of the same clip vector, the only scale-free quantity available here \u2014 but it is a separate documented predicate, never a substitute for the divide guard. Fix the sibling sites in the same change or the class survives: m4_transform_point (src/mat4.cyr:150), same guard on the same quantity, different fabrication; screen_to_world_ray (src/transforms.cyr:187-188), no guard at all.",
   "scale_test": "FOUR PARTS. Parts (a) and (b) are the triage's two sweeps, CONFIRMED by measurement but corrected in three places where following the text literally produces a false failure or a vacuous case; parts (c) and (d) are additions the original set could not do without. FIXTURE (all parts): proj = m4_perspective_rh(pi/4, 800/600, 0.1t, 100t); view = m4_identity() with m4_set(view, 3, 2, -3t) HAND-BUILT; point = (0.5t, 0, 0); viewport 800x600. \u26d4 Do NOT build the view with m4_look_at_rh \u2014 its internal hvec3_normalize (src/vec3.cyr:110) carries an identical EPSILON_F64 guard firing at the same decade on the same magnitude, so the sweep then passes or fails for the wrong reason. (a) UNIT COVARIANCE: for every e in 0..308 with t = 10^-e, assert screen_x and screen_y BIT-EXACTLY against their e=0 values (520.7106781186548 / bits 408045af78026cd7, and 300.0). \u26a0 CORRECTION 1, MEASURED: do NOT assert the third component (depth) bit-exactly \u2014 the CORRECT repair already differs there at e = 5, because m[3][2] = 2*far*near/(near-far) is QUADRATIC in t and (far+near)/(near-far) re-rounds per decade; asserting the whole Vec3 makes the fix fail its own test. Assert depth to <= 4 ulp or omit it. (b) HOMOGENEITY, unit-free: fix the e=0 MVP and for c = 10^-e over e in 0..308 assert world_to_screen(p, c*MVP, 800, 600) is bit-identical in x and y to world_to_screen(p, MVP, 800, 600). MEASURED to kill at exactly the same decades as (a), which is the confound-free confirmation. (c) \u26a0 ADDITION \u2014 LOW-SIDE BRACKET, which the original set omits and therefore cannot kill an under-guard: for e in 309..323 assert world_to_screen returns hvec3_zero(). MEASURED: w is subnormal there (3.0e-309 down to 3e-323), F64_TINY returns 0.0 and a bare `cw == 0` repair returns +inf, so this is the only part that discriminates the two, and it also pins DBL_MIN as the exact boundary rather than leaving it \"just past where the sweep stops\". (d) \u26a0 CORRECTION 2 \u2014 EXACT w == 0, so the sweep cannot be satisfied by deleting the guard: the point must be at world z = +3t, NOT -3t. With the camera translation m4_set(view,3,2,-3t) the camera sits at +3t looking down -z; z = -3t gives w = +6.0 and the case is VACUOUS (measured \u2014 I made this exact error before checking). At z = +3t the measured clip w is exactly 0.0, shipped and F64_TINY both return (0,0,0) while `== 0` and DELETED both return (inf, inf, inf). Assert the run counter on (a), (b) and (c) \u2014 309, 309 and 15 iterations. BRACKETING, MEASURED first-wrong decade under (a) and (b) alike: 1e-9 -> e=10, shipped 1e-12 -> e=13, 1e-15 -> e=16, DBL_EPSILON 2.2e-16 -> e=17, 1e-100 -> e=101, 1e-200 -> e=201, 1e-300 -> e=301; F64_TINY, `== 0` and DELETED all survive 0..308 and are then separated by (c) and (d). A 20-decade sweep kills only the first four and leaves 1e-100, 1e-200 and 1e-300 alive \u2014 the 2.11.1 mistake, confirmed by running it. ORTHO CONTROL (negative control, must be part of the fixture comment): the same sweep on m4_ortho_rh shows w == 1.0 bit-exactly at every decade and ship == repair == 600.0, proving the sweep's signal comes from the perspective path and not from the loop."
  }
 ]
}